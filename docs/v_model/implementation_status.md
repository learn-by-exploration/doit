# Implementation Status

Status: draft, created 2026-06-13. Last updated 2026-06-14 (Phases 5+6+7 closed).

This doc tracks what is done, what is in flight, and what is next,
mapped to the V-Model stages. Update it as work progresses.

## V-Model stage: left side (requirements)

| Artifact | Status | Owner | Notes |
| --- | --- | --- | --- |
| `plan.md` | Done | — | V-Model stages + milestones + working assumptions |
| `conops.md` | Done | — | Mission, actors, modes, normal scenario, constraints, success |
| `workflows.md` | Done | — | WF-001..WF-016 |
| `requirements.md` | Done | — | SYS-001..SYS-030 |
| `v0_1_baseline.md` | Done | — | Lean v0.1 scope, decisions, acceptance criteria |
| `architecture_options.md` | Done | — | Tech stack, packages, modules, manifest baseline |
| `decision_record.md` | Done (17 ADRs) | — | Append-only |
| `traceability_matrix.md` | Done | — | Need ↔ Requirement ↔ Design ↔ Verification |
| `mission_catalog.md` | Done | — | Spec for each of the 5 mission types |
| `notification_reliability.md` | Done | — | Doze, exact-alarm, boot survival |
| `open_questions.md` | Done (20 items) | — | Not blocking; can be answered in v0.2 |

## V-Model stage: right side (verification)

| Artifact | Status | Owner | Notes |
| --- | --- | --- | --- |
| `test/` directory | Done (2026-06-14) | — | 237 tests passing at the Phase 6 tip (commit `5f4f31d`). Coverage ≥ 80% on `lib/services/`, `lib/screens/`, `lib/reminders/`, `lib/missions/` per Phase 5/6 reports. |
| 3-gate in CI | Done (2026-06-14) | — | v0.4a.1: `.github/workflows/ci.yml` runs the 3-gate on every PR and push to `main`. `test/ci_workflow_test.dart` parses the workflow and asserts the three steps in order. See commit _v0.4a.1_. |
| Coverage ≥ 80% on changed files | Done (2026-06-14) | — | Per-phase reports; the 3-gate enforces it on every commit. |
| 14-day real-device acceptance run | Documented (2026-06-14) | — | Runbook at [`acceptance_run.md`](acceptance_run.md); pending install on the primary phone. |

## V-Model stage: bottom (implementation)

| Slice | Status | Notes |
| --- | --- | --- |
| Flutter app scaffold | Done (2026-06-13) | `flutter create` with package `com.common_games.streak`; `flutter build apk --debug` succeeds. See commit `e5404ac` plus the workmanager/desugaring fixes in the same commit. |
| `analysis_options.yaml` (18 lints) | Done (2026-06-13) | Inherited from `board_box`; in commit `e5404ac`. |
| `lib/habits/` model + schedule engine | Done (Phase 1) | Pure Dart, ≥ 80% coverage; 4 schedule types + sealed Habit hierarchy. |
| `lib/people/` model + contact resolution | Done (Phase 1) | Pure Dart + thin platform channel; sealed `PersonChannel` + `PersonCadence`. |
| `lib/missions/` 5 types + chain executor | Done (Phase 3) | Pure Dart where possible; `ShakeDetector` is the only sensor adapter; chain executor is pure. |
| `lib/reminders/` scheduler + service | Done (Phase 4) | Platform channel to Kotlin; `AlarmScheduler`, `NotificationService`, `FullScreenIntent`, `AnchorDetector`. |
| `android/app/src/main/.../BootReceiver.kt` | Done (Phase 4) | Native Kotlin; re-schedules all pending alarms on `BOOT_COMPLETED`. |
| `android/app/src/main/.../HomeWidgetProvider.kt` | Done (Phase 4) | Native Kotlin; today's due-now strip on the launcher widget. |
| `lib/services/db.dart` (Drift) | Done (Phase 2) | Migrations live in `lib/services/db/migrations/`. |
| `lib/services/backup_service.dart` | Done (Phase 6) | SAF-based; JSON envelope `{version, exportedAtMillis, tables}`; 6 tables round-trip. |
| `lib/screens/onboarding.dart` | Done (Phase 5a+5b) | Permission-first UX; rationale screens for `POST_NOTIFICATIONS`, `READ_CONTACTS`, `SCHEDULE_EXACT_ALARM`, battery-opt, OEM card, backup folder, anchor mode. |
| `lib/screens/home.dart` | Done (Phase 5a+5b) | Catalog + due-now strip + "I'm up" anchor button. |
| `lib/screens/stats.dart` | Done (Phase 5a+5b) | Streaks, completion rate, time-of-day; consumes `StreakCalculator` over `CompletionLogService`. |
| `lib/screens/settings.dart` | Done (Phase 5a+5b, restore link in Phase 6) | Theme, anchor mode, reliability row, restore-from-backup tile. |
| `lib/screens/add_habit.dart`, `add_person.dart` | Done (Phase 5a+5b) | Multi-step form; mission chain composer. |
| `lib/screens/mission_<name>.dart` (5) | Done (Phase 5c) | Shake, Type, Hold, Math, Memory; per-screen widget tests. |
| `lib/screens/settings_restore.dart` | Done (Phase 6) | SAF file picker + confirm dialog; consumes `BackupService`. |
| `lib/services/backup_service.dart` tests | Done (Phase 6) | `test/services/backup_service_test.dart` — 6 cases (envelope, round-trip, missing file, malformed JSON, future version, missing tables). |

## Phase plan (proposed)

The v0.1 baseline has 4 phases (see
[`plan.md`](plan.md#initial-milestones)). The implementation is
sliced into the following sub-phases. Each ends with a green
3-gate and at least one new test:

### Phase 0 — Scaffold

- `flutter create`.
- Copy `analysis_options.yaml`, `.gitignore`, `lib/main.dart`
  theme.
- Add the 6 confirmed dependencies (`flutter_local_notifications`,
  `android_alarm_manager_plus`, `workmanager`, `permission_handler`,
  `drift`, `flutter_contacts`).
- Confirm 3-gate passes on the empty app.

**Status: Done (2026-06-13).** `flutter create` ran with
`--org com.common_games --project-name streak --platforms android`,
pubspec set to `name: common_games` (monorepo convention), the
v0.1 permission baseline added to `AndroidManifest.xml`, the
18-lint analysis options in place, `minSdk = 28`, and core
library desugaring enabled (required by
`flutter_local_notifications` 17.x). `workmanager` was bumped
to `^0.6.0` because 0.5.x uses the removed v1 Flutter plugin
embedding. 3-gate green (`dart format` clean, `flutter analyze
--fatal-infos` reports "No issues found!", `flutter test` passes
the placeholder widget test); `flutter build apk --debug`
produces `build/app/outputs/flutter-apk/app-debug.apk`. See
commit `e5404ac` ("docs+chore(playbook): finish streak sync —
pointer table, doc notices, Phase 0 scaffold").

### Phase 1 — Models and schedule engine

- `lib/habits/habit.dart` (sealed, with `HabitFixed`,
  `HabitInterval`, `HabitAnchor`, `HabitDayOfX`).
- `lib/habits/schedule.dart` (pure-Dart `nextOccurrence()` per
  type).
- `lib/habits/proof_mode.dart` (sealed Soft/Strong/Auto).
- `lib/habits/streak_calculator.dart` (pure-Dart streak from a
  completion log).
- `lib/habits/rest_day_budget.dart`.
- `lib/people/person.dart` (sealed channel type).
- Unit tests for all of the above.
- 3-gate green.

### Phase 2 — Local DB

- `lib/services/db.dart` (Drift).
- Tables: `habits`, `people`, `completions`, `missions`,
  `settings`.
- `lib/services/migrations/` with `v1_to_v2` placeholder.
- Migrations test (downgrade → upgrade round-trip).
- 3-gate green.

### Phase 3 — Mission engine

- `lib/missions/mission.dart` (sealed Shake/Type/Hold/Math/Memory).
- `lib/missions/chain.dart` (chain executor).
- `lib/missions/shake_detector.dart` (sensor integration).
- `test/missions/<name>_test.dart` for each.
- 3-gate green.

### Phase 4 — Reminder scheduling

- `lib/reminders/alarm_scheduler.dart`.
- `lib/reminders/notification_service.dart`.
- `lib/reminders/full_screen_intent.dart`.
- `lib/reminders/anchor_detector.dart`.
- `android/app/src/main/.../BootReceiver.kt`.
- `android/app/src/main/.../HomeWidgetProvider.kt`.
- `AndroidManifest.xml` (manifest baseline from
  `architecture_options.md`).
- Integration test for alarm + boot survival.
- 3-gate green.

### Phase 5 — Screens and onboarding

- `lib/screens/onboarding.dart` (permission-first flow).
- `lib/screens/home.dart` (catalog + due-now + "I'm up").
- `lib/screens/add_habit.dart`, `add_person.dart`.
- `lib/screens/stats.dart`.
- `lib/screens/settings.dart` (with OEM guide card).
- Widget tests for each.
- 3-gate green.

### Phase 6 — Backup

- `lib/services/backup_service.dart`.
- `lib/screens/settings_restore.dart`.
- `test/backup/auto_backup_test.dart`.
- `test/backup/restore_test.dart` (idempotent).
- 3-gate green.

### Phase 7 — 14-day real-device run

- Install on primary phone.
- Run all 4 presets + 1 custom habit.
- Verify the 8 acceptance criteria in `v0_1_baseline.md`.
- File any defects as `fix:` commits; do not start v0.2 until
  every criterion is "yes".

**Status: Documented (2026-06-14, commit `eeb87a0`).** The runbook
is at [`acceptance_run.md`](acceptance_run.md) — 11 scenarios, 17
SYS- IDs, 9 WF- IDs, per-day log template, exit criteria. The run
itself starts after the user installs the Phase 6 tip on the
primary phone; the run does not require new code.

## Phase log

| Phase | Tip commit | 3-gate | Tests | Coverage on changed files |
|-------|------------|--------|-------|---------------------------|
| 0 — Scaffold | `e5404ac` | ✓ | 1 (placeholder) | n/a |
| 1 — Models + schedule | _see git log_ | ✓ | ≥ 30 (4 schedule types × edge cases + streak + rest day) | ≥ 80% |
| 2 — Local DB (Drift) | _see git log_ | ✓ | migrations + repositories | ≥ 80% |
| 3 — Mission engine | _see git log_ | ✓ | 5 mission types + chain + shake detector | ≥ 80% |
| 4 — Reminders + Kotlin | `79eb931` | ✓ | alarm scheduler + boot + Doze + tz + anchor | ≥ 80% |
| 5a+5b — UI: 6 main screens | `4433004` | ✓ | ≥ 6 widget tests | ≥ 80% |
| 5c — UI: 5 mission screens | `d4fd786` | ✓ | 5 widget tests | ≥ 80% |
| 6 — Backup + restore | `5f4f31d` | ✓ | 6 backup_service tests | ≥ 80% |
| 7 — Acceptance run | `eeb87a0` (doc only) | n/a (no code) | n/a | n/a |
| v0.2 proposal | `76085e4` (doc only) | n/a (no code) | n/a | n/a |
| **v0.2a — Completeness** | `9c032fc` | ✓ | 4 (category, icon, edit, pause) widget + model tests | ≥ 80% |
| v0.2b — Events | `a828333` | ✓ | event_repository + add_event + events_list | ≥ 80% |
| v0.2c — Groups | `457de5b` | ✓ | person_group + person_group_repository + rotation + screens | ≥ 80% |
| v0.2d — UX delight | `54be40f` | ✓ | test reminder (WF-028) + bulk complete (WF-029) + fasting timer (WF-019) | ≥ 80% |
| v0.2e — Run #2 runbook | `fd1a4d9` (doc only) | n/a (no code) | n/a | [`acceptance_run_v2.md`](acceptance_run_v2.md) kicked off 2026-06-14 at SHA `c1b9e64`; Day 0 prep passed (3-gate green, debug apk built, v0.2 changed-files coverage 89.8%) |
| **v0.3a — Public docs + version constant** | `6502432` | ✓ | 4 build_info_test | n/a (docs + a 2-line constant) |
| v0.3b — Release signingConfig | `bcb5c9b` | ✓ | 8 release_signing_test (static analysis) | n/a (Gradle config) |
| v0.3c — In-app About / Licenses | `78b8302` | ✓ | 3 settings_licenses_test | ≥ 80% on `lib/screens/settings.dart` About changes |
| v0.3d — Fresh-install smoke | `50781ce` | ✓ | 1 fresh_install_test (end-to-end) | n/a (integration test) |
| v0.3e — Release (sign-off) | _pending_ | _tbd_ | _tbd_ | [`v0_3_release_checklist.md`](v0_3_release_checklist.md) is the right-side gate; `flutter build appbundle --release` is the user's hands-on step. |
| **v0.4a.1 — CI 3-gate** | _v0.4a.1_ | ✓ | _v0.4a.1_ | `.github/workflows/ci.yml` + `test/ci_workflow_test.dart`. Closes the "Not started" row in the right-side table. |
| v0.4a.2 — CHANGELOG | _v0.4a.2_ | ✓ | _v0.4a.2_ | `CHANGELOG.md` (Keep-a-Changelog shape, v0.4 [Unreleased] section). |
| v0.4a.3 — firstLaunch persisted | _v0.4a.3_ | ✓ | 7 first_launch_persisted + 3 widget_test (SYS-059) | `SettingsService.firstLaunchCompleted` backed by `SharedPreferences`. The "onboarding re-appears on reinstall" caveat in `PRIVACY.md` is removed. |
| v0.4b — WorkManager periodic backup | _v0.4b_ | ✓ | 5 backup_scheduler_test + 3 backup_task_dispatcher_test (SYS-060) | `lib/services/backup_scheduler.dart` wires the `workmanager` plugin to register a 24-hour periodic task. The "scheduling call not yet wired" caveat in `PRIVACY.md` is removed. |
| v0.4c.1 — Backup encryption | _v0.4c.1_ | ✓ | 5 backup_encryption_test (SYS-061) | `kBackupFormatVersion` bumped to 2; AES-256-GCM with PBKDF2-HMAC-SHA256 (100k iterations) behind a user passphrase. v1 plain-JSON stays importable for back-compat. The "plain JSON backups" caveat in `PRIVACY.md` is updated. |
| v0.4c.2 — TalkBack / a11y static review | _v0.4c.2_ | ✓ | 18 a11y/semantics_labels_test (SYS-062) | Static analysis walks every `lib/screens/*.dart` and `lib/widgets/*.dart` and asserts every `IconButton`, `ListTile`, button, `GestureDetector`, and `InkWell` has a `tooltip` / `semanticLabel` / `Text` / `Semantics` / `excludeFromSemantics: true`. Adds 6 missing tooltips to `add_habit.dart`. |
| v0.4d — sign-off | `efbfbdc` | ✓ | 373 / 373 (no new tests; CHANGELOG.md `[0.4.0]` section appended) | v0.4 release checklist updated; sign-off line is "Pending. Awaiting user's hands-on TalkBack pass (SYS-062)." The right-side gate is `v0_4_release_checklist.md`. |
| v0.4b-release-fix (ADR-013) | `384cfb2` | ✓ | 375 / 375 (373 prior + 2 new: cold-start platform-throw + dispatcher symbol) | Post-mortem on the v0.4b release-mode launch crash. Renamed `_backupTaskDispatcher` to public `backupTaskDispatcher` so `PluginUtilities.getCallbackHandle` resolves it in release AOT. Made `init()` swallow platform exceptions (defense in depth). Wrapped `main()`'s `await BackupScheduler.instance.init()` in try/catch. Rebuilds the release APK. See `decision_record.md` ADR-013. |
| v0.4b-release-fix-2 (ADR-013 follow-up) | `1bcc29f` | ✓ | 377 / 377 (375 prior + 2 new: isMinifyEnabled pin + manifest auto-init pin) | The v0.4b-release-fix was a misdiagnosis — the real cold-start crash was R8 stripping workmanager's `WorkDatabase_Impl` class, triggered by `androidx.work.WorkManagerInitializer` running at process start via `androidx.startup.InitializationProvider` (before any Dart code). Surgical fix: `tools:node="remove"` removes the `WorkManagerInitializer` from the merged manifest. Defense in depth: `isMinifyEnabled = false` and `isShrinkResources = false` are pinned explicitly in `buildTypes.release`. Two new tests pin both invariants. Release APK rebuilt (69.7 MB) and verified launches on a real SM-S918B device. See `decision_record.md` ADR-013 (follow-up). |
| v0.5-pre (docs) | `5379c80` | ✓ | 377 / 377 (no new tests) | Pre-work for the v0.5 milestone. Appended SYS-063 (`POST_NOTIFICATIONS`), SYS-064 (`READ_CONTACTS`), SYS-065 (`SCHEDULE_EXACT_ALARM` / `USE_EXACT_ALARM`), SYS-066 (SAF backup folder picker) to `requirements.md`. Added a 4-row traceability block. Closed open questions #5 (READ_CONTACTS revocation) and #6 (SAF URI revocation) — both answered by v0.5's wiring. |
| v0.5a — rename to "do it" | _pending v0.5a commit_ | ✓ | 377 / 377 + v0.5a pin tests in `release_signing_test.dart` (TBD at v0.5a) | App-level rename from "Streak" to "do it". `applicationId` / `namespace` → `com.doit`. Dart package `name:` → `doit`. Directory `streak/` → `doit/`. Kotlin tree `com/common_games/streak/` → `com/doit/`. `MethodChannel('doit/reminders')`, notification channel `doit.reminders`, WorkManager task `doit.backup.nightly`. Version bump `0.4.0+5` → `0.5.0+6`. `StreakCalculator` / `StreakService` / `StreakSnapshot` / `StreakConfig` / `streak_calculator.dart` / `streak_service.dart` stay (feature identifiers). All user-facing display strings updated. v0.5e-fix corrects the v0.5a draft that picked `com.doit.package` (invalid Java namespace) to `com.doit`. v0.5b (PermissionService) and v0.5c (onboarding wiring) follow. |
| **v0.5b — `PermissionService` + sealed result** | _pending v0.5b commit_ | ✓ | 377 / 377 + 9 permission_service_test (TBD at v0.5b) | New `lib/services/permission_service.dart` singleton with the `_ready`-gated init pattern from `.claude/rules/lib-services.md`. Sealed `PermissionResult` (`granted` / `denied(canOpenSettings)` / `permanentlyDenied`) in `lib/services/permission_result.dart`; sealed `BackupFolderResult` (`picked` / `cancelled` / `error`). Public methods: `requestNotifications`, `requestContacts`, `requestExactAlarm`, `requestBackupFolder`, `openAppSettings`, `init`. 9 service tests pin the result branches. The widget layer is untouched; the seam exists. |
| **v0.5c — wire onboarding CTAs** | _pending v0.5c commit_ | ✓ | 386 / 386 + 3 settings_backup_uri + 6 onboarding_wiring (TBD at v0.5c) | `lib/screens/onboarding.dart` `_handleStepCta` dispatches on `_step` to the right `requestX()` method. The four CTAs now do what the rationale text always promised. `SettingsService.backupFolderUri` is a `ValueNotifier<String?>` (defaults `null`); `setBackupFolderUri(String?)` mutates it. 3 `settings_service_backup_uri_test` and 6 `onboarding_permission_wiring_test` pin the behavior. The widget layer no longer imports `permission_handler` or `file_picker` directly. `lib/screens/onboarding.dart` file-level comment is updated to reflect the new reality. |
| **v0.5d — Settings → Permissions tile** | `a04e392` | ✓ | 406 / 406 (386 prior + 4 settings_permissions + 16 settings_test header; matches the v0.5d row in `v0_5_release_checklist.md`) | New `Permissions` section in `lib/screens/settings.dart` between `Wake-up anchor` and `Reliability`. `_PermissionsRow` subscribes to `PermissionService.instance.statuses` and renders one `ListTile` per permission. `_PermissionTile` shows the status text and a "Settings" `TextButton` for `permanentlyDenied` rows that deep-links to `openAppSettings()`. `_BackupFolderTile` re-picks via `requestBackupFolder()` and persists via `setBackupFolderUri()`. 4 `settings_permissions_test` pin the recovery affordance. `docs/v_model/notification_reliability.md` line 126-127 is updated to point at the onboarding probe + Settings → Permissions tile recovery. ADR-016 is appended to `decision_record.md`. |
| **v0.5e-fix (ADR-017)** | `ce6dd83` (local) | ✓ | 407 / 407 (406 prior + 1 net new assertion in `release_signing_test.dart` — the v0.5a pin test was rewritten in place to assert `com.doit` and a new regression guard `isNot(contains('com.doit.package'))` was added) | The v0.5a draft picked `applicationId = "com.doit.package"` and `namespace = "com.doit.package"`. `flutter build appbundle --release` failed: "Namespace 'com.doit.package' is not a valid Java package name as 'package' is a Java reserved keyword". Five surgical changes: `android/app/build.gradle.kts` (`com.doit` / `com.doit`), `AndroidManifest.xml` (`com.doit.FIRE_ALARM`), `android/app/src/main/kotlin/com/doit/package/` → `com/doit/` via `git mv` with intermediate name (the parent already exists), `test/release_signing_test.dart` rewrite + regression guard, four doc files updated. Release AAB (61.0 MB) and APK (69.8 MB) rebuilt successfully. The user's push of `ce6dd83` to `main` is blocked by the auto-classifier (default-branch push); the v0.5e on-device verification is still pending the user attaching the SM-S918B. See `decision_record.md` ADR-017 for the full post-mortem. |
| **v0.5e — release APK + on-device verification** | _pending user's hands-on step_ | _tbd_ | _tbd_ | The user runs `adb uninstall com.doit` (the v0.5e-fix applicationId; was `com.common_games.streak` pre-v0.5) + `adb install build/app/outputs/flutter-apk/app-release.apk` + `adb shell monkey -p com.doit -c android.intent.category.LAUNCHER 1` + the seven-step on-device verification on a real SM-S918B device. The release APK is the install boundary (applicationId changed). v0.5e is the right-side gate; v0.5f is the sign-off commit. No code lands in v0.5e — the user is the executor. |
| **v1.0a.1 — `Habit` → `Do` class rename** | `fee9694` | ✓ | _see git log_ | Pure rename pass: `Habit` → `Do` (sealed hierarchy kept), `HabitRepository` → `DoRepository`, `HabitFixed`/`HabitInterval`/etc. → `DoFixed`/`DoInterval`/etc. DB column names unchanged (no migration). DB column names stay to avoid a needless v2→v3 rename migration. |
| **v1.0a.2 — user-facing copy rename** | `2e6b69d` | ✓ | _see git log_ | "Habit" → "Do" / "Add a do" / "I'm up" → "Start my day" / "Streak" → "Consecutive done" in every screen and widget. Renamed `HabitCategory` → `DoCategory`, `HabitIcons` → `DoIcons`. DB column names unchanged. |
| **v1.0a.3 — V-Model docs sync** | `373913c` | ✓ | _see git log_ | `conops.md` / `requirements.md` / `workflows.md` updated to the "Do / consecutive done" framing. WF-002 → WF-002a. SYS- IDs renamed in the docs only. |
| **v1.0b.1 — templates: model + Drift v2→v3 + repository** | `5b51714` | ✓ | ≥ template_repository_test + template_library_test | `lib/templates/{template.dart,template_library.dart,template_repository.dart}`. `Templates` table added (v2→v3 migration). 25 hand-crafted templates seeded on first run. |
| **v1.0b.2 — templates: catalog UI + save-as-template** | `142df92` | ✓ | ≥ templates_test + add_event extraction tests | New `lib/screens/templates.dart` grid. "Save as template" action on `AddDoScreen` / `AddEventScreen` / `AddPersonScreen`. FAB on home shows "Browse templates" alongside "Create blank". |
| **v1.0b.3 — V-Model docs (templates)** | `6e44a46` | ✓ | _docs only_ | `requirements.md` SYS-067/068; `workflows.md` WF-032/033; `decision_record.md` ADR-020 (template JSON shape); `conops.md` "Templates" section. |
| **v1.0c.1 — `Trigger` / `Condition` / `Action` sealed types** | `32f807d` | ✓ | ≥ triggers + condition + action unit tests | `lib/triggers/{trigger.dart,condition.dart,action.dart}`. Each entity gets optional `List<Automation>` field. Migration v3→v4 (nullable JSON column on each table). `RoutineExecutor` skeleton. |
| **v1.0c.2 — `GeofenceService` + `TriggerLocationEnter`/`Exit`** | `bde1284` | ✓ | ≥ geofence_service_test + location_trigger_test | `lib/services/geofence_service.dart` wraps `flutter_geofence` + `geolocator`. `PermissionKind.location` (ACCESS_COARSE_LOCATION). `RoutineExecutor._onGeofence` wired. ADR-021. |
| **v1.0d.1 — `DeviceStateChannel` + `DeviceStateService`** | `9ed6abe` | ✓ | ≥ device_state_probe_test | Kotlin `DeviceStateChannel.kt` exposes charging / battery range / BT device / Wi-Fi SSID / headphones / ringer / foreground app. `lib/services/device_state_probe.dart` `Stream<DeviceStateSnapshot>`. `PermissionKind.bluetooth` (BLUETOOTH_CONNECT). |
| **v1.0d.2 — `TriggerDeviceState` wired** | `c7035cc` | ✓ | ≥ device_state_trigger_test | All 7 device-state trigger subtypes wired. Settings → Triggers screen (live dashboard). ADR-022. |
| **v1.0e.1 — `CalendarService` + `CalendarChannel.kt`** | `f61b718` | ✓ | ≥ calendar_service_test | `lib/services/calendar_probe.dart` wraps `device_calendar`. `PermissionKind.calendar` (READ_CALENDAR). `TriggerCalendarEvent` (sealed: starts, ends, busy, free). ADR-023. |
| **v1.0e.2 — `CalendarPicker` + Routines section** | `febeac5` | ✓ | ≥ calendar_picker_test + routines_section_test | UI for calendar account picker; Routines section on home screen lists the user's configured automations. |
| **v1.0f.1 — `CallInterceptor` (Kotlin `CallScreeningService`)** | `e00a97f` | ✓ | ≥ call_dispatch_test (TestDefaultBinaryMessengerBinding) | `android/.../CallInterceptor.kt` `CallScreeningService` implementation. `ActionCallIntercept` + `ActionOverrideSilent` Dart wrappers. `PermissionKind.phoneState`. `ReminderBridge` extended with `setCallInterceptorConfig`, `setRingerMode`. ADR-019. |
| **v1.0f.2 — Japan silent-mode template + UI** | `ff56021` | ✓ | 741 / 741 (716 prior + 25 new) — coverage ≥ 80% on every new/changed file: `call_interceptor.dart` 84.2%, `japan_routine_config.dart` 100%, `settings_service.dart` 95%, `templates.dart` 81.7%, `onboarding.dart` 93.1%, `add_routine.dart` 83.6%, `settings.dart` 92.6% | Template #16 from the curated library routes to a real `AddRoutineScreen` (enable toggle + contact picker + target-mode radio). `SettingsService.japanRoutine` persists the config. `Settings` → Call-screening tile surfaces `isCallScreeningRoleHeld()` and offers `requestCallScreeningRole()` via `RoleManager`. Onboarding step 4 (call-screening role) appended. Kotlin `CallInterceptor.kt` extended with `isCallScreeningRoleHeld` + `requestCallScreeningRole`. All six v1.0 phases (A–F) closed. |
| **v1.0g — sign-off + version bump + CHANGELOG `[1.0.0]`** | `b938276` | ✓ | 741 / 741 (no new tests; doc-only + version bump) | `pubspec.yaml` → `1.0.0+7`, `lib/build_info.dart` → `kAppVersion = '1.0.0'` / `kAppVersionCode = 7`. `CHANGELOG.md` `[Unreleased]` block now contains `### v1.0/Phase A — Do rename (ADR-024)`, `### v1.0/Phase D — Device-state triggers (ADR-022)`, `### v1.0/Phase F — CallInterceptor + Japan silent-mode (ADR-019 + 019 follow-up)`. New `docs/v_model/v1_0_release_baseline.md` (left-side) + `v1_0_release_checklist.md` (right-side gate). `notification_reliability.md` Device-state + Calendar + Call-screening trigger sections updated from `(planned)` to shipped state. `plan.md` Milestone 7 (v1.0) + Milestone 8 (v1.1 stub) appended. `workflows.md` WF-036 (Phase D device-state routine workflow) appended. `architecture_options.md` Calendar + CallInterceptor + Device-state PR 2 rows appended. v1.0h is the user's hands-on on-device verification on a real SM-S918B. |
| **v1.1a — `RoutineConfig` value class + per-template persistence** | `<v1.1a SHA>` | ✓ | 760 / 760 (741 prior + 19 new — 12 codec + 5 settings-service + 2 routine-executor dispatch regression) | `lib/services/routine_config.dart` (SYS-080 / ADR-025). Structural `==`, deterministic `hashCode`, `copyWith`, `toJson` / `fromJson` codec with version-free per-shape discriminators. `SettingsService.setRoutine` / `getRoutine` / `deleteRoutine` / `routines` (ValueNotifier<Map>) persisted under `doit.routine.<templateId>`. Singleton-with-`_ready` pattern (`.claude/rules/lib-services.md`). |
| **v1.1b — Routine executor: dispatch + reactive settings** | `<v1.1b SHA>` | ✓ | 776 / 776 (760 prior + 16 new — 4 dispatch + 12 reactive subscription tests) | `RoutineExecutor` consumes `SettingsService.routines` reactively via a `ValueNotifier` listener. `_dispatchAction` is a single exhaustive `is`-switch over all five `Action` leaves. ADR-021 wired. Per the V-Model, no widget code; this PR is the executor-side half of v1.1. |
| **v1.1c — `ActionOpenApp` + `RoutineOpenAppRequest` + `RoutineBanner`** | `<v1.1c SHA>` | ✓ | 780 / 780 (776 prior + 4 new banner tests) | `ActionOpenApp` leaf completed (SYS-082 / ADR-026). `RoutineOpenAppRequest` value class + `pendingOpenApp` `ValueListenable`. Passive `RoutineBanner` widget drains FIFO. Captures `NavigatorState` synchronously inside `build` to avoid stale-BuildContext. Home screen places the banner under `ReliabilityBanner.fromService()`. |
| **v1.1d — generic `RoutineApplyScreen` for templates #17..#21** | `c6a8f48` | ✓ | 807 / 807 (780 prior + 27 new — 12 codec + 13 settings-service + 6 widget + 2 catalog regression updates) | `lib/routines/routine_template_payload.dart` (SYS-083 / ADR-027). Fail-soft decoder for `{k:1, routine:{trigger, condition, action, note}}` envelope. `lib/screens/routine_apply.dart` generic apply UX with enable toggle, Save / Update / Delete, malformed-envelope fallback. `SettingsService.deleteRoutine`. `TemplatesScreen._onUse` routes templates #17..#21 to the new screen; the "Coming in v1.1" badge is removed (replaced by the existing "Use this" button). |
| **v1.1e — offline `LocationMapPreview` for `LocationPicker`** | `8aeabaa` | ✓ | 822 / 822 (807 prior + 15 new — 11 preview widget/helper tests + 4 picker tests) | `lib/widgets/location_map_preview.dart` (SYS-084 / ADR-028). Pure `CustomPaint` widget with stylised grid + pin + geofence ring; no `flutter_map`, no `INTERNET`. Equirectangular projection helpers exposed for tests. `LocationPicker` mounts the preview between the lat/lon fields and the "Use current location" button; lat/lon `TextFormField`s gain `onChanged: (_) => setState(() {})` so the pin follows typed coordinates. `test/widgets/location_map_preview_test.dart` (11 tests). `test/widgets/location_picker_test.dart` adds 3 new tests + a scroll-up drag for the On exit path (the preview's added height made the Save button sit at the bottom-sheet's scroll-detector overlap). v1.2 candidate: swap the `CustomPaint` body for `flutter_map` + cached tiles + the `INTERNET` permission. |
| **v1.1f — per-automation reliability badges** | `8d97f02` | ✓ | 850 / 850 (822 prior + 28 new — 16 pure-function unit tests + 11 widget tests + 1 catalog regression test) | `lib/routines/automation_reliability.dart` (SYS-085 / ADR-029). New `AutomationReliability` enum + pure `automationReliability(Automation, statuses)` function exhaustive over the sealed `Trigger` hierarchy via `_requiredPermissionForTrigger` (`TriggerLocation*` → `PermissionKind.location`, `TriggerCalendarEvent*` → `PermissionKind.calendar`, `TriggerDeviceState*` / `TriggerCallIncoming*` / `TriggerTimeOfDay` → `null`). `lib/widgets/automation_reliability_badge.dart` — 40×40 dp `IconButton` wrapped in a `ValueListenableBuilder` over `PermissionService.instance.statuses`. Hides itself for `optimal`; warning-amber for `degraded` (matches `ReliabilityBanner`); info-outline for `unknown`. Three `_RoutineRow` widgets (`add_habit` / `add_person` / `add_event`) restructure their trailing slot from a single `IconButton` into a `Row(min, [badge, remove])`. `test/routines/automation_reliability_test.dart` (16 tests covering every Trigger leaf × every PermissionResult variant). `test/widgets/automation_reliability_badge_test.dart` (11 tests covering optimal-hide / degraded-warning / unknown-info / `onTap` callback / non-interactive mode / ValueNotifier reactivity both directions). The `onTap` dialog wiring is deferred to a follow-up PR. v1.2 candidate: fold `TriggerCallIncoming*` into the badge once `RoleManager` is wired through `PermissionService`. |
| **v1.1g — `PACKAGE_USAGE_STATS` permission + rationale UX** | `c620cb0` | ✓ | 858 / 858 (850 prior + 8 new — 8 `UsageStatsService` unit tests) | `lib/services/usage_stats_service.dart` (SYS-086 / ADR-030). New `UsageStatsService` singleton — `isGranted()` (probe) + `openSettings()` (deep-link to Settings → Special access → Usage access). Abstract `UsageStatsSource` seam; production `_MethodChannelUsageStatsSource` talks to `doit/device_state`; test `ScriptedUsageStatsSource` is hand-driven. The `_ready` Completer is constructed with `..complete()` so production callers do not need to await `init()` first; `resetForTesting` is the only path that re-creates the gate. `PermissionService` gains `PermissionKind.usageStats`, `requestUsageStats()`, `refreshUsageStats()`. The probe is **fire-and-forget** from `init()` via `unawaited(_refreshUsageStatsAfterInit())` — mandatory because `MethodChannel.invokeMethod` returns a real Future that does NOT advance in a widget-test fake-async zone (the v1.1g diagnostic caught this when `calendar_picker_test.dart` hung at `await PermissionService.instance.init()`; the `unawaited` workaround is documented inline in `permission_service.dart`). `PermissionSheet` extends its sealed `PermissionKind` switch with `_meta` rationale copy + routes the "Allow" CTA to the deep-link (no system dialog). `Settings._PermissionTile._reProbe` calls `refreshUsageStats()` on `usageStats` (the only kind that re-probes rather than re-requests). Manifest entry `<uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" tools:ignore="ProtectedPermissions"/>` (cross-checked against the v0.1 permission baseline). Kotlin side (`DeviceStateChannel.kt`) gains `isUsageStatsGranted` (uses `AppOpsManager.unsafeCheckOpNoThrow(OPSTR_GET_USAGE_STATS, Process.myUid(), packageName)`) + `openUsageAccessSettings` (launches `Settings.ACTION_USAGE_ACCESS_SETTINGS` with `FLAG_ACTIVITY_NEW_TASK`). `test/services/usage_stats_service_test.dart` (8 tests covering isGranted (granted/denied/reflects-setGranted) + openSettings (resolved/not-resolved/multi-call recording) + singleton lifecycle (idempotent-init / resetForTesting-installs-new-source-resets-gate)). The `TriggerForegroundApp` leaf that will consume `PermissionKind.usageStats` is deferred to v1.2. |
| **v1.1h — i18n scaffolding (ARB extraction + `es` locale + `localizedApp` test helper)** | `43c1b0c` | ✓ | 879 / 879 (858 prior + 11 new l10n tests + 10 screen-test files migrated through `localizedApp`) | `lib/l10n/app_en.arb` (~60 keys — source-of-truth: AppBar titles, snackbars, empty states, settings sections, theme/anchor/reliability copy, permission tile labels, onboarding step labels, licenses/version rows, ICU plural in `homeSelectionAppBarTitle` / `homeSnackbarMarkedCount`). `lib/l10n/app_es.arb` (Spanish translation of every key — smoke-test locale for the codegen + delegate pipeline; NOT a professional translation, a v1.2 follow-up with a native Spanish speaker). New top-level `l10n.yaml` drives `flutter gen-l10n` (`arb-dir: lib/l10n`, `template-arb-file: app_en.arb`, `output-class: AppLocalizations`, `output-dir: lib/l10n/gen`, `nullable-getter: false`). `pubspec.yaml` adds `flutter_localizations: { sdk: flutter }` + `intl: any` + `flutter: { generate: true }`. Generated `lib/l10n/gen/app_localizations*.dart` produced by `flutter gen-l10n`. `lib/main.dart` wires `localizationsDelegates: AppLocalizations.localizationsDelegates` + `supportedLocales: AppLocalizations.supportedLocales`. `lib/screens/{home,settings,onboarding}.dart` read every user-facing string from `AppLocalizations.of(context)`; onboarding's `static const _steps = [...]` becomes `static List<_OnboardingStep> _buildSteps(AppLocalizations l) => [...]` because each entry now reads its title/body/cta from the ARB catalog; `_kStepCount = 5` stays `const` so the "Skip to last step" handler stays compile-time. New `test/support/localized_app.dart` helper (~30 lines) builds a `MaterialApp` with the generated delegates pre-installed so existing widget tests do not crash on `AppLocalizations.of(context)!`; 10 screen-test files route through it (`home_test.dart`, `settings_test.dart`, `settings_permissions_test.dart`, `settings_licenses_test.dart`, `settings_test_reminder_test.dart`, `settings_call_screening_tile_test.dart`, `onboarding_test.dart`, `onboarding_permission_wiring_test.dart`, `onboarding_call_screening_step_test.dart`, `integration/fresh_install_test.dart`). New `test/l10n/app_localizations_test.dart` (11 tests — 5 structural assertions on ARB catalogs (presence, `@@locale` headers, key-set parity across locales, ICU plural metadata), 4 widget tests (Spanish/English resolution, unsupported-locale fallback to English via `supportedLocales.first`, ICU plural branches in `es`), 2 class-API tests on `supportedLocales` / delegate types). The `_PermissionTile._reProbe` channel-call path still works because `flutter test` exercises it via the existing permissions-channel mock; the Settings screen retains its full test surface (the per-tile `requestX` taps, the permanentlyDenied → `openAppSettings` path, the `_CallScreeningTile` role probe, the `ThemeMode` radio / `AnchorMode` radio / `Reliability.optimal|degraded` copy, the backup-folder picker, the licenses route, the test-reminder snackbar). No permission baseline change, no manifest change, no `INTERNET` (ARB catalogs are in-tree, generated code is a Flutter SDK call). SYS-087 / ADR-031. The user's hands-on step (parallels v0.5e / v1.0h) is the on-device verification: pick Spanish in Android Settings → System → Languages, launch do it, confirm the AppBar / settings sections / onboarding steps render in Spanish. |
| **v1.1i — Custom app icon + splash (adaptive icon + brand color)** | `78b1267` | ✓ | 893 / 893 (879 prior + 9 new `app_icon_test` + 2 new `release_signing_test` + 3 v1.1h gap-filling tests) | Hand-authored adaptive-icon vectors replace the default Flutter launcher icon. Three new drawables in `android/app/src/main/res/drawable/`: `ic_launcher_background.xml` (solid brand purple `#FF6750A4`, 108dp × 108dp viewport), `ic_launcher_foreground.xml` (white sans-serif lowercase 'd' — stem x ∈ [23, 29], bowl centered on (54, 54) outer R = 25 inner R = 16, evenOdd fill carves the counter — plus check dot at (80, 80) R = 4, all inside the 66dp safe zone), `ic_launcher_monochrome.xml` (same glyph painted pure white for Android 13+ themed icons). New `mipmap-anydpi-v26/ic_launcher.xml` is the `<adaptive-icon>` entry point referencing all three layers; `AndroidManifest.xml:66`'s `android:icon="@mipmap/ic_launcher"` reference is unchanged (Android resolves to the adaptive-icon XML on API 26+ and to the legacy PNGs on API 21..25). The five `mipmap-{mdpi,hdpi,xhdpi,xxhdpi,xxxhdpi}/ic_launcher.png` legacy density buckets stay in place as the API 21..25 fallback (a v1.2 follow-up can regenerate them from the master vector). Both `drawable/launch_background.xml` and `drawable-v21/launch_background.xml` are rewritten as `<layer-list>` that references the new named color `@color/launch_background` (defined in `values/colors.xml` as `#FF6750A4`; AAPT2 rejects inline color values inside `drawable-v21/` `<item android:drawable>`) then layers the foreground vector centered on a 96dp × 96dp box; the `?android:colorBackground` reference in the API 21+ variant is dropped so the splash stays on-brand in both light and dark modes. Pre-existing `drawable/ic_streak_notification.xml` gap (`architecture_options.md:191-192`) closed — a monochrome white-on-transparent copy of the foreground glyph with the check dot dropped (unreadable at 24dp). Version bumped `1.0.0+7` → `1.1.0+8` in `pubspec.yaml` + `lib/build_info.dart` (the mirror-pin assertions in `test/release_signing_test.dart` update in lockstep; two new tests pin the manifest icon reference + `app_name` + `kAppVersion` together). New `test/app_icon_test.dart` (9 filesystem tests — adaptive-icon manifest presence + three-layer references, foreground white-on-transparent (no root fillColor), background brand purple, monochrome pure white (no brand purple), notification icon shape and 24dp size with dot dropped, all five legacy density buckets present, both splash drawables reference `@color/launch_background` + centered foreground, `values/colors.xml` defines `launch_background` as the brand purple). **Bundled platform maintenance** (single-commit per user direction): `android/app/build.gradle.kts` compileSdk 34 → 36 (required by `file_picker` / `flutter_plugin_android_lifecycle` transitive AARs) + minSdk 28 → 30 (in lockstep — `CallScreeningService.CallResponse.Builder` requires API 30+); `CallInterceptor.kt` migrates from the removed `Call.Response.Builder` to `CallScreeningService.CallResponse.Builder`; `MainActivity.kt` passes the Activity explicitly via `setActivity(this / null)` because `FlutterEngine.activity` was removed in the modern embedding (the screening-role request needs `startActivityForResult` on a live Activity). **Bundled v1.1h gap-filling** (single-commit per user direction): `test/integration/app_localizations_wiring_test.dart` (1 integration test — pumps the production `DoItApp` root and asserts the `AppLocalizations` delegate + `[en, es]` `supportedLocales` are wired on the returned `MaterialApp`, closing the gap the v1.1h author flagged in `test/l10n/app_localizations_test.dart`); `test/screens/onboarding_test.dart` gains `all five ARB step titles are reachable in order` (walks all 5 steps and asserts each `onboardingStep{X}Title` is visible at the right moment + a distinctness + non-empty guard on the 5 ARB keys); `test/screens/settings_test.dart` gains `theme tile renders all three ARB labels and tapping each updates SettingsService.themeMode` (asserts all three ARB labels render and exercises the System → Light → Dark cycle through the ARB-driven `_ThemeModeTile`). No new permissions, no `INTERNET`. SYS-088 / ADR-032. The user's hands-on step (parallels v0.5e / v1.0h / v1.1h) is `flutter build appbundle --release` (asks first per CLAUDE.md) → `adb install -r build/app/outputs/bundle/release/app-release.aab` (asks first) → visual checks: app drawer shows the 'd' + dot icon on the brand purple background, masked into a circle (Pixel launcher) / squircle (Samsung) / teardrop (Xiaomi); Settings → Apps → do it → Icon shows the adaptive icon previews (background + foreground layers visible separately); splash on cold start: brand purple flash with the 'd' icon centered for ~100ms before the home screen draws; status-bar notification (Settings → Test reminder): white 'd' glyph, no dot; on Android 13+: Settings → Wallpaper & style → Themed icons → enable, the launcher icon re-tints against the wallpaper palette. |
| **v1.1j — sign-off + CHANGELOG `[1.1.0]`** | `222f860` *(bundled into the v1.1i debug-signed-APK release per user direction — no separate v1.1j commit was created; the CHANGELOG `[1.1.0]` block and `plan.md` milestone-8 flip landed inside the `release(v1.1i)` commit)* | ✓ | 893 / 893 (no new tests; doc-only + CHANGELOG pass) | `CHANGELOG.md` now contains a `## [1.1.0] — 2026-06-21 — Polish + expansion` block with nine sub-entries (`### v1.1a` through `### v1.1i`) covering the headline themes: **routines** (RoutineConfig value class + per-template persistence, SYS-080 / ADR-025; RoutineExecutor dispatch + reactive settings; ActionOpenApp + RoutineOpenAppRequest + RoutineBanner, SYS-082 / ADR-026; generic RoutineApplyScreen for templates #17..#21, SYS-083 / ADR-027), **location** (offline LocationMapPreview for LocationPicker, SYS-084 / ADR-028), **reliability** (per-automation reliability badges, SYS-085 / ADR-029; PACKAGE_USAGE_STATS permission + rationale UX, SYS-086 / ADR-030), **i18n** (ARB extraction + `es` locale + localizedApp test helper, SYS-087 / ADR-031), and **branding** (custom launcher icon + splash, SYS-088 / ADR-032). The pre-existing `### v1.1i` entry under `## [Unreleased]` is moved into the new `[1.1.0]` block (the `[Unreleased]` block now contains only `### v1.0/Phase A` / `### v1.0/Phase E` / `### v1.0/Phase C` / `### v1.0/Phase D` / `### v1.0/Phase B` / `### v1.0/Phase F` content, untouched). `plan.md` Milestone 8 (v1.1) flipped from `stub` to `shipped` with the closing date 2026-06-21. No code changes, no new tests, no new permissions, no `INTERNET`, no `flutter pub` changes. v1.1k is the user's hands-on on-device verification (parallels v0.5e / v1.0h / v1.1h): run the visual checks listed in the v1.1h + v1.1i + v1.1j CHANGELOG entries (Spanish locale → app re-renders in Spanish; brand-purple launcher icon + 'd' glyph + check dot; on-brand splash; status-bar notification icon). |
| **v1.2a — Phase-1 trigger-set baseline** | `_doc-only stub_` | ✓ | n/a (doc-only) | `lib/routines/trigger_set.dart` — pure `Set<Trigger>` value class with structural equality + set-of-set union/intersection helpers. The Phase-2 → Phase-6 routines engine consumes this as its "what triggers does this routine listen to" shape. Doc-only baseline stub — no SYS- ID is assigned to the value class itself; the `SYS-099` ActionFullscreen + `SYS-100` ActionCallIntercept wiring in v1.2f is the work that consumes this type. |
| **v1.2b — Phase-2 condition-predicate baseline** | `_doc-only stub_` | ✓ | n/a (doc-only) | `lib/routines/condition_predicate.dart` — pure `Condition` sealed family extended with `_PredicateCondition(predicate: (AutomationSnapshot) -> bool)` leaf so the engine can defer the predicate evaluation to the widget layer (where the snapshot lives). Doc-only baseline stub — no SYS- ID is assigned to the value class itself. |
| **v1.2c — `TriggerForegroundApp` leaf + `PermissionKind.callScreening`** | `e60597c` | ✓ | 917 / 917 (909 prior + 8 new — 5 `TriggerForegroundApp` model tests + 3 `PermissionKind.callScreening` enum tests) | `lib/routines/trigger.dart` extends the sealed `Trigger` hierarchy with `TriggerForegroundApp(packageName)` (the v1.1f deferred leaf — fires when the user opens a foreground app). `lib/services/permission_service.dart` extends `PermissionKind` with `callScreening` (the v1.1f deferred fold-in target for `RoleManager.isRoleHeld(ROLE_CALL_SCREENING)`). The `_requiredPermissionForTrigger` map stays at `null` for `TriggerForegroundApp` (the v1.2h reliability dialog will fold in `PermissionKind.usageStats` once the arm lands — see v1.2g). |
| **v1.2d — `PauseService._ready` eager-complete + `PositionSource.dispose` contract** | `2a0a5a7` | ✓ | 925 / 925 (917 prior + 8 new — 5 `PauseService` lifecycle tests + 3 `PositionSource.dispose` contract tests) | `lib/services/pause_service.dart` — the `_ready` Completer is constructed with `..complete()` (mirrors the v1.1g `UsageStatsService` pattern) so production callers do not need to await `init()` first; `resetForTesting` is the only path that re-creates the gate. `lib/services/geofence_service.dart` — `PositionSource.dispose()` is now an explicit contract (`Future<void> dispose()`) and the `StreamSubscription` is cancelled inside it (was a silent leak before v1.2d; caught by `test/services/geofence_service_test.dart` when the test framework's leaked-stream detector tripped). |
| **v1.2e — Alarm fire → notification render path wire-up** | `33b08f3` | ✓ | 936 / 936 (925 prior + 11 new per CHANGELOG 3-gate at this commit — `00:27 +936: All tests passed!`) | `lib/reminders/notification_service.dart` (SYS-098 / ADR-033). The `show(ReminderEvent event)` path that was scaffolded in v0.4b is wired: `AlarmScheduler` no longer calls `flutter_local_notifications` directly; it goes through `NotificationService.show`, which builds the `AndroidNotificationDetails` (channel id `doit.reminders`, importance high, the new monochrome `ic_streak_notification` icon, the "Done" / "Open" action per habit mode), then calls `flutterLocalNotificationsPlugin.show(event.id, title, body, details)`. `NotificationService.dismiss(AlarmId id)` is the cancel path (mirrors the show path). The `RoutineExecutor._dispatchAction(Action)` exhaustive switch is unchanged; the alarm-side half of the routine path closes the Phase-5 wiring. **SYS-098 + ADR-033 appended.** |
| **v1.2f — `ActionFullscreen` + `ActionCallIntercept` + `Person.pauseUntil` + `DoFixed.weekday` display** | `b4089b4` | ✓ | 936 / 936 (no test-count delta from v1.2e per CHANGELOG 3-gate at this commit) | `lib/routines/action.dart` extends `Action` with `ActionFullscreen(route)` (SYS-099 / ADR-034) and `ActionCallIntercept(personId)` (SYS-100 / ADR-034) leaves. `lib/people/person.dart` gains `pauseUntil: DateTime?` (SYS-101) and `PersonRepository` round-trips it through a v5_to_v6 Drift migration; `AddPersonScreen` shows a Pause section (visible ONLY after a contact is picked) that opens a date picker, and the cadence scheduler skips `pauseUntil`-active persons. `lib/do/do_description.dart` adds the pure `String describeDo(Do h)` function so a `DoFixed` habit's home-screen tile subtitle renders `"<weekday-set> · HH:MM"` (SYS-102) — the v0.1 baseline only showed the time; v1.2f makes the days-of-week visible at a glance. `RoutineExecutor._dispatchAction` exhaustive switch grows two more leaves; the test coverage is exhaustive across all 7 Action leaves now. **SYS-099, SYS-100, SYS-101, SYS-102 + ADR-034 appended.** |
| **v1.2g — BOOT_COMPLETED coverage confirmation + calendar-trigger reliability badge coverage closeout** | `a31331c` | ✓ | 936 / 936 (no test-count delta from v1.2f — v1.2g is a doc-only Phase-7 closeout per its CHANGELOG entry title) | `android/app/src/main/kotlin/.../BootReceiver.kt` — the receiver that re-schedules all pending alarms from the local DB on `BOOT_COMPLETED` / `LOCKED_BOOT_COMPLETED` / `MY_PACKAGE_REPLACED` is now exhaustive over all 3 receivers (was missing the `MY_PACKAGE_REPLACED` arm in v0.4b; v1.2g adds it and pins it in `test/reminders/reboot_survival_test.dart`). The Phase-7 calendar-trigger reliability badge coverage (the v1.1f deferred fold-in for `TriggerCalendarEvent*`) is closed out in the same doc commit. `B9` — Android home-widget re-arm indicator — is **explicitly deferred** to a v1.x follow-up (the project does not yet ship a home widget; tracking lives in `feature.md` §2.8). |
| **v1.2h — `AutomationReliabilityDialog` on tap (per-automation)** | `6e47be7` | ✓ | 925 / 925 (936 prior − 11 per the v1.2h CHANGELOG entry — the badge-dialog rewrite consolidated prior badge-only widget tests into fewer dialog tests) | `lib/widgets/automation_reliability_dialog.dart` (SYS-103 / ADR-035). The dialog renders the trigger's required permission (if any) + current `PermissionResult` + rationale copy (read from `AppLocalizations`) + an "Open settings" CTA that deep-links to the relevant OS settings screen. Trigger-side only — action-side permission disambiguation (the `ActionOverrideSilent` → `ACCESS_NOTIFICATION_POLICY`, the contact-requiring actions → `READ_CONTACTS`) is **explicitly deferred** to a v1.x follow-up (see `feature.md` §2.2). The `TriggerCallIncoming*` reliability arm is **also deferred** until `PermissionService.callScreening` is fully probed (see `feature.md` §2.3). **Update v1.3b / Phase 25:** the `TriggerCallIncoming*` → `PermissionKind.callScreening` arm is now folded in via `lib/routines/automation_reliability.dart`'s `_requiredPermissionForTrigger` exhaustive switch. PR #27 (v1.5b / Phase 25) closed the §2.2 follow-up as well; the dialog renders both sides today. See `decision_record.md` ADR-035 for the original ADR. |
| **v1.2i — `PermissionLifecycleReProbe` + `PermissionService.refresh()`** | `f0990fe` | ✓ | 984 / 984 (925 prior + 59 new per CHANGELOG 3-gate at this commit — `00:22 +984: All tests passed!`) | `lib/services/permission_service.dart` (SYS-104 / ADR-036). New `refresh()` method that re-probes every `PermissionKind` and emits a fresh `statuses` snapshot to all `ValueListenable`s; the `PermissionSheet` re-renders its tiles when `refresh()` resolves. `lib/app/app_lifecycle_observer.dart` — new `WidgetsBindingObserver` that calls `PermissionService.refresh()` on `AppLifecycleState.resumed` AFTER the cold-launch first `resumed` (which is a no-op because the initial probe in `main()` already ran). Re-enters the "optimal" badge state when the user toggles a permission in OS settings and comes back to the app. |
| **v1.2j — DST transition banner + streak-recovery card + pre-notification 5-min/1-min heads-up** | `d80b9f1` | ✓ | 934 / 934 (984 prior − 50 per CHANGELOG 3-gate at this commit) | `lib/widgets/dst_transition_banner.dart` (SYS-105 / ADR-037) — shows on the home screen when the device's local zone enters or exits DST within the next 24 h; renders `SizedBox.shrink()` when the adjustment list is empty (zero layout cost in the steady state). `lib/widgets/streak_recovery_card.dart` (SYS-106 / ADR-038) — shows on the home screen when a habit's consecutive-run counter reports 3+ missed days; renders nothing when `missedDays < 3` (defensive against a stale snapshot). `lib/reminders/reminder_service.dart` (SYS-107 / ADR-039) — `schedulePreAlarms({alarmId, fireAt, now})` enqueues a 5-min heads-up when the lead time `> 5 * 60 s` and a 1-min heads-up when `> 60 s`; `cancelPreAlarms(alarmId)` forwards to the bridge so a cancelled habit leaves no dangling pre-alarms. |
| **v1.2k — Hard-delete affordance on edit screen** | `7bb126e` | ✓ | 926 / 926 (934 prior − 8 per CHANGELOG 3-gate at this commit) | `lib/screens/add_habit.dart` (SYS-108) gains a `Delete…` popup-menu entry ONLY in edit mode (`widget.habitId != null`); new-do mode has no menu entry at all. Tapping it opens an `AlertDialog` titled `Delete "<name>"?` with destructive copy `"This will remove the do and its completion log. This cannot be undone."` and two actions: `Cancel` (no-op pop) and `Delete` (pop `true`). After the dialog pops with `true`, the screen calls `DoRepository.instance.deleteById(habitId)` and pops the route with `true` so the caller (`HomeScreen._onTileTap`) can refresh the home list immediately. A `deleteById` failure is caught and surfaced as a `Delete failed. Please try again.` snackbar; the screen stays mounted so the user can retry. |
| **v1.2l — Shared `MissionWrongAttempts` module (uniform 3-wrong take-a-break across Math + Type)** | `6f83d47` | ✓ | 931 / 931 (926 prior + 5 new per CHANGELOG 3-gate at this commit) | `lib/missions/mission_attempts.dart` (SYS-109 / ADR-040). New shared module that owns the "max wrong attempts before take-a-break" policy; the policy is per-mission-mode (Math = 3, Type = 3, Shake / Hold / Memory = N/A). The Math + Type missions that were each tracking their own wrong-attempt counter now consume the shared module; the take-a-break `SnackBar` + the 60-second `Timer` is one implementation. The opt-in pattern (Shake / Hold / Memory have no "wrong attempt" notion) is the future-proof shape for similar shared modules — see `feature.md` §3.3 lesson L4. |
| **v1.2m — `CompletionLogSection` for review + undo** | `8b3f83e` | ✓ | 1001 / 1001 (931 prior + 70 new per CHANGELOG 3-gate at this commit — `00:24 +1001: All tests passed!`) | `lib/widgets/completion_log_section.dart` (SYS-110 / ADR-041). New widget rendered on the edit-habit screen (`AddHabitScreen` in edit mode) that lists the most-recent `kCompletionLogSectionMaxRows = 30` completions for the habit, newest-first, sorted by `completedAtMillis` descending. Each row shows the local-day (`YYYY-MM-DD`), the local-time (`HH:MM`), and the `source` (manual / notification / mission / rest_day). A trailing delete icon opens an `AlertDialog` titled `Delete this completion?`; a confirmed delete calls `CompletionLogService.instance.deleteById(row.id)`. Older rows beyond the 30-cap surface the text `"N older entries are hidden."` below the list. |
| **v1.2 — sign-off + CHANGELOG `[1.2.0]`** | `1285746` (doc close-out) + `8684a6e` (version bump) | ✓ | 1001 / 1001 (no new tests; doc-only + CHANGELOG pass + version bump) | `CHANGELOG.md` now contains a `## [1.2.0] — 2026-06-23 — Code-TODO closure` summary block referencing the 11 v1.2c..v1.2m sub-entries that landed in the cycle (the v1.2a + v1.2b doc-only baseline stubs are tracked in `implementation_status.md` only and have no CHANGELOG sub-entry). The headline themes: **wire-up** (`NotificationService.show` / `dismiss` path v1.2e; the routine `Action` leaves `ActionFullscreen` + `ActionCallIntercept` v1.2f; BOOT_COMPLETED coverage confirmation v1.2g), **UX completeness** (Person pauseUntil UI v1.2f, DoFixed weekday display v1.2f, DST transition banner v1.2j, streak-recovery card v1.2j, pre-notification 5-min / 1-min heads-up v1.2j), **reliability disambiguation** (per-automation `AlertDialog` on tap v1.2h, `AppLifecycleState.resumed` re-probe hook v1.2i), **edit affordances** (hard delete with confirm v1.2k, completion-log review + undo v1.2m, uniform 3-wrong take-a-break across Math + Type v1.2l). The pre-existing v1.2 sub-entries (v1.2e through v1.2m) remain under `## [Unreleased]` in alphabetic order (the project convention is "summary in `[1.2.0]`, detail in `[Unreleased]`" — the `[1.2.0]` block points to them). The pre-existing `### v1.2l` / `### v1.2m` CHANGELOG conflict at lines 1206-1303 is resolved (the duplicated 3-gate blocks are collapsed into a single consolidated block at the end of the v1.2 cycle). `plan.md` Milestone 9 (v1.2) flipped from `stub` to `shipped` with the closing date 2026-06-23. `pubspec.yaml` bumped `1.1.0+8` → `1.2.0+9` (commit `8684a6e`); `lib/build_info.dart` mirrors (`kAppVersion = '1.2.0'`, `kAppVersionCode = 9`); `test/release_signing_test.dart` mirror-pin assertions updated in lockstep. `docs/v_model/v1_2_release_baseline.md` + `docs/v_model/v1_2_release_checklist.md` are added as the left-side baseline + right-side gate. `decision_record.md` ADR-033..ADR-041 are appended. `requirements.md` SYS-098..SYS-110 are appended (SYS-098..SYS-104 in earlier PRs; SYS-105..SYS-107 in earlier PRs; SYS-108 / SYS-109 / SYS-110 in this sign-off commit). No code changes, no new tests, no new permissions, no `INTERNET`, no `flutter pub` changes. v1.2x is the user's hands-on on-device verification (parallels v0.5e / v1.0h / v1.1h / v1.1k): run the visual + interaction checks listed in `v1_2_release_checklist.md` (delete-confirm flow, pause UI cycle, automation reliability dialog, 3-wrong take-a-break, DST banner, completion-log undo, pre-notification heads-up). |
| **v1.3a — Monthly stats + per-do grace factory (Phase 12)** | `a4a5393` (PR #18) | ✓ | 1019 / 1019 (999 prior + 20 new per CHANGELOG 3-gate at this commit — `Tests: 999 -> 1019 (+20). 3-gate green.`) | `lib/do/do.dart` (sealed base) gains `Do.effectiveStreakConfig({required SkipBudget skipBudget})` — returns `StreakConfig(graceWindow: graceWindowOverride ?? kDefaultGraceWindow, skipBudget: skipBudget)`. `lib/do/consecutive_counter.dart` gains the top-level `kDefaultGraceWindow = Duration(hours: 3)` constant (single source of truth, replaces 3 hard-coded literals). `graceWindowOverride` + `copyWith` plumbing threaded through all 5 `Do` subclasses. `lib/screens/stats.dart` — 30-day count + signed MoM delta (`+N%` / `-N%` / `0%` / `new` when prior 30 days had zero completions), `'On time: N%'` completion rate, 7-bar `_Last7DaysChart` with per-bar `Semantics`, frozen `asOf` reference time. `lib/screens/settings.dart` — new `_StatsTile` between Reliability and Backup. **SYS-111 appended.** |
| **v1.3b — Unified reliability source-of-truth (Phase 13)** | `e9fc922` (PR #19) | ✓ | 1032 / 1032 (1019 prior + 13 new — 10 reliability-service + 3 scheduler-rel rewritten per CHANGELOG 3-gate at this commit) | `lib/services/reliability_service.dart` (NEW) — singleton with `Stream<Reliability>` getter, `ValueListenable<Reliability>` mirror, synchronous `value` getter, merges bridge probe + `PermissionService.statuses` listener. Initial value `Reliability.optimal` (closes v1.3a first-read race). `lib/services/platform_alarm_scheduler.dart` — `reliability` getter is now a thin pass-through `ReliabilityService.instance.value`; `_cachedReliability` / `_refreshReliability` / `clearReliabilityCache` removed; `kReliabilityCacheTtl` re-exported from the new module. `lib/widgets/reliability_banner.dart` — new `ReliabilityBanner.fromStream({VoidCallback? onTap})` factory wraps the service notifier in a `ValueListenableBuilder`; `fromService` is a `@Deprecated` shim. `lib/screens/settings.dart` — `_ReliabilityRow` wraps its `ListTile` in `ValueListenableBuilder<Reliability>` bound to the service. `lib/app/app_lifecycle_observer.dart` — resume hook also calls `ReliabilityService.instance.refresh()`. `lib/main.dart` awaits `ReliabilityService.init(bridge:, permissionService:)` after the lifecycle observer. **SYS-112 + ADR-042 appended.** |
| **v1.3b — `TriggerCallIncoming*` reliability arm closeout (docs)** | `a054937` (PR #28) | ✓ | _no test count delta_ | Doc-only closeout: the `TriggerCallIncoming* → PermissionKind.callScreening` arm is now folded in via `lib/routines/automation_reliability.dart`'s `_requiredPermissionForTrigger` exhaustive switch + `PermissionService.callScreening` is fully probed through `refreshCallScreening()` + `_refreshCallScreeningAfterInit()` + the `refresh()` cascade (all landed in v1.2c/v1.2i; the arm was the only missing wire). This PR closes `feature.md` §2.3 (the v1.2 baseline's explicit deferral). `implementation_status.md` v1.2h row is annotated with the same v1.3b update line; `decision_record.md` ADR-035 is annotated with a "Update v1.3b" block; `v1_2_release_baseline.md` §5 is annotated with the shipped-in-PR-#28 note. No code changes, no new tests, no new permissions. |
| **v1.3c — `USE_FULL_SCREEN_INTENT` probe + reliability wiring (Phase 14)** | `4c721af` (PR #20) | ✓ | 1047 / 1047 (1032 prior + 15 new — 8 FSI-service + 3 permission-service + 1 reliability-service + 2 settings-permissions + 1 reliability-banner per CHANGELOG 3-gate at this commit) | `lib/services/permission_service.dart` — `PermissionKind.fullScreenIntent` joins the enum (after `callScreening`, before `backupFolder`); opt-in, ADR-030 precedent. `lib/services/full_screen_intent_service.dart` (NEW) — singleton wrapping the `doit/full_screen` MethodChannel (mirrors `UsageStatsService` shape; ADR-013 swallows). `lib/services/permission_kind_meta.dart` — `title: 'Full-screen access'`, `icon: Icons.open_in_full`, rationale copy. `lib/services/permission_service.dart` — `requestFullScreenIntent()` deep-links via `openSettings()`; `refreshFullScreenIntent()` re-probes + merges into `statuses`. `fullScreenIntent` joins `_kReliabilityGatedKinds` (now 5). `lib/screens/settings_permissions.dart` — 5th `_PermissionTile` with the localized title. `lib/widgets/reliability_banner.dart` — `fromStream` wires `onTap` callback. `android/app/src/main/AndroidManifest.xml` declares `<uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" tools:ignore="ProtectedPermissions" />`. **SYS-113 + ADR-043 appended.** |
| **v1.3d — Full-screen activity launch path (Phase 15)** | `5eca37b` (PR #21) | ✓ | 1064 / 1064 (1048 prior + 16 new: 6 fsi-service + 6 launcher + 4 overlay per CHANGELOG 3-gate at this commit) | `android/app/src/main/kotlin/com/doit/FullScreenActivity.kt` (NEW) — `FlutterActivity` subclass with lockscreen-bypass window flags (`FLAG_SHOW_WHEN_LOCKED | FLAG_TURN_SCREEN_ON | FLAG_DISMISS_KEYGUARD | FLAG_KEEP_SCREEN_ON`) + `getInitialRoute()` query-string encoding. `FullScreenIntentChannel.kt` grows two new `when` arms (`showHabitMission`, `showRoutineOverlay`). `MainActivity.buildReminderNotification` splits the strong-mode branch: targets `FullScreenActivity` + `setFullScreenIntent(openPi, true)`. `AndroidManifest.xml` declares the new `<activity>` (exported=false, singleTask, taskAffinity="", excludeFromRecents, lockscreen-bypass attrs, LaunchTheme). `lib/screens/mission_launcher.dart` (NEW, SYS-114) — chain-level orchestrator that loads the habit by id, iterates the `MissionChain`, runs the executor, and appends the completion on `ChainPassed`. `lib/screens/routine_overlay_screen.dart` (NEW) — banner widget for routine-fired overlays. `lib/main.dart` gains `MaterialApp.onGenerateRoute` mapping `/mission` to `MissionLauncherScreen` or `RoutineOverlayScreen` by `mode=...`. `lib/services/platform_full_screen_intent.dart` gains `Future<LaunchIntent?> getLaunchIntent()` + `_safeResult<T>` defense-in-depth wrapper. `lib/reminders/full_screen_intent.dart` gains `LaunchIntent` + `LaunchMode` + the interface read; `FakeFullScreenIntent` records `launchIntents` (additive). **SYS-114 + ADR-044 appended.** No new `<uses-permission>` (the v1.3c `USE_FULL_SCREEN_INTENT` baseline covers the launch path); no `wakelock_plus` (the activity-level `FLAG_KEEP_SCREEN_ON` is sufficient). |
| **v1.3 — sign-off + CHANGELOG `[1.3.0]`** | _this PR_ (commit hash on merge) | ✓ | 1064 / 1064 (no new tests; doc-only + CHANGELOG pass + version bump) | `CHANGELOG.md` now contains a `## [1.3.0] — 2026-06-25 — Reliability + lifecycle hardening` summary block referencing the four v1.3a..v1.3d sub-entries that landed in the cycle. The headline themes: **stats-side groundwork** (v1.3a — monthly 30-day completion-rate + MoM delta + 7-day bar chart + `Do.effectiveStreakConfig(...)` factory + `kDefaultGraceWindow` constant + Settings → Stats tile), **reliability unification** (v1.3b — `ReliabilityService.instance` as the unified `Stream<Reliability>` source-of-truth; `PlatformAlarmScheduler.reliability` is a thin pass-through; `ReliabilityBanner.fromStream` is the new factory; `_ReliabilityRow` binds to the service; `AppLifecycleState.resumed` re-probe extends to the new service), **special-access gating** (v1.3c — `PermissionKind.fullScreenIntent` joins the gated set; 5th `_PermissionTile` in Settings → Permissions; home banner's `onTap` deep-links the user to the tile; manifest declares `<uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" tools:ignore="ProtectedPermissions" />`), **strong-mode interruption end-to-end** (v1.3d — `FullScreenActivity` Kotlin class with lockscreen-bypass window flags; launch handlers on `doit/full_screen`; `setFullScreenIntent(openPi, true)` on strong-mode notifications; `MissionLauncherScreen` chain-level orchestrator). The pre-existing v1.3 sub-entries (v1.3a through v1.3d) remain under `## [Unreleased]` (the project convention is "summary in `[1.3.0]`, detail in `[Unreleased]`" — the `[1.3.0]` block points to them). `plan.md` Milestone 10 (v1.3) flipped from `stub` to `shipped` with the closing date 2026-06-25. `pubspec.yaml` bumped `1.2.0+9` → `1.3.0+10`; `lib/build_info.dart` mirrors (`kAppVersion = '1.3.0'`, `kAppVersionCode = 10`); `test/release_signing_test.dart` mirror-pin assertions updated in lockstep (3 tests renamed from "(v1.2)" to "(v1.3)"). `docs/v_model/v1_3_release_baseline.md` + `docs/v_model/v1_3_release_checklist.md` are added as the left-side baseline + right-side gate. `decision_record.md` ADR-042..ADR-044 are appended. `requirements.md` SYS-112..SYS-114 are appended (SYS-112 in v1.3b; SYS-113 in v1.3c; SYS-114 in v1.3d). No code changes, no new tests, no new permissions, no `INTERNET`, no `flutter pub` changes. v1.3x is the user's hands-on on-device verification (parallels v0.5e / v1.0h / v1.1h / v1.1k / v1.2x): run the visual + interaction checks listed in `v1_3_release_checklist.md` (reliability banner state from the unified service, FSI tile deep-link, strong-mode schedule-and-lock, routine overlay). |
| **v1.3d — Light-theme icon variant for the FSI tile (feature.md §2.7)** | `2cb8e4e` (PR #30) | ✓ | 1049 / 1049 (1047 prior + 2 new widget tests in `settings_permissions_test.dart`) | `lib/screens/settings.dart` — the `_PermissionsRow` builder branches the FSI tile's leading icon on `Theme.of(context).brightness`. On `Brightness.light` the tile renders `Icons.open_in_full_outlined` (matches every other permission tile which uses `_outlined` variants — the missing symmetry §2.7 called out); on `Brightness.dark` the tile keeps the filled `Icons.open_in_full` (better contrast on the dark surface). The `permissionKindMeta[PermissionKind.fullScreenIntent].icon` (consumed by `PermissionSheet` + `AutomationReliabilityDialog`) is unchanged — those surfaces do not branch on brightness and the meta is the canonical permission glyph. `test/screens/settings_permissions_test.dart` — 2 new widget tests pin both branches (filled on dark, outlined on light) by inspecting the leading `Icon` widget's `codePoint` + `fontFamily` via `find.byWidgetPredicate` (the trailing chevron is also an `Icon` widget so the predicate filters by the `open_in_full*` codePoint family). The `_wrap` helper gains a `brightness: Brightness` parameter (default `Brightness.dark` — preserves the existing test surface). Closes `feature.md` §2.7 (the last v1.2 baseline follow-up that was a code change — the rest are doc-only or already shipped via PRs #27/28/29). No new permissions, no `INTERNET`, no `flutter pub` changes. `dart format` clean, `flutter analyze --fatal-infos` clean. Right-side gate: visual check on a light-theme device — Settings → Permissions → "Full-screen access" tile renders the outlined glyph. |
| **v1.3d — Regenerate legacy launcher PNGs from the master vector (feature.md §2.6)** | `b1a319d` (PR #31) | ✓ | 1048 / 1048 (1047 prior + 1 new test in `app_icon_test.dart`) | `tool/regen_launcher_icons.py` (NEW) — Pillow-based, idempotent script that regenerates the 5 API 21..25 launcher icon density buckets (`mipmap-{mdpi,hdpi,xhdpi,xxhdpi,xxxhdpi}/ic_launcher.png`) from the v1.1i / ADR-032 master vector. The script renders the brand-purple canvas (`#FF6750A4`, mirrors `lib/theme/app_theme.dart`'s `streakSeed`) + the 'd' stem rectangle + outer bowl ellipse + inner counter ellipse + check dot ellipse — the same shape geometry used by `mipmap-anydpi-v26/ic_launcher.xml`'s `<adaptive-icon>` foreground layer. Density bucket sizes match the standard Android launcher icon dimensions (48 / 72 / 96 / 144 / 192 px). `android/app/src/main/res/mipmap-{mdpi,hdpi,xhdpi,xxhdpi,xxxhdpi}/ic_launcher.png` — 5 regenerated binary files (the prior content was the default Flutter blue 'F' from the v0.1 scaffold, never updated when v1.1i shipped the adaptive-icon vectors). `test/app_icon_test.dart` — 1 new test pins the PNG signature + IHDR width/height for all 5 buckets so a half-written regen is caught at CI (the test parses the IHDR chunk manually to avoid pulling `package:image` for a single assertion). No new permissions, no `INTERNET`, no `flutter pub` changes (Pillow is a dev-only tool dependency; the script is not in the runtime pubspec). `dart format` clean, `flutter analyze --fatal-infos` clean. Closes `feature.md` §2.6. Right-side gate: visual check on an API 21..25 device (or via `flutter build appbundle --release` + `aapt2 dump badging` on the resulting AAB) — the launcher icon shows the brand-purple 'd' + check dot on devices that do not support adaptive icons. |
| **v1.3d — CI grep rejects network calls in production code (SYS-026)** | `d0ad834` (PR #32) | ✓ | 1048 / 1048 (1047 prior + 1 new test in `ci_workflow_test.dart`) | `.github/workflows/ci.yml` — new `Reject network calls in production code (SYS-026)` step in the `quality` job. The step greps `lib/` + `android/app/src/main/` for `import 'package:http`, `Uri.http(s)(`, and `HttpClient()` (the patterns called out in `.claude/rules/lib-services.md` "No network calls"); benign `import 'dart:io'` lines (for `File`, `Directory`, `Platform`, `Process`) are filtered via a second-pass `grep -v` so only real HTTP usage fails. The grep excludes `build/`, `.dart_tool/`, `tool/` (design-time scripts), `test/` (the dev-only test harness SYS-026 explicitly whitelists), and `.git/`. `test/ci_workflow_test.dart` — 1 new test pins the step's existence, the SYS-026 ID reference, the `package:http` + `Uri.https?(` patterns, and the `test/` + `tool/` exclusion list. Closes the SYS-026 verification gap (the contract was a code-review discipline only since v0.1). No new permissions, no `INTERNET`, no `flutter pub` changes. `dart format` clean, `flutter analyze --fatal-infos` clean. |

## How to update this file

When you finish a phase, change its status from "Not started" to
"Done" and add a one-line note (e.g., "Phase 1 done on
2026-06-15; tests in test/habits/"). When you start a new phase,
add a new row to the implementation table and a new entry to the
phase plan.

Do not delete rows. The history of "what was done when" is part of
the V-Model audit trail.

| **v1.4a — Android home-screen widget (Phase 28)** | _this PR_ (commit hash on merge) | ✓ | 1130 / 1130 (1064 prior + 66 new — 8 widget-state-builder + 11 widget-bridge + 5 widget-state-cache + 4 widget-state-locator + 6 widget-service + per-test re-organizations across `widget_state_locator_test.dart` / `widget_bridge_test.dart` (was mockito-based, refactored to hand-rolled fakes per project convention) + the 4 linter-driven date / rest-days tweaks across the test files) | `android/app/src/main/kotlin/com/doit/DoitWidgetProvider.kt` (NEW) — `AppWidgetProvider` subclass; `onUpdate` reads `WidgetStateCache.cachedFromPrefs(ctx)` FIRST for cold-start fallback then attempts a Dart round-trip via `WidgetUpdater.refreshIds(ctx, ids)`; `onEnabled` + `onDisabled` lifecycle hooks; `onReceive` dispatches `ACTION_REFRESH_WIDGET` + `ACTION_MARK_DONE`. `android/app/src/main/kotlin/com/doit/WidgetChannel.kt` (NEW) — `object` mirroring `FullScreenIntentChannel.kt` shape with `attach(engine)` / `detach()` / `setAppContext(ctx)`; dispatches `snapshot` + `cacheSnapshot` + `markDone` MethodChannel calls. `android/app/src/main/kotlin/com/doit/WidgetUpdater.kt` (NEW) — boots a one-shot `FlutterEngine` via `FlutterEngineCache`, applies the cached `RemoteViews`, then triggers a Dart round-trip. `android/app/src/main/kotlin/com/doit/WidgetRenderer.kt` (NEW) — `render(ctx, state)` / `renderEmpty(ctx)` / `renderError(ctx, reason)`; reliability-string-to-icon mapping (`optimal`→`ic_widget_optimal`, `degraded`→`ic_widget_degraded`, `unknown`→`ic_widget_unknown`). `android/app/src/main/kotlin/com/doit/WidgetStateCache.kt` (NEW) — Kotlin-side `SharedPreferences` at `doit_widget` prefs, key `doit.widget.cached_v1`; mirrors the Dart `WidgetStateCache` so the widget is never blank between OS process-kill and first Dart frame. `android/app/src/main/AndroidManifest.xml` — new `<receiver android:name=".DoitWidgetProvider" android:exported="false">` block with `APPWIDGET_UPDATE` intent-filter + `@xml/doit_widget_info` meta-data. `android/app/src/main/res/xml/doit_widget_info.xml` (NEW) — 4×2 cell, `updatePeriodMillis=1800000`, `targetCellWidth=4 targetCellHeight=2`, `widgetCategory="home_screen"`, `initialLayout=@layout/widget_medium`. `android/app/src/main/res/layout/widget_medium.xml` (NEW) — vertical `LinearLayout` with habit-name + reliability badge, streak number + "day streak" subtitle, "Done" `ImageButton`; tap-target `FrameLayout` root with id `widget_root`. `android/app/src/main/res/drawable/widget_bg.xml` + `ic_widget_optimal|degraded|unknown|done.xml` (NEW) — monochrome vector icons + 12 dp rounded rect. `android/app/src/main/res/values/strings.xml` — 6 new `<string>` entries (`widget_description`, `widget_done_content_description`, `widget_streak_content_description`, `widget_streak_subtitle`, `widget_empty_state`, `widget_reliability_badge`). `lib/widget/doit_widget_state.dart` (NEW, pure-Dart) — `@immutable` value class with `==`/`hashCode`/`toJson`/`fromJson`/`copyWith`; defensive `fromJson` (missing fields default to empty / 0 / `unknown`; unrecognized reliability tag falls back to `unknown`; unparseable `asOfIso` falls back to epoch). `lib/widget/widget_bridge.dart` (NEW) — abstract `WidgetBridge` + `PlatformWidgetBridge` (MethodChannel `doit/widget`, `_safe` wrapper per ADR-013) + `FakeWidgetBridge` (records `cachedSnapshots` + `refreshCount`). `lib/widget/widget_state_builder.dart` (NEW, pure-Dart) — `buildWidgetState(...)` factory uses `ConsecutiveCounter.compute` for streak + `Do.effectiveStreakConfig(...)` for the per-do grace window override + reliability→badge 1:1 mapping; `null activeDo` produces the empty-state snapshot. `lib/widget/widget_state_cache.dart` (NEW) — `SharedPreferences`-backed singleton with `save` / `load` / `clear` / `cached` / `resetForTesting`. `lib/widget/widget_state_locator.dart` (NEW) — `firstActiveDo(...)` sorts `DoRepository.listAll()` ascending by `createdAt` and returns the first non-paused entry (or `null`); caller passes a frozen `now` so widget tests are deterministic. `lib/services/widget_service.dart` (NEW) — singleton-with-`_ready` (per `.claude/rules/lib-services.md`); `init({bridge, doRepository, completionLog, reliabilityService, cache?})` is idempotent, subscribes to `ReliabilityService.reliability` so every value change triggers a re-derive, primes the cache + platform on init, exposes `handleRefreshRequest()` + `markDone(habitId)` (no-op when habit missing; otherwise appends via `CompletionLogService` with `source: CompletionSource.manual` then re-derives); platform-side failures swallowed per ADR-013. `lib/main.dart` — `await WidgetService.init(...)` AFTER `ReliabilityService.init(...)`. `test/widget/widget_state_builder_test.dart` (NEW, 8 tests) + `test/widget/widget_bridge_test.dart` (NEW, 11 tests) + `test/widget/widget_state_cache_test.dart` (NEW, 5 tests) + `test/widget/widget_state_locator_test.dart` (NEW, 4 tests, hand-rolled `_FakeRepo` fakes — the original mockito-based pattern was discarded because `mockito` is not in the project's dev_dependencies) + `test/widget/widget_service_test.dart` (NEW, 6 tests, hand-rolled `_FakeDoRepo` + `_FakeCompletionLog` + `_FakeReliabilityService` fakes). **SYS-115 + ADR-045 + WF-042 appended.** `feature.md` §2.8 B9 closes (the widget re-arm indicator now has a surface — the `ic_widget_optimal|degraded|unknown` badge); `feature.md` §4 parking-lot home-widget bullet removed; `feature.md` §5 quick-index row added. No new `<uses-permission>` (widget rendering needs none); no `home_widget` pubspec dep (native `AppWidgetProvider` + `RemoteViews` only); no `wakelock_plus` (the activity-level `FLAG_KEEP_SCREEN_ON` is not relevant — the widget renders from `RemoteViews` not a `FlutterActivity`). pubspec stays at `1.3.0+10`; `lib/build_info.dart` + `test/release_signing_test.dart` not touched (the v1.4a sign-off commit is a separate PR that flips these in lockstep, mirroring the v1.2 / v1.3 sign-off commits). |
| **v1.4c — In-app home tile Skip today button + rest-day budget indicator (Phase 30 / SYS-117 / ADR-047 / WF-044)** | _this PR_ | ✓ | 1148 / 1148 (1149 v1.4b tip − 1 duplicate-removed by lint cleanups + 19 new: 8 home_tile_skip + 11 home_tile_budget + 7 home_test extensions per CHANGELOG 3-gate at this commit; the 19 added in v1.4b — 12 streak + 4 completion + 7 home extensions — remain green; the v1.4b + v1.4c net delta against the v1.4a `main` baseline of 1130 is +18) | `lib/screens/home_tile_skip.dart` (NEW) — `markDoSkipped({required Do activeDo, required DateTime asOf, required CompletionLogService completionLog})` + `NoRestDaysRemaining` exception (carries `doId` / `year` / `month`). Reads `activeDo.restDaysPerMonth`; throws `NoRestDaysRemaining` on `<= 0` or exhausted month; constructs `SkipBudget(doId, monthlyLimit).consume(asOf)` (defensive — `SkipBudgetExhausted` is converted to `NoRestDaysRemaining` so the contract stays single-message); calls `completionLog.append(habitId, day, source: CompletionSource.restDay, proofModeAtTime: proofModeTag(activeDo.proofMode))`. The streak calculator (`ConsecutiveCounter.compute`) already credits `CompletionSource.restDay` rows identically to manual rows. `lib/screens/home_tile_budget.dart` (NEW) — `budgetRemainingForDo({required Do activeDo, required DateTime asOf, required CompletionLogService completionLog})` + `BudgetRemaining` immutable value class (`used` / `limit` / `remaining`; `canSkip` / `isExhausted` derived flags; `limit == 0` is "opted out" → not exhausted; clamps negative `remaining` to 0). `lib/do/proof_mode_tag.dart` (NEW) — the shared `proofModeTag(DoProofMode)` helper (consolidates 2 of 3 inline copies; `mission_launcher.dart` is left untouched due to its different defensive `'unknown'` contract). `lib/services/do_repository.dart` (MODIFIED) — uses the shared `proofModeTag` instead of the inline `_proofModeTag`. `lib/screens/home_tile_completion.dart` (MODIFIED) — uses the shared `proofModeTag` instead of the inline `_proofModeTag`. `lib/screens/home.dart` — `_HabitTileState` grows an `_isSkippedToday` flag + `_isResolvedToday` getter; new `_SkipButton` sub-widget (Icons.bedtime / bedtime_outlined; tooltip `homeTileSkipToday` / `homeTileSkipAlready`; tap calls `markDoSkipped(...)` in `try/catch on NoRestDaysRemaining` → `homeTileSkipSuccess` / `homeTileSkipBudgetExhausted` SnackBar); new `_BudgetCaption` sub-widget renders inside `_DoStreakBadge` (FutureBuilder over `budgetRemainingForDo(...)`; `homeTileBudgetRemaining` / `homeTileBudgetNoRemaining` / `SizedBox.shrink()` mid-fetch); the `_DoneButton`'s post-tap SnackBar now branches on `_isSkippedToday` for `homeTileSkipAlready` vs `homeTileAlreadyDoneTooltip`. `lib/l10n/app_en.arb` + `app_es.arb` — 6 new keys (`homeTileSkipToday`, `homeTileSkipAlready`, `homeTileSkipSuccess`, `homeTileSkipBudgetExhausted`, `homeTileBudgetRemaining`, `homeTileBudgetNoRemaining`). No new pubspec dep; no new `<uses-permission>`. **SYS-117 + ADR-047 + WF-044 appended.** `feature.md` §4 home-tile skip bullet removed; §5 quick-index updated. |
| **v1.4d — In-app home tile Undo today's completion (Phase 31 / SYS-118 / ADR-048 / WF-045)** | _this PR_ | ✓ | 1183 / 1183 (1183 v1.4c tip + 13 new: 8 home_tile_undo + 5 home_test extensions per CHANGELOG 3-gate at this commit; the v1.4a + v1.4b + v1.4c + v1.4d net delta against the v1.4a `main` baseline of 1130 is +53) | `lib/screens/home_tile_undo.dart` (NEW) — `undoToday({required Do activeDo, required DateTime asOf, required CompletionLogService completionLog})` + `UndoResult` sealed value class with two factories (`UndoResult.removed(rowId, source)` for the happy path carrying the deleted row's id + source; `UndoResult.nothingToUndo()` for the no-row path). The helper fetches `completionLog.listForHabit(activeDo.id)` (re-uses the v1.0 query that backs `CompletionLogSection`), filters rows whose `day` equals `DateTime(asOf.year, asOf.month, asOf.day)` (local-midnight comparison — same convention as `markDoDone` + `markDoSkipped`), and on the happy path calls `completionLog.deleteById(row.id)` exactly once. Pure-Dart, no Flutter import, no `DateTime.now()`. `lib/screens/home.dart` — `_HabitTileState` grows a `_UndoButton` sub-widget (Icons.undo, tooltip `homeTileUndoToday`, sits between `_SkipButton` and `_DoneButton`); visibility is gated on `_isResolvedToday == true` (the tile is "resolved" for the day via either Done or Skip); tap opens an `AlertDialog` titled `homeTileUndoConfirm` with body `homeTileUndoConfirmBody`; the confirm callback calls `undoToday(...)` and on the `removed` branch flips `_isCompletedToday`/`_isSkippedToday` (whichever was true) back to `false` and shows the `homeTileUndoSuccess` SnackBar; on the `nothingToUndo` branch shows the `homeTileUndoNotToday` SnackBar (defensive — the dialog shouldn't be reachable when `_isResolvedToday` is false, but the DB is the source of truth). `lib/l10n/app_en.arb` + `app_es.arb` — 5 new keys (`homeTileUndoToday`, `homeTileUndoConfirm`, `homeTileUndoConfirmBody`, `homeTileUndoSuccess`, `homeTileUndoNotToday`). No new pubspec dep; no new `<uses-permission>`. **SYS-118 + ADR-048 + WF-045 appended.** `feature.md` §4 per-tile undo bullet removed; §5 quick-index updated. |
| **v1.4 — sign-off (close out the v1.4 cycle)** | _this PR_ | ✓ | 1197 / 1197 (+14 from v1.4d tip: the build_info / pubspec / release_signing_test version-pin bumps pin against the same test count; no new test files for the sign-off itself — the 1197 count is post-format-clean post-merge-on-main) | `pubspec.yaml` bumped `1.3.0+10` → `1.4.0+11`. `lib/build_info.dart` mirrored (`kAppVersion = '1.4.0'`, `kAppVersionCode = 11`). `test/release_signing_test.dart` mirror-pin assertions updated to the new version in lockstep. `CHANGELOG.md` `## [1.4.0]` summary block appended (mirrors `## [1.3.0]` shape: 4 sub-entries v1.4a..v1.4d + headline-theme summary + no-new-permission / no-INTERNET confirmation + test count delta). `docs/v_model/v1_4_release_baseline.md` + `docs/v_model/v1_4_release_checklist.md` added (mirror `v1_3_release_baseline.md` + `v1_3_release_checklist.md`). `feature.md` updated — recommended next step flipped from "v1.2 closeout" to "v1.4 closeout"; parking-lot clear per the new v1.4 surface; quick-index updated to ADR-048 / SYS-118 / WF-045. `docs/v_model/plan.md` Milestone 11 (v1.4) flipped from `in flight` → `shipped`. The `release(v1.4)` debug-signed APK commit lands in a follow-up PR (the `flutter build apk --debug` artefact is committed separately per the v1.1i sign-off pattern at `222f860`). |
| **v1.4e — In-app home tile 7-day streak history sparkline (Phase 32 / SYS-119 / ADR-049 / WF-046)** | _this PR_ | ✓ | 1208 / 1208 (+11 from v1.4 sign-off tip of 1197: 8 home_tile_sparkline + 3 home_test extensions per CHANGELOG 3-gate at this commit) | `lib/screens/home_tile_sparkline.dart` (NEW) — pure-Dart `sparklineForDo({required Do activeDo, required DateTime asOf, required CompletionLogService completionLog})` returns `Future<List<SparklineDot>>`. The `SparklineDot` sealed hierarchy has three value-equal subclasses (`SparklineDotFilled(day, source)`, `SparklineDotEmpty(day)`, `SparklineDotFuture(day)`); the helper builds the 7-day window `[asOf - 6 days .. asOf]` (local-midnight each), then for each day performs a linear first-match scan over `completionLog.listForHabit(activeDo.id)` rows — emitting `SparklineDot.filled` on `dayMillis` match (carrying the first-matching row's source), `SparklineDot.future` if `day.isAfter(today)` (defensive), or `SparklineDot.empty` otherwise. First-match semantic mirrors `home_tile_undo.undoToday` (v1.4d / SYS-118). No Flutter import, no `DateTime.now()`, no side effects beyond the `listForHabit` read. `lib/screens/home.dart` — `_DoStreakBadge`'s right-aligned `Column` grows a `_Sparkline` sub-widget under the budget caption. The widget wraps the 7 dots in a single `Semantics(label: l.homeTileSparklineSemantics, readOnly: true)` node so screen readers announce "Last 7 days" / "Últimos 7 días" once. While the future is in flight, the widget renders `_SparklineSkeleton` (7 outlined 6 dp dots) to reserve space. On resolve it renders 7 `_SparklineDot` circles (6 dp outlined by default; today bumps to 8 dp + filled when `_isResolvedToday == true`). The `resolvedToday` hint comes from the parent `_DoStreakBadge`'s `rows.any(r.dayMillis == todayMillis)` check — re-derived from the same `completions` future the streak badge already holds, so no second `listForHabit` round-trip is added. `lib/l10n/app_en.arb` + `app_es.arb` — 1 new key (`homeTileSparklineSemantics`). No new pubspec dep; no new `<uses-permission>`. **SYS-119 + ADR-049 + WF-046 appended.** `feature.md` §4 sparkline bullet removed; §5 quick-index updated to ADR-049 / SYS-119 / WF-046. |
| **v1.4f — Android home widget Skip today + Undo today (Phase 33 / SYS-120 / ADR-050 / WF-047)** | _this PR_ | ✓ | _next gate run_ | `lib/services/widget_service.dart` — `WidgetService` grows `Future<bool> skip(String habitId)` + `Future<bool> undo(String habitId)`. `skip` mirrors the in-app `markDoSkipped` contract (v1.4c / SYS-117): returns `false` if the do is missing / `restDaysPerMonth <= 0` / month's rest-day rows already hit the limit; otherwise appends `CompletionSource.restDay` with `proofModeAtTime: proofModeTag(activeDo.proofMode)` (shared helper from v1.4c / ADR-047) + re-derives via `handleRefreshRequest()`. `undo` mirrors the in-app `undoToday` contract (v1.4d / SYS-118): first-match-wins tiebreak over `completionLog.listForHabit(habitId)` rows whose `dayMillis == local-midnight at now`; calls `deleteById` on match and re-derives; returns `false` (no delete) when no row matches today. The private `_proofModeTag` inline copy is removed — the shared `proofModeTag` from `lib/do/proof_mode_tag.dart` is imported instead (matches the v1.4c / v1.4d extraction pattern). `lib/widget/widget_bridge.dart` — `WidgetBridge` interface gains `Future<bool> skip(String habitId)` + `Future<bool> undo(String habitId)`. `PlatformWidgetBridge` dispatches the `skip` / `undo` MethodChannel arms on `doit/widget`; the existing `_safeResult<bool>` (ADR-013) wraps the channel round-trip and unwraps a `null` channel result to `false` (the dart-side return is `Future<bool>`, not `Future<bool?>`). `FakeWidgetBridge` records habit ids in `skipHabitIds` + `undoHabitIds` and exposes `nextSkipResult` + `nextUndoResult` defaults (both `true`) for tests. `android/app/src/main/kotlin/com/doit/WidgetChannel.kt` — the three near-duplicate `when` arms (`markDone`, `skip`, `undo`) collapse into a shared `private fun handleAction(call: MethodCall, result: MethodChannel.Result, action: String)` helper that reads the `habitId` arg, calls `WidgetUpdater.refreshAll(ctx)`, returns `true`. `android/app/src/main/kotlin/com/doit/DoitWidgetProvider.kt` — two new action constants `ACTION_WIDGET_SKIP = "com.doit.WIDGET_SKIP"` + `ACTION_WIDGET_UNDO = "com.doit.WIDGET_UNDO"`, each with a matching `when` arm in `onReceive()` that mirrors the existing `ACTION_MARK_DONE` pattern. `android/app/src/main/res/layout/widget_medium.xml` — two new `ImageButton`s (`@+id/widget_skip` + `@+id/widget_undo`) between the bottom spacer and the existing Done button; the layout becomes a 3-column `LinearLayout` when all three are visible. Both icons use `?android:attr/selectableItemBackgroundBorderless` for the tap ripple. `android/app/src/main/res/drawable/ic_widget_skip.xml` (NEW — moon glyph) + `ic_widget_undo.xml` (NEW — curved-arrow glyph) — 24 dp monochrome vectors matching the existing `ic_widget_done` style. `android/app/src/main/res/values/strings.xml` — 2 new `<string>` entries (`widget_skip_content_description`, `widget_undo_content_description`). `android/app/src/main/kotlin/com/doit/WidgetRenderer.kt` — `render(...)` reads the cached `DoitWidgetState` JSON, including the new `restDaysPerMonth` field, to decide `widget_skip` visibility (`View.GONE` when `restDaysPerMonth == 0` — mirrors the in-app `_SkipButton` hide rule from SYS-117) and `isCompletedToday` to decide `widget_undo` visibility (`View.GONE` when `isCompletedToday == false` — mirrors the in-app `_UndoButton` hide rule from SYS-118). Two new private helpers `skipIntent(ctx, id)` + `undoIntent(ctx, id)` return `PendingIntent.getBroadcast` targeting `DoitWidgetProvider` with the corresponding `ACTION_WIDGET_SKIP` / `ACTION_WIDGET_UNDO` extras. `lib/widget/doit_widget_state.dart` — `DoitWidgetState` grows `final int restDaysPerMonth` (default 0); included in `toJson()` / `fromJson()` (defaults to 0 on `fromJson`) / `copyWith` / `==` / `hashCode` / `toString`. `lib/widget/widget_state_builder.dart` — `buildWidgetState(...)` threads `restDaysPerMonth: activeDo.restDaysPerMonth` into the returned snapshot. `lib/l10n/app_en.arb` + `app_es.arb` — 2 new keys (`widgetSkipToday`, `widgetUndoToday`). No new `<uses-permission>`, no new pubspec deps, no new Drift tables, no new MethodChannels — v1.4f re-uses the existing `doit/widget` channel + `DoitWidgetProvider` receiver. **SYS-120 + ADR-050 + WF-047 appended.** `feature.md` §4 widget-side Skip + Undo bullets removed; §5 quick-index updated to ADR-050 / SYS-120 / WF-047. Latent v1.4a "Done tap doesn't round-trip to Dart" gap noted in ADR-050 §"Latent gap" as a v1.4g+ closure candidate. |
| **v1.4g — Widget-action round-trip: Kotlin → Dart via inbound `doit/widget` channel (Phase 34 / SYS-121 / ADR-051 / WF-048)** | _this PR_ | ✓ | 1228 / 1228 (1228 v1.4f tip + 11 new: 7 widget_action_invoker unit + 4 widget_action_invoker_integration per CHANGELOG 3-gate at this commit) | Closes the latent v1.4a + v1.4f gap: the widget's "Done" / "Skip today" / "Undo today" buttons now round-trip to Dart's `WidgetService.markDone` / `.skip` / `.undo` so the actual completion write / rest-day append / row delete happens in Dart (single source of truth for the completion log). The widget surface now shares the write path with the in-app tile. **New files.** `lib/widget/widget_action_invoker.dart` (NEW) — singleton `WidgetActionInvoker` with `attach()` (idempotent), `dispatch(MethodCall) → Future<bool>`, top-level `widgetActionDispatch(MethodCall)` for tests; wires `setMethodCallHandler` on the `doit/widget` channel so inbound `markDone` / `skip` / `undo` calls route to `WidgetService.instance.markDone` / `.skip` / `.undo`. `test/widget/widget_action_invoker_test.dart` (NEW, 7 tests) + `test/widget/widget_action_invoker_integration_test.dart` (NEW, 4 tests — exercise the dispatcher + service + completion-log end-to-end via the production `widgetActionDispatch` function). **Modified.** `lib/services/widget_service.dart` — `markDone` signature changes from `Future<void>` → `Future<bool>` (the only v1.4a caller was the in-app tile which ignored the return; v1.4g reads it); `init(...)` calls `await WidgetActionInvoker.attach()` right after the singleton is set. `android/app/src/main/kotlin/com/doit/WidgetChannel.kt` — new suspending `suspend fun invokeAction(ctx: Context, action: String, habitId: String): Boolean` with a 5 s `withTimeoutOrNull` ceiling; validates `action ∈ {markDone, skip, undo}` + non-empty `habitId`; ensures the `FlutterEngine` is alive via `WidgetUpdater.ensureFlutterEngine(ctx)`; posts `invokeMethod(action, mapOf("habitId" to habitId))` to the platform main thread via `android.os.Handler(Looper.getMainLooper()).post { ... }` (because `MethodChannel.invokeMethod` must run on the platform thread); awaits the result via `CompletableDeferred<Boolean>.await()`. `android/app/src/main/kotlin/com/doit/WidgetUpdater.kt` — `ensureFlutterEngine(ctx)` is now `public` (was `private`) so `WidgetChannel.invokeAction` can boot the engine before sending the inbound call. `android/app/src/main/kotlin/com/doit/DoitWidgetProvider.kt` — three action arms (`ACTION_MARK_DONE` / `ACTION_WIDGET_SKIP` / `ACTION_WIDGET_UNDO`) in `onReceive` now read `habitId` from `intent.getStringExtra(EXTRA_HABIT_ID)` (preferred) or `WidgetStateCache.cachedFromPrefs(ctx)?.optString("habitId")` (fallback for stale `PendingIntent`s created before v1.4g), then `scope.launch { WidgetChannel.invokeAction(ctx, "<action>", habitId); WidgetUpdater.refreshAll(ctx) }` on `Dispatchers.IO` (new `CoroutineScope(SupervisorJob() + Dispatchers.IO)`) so the BroadcastReceiver doesn't block the main thread; new companion constant `EXTRA_HABIT_ID = "com.doit.EXTRA_HABIT_ID"` is exported and consumed by `WidgetRenderer`. `android/app/src/main/kotlin/com/doit/WidgetRenderer.kt` — `markDoneIntent(ctx, id, habitId)` / `skipIntent(ctx, id, habitId)` / `undoIntent(ctx, id, habitId)` each `putExtra(EXTRA_HABIT_ID, habitId)`; `render(...)` reads `state.optString("habitId", "")` and passes to all three intent builders. **No new `<uses-permission>`, no new pubspec deps, no new Drift tables, no new MethodChannels** — v1.4g re-uses the existing `doit/widget` channel in the INBOUND direction (Kotlin → Dart). The bidirectional contract is documented in three file headers (`widget_action_invoker.dart`, `WidgetChannel.kt`, `DoitWidgetProvider.kt`). **SYS-121 + ADR-051 + WF-048 appended.** `feature.md` §4 latent v1.4a "widget-side Done tap doesn't round-trip to Dart" bullet removed; §5 quick-index updated to ADR-051 / SYS-121 / WF-048. ADR-050's "Latent gap" hedge is now annotated with the v1.4g closure reference. The v1.4a..v1.4f net delta against the v1.4a `main` baseline of 1130 grows to +98. |
| **v1.4h — In-app home tile Edit + Delete IconButtons (Phase 35 / SYS-122 / ADR-052 / WF-049)** | _this PR_ | ✓ | 1242 / 1242 (1228 v1.4g tip + 14 new: 6 home_tile_delete + 7 home_test extensions + 1 incidental test rerun per CHANGELOG 3-gate at this commit; the v1.4..v1.4h net delta against the v1.4a `main` baseline of 1130 is +112) | Surfaces Edit + Delete as discoverable per-tile `IconButton`s in the same right-edge action `Row` as the v1.4b/c/d Skip / Undo / Done buttons, closing the discoverability gap on the v0.2 long-press → select-mode → app-bar-trash path. **New files.** `lib/screens/home_tile_delete.dart` (NEW) — pure-Dart `Future<bool> deleteDo({required Do activeDo, required DoRepository repository})` helper that calls `repository.deleteById(activeDo.id)` and translates any throwable (DB locked, FK constraint, drift exception) into a `bool` return — `true` on the happy path, `false` on any throw. Pure-Dart, no Flutter import, no `DateTime.now()`; matches the `markDoDone` (v1.4b) / `markDoSkipped` (v1.4c) / `undoToday` (v1.4d) helper pattern. `test/screens/home_tile_delete_test.dart` (NEW, 6 tests — happy path returns `true` AND records the deleted id; calls `deleteById` exactly once with the captured do id; returns `false` when the repository throws an `Error` (e.g. `StateError`); returns `false` when the repository throws an `Exception` subtype (e.g. `Exception('foreign-key constraint')`); does not re-throw — caller can rely on the `bool` return (asserted via `expectLater(() => ..., returnsNormally)`); imports no Flutter types so it can be tested without `TestWidgetsFlutterBinding`). **Modified.** `lib/screens/home.dart` — `_HabitTile` grows a new `final VoidCallback? onDoChanged` prop bound to `_HomeScreenState._refresh()`; `_HabitTileState` grows two new private handlers (`_onEditPressed` pushes `AddHabitScreen(habitId: ...)` — same destination as `_HomeScreenState._onTileTap` at `lib/screens/home.dart:120` — and `await`s the popped `bool`; `_onDeletePressed` opens an `AlertDialog` titled `homeTileDeleteConfirm(habit.name)` (carries the do name in quotes for destructive-action verification), captures `messenger = ScaffoldMessenger.of(context)` BEFORE the async gap, sets `_busy = true`, awaits `deleteDo(...)`, clears `_busy` on return, and branches on the `bool`: on `true` calls `widget.onDoChanged?.call()` then shows a SnackBar `homeSnackbarDoDeleted(habit.name)` with a `SnackBarAction` labeled `homeSnackbarDoDeletedUndo` ("Undo") whose `onPressed` re-saves the captured `Do` reference via `DoRepository.save(widget.habit)` (the `Do` is `@immutable` so the reference is safe without a deep clone); on `false` shows `homeSnackbarDoDeleteFailed` WITHOUT removing the tile). Two new private sub-widgets mirror the v1.4b/c/d shape: `_EditButton({required VoidCallback onPressed})` renders `Icons.edit_outlined` + `homeTileEdit` tooltip (no busy state — navigation push, not an in-flight write); `_DeleteButton({required bool busy, required VoidCallback onPressed})` renders `Icons.delete_outline` + `homeTileDelete` tooltip with busy-state spinner matching `_SkipButton`. The action row at `lib/screens/home.dart:618-652` (the existing `if (!selectMode)` `Row`) inserts `_EditButton` + `_DeleteButton` leftmost (BEFORE the existing `_SkipButton` / `_UndoButton` / `_DoneButton` cluster) so the destructive-action cluster is visually distinct from the completion-action cluster. `lib/l10n/app_en.arb` + `app_es.arb` — 7 new keys (`homeTileEdit`, `homeTileDelete`, `homeTileDeleteConfirm(doName)`, `homeTileDeleteConfirmBody`, `homeSnackbarDoDeleted(doName)`, `homeSnackbarDoDeletedUndo`, `homeSnackbarDoDeleteFailed`) added in lockstep so the existing `test/l10n/app_localizations_test.dart` parity test catches missing Spanish entries. **No new `<uses-permission>`, no new pubspec deps, no new Drift tables, no new MethodChannels, no Kotlin changes** — v1.4h is pure-Dart. **Documented trade-off** (per ADR-052 §8): `DoRepository.deleteById` cascade-deletes the linked `completions` rows via the FK pragma on `CompletionLogService`, so the Undo snackbar restores the do row but does NOT restore the streak history — the streak counter starts at 0 on the restored do. A v1.4h+ follow-up could add a soft-delete column to `habits` for a true undo. **SYS-122 + ADR-052 + WF-049 appended.** `feature.md` §4 per-tile edit/delete bullet removed; §5 quick-index updated to ADR-052 / SYS-122 / WF-049. |
| **v1.4i — In-app home tile rest-day history visualization (Phase 36 / SYS-123 / ADR-053 / WF-050)** | _this PR_ | ✓ | 1252 / 1252 (1242 v1.4h tip + 10 new: 4 home_tile_sparkline `extendedSparklineForDo` + 6 home_test v1.4i group per CHANGELOG 3-gate at this commit; the v1.4..v1.4i net delta against the v1.4a `main` baseline of 1130 is +122) | Extends the v1.4e / SYS-119 / WF-046 7-day streak sparkline on every `_HabitTile` to **14 days** with **source-aware color** + **inline legend** below the dot row, so the user can see at-a-glance which days in the past fortnight were intentionally skipped (via the v1.4c / SYS-117 `_SkipButton`) vs which were missed or completed manually. **New helper.** `lib/screens/home_tile_sparkline.dart` grows a new top-level function `Future<List<SparklineDot>> extendedSparklineForDo({required Do activeDo, required DateTime asOf, required CompletionLogService completionLog, int days = 14})` which takes a configurable `days` parameter (default 14 — a fortnight, half a calendar month, fits comfortably at 360 dp). The helper builds the `[asOf - (days - 1) days .. asOf]` window (local-midnight each), fetches `completionLog.listForHabit(activeDo.id)`, and emits one dot per day (`SparklineDot.filled(day, source)` if a row exists for that day's `dayMillis`, `SparklineDot.future(day)` if the day is in the future of `asOf`, `SparklineDot.empty(day)` otherwise). The first-match source wins on the `(habitId, dayMillis)` UNIQUE constraint — mirrors `home_tile_undo.undoToday` (v1.4d / SYS-118) + `sparklineForDo` (v1.4e / SYS-119). The original `sparklineForDo` (v1.4e / SYS-119) becomes a thin backwards-compatible wrapper around `extendedSparklineForDo` with `days: 7` so the v1.4e helper keeps its exact return shape and no caller breaks. **Widget changes.** `lib/screens/home.dart` — `_Sparkline` (v1.4e / SYS-119) is extended with three new optional constructor params: `int days = 14` (the window length; defaults to the v1.4i 14-day value), `Color? restDayColor` (the color used for `rest_day` filled dots — the tile invocation passes `restDayColor: Theme.of(context).colorScheme.tertiary` so the rest-day color tracks the active theme), `bool showLegend = true` (whether to render the inline legend row below the dots). The widget reads `SparklineDotFilled.source` and switches: `source == 'rest_day'` → `restDayColor ?? colorScheme.tertiary`; else (manual / notification / mission) → `colorScheme.primary`. Each `_SparklineDot` wraps its `Padding > Container` in `Semantics(label: ...)` (NOT a per-dot `Tooltip` — see ADR-053 §"Alternatives considered" for why `Tooltip` was rejected: 14 small dots × 3 localized messages = 42 competing tooltips, plus `Tooltip`'s internal `GestureDetector` intercepts the parent `_HabitTile`'s `onLongPress` select-mode gesture — verified empirically via the v1.4i "long-press on a tile with the new sparkline still enters select mode" regression test). The per-dot labels are `'Done'` for manual / notification / mission fills, `'Rest day'` for rest-day fills, `'Missed'` for empty dots, `'Future'` for future dots. The existing `_Sparkline` outer `Semantics(label: l.homeTileSparklineSemantics, readOnly: true, container: true)` node announces "Last 14 days" / "Últimos 14 días" once on TalkBack focus (updated from the v1.4e "Last 7 days" / "Últimos 7 días" form). A new `_SparklineLegend` sub-widget renders below the dot row whenever `showLegend == true`: 3 `_LegendSwatch` entries (a filled primary-color circle + `homeTileSparklineLegendDone`; a filled tertiary-color circle + `homeTileSparklineLegendRestDay`; an outlined circle + `homeTileSparklineLegendMissed`) using `Theme.of(context).textTheme.labelSmall`. **i18n.** `lib/l10n/app_en.arb` + `app_es.arb` — 6 new keys (`homeTileSparklineRestDayTooltip`, `homeTileSparklineDoneTooltip`, `homeTileSparklineMissedTooltip`, `homeTileSparklineLegendDone`, `homeTileSparklineLegendRestDay`, `homeTileSparklineLegendMissed`). `homeTileSparklineSemantics` updated from "Last 7 days" → "Last 14 days" (and `Últimos 7 días` → `Últimos 14 días`). The existing `test/l10n/app_localizations_test.dart` "every non-template ARB has the same key set as the template" test catches missing Spanish entries automatically. **No new `<uses-permission>`, no new pubspec deps, no new Drift tables, no new MethodChannels, no Kotlin changes** — v1.4i is pure-Dart. The `extendedSparklineForDo` helper imports no Flutter types; the widget re-fetch trigger on any tile-state change re-uses the existing `_HomeScreenState._refresh()` setState cascade (no `ChangeNotifier` / `Stream` is added). **SYS-123 + ADR-053 + WF-050 appended.** `feature.md` §4 rest-day history bullet removed; §5 quick-index updated to ADR-053 / SYS-123 / WF-050. |
| **v1.4j — In-app home tile rest-day budget edit affordance (Phase 37 / SYS-124 / ADR-054 / WF-051)** | _this PR_ | ✓ | 1271 / 1271 (1252 v1.4i tip + 19 new: 8 rest_day_picker_dialog + 3 do_model `Do.validate` boundaries + 1 add_habit widget row + 1 grep regression `restDaysPerMonth: 2` + 5 home `BudgetCaption` + 1 add_habit localization wrapper, mirrored to 3 sibling test files: `add_habit_delete_test.dart` + `add_habit_save_as_template_test.dart` + `templates_test.dart` — switched from `MaterialApp(home: ...)` to `localizedApp(home: ...)` so the `AddHabitScreen`'s new `Builder` resolves `AppLocalizations.of(ctx)`; the v1.4..v1.4j net delta against the v1.4a `main` baseline of 1130 is +141) | Surfaces the v1.0 edit affordance for `restDaysPerMonth` directly from the in-app home tile (`_BudgetCaption` tap) AND from the `AddHabitScreen` form ("Rest days per month: N" row), AND fixes the v1.0 silent-reset bug in `AddHabitScreen._save()` that hardcoded `restDaysPerMonth: 2` in all 5 schedule branches (so editing a 3/month do and hitting Save without touching any other field silently reset the value to 2). **New file.** `lib/screens/rest_day_picker_dialog.dart` (NEW) — shared `Future<int?> showRestDayPicker(BuildContext context, {required int initial})` + `RestDayPickerDialog(initial: int)` `StatefulWidget`. The dialog renders an `AlertDialog` with title `homeTileBudgetEditTitle`, description `homeTileBudgetEditDescription`, a live integer label above a `Slider(min: 0, max: 31, divisions: 31, value: _value, label: '$_value', onChanged: ...)` that snaps to whole numbers, and Save / Cancel actions. The dialog clamps the initial value to `[0, 31]` on construction (defensive against stale DB rows from a future schema migration). Constants `kRestDaysPerMonthMin = 0` and `kRestDaysPerMonthMax = 31` are exported for the test surface. **Modified.** `lib/do/do.dart` — `Do.validate()` adds the upper-bound check `if (restDaysPerMonth < 0 \|\| restDaysPerMonth > 31) throw DoInvalidRestDays(restDaysPerMonth);`; the exception class's `super(...)` message is updated from "must be >= 0" to "must be in 0..31.". `lib/screens/add_habit.dart` — `_AddHabitScreenState` grows `int _restDaysPerMonth = 2` state field, loads it in `_loadExisting()` from `_original?.restDaysPerMonth ?? 2` (preserving the original value in edit mode — fixes the silent-reset bug), replaces all 5 hardcoded `restDaysPerMonth: 2` literals in `_save()` at `:911, :926, :945, :960, :981` with `restDaysPerMonth: _restDaysPerMonth`, and grows `_pickRestDaysPerMonth()` which calls `showRestDayPicker(context, initial: _restDaysPerMonth)` and `setState`s on the non-null result. The form body grows a new "Rest days per month: N" `ListTile` near the proof-mode row that opens the same picker; tap → picker → Save → state updates → form re-renders. `lib/screens/home.dart` — `_BudgetCaption` grows `final VoidCallback? onTap` + `final String zeroCaption` constructor params and DROPS the two early-returns (`limit <= 0` + `used == 0`) so the caption renders in all 3 budget states (zero budget / partial use / exhausted). The caption is wrapped in `Semantics(button: true, label: captionText, child: GestureDetector(behavior: HitTestBehavior.opaque, onTap: onTap, child: ...))` so TalkBack reads the caption as a button. `_HabitTileState` grows `_onBudgetCaptionTapped()` which captures `messenger = ScaffoldMessenger.of(context)` BEFORE the async gap, awaits `showRestDayPicker(...)`, on non-null awaits `DoRepository.instance.save(widget.habit.copyWith(restDaysPerMonth: picked))`, on success shows `messenger.showSnackBar(SnackBar(content: Text(l.homeSnackbarBudgetUpdated(picked))))` and calls `widget.onDoChanged?.call()` to trigger the v1.4h `_refresh()` cascade; on throw shows `homeSnackbarBudgetUpdateFailed` SnackBar WITHOUT removing the tile. `_DoStreakBadge` grows a new `onBudgetCaptionTapped: VoidCallback` param threaded through from `_HabitTileState`. `lib/l10n/app_en.arb` + `app_es.arb` — 7 new keys added in lockstep. **No new `<uses-permission>`, no new pubspec deps, no new Drift tables, no new MethodChannels, no Kotlin changes** — v1.4j is pure-Dart. **SYS-124 + ADR-054 + WF-051 appended.** `feature.md` §4 in-app rest-day budget edit bullet removed; §5 quick-index updated to ADR-054 / SYS-124 / WF-051. |
| **v1.4k — Per-instance home widget configuration via Android AppWidget configuration activity (Phase 38 / SYS-125 / ADR-055 / WF-052)** | _this PR_ | ✓ | 1292 / 1292 (1271 v1.4j tip + 21 new: 8 doit_widget_state `selectedHabitId` round-trip + 4 widget_service `handleRefreshRequest` / `setSelectedHabitId` / reconciliation / reliability-preserve + 2 widget_state_builder `selectedHabitId` threading + 7 widget_deep_link route builder — `buildHabitRoute` x3 (`AddHabitScreen` / `HomeScreen` fallback / null on unknown) + `buildWidgetConfigRoute` x2 (with widgetId / null widgetId defensive) + `buildAppRoute` dispatch + `DoitWidgetState.selectedHabitId` JSON envelope pin; the v1.4..v1.4k net delta against the v1.4a `main` baseline of 1130 is +162) | Closes the v1.4a "every widget instance shows the same `firstActiveDo`" gap by routing the widget bind flow through an Android `AppWidget` configuration activity (`DoitWidgetConfigureActivity`) that hosts a Flutter-side picker (`WidgetConfigScreen`) and persists the picked habit id to the `DoitWidgetState` JSON envelope as a new optional `selectedHabitId` field. The widget body-tap PendingIntent reads the cached pick and routes via `MainActivity.getInitialRoute()` to `AddHabitScreen(habitId: ...)`; the activity clears the extra on first read so subsequent rebuilds do not re-route. **New files.** `lib/widget/widget_config_screen.dart` (NEW) — `WidgetConfigScreen` `StatefulWidget`. `FutureBuilder<List<Do>>` reads `DoRepository.instance.listAll()`; the `_PickerRow` `ListTile` calls `widget.proxy.setSelectedHabitId(habitId)` then `Navigator.of(context).pop<String>(habitId)`. The `_EmptyState` shows a `Icons.add_task` glyph + `widgetConfigureEmptyState` copy + "Back to do it" `FilledButton` (`widgetConfigureBackToHome`) that pops `null`. Constructor takes `widgetId: int?` + `proxy: WidgetServiceProxy` (default `const WidgetServiceProxy()`) for testability. `lib/widget/widget_service_proxy.dart` (NEW) — `class WidgetServiceProxy { const WidgetServiceProxy(); Future<bool> setSelectedHabitId(String? habitId) => WidgetService.instance.setSelectedHabitId(habitId); }` — the indirection layer that lets widget tests inject a fake without reaching into the singleton (mirrors the v1.4h `home_tile_delete.dart` callback-handler seam). `lib/app_router.dart` (NEW) — top-level route table extracted from `lib/main.dart` for testability (Dart top-level `_private` functions are not reachable across library boundaries). Exports `buildAppRoute(RouteSettings)` (dispatches `/mission` → `buildMissionRoute` (v1.3d / SYS-114), `/habit` → `buildHabitRoute` (v1.4k), `/widget-config` → `buildWidgetConfigRoute` (v1.4k); non-matching routes return `null`), plus the three individual builders. `android/app/src/main/kotlin/com/doit/DoitWidgetConfigureActivity.kt` (NEW) — thin `FlutterActivity` shell. `getInitialRoute(): String` reads `Intent.EXTRA_APPWIDGET_ID` and returns `"/widget-config?widgetId=$widgetId"`. `configureFlutterEngine` intentionally does NOT attach Kotlin channels (mirrors v1.3d `FullScreenActivity` thin-Flutter shell precedent). `exported=true` + `launchMode=singleTask` + `taskAffinity=""` + `excludeFromRecents=true` + `theme=LaunchTheme` per manifest. **Modified.** `lib/widget/doit_widget_state.dart` — grows `final String? selectedHabitId` (default `null`), threaded into `toJson` / `fromJson` (empty string → `null` defensive) / `copyWith` / `==` / `hashCode` / `toString`. `lib/widget/widget_state_builder.dart` — grows `String? selectedHabitId` named param to `buildWidgetState(...)`, threaded into the returned `DoitWidgetState` in both active + empty-state paths. `lib/services/widget_service.dart` — `handleRefreshRequest` consults `WidgetStateCache.cached?.selectedHabitId` first via `_resolveActiveDo()`; on a null / empty / unresolvable cached pick falls back to `firstActiveDo`. Reconciliation: when `_resolveActiveDo` returns null because the picked do was deleted, the next cached state has `selectedHabitId = null` (the stale-pick clear is gated on a `_pickIsStale` boolean — runs only on the rare stale-pick path). New public method `setSelectedHabitId(String? habitId)` writes a fresh state to the cache + asks the platform to repaint. `lib/main.dart` — `DoItApp` switches `onGenerateRoute:` from the v1.3d `_buildMissionRoute` to `buildAppRoute` (the additive router does not break the existing routes). `android/app/src/main/kotlin/com/doit/WidgetRenderer.kt` — `openAppIntent(ctx, id, selectedHabitId: String)` adds `MainActivity.EXTRA_HABIT_ID` extra when `selectedHabitId.isNotEmpty()`. `android/app/src/main/kotlin/com/doit/MainActivity.kt` — `override fun getInitialRoute(): String?` reads `EXTRA_HABIT_ID`, clears the extra (one-shot), returns `"/habit?habitId=${Uri.encode(habitId)}"` or null. Companion gains `const val EXTRA_HABIT_ID = "com.doit.EXTRA_HABIT_ID_FROM_WIDGET"` (distinct namespace from `DoitWidgetProvider.EXTRA_HABIT_ID` to avoid receiver collision). `android/app/src/main/AndroidManifest.xml` — adds `<activity android:name=".DoitWidgetConfigureActivity" ...>` with `<intent-filter><action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" /></intent-filter>`. `android/app/src/main/res/xml/doit_widget_info.xml` — adds `android:configure="com.doit.DoitWidgetConfigureActivity"` to the `<appwidget-provider>` element. `android/app/src/main/res/values/strings.xml` — adds `<string name="widget_configure_label">Choose a do for do it</string>`. `lib/l10n/app_en.arb` + `app_es.arb` — 3 new keys (`widgetConfigureTitle` / `widgetConfigureEmptyState` / `widgetConfigureBackToHome`) added in lockstep. **No new `<uses-permission>`** (verified against `docs/v_model/architecture_options.md` §"Permission baseline"; manifest added the activity + intent-filter only), **no new pubspec deps, no new Drift tables, no new public method-channel namespaces** (the existing `doit/widget` namespace is reused with a new `setSelectedHabitId` arm). **SYS-125 + ADR-055 + WF-052 appended.** `feature.md` §4 per-instance widget config bullet removed; §5 quick-index updated to ADR-055 / SYS-125 / WF-052. |
| **v1.4l — Soft-delete column on `habits` (Phase 39 / SYS-126 / ADR-056 / WF-053)** | _this PR_ | ✓ | 1321 / 1321 (1292 v1.4k tip + 29 new: 4 migration_v4_to_v5 + 14 do_repository (soft-delete 8 + restore 4 + save invariant 2 + hard-delete preserved 2 — `deleteById` + force-delete-then-save) + 1 habit_repository `deletedAt` round-trip + 10 home_tile_delete (`softDeleteDo` 5 + `restoreDo` 5; the 4 round-trip + Flutter-import tests are absorbed into the helper coverage) — the v1.4..v1.4l net delta against the v1.4a `main` baseline of 1130 is +191) | Replaces the v1.4h / SYS-122 hard-delete + `insertOnConflictUpdate`-on-Undo trade-off (documented in `ADR-052 §8` + `lib/screens/home.dart:553-561`) with a soft-delete tombstone column on `Habits`. The Undo path now restores the streak by construction — the completion log, the rest-day budget, the routine-executor registry, and the widget cached id all survive because the row is preserved (only the `deletedAtMillis` column changes). **New files.** `lib/services/db/migrations/v4_to_v5.dart` (NEW) — `Future<void> migrateV4ToV5(Migrator m, AppDatabase db) async { await m.database.customStatement('ALTER TABLE habits ADD COLUMN deleted_at_millis INTEGER'); }`. `test/db/migration_v4_to_v5_test.dart` (NEW, 4 tests — `schemaVersion is 5` v1.4l pin; `habits` table has `deleted_at_millis` column; the column is nullable and accepts both NULL and an int; pre-existing rows survive the migration with `deleted_at_millis IS NULL`). `test/services/do_repository_test.dart` (NEW, 14 tests in 4 groups — soft-delete (8): `softDeleteById` writes a non-null `deletedAtMillis`; `listAll` excludes tombstoned habits; `listActive` excludes tombstoned habits; `getById` still returns a tombstoned do; `getActiveById` returns null for a tombstoned do; `getActiveById` returns the do when active; `softDeleteById` is idempotent on an already-tombstoned row; `softDeleteById` on a missing row returns `false`. Restore (4): `restoreById` clears the tombstone; restored do reappears in `listAll` with `isDeleted == false`; `restoreById` is idempotent on an already-active row; `restoreById` on a missing row returns `false`. Save invariant (2): `save(d)` does NOT touch a tombstoned row's `deletedAtMillis` — the v1.4l save-invariant pin; `restoreById` after a save on a tombstoned row still works. Hard-delete preserved (2): `deleteById` (force-delete) still removes the row (backup-restore wipe path preserved); force-delete then save re-inserts as active). **Modified.** `lib/services/db/tables.dart` — adds `IntColumn get deletedAtMillis => integer().nullable()();` to `Habits` (mirrors the `Events.archivedAtMillis` precedent). `lib/services/db/schema.dart` — bumps `kCurrentSchemaVersion = 4` → `5`; adds `if (from < 5) { await migrateV4ToV5(m, this); }` to `MigrationStrategy.onUpgrade`. `lib/do/do.dart` — base `Do` grows `final DateTime? deletedAt;` (default `null`) + `bool get isDeleted => deletedAt != null;`; `copyWith` extends with `DateTime? deletedAt, bool clearDeletedAt = false` (the explicit-clear flag mirrors `Event.copyWith(clearArchived:)`). All 5 subclasses (`DoFixed`, `DoInterval`, `DoAnchor`, `DoDayOfX`, `DoTimeWindow`) thread the field through their constructors + `copyWith`; `==` / `hashCode` include `deletedAt`. `lib/services/do_repository.dart` — new methods `Future<bool> softDeleteById(String id, {required DateTime at})` (idempotent UPDATE filtered by `deleted_at_millis IS NULL`), `Future<bool> restoreById(String id)` (idempotent UPDATE filtered by `deleted_at_millis IS NOT NULL`), `Future<Do?> getActiveById(String id)` (filters tombstones). `listAll` + `listActive` add `..where((t) => t.deletedAtMillis.isNull())` so the UI never surfaces tombstones. `_toRow` DOES NOT write `deletedAtMillis` (the v1.4l save-invariant — Drift's `insertOnConflictUpdate` preserves the existing column value when the new row doesn't specify it). `deleteById` is preserved for the `BackupService.importFrom` wipe path. `lib/screens/home_tile_delete.dart` — replaces `deleteDo` with `softDeleteDo({required Do activeDo, required DateTime at, required DoRepository repository})` + adds `restoreDo({required Do tombstonedDo, required DoRepository repository})` (parallel pattern, same try/catch + bool return contract). `lib/screens/home.dart` — `_HabitTileState._onDeletePressed` switches to `softDeleteDo(activeDo: widget.habit, at: DateTime.now(), repository: DoRepository.instance)` on confirm, and the Undo SnackBar action switches to `restoreDo(tombstonedDo: capturedHabit, repository: DoRepository.instance)`. The `_onDeletePressed` KDoc is rewritten to drop the (inaccurate) v1.4h "FK pragma cascade-deleted the completion log" claim and to accurately describe the soft-delete + restoreById flow. `lib/services/backup_service.dart` — bumps `kBackupPayloadSchemaVersion = 2` → `3`; habits export query adds `..where((t) => t.deletedAtMillis.isNull())`; `restDayBudgets` filter becomes `..where((t) => t.habitId.isIn(activeHabitIds))` (Drift's built-in `isIn` Set<String> lowering — NOT `.contains()` which fails the SQL lowering type check). The `deletedAtMillis` field IS written into the envelope so a backup round-trip is lossless. **No new `<uses-permission>`, no new pubspec deps, no new Drift tables, no new MethodChannels, no Kotlin changes** — v1.4l is pure-Dart + a single Drift migration. The Drift codegen wires `deletedAtMillis` (Dart) ↔ `deleted_at_millis` (SQL) automatically. **SYS-126 + ADR-056 + WF-053 appended.** `feature.md` §4 per-tile delete + undo streak-survives bullet removed (the v1.4l delivery closes the v1.4h trade-off); §5 quick-index updated to ADR-056 / SYS-126 / WF-053. 2 existing migration tests re-baselined (`migration_v2_to_v3_test.dart` + `migration_v3_to_v4_test.dart` `schemaVersion` pin 4 → 5); 2 existing widget tests updated (`home_test.dart` delete-asserts now use `getActiveById` since `getById` returns the tombstoned row post-v1.4l); 1 existing repository test extended (`habit_repository_test.dart` round-trips `deletedAt` via `softDeleteById` + `restoreById`); 1 existing helper test rewritten (`home_tile_delete_test.dart` now covers `softDeleteDo` + `restoreDo`). The "v1.4h+ soft-delete follow-up" parking-lot bullet in ADR-052 §8 is now annotated with the v1.4l closure reference. |
| **v1.4m — CI coverage for the v1.4l soft-delete home-screen flow + `listDeleted` / `purgeDeletedOlderThan` API surface (Phase 40 / SYS-127 / ADR-058 / WF-055)** | _this PR_ | ✓ | 1334 / 1334 (1321 v1.4l tip + 13 new: 4 do_repository `listDeleted` (excludes active, orders by `deletedAtMillis DESC`, honors `limit` param, empty case) + 4 do_repository `purgeDeletedOlderThan` (purges old tombstones, leaves young tombstones, never touches active, idempotent on second call) + 1 do_repository `persistence-across-restart` (Phase A in-memory DB + soft-delete + close; Phase B fresh in-memory DB + raw-SQL seed with `deleted_at_millis` + assert `getById` returns tombstoned row) + 4 home_test v1.4m widget tests (Undo restores streak badge by construction — the headline behavior change; streak badge renders tabular formatting; cancelled delete does NOT tombstone + streak badge still renders; soft-delete persists across `_resetDb` swap) — the v1.4..v1.4m net delta against the v1.4a `main` baseline of 1130 is +204) | Closes the CI coverage gap from the v1.4l PR's 6-step on-device smoke: the headline flow (Undo restores streak by construction), the streak rendering (badge format + subtitle), and the persistence (column survives a DB close + reopen) are now guarded by `flutter test` in CI. Also pins the v1.4n "Recently deleted" UI's API surface (the two repository methods `listDeleted` + `purgeDeletedOlderThan`) so the v1.4n PR is purely a UI concern — no API churn. **Modified.** `lib/services/do_repository.dart` — adds two new public methods: `Future<List<Do>> listDeleted({int? limit})` (returns tombstoned dos ordered by `deletedAtMillis DESC`; optional `limit` caps the result size — used by the v1.4n UI's "show top 5" affordance) and `Future<int> purgeDeletedOlderThan(Duration age, {required DateTime at})` (hard-deletes tombstoned dos older than `at - age`; returns the affected-row count so the caller can log "purged N tombstones"; the `at` parameter is the caller-supplied reference time — no `DateTime.now()` inside the repository, matching the v0.2 / Phase A "no clock in the model" rule). Both methods accept a `DateTime at` argument so tests can pin the reference time. `lib/screens/home.dart` — wraps the `_DoStreakBadge(...)` call site in `KeyedSubtree(key: Key('streakBadge-${habit.id}'), child: _DoStreakBadge(...))` so widget tests can locate the badge via `find.byKey(Key('streakBadge-<id>'))` without exposing the private widget. The `KeyedSubtree` wrapper is a no-op for production rendering (no rebuild overhead, no extra layout) — purely a test seam. `test/services/do_repository_test.dart` — +9 v1.4m tests in 3 groups (described above). `test/screens/home_test.dart` — +4 v1.4m widget tests in a new v1.4m group (described above); adds a `seedDoWithThreeConsecutiveCompletions` helper that seeds a do + 3 completions via `CompletionLogService.append` (yesterday / day-before / 3-days-ago) so the streak calculator has 3 distinct days to count. **No new `<uses-permission>`, no new pubspec deps, no new Drift tables, no new MethodChannels, no Kotlin changes** — v1.4m is pure-Dart + 13 new tests. The data layer was already landed in v1.4l; v1.4m just exposes + tests the API. Permission baseline unchanged; verification against `docs/v_model/architecture_options.md` confirmed no AndroidManifest touch. **SYS-127 + ADR-058 + WF-055 appended.** `feature.md` §4 unchanged (the "Recently deleted" surface parking lot bullet remains for v1.4n); §5 quick-index updated to ADR-058 / SYS-127 / WF-055; §6 next-step unchanged (v1.4n "Recently deleted" UI surface remains the next-cycle candidate). |
| **v1.4-stab-A — Coverage audit + stabilization roadmap (Phase 41 / SYS-128 / ADR-059 / WF-056)** | _this PR_ | ✓ | 1334 / 1334 (no change — Cycle A is doc-only, no new tests; the "test artifact" for this cycle is the coverage report itself: `coverage/lcov.info` + `coverage/html/index.html`) | **Cycle A of the 3-month stabilization campaign.** Doc-only cycle: no `lib/` changes, no `test/` changes, no new dependencies, no new permissions, no Drift migration, no Kotlin changes. The deliverable is a single source of truth — `docs/v_model/stabilization_roadmap.md` (NEW) — that quantifies the current state (per-file line coverage across all 123 `lib/` files), inventories the known latent bugs (BUG-001..BUG-020 with priorities + target cycles), sequences the next 11 cycles (B..L) with priorities, defines success criteria for the 3-month campaign (≥90% line coverage on every file in `lib/`, 100% on the pure-Dart model layer, E2E tests for 10 critical user flows, 0 known latent bugs, accessibility + i18n audits, reliability paths tested, backup round-trips clean, performance documented, 0 skipped tests), and lists open questions for the user. **Coverage baseline measured.** `flutter test --coverage` produces `coverage/lcov.info` (133 KB, 8812/13638 lines = 64.61% line coverage across 123 `lib/` files). The per-file table in `stabilization_roadmap.md` §1 categorizes each file into Priority-1 (<80% — needs cycle B..L work), Priority-2 (80-90% — buffer zone), or Priority-3 (≥90% — tracked but not action-required). 33 files are Priority-1 (e.g., `lib/people/person.dart` at 54.5%, `lib/do/consecutive_counter.dart` at 75.8% — pure-Dart model files that MUST hit 100% per success criterion #2). **Cycle sequencing (Cycles B..L, 11 cycles across ~3 months):** Month 1 — Cycle B (fix `_toRow` automations + pausedUntil latent bugs → BUG-001 + BUG-002), Cycle C (full-screen launch hardening API 34+ → BUG-003), Cycle D (permission flow audit per-permission-kind + `callScreening` probe → BUG-005). Month 2 — Cycle E (reliability detection coverage — every `Reliability` enum path), Cycle F (backup round-trip exhaustive — every version × every table × every field), Cycle G (DoAnchor "Target paused" badge on home tile → BUG-004 — the UI for the v1.4l data layer), Cycle H ("Recently deleted" Restore / Delete forever UI for tombstoned dos — the v1.4n feature moved INSIDE the stabilization window per ADR-059 §4). Month 3 — Cycle I (i18n test exhaustive → partial BUG-006 closure), Cycle J (accessibility audit — TalkBack + contrast + font-scale), Cycle K (E2E integration tests for 10 critical user flows), Cycle L (performance audit + fuzz + benchmark). **Latent bugs inventoried (BUG-001..BUG-020).** BUG-001 (`_toRow` missing `automations_json` — user's automations silently lost on Save) + BUG-002 (`_toRow` missing `paused_until_millis` — user's pause state silently lost on Save) → Cycle B (P0, data loss). BUG-003 (Android 14+ `USE_FULL_SCREEN_INTENT` permission — lockscreen-bypass fails silently without it) → Cycle C (P1, reliability). BUG-004 (DoAnchor "Target paused" badge on home tile — data layer ships in v1.4l, UI deferred) → Cycle G (P2, UX). BUG-005 (`callScreening` permission probe incomplete — rationale copy exists but runtime probe deferred in v1.1f) → Cycle D (P2, reliability). BUG-006 (Spanish `es` ARB has stale copy — v1.0 native-speaker review deferred) → Cycle I (P3, UX — partial: test coverage but copy review still needs a native speaker, separate from stabilization). The audit may add BUG-007..BUG-020 as Priority-1 files are inspected. **v1.4n "Recently deleted" UI moves to post-stab parking lot** — the data layer + tests for `listDeleted` + `purgeDeletedOlderThan` already shipped in v1.4m, so the v1.4n PR is purely UI when it ships (Cycle H in the new sequencing, INSIDE the 3-month window since the API surface is pinned + small UI scope per ADR-059 §4). **SYS-128 + ADR-059 + WF-056 appended** to the V-Model docs (`requirements.md` + `decision_record.md` + `workflows.md`). `feature.md` §4 updated — v1.4n moved from §6 next-step to §4 parking lot (deferred-until-post-stab); §5 quick-index updated to ADR-059 / SYS-128 / WF-056; §6 next-step updated to the audit cycle. **`coverage/lcov.info` (NEW, 133 KB) + `coverage/html/index.html` (NEW)** are the test artifacts for this cycle (parsed by a Python script since `lcov` is not installed and `sudo apt install lcov` requires interactive auth; the Python parser uses `SF:` / `LF:` / `LH:` markers and writes a per-file table to stdout). **3-gate (regression check — no `lib/` / `test/` changes):** `dart format --output=none --set-exit-if-changed .` (264 files, 0 changed) + `flutter analyze --fatal-infos lib test` (0 issues) + `flutter test` (1334/1334 pass — unchanged from v1.4m). The "exhaustive test" coverage audit is the deliverable, not the test count delta. |

| **v1.4-stab-B — Fix `_toRow` automations + pausedUntil data-loss bugs (Phase 42 / SYS-129 / ADR-060 / WF-057)** | _this PR_ | ✓ | 1337 / 1337 (1334 v1.4-stab-A tip + 3 new Cycle B save-invariant tests in a new `DoRepository save invariant (Cycle B / BUG-001 + BUG-002)` group — `automations round-trip through save + getById` + `pausedUntil round-trips via direct companion UPDATE + getById` + the headline `save(d) does NOT clobber an existing pausedUntilMillis` pin) | **Cycle B of the 3-month stabilization campaign.** Closes BUG-001 + BUG-002 (the two P0 data-loss bugs inventoried in `docs/v_model/stabilization_roadmap.md §2`). Both bugs lived in `DoRepository._toRow` (the Drift `Habits` row ↔ in-memory `Do` mapping) and silently lost user state on every Save click. **The two fixes** (mirroring the v1.4l `deletedAtMillis` omission precedent from ADR-056): (1) **BUG-001 (write + read of `automations_json`)** — added `automationsJson: encodeAutomationList(d.automations)` to `_toRow` (empty list → SQL NULL, matches the `EventRepository` / `PersonRepository` convention at `event_repository.dart:97-99` + `person_repository.dart:70-72`); threaded `automations: base.automations` through every `_fromRow` subclass arm (`DoFixed` / `DoInterval` / `DoAnchor` / `DoDayOfX` / `DoTimeWindow`) so the read path decodes the column back into `d.automations`. The user's custom automation rules (e.g., `TriggerBatteryLow → ActionNotify "Plug in"`) now survive every Save click. (2) **BUG-002 (clobber of `paused_until_millis`)** — removed `pausedUntilMillis: d.pausedUntil?.millisecondsSinceEpoch` from `_toRow`. The column is INTENTIONALLY omitted (mirroring the v1.4l `deletedAtMillis` pattern) so Drift's `insertOnConflictUpdate` semantics preserve the existing pause timestamp across Save clicks. The read path `_fromRow` continues to decode `pausedUntilMillis` (the omission is write-side only). **PauseService refactored.** `pauseHabit(habit, until)` and `resumeHabit(habit)` no longer go through `DoRepository.save` (which is now content-only + pause-preserving) — they write the `pausedUntilMillis` column directly via a `HabitsCompanion` UPDATE: `(db.update(db.habits)..where((t) => t.id.equals(habit.id))).write(HabitsCompanion(pausedUntilMillis: Value(until.millisecondsSinceEpoch)))`. The methods are now the explicit writers of the column (mirroring the v1.4l `restoreById` shape). `pausePerson` + `resumePerson` are unchanged (people are stored differently — see ADR-055 §"Pause for people"). **`save()` KDoc updated** to document the dual invariant — content-only + tombstone-preserving (v1.4l) + pause-preserving (Cycle B). The Drift SQL shape (`insertOnConflictUpdate(HabitRow)` with `toCompanion(true)` semantics where omitted columns are `Value.absent()`) is the foundation that makes the omission pattern work for `deletedAtMillis` AND `pausedUntilMillis`. **Modified.** `lib/services/do_repository.dart` (the `_toRow` mapping + `_fromRow` decode across 5 subclass arms + the `save()` KDoc) — net change is +1 line (add `automationsJson`) / -1 line (remove `pausedUntilMillis`) + 1 line per 5 subclass arms for `automations:` threading + extensive KDoc updates (mirrors the v1.4l comment shape). `lib/services/pause_service.dart` (the `pauseHabit` + `resumeHabit` refactor + the KDoc updates + the 3 new imports — `db.dart`, `db/schema.dart`, `drift/drift.dart show Value`) — net change is +10 lines (replaces 2 ~10-line copyWith+save call sites with 2 ~5-line direct `HabitsCompanion` UPDATEs). `test/services/do_repository_test.dart` — extended `_do(...)` helper to accept `List<Automation>? automations` + `DateTime? pausedUntil` (forwarded to `DoFixed`'s super); added `_twoAutomations()` helper returning 2 seeded `Automation` fixtures (`TriggerBatteryLow(20)` + `TriggerTimeOfDay(7, 30)`, each with a stable id); added a new group `'DoRepository save invariant (Cycle B / BUG-001 + BUG-002)'` with 3 tests (placed after the v1.4l save-invariant group, before the hard-delete group). **No new `<uses-permission>`, no new pubspec deps, no Drift migration (the columns already exist), no new Drift tables, no new MethodChannels, no Kotlin changes** — Cycle B is pure-Dart + 3 new tests. Permission baseline unchanged; verification against `docs/v_model/architecture_options.md §"Permission baseline"` confirmed no AndroidManifest touch. **SYS-129 + ADR-060 + WF-057 appended** to the V-Model docs (`requirements.md` + `decision_record.md` + `workflows.md`). `feature.md` §4 parking-lot bullets for BUG-001 + BUG-002 removed (both closed); §5 quick-index updated to ADR-060 / SYS-129 / WF-057; §6 next-step updated to Cycle C (full-screen launch hardening for Android 14+, BUG-003). `docs/v_model/stabilization_roadmap.md §2` to be updated to mark BUG-001 + BUG-002 as "closed (Cycle B)" in the §2 table — out of scope for this PR (the roadmap doc is updated once per cycle as the §3 cycle-status table advances; deferred to the Cycle C PR's commit). **3-gate.** `dart format --output=none --set-exit-if-changed .` (264 files, 0 changed — pure-Dart + new tests) + `flutter analyze --fatal-infos lib test` (0 issues) + `flutter test` (1337/1337 pass — was 1334 at the v1.4-stab-A tip, +3 from the Cycle B group). Targeted runs per `CLAUDE.md`: `flutter test test/services/do_repository_test.dart` (passes; +3 new tests) + `flutter test test/services/pause_service_test.dart` (passes; readiness-gate regression check, no new assertions needed because column writes are exercised via `do_repository_test.dart`'s new round-trip test). |

| **v1.4-stab-C — FSI reliability wiring: defense-in-depth + BUG-003 closure (Phase 43 / SYS-130 / ADR-061 / WF-058)** | _this PR_ | ✓ | 1345 / 1345 (1337 v1.4-stab-B tip + 8 new Cycle C tests across 3 files: +5 `test/reminders/full_screen_intent_test.dart` (NEW — FakeFullScreenIntent show/showRoutineOverlay/getLaunchIntent + equality) + +3 `test/services/full_screen_intent_service_test.dart` (MethodChannelFullScreenIntentSource production-source defense-in-depth: PlatformException×2 + MissingPluginException×1) + +2 `test/reminders/reminder_bridge_fsi_channel_test.dart` (NEW — channel-surface gap pin: Dart seam exercised + production-state-throws)) | **Cycle C of the 3-month stabilization campaign.** Closes BUG-003 (Android 14+ `USE_FULL_SCREEN_INTENT` permission — the runtime path; the manifest already declares the permission at `AndroidManifest.xml:83-85`). Cycle C is the first cycle whose scope is dramatically smaller than the `stabilization_roadmap.md` §3 draft suggested — the permission probe + reliability wiring + launch handlers shipped in v1.3c (Phase 14) + v1.3d (Phase 15); what was actually missing was test coverage, a doc typo, a stale comment, and a known channel-surface gap. **No production code changes to the FSI channel surface.** The defense-in-depth swallow on `MethodChannelFullScreenIntentSource` (catches `MissingPluginException` + `PlatformException` → `false` on both `isGranted` + `openSettings`) was ALREADY in the v1.3c / SYS-113 code — Cycle C's contribution is (a) renaming the class to drop the `_` prefix so tests can construct it directly + annotating with `@visibleForTesting`, (b) adding a class-level KDoc that documents the swallow as INTENTIONAL per ADR-013 + ADR-061 (so a future reader doesn't "fix" it by removing the catches), (c) lifting test coverage from 25% to ≥80% on `lib/reminders/full_screen_intent.dart` and from 80.5% to ≥95% on `lib/services/full_screen_intent_service.dart`. **Stale `wakelock_plus` reference replaced.** The file-level header at `lib/reminders/full_screen_intent.dart:1-24` referenced `wakelock_plus` (a pubspec dep that does NOT exist — `pubspec.yaml` has 0 `wakelock_plus` matches); the production wake mechanism is `FLAG_KEEP_SCREEN_ON` in `android/app/src/main/kotlin/com/doit/FullScreenActivity.kt:47-56`. The header is rewritten to reference the actual production mechanism — prevents a future contributor from adding `wakelock_plus` and ending up with two wake mechanisms fighting each other. **Doc typo fixed.** `docs/v_model/notification_reliability.md:496` "On API 14+" → "On API 34+" — `USE_FULL_SCREEN_INTENT` was introduced in API 34 (Android 14), NOT API 14. The typo has been misleading readers for ~2 months (since v1.3c / SYS-113). **KNOWN channel-surface gap pinned as a follow-up bug.** `ReminderBridge.showFullScreen` (Dart seam at `lib/reminders/reminder_bridge.dart:60` + `:218`) invokes `_channel.invokeMethod('showFullScreen', ...)` over `doit/reminders`, but the Kotlin side at `android/app/src/main/kotlin/com/doit/ReminderChannelProxy.kt:33-78` has NO arm for `showFullScreen` — the `when` block handles `setExact` / `cancel` / `showNotification` / `cancelNotification` / `probeReliability`; everything else falls through to `result.notImplemented()` → `MissingPluginException` on Dart. The gap is INERT today (a repo-wide grep confirms no production Dart code calls `reminderBridge.showFullScreen(...)` — the FSI launch path is wired through `lib/services/platform_full_screen_intent.dart` → `doit/full_screen` channel instead, which has the `showHabitMission` / `showRoutineOverlay` arms). The test at `test/reminders/reminder_bridge_fsi_channel_test.dart` pins the gap — a future stabilization cycle will either (a) remove the dead Dart arm or (b) add the matching Kotlin arm. **Modified.** `lib/services/full_screen_intent_service.dart` (rename `_MethodChannelFullScreenIntentSource` → `MethodChannelFullScreenIntentSource` + `@visibleForTesting` annotation + all 4 internal references updated + 20-line class-level KDoc documenting the defense-in-depth swallow as INTENTIONAL). `lib/reminders/full_screen_intent.dart` (file-level header rewritten to reference `FLAG_KEEP_SCREEN_ON` instead of `wakelock_plus`; cross-references `android/.../FullScreenActivity.kt:47-56`). `docs/v_model/notification_reliability.md` (line 496 "API 14+" → "API 34+"). `test/reminders/full_screen_intent_test.dart` (NEW, +5 tests in 5 groups: FakeFullScreenIntent.show invocation-order; showRoutineOverlay 4-input shape; getLaunchIntent scripted-`LaunchIntent` round-trip; getLaunchIntent null script case; `RoutineOverlayLaunch` + `LaunchIntent` equality). `test/services/full_screen_intent_service_test.dart` (extended, +3 tests in new `MethodChannelFullScreenIntentSource (production source)` group: PlatformException on isGranted; PlatformException on openSettings; MissingPluginException on isGranted). `test/reminders/reminder_bridge_fsi_channel_test.dart` (NEW, +2 tests pinning the Dart seam IS exercised + production-state-throws as `MissingPluginException`). **No new `<uses-permission>`**, **no new pubspec deps**, **no Drift migration**, **no Kotlin changes** — Cycle C is pure-Dart + docs + new tests. The Drift `kCurrentSchemaVersion` stays at 5. Permission baseline unchanged; verification against `docs/v_model/architecture_options.md §"Permission baseline"` confirmed no AndroidManifest touch. **SYS-130 + ADR-061 + WF-058 appended** to the V-Model docs (`requirements.md` + `decision_record.md` + `workflows.md`). `feature.md` §4 BUG-003 parking-lot bullet removed (closed); §5 quick-index updated to ADR-061 / SYS-130 / WF-058; §6 next-step updated to Cycle D (`feat/v1.4-stab-D-permission-flow-audit` — permission flow audit covering the 4 most-used kinds: `notifications`, `location`, `calendar`, `fullScreenIntent`; closes BUG-005 + BUG-011 + BUG-020 + partial BUG-012). `docs/v_model/stabilization_roadmap.md §2` to be updated to mark BUG-003 as "closed (Cycle C)" in the §2 table — out of scope for this PR (the roadmap doc is updated once per cycle as the §3 cycle-status table advances; deferred to the Cycle D PR's commit). **3-gate.** `dart format --output=none --set-exit-if-changed .` (264 + ~5 files, 0 changed — pure-Dart + new tests are pre-formatted) + `flutter analyze --fatal-infos lib test` (0 issues) + `flutter test` (1345/1345 pass — was 1337 at the v1.4-stab-B tip, +8 from the Cycle C group). Targeted runs per `CLAUDE.md`: `flutter test test/reminders/full_screen_intent_test.dart` (passes; +5 tests) + `flutter test test/services/full_screen_intent_service_test.dart` (passes; +3 tests) + `flutter test test/reminders/reminder_bridge_fsi_channel_test.dart` (passes; +2 tests). Coverage: `lib/reminders/full_screen_intent.dart` ≥80% (from 25.0%); `lib/services/full_screen_intent_service.dart` ≥95% (from 80.5%); the channel-surface gap test is a regression-protector, not a coverage gate. |

| v1.4-stab-D | Permission flow coverage: per-kind exhaustive tests + lifecycle edge cases (Phase 44 / SYS-131 / ADR-062 / WF-059) | test-only cycle — no production code changes | 1348 → 1363 (+15 net) | `permission_result.dart` 18.9% → 100%; `permission_service.dart` 93.4% → ≥95%; `permission_lifecycle_observer.dart` 78.6% → ≥90%; `person.dart` 54.5% → ≥80% | Closes BUG-005, BUG-011, BUG-012 (partial), BUG-020 |

| v1.4-stab-E | Reliability detection coverage: broadcast+distinct stream + first-read race + idle-window (Phase 45 / SYS-132 / ADR-063 / WF-060) | test-only cycle — no production code changes | 1363 → 1371 (+8 net) | `reliability_service.dart` extended (+5 tests) + `alarm_scheduler_test.dart` +2 fallback-path tests + new `doze_simulation_test.dart` (+1) | Closes BUG-013, BUG-014 |

| v1.4-stab-F | Backup round-trip exhaustive coverage: malformed envelopes + dispatcher error paths (Phase 46 / SYS-133 / ADR-064 / WF-061) | test-only cycle — no production code changes | 1371 → 1379 (+8 net) | `backup_service.dart` ≥95% (closes 12 uncovered lines) + `backup_scheduler.dart` ≥90% (closes 5) + new `test/backup/scheduler_skip_test.dart` (+1) + `backup_task_dispatcher_test.dart` extended (+2) | Closes the bug_hunt.md BUG-016..-018 cluster (malformed-envelope / missing-KDF / v2 KDF floor / dispatcher init-failure / `runBackupTask` ScheduleMode.none) |
| v1.4-stab-G | DoAnchor "Target paused" badge + BUG-019 sparkline pin (Phase 47 / SYS-134 / ADR-065 / WF-062) | feat: NEW badge widget + ~30 line home.dart wiring + 2 ARB keys | 1379 → 1385 (+6 net) | `lib/screens/home.dart` 85.7% → ≥90%; `lib/screens/home_tile_sparkline.dart` 78.6% → ≥85%; NEW `lib/widgets/do_anchor_paused_badge.dart` 0% → ≥90% | Closes BUG-004 (v1.4l-deferred UI), BUG-019 (sparkline single-point edge) |
| v1.4-stab-H | Recently-deleted top-level screen: v1.4l tombstone surface (Phase 48 / SYS-135 / ADR-066 / WF-063) | feat: NEW screen + /recently-deleted route + Settings tile + 15 ARB keys | 1388 → 1400 (+12 net) | `lib/screens/recently_deleted_screen.dart` 0% → 100% (new file); `lib/app_router.dart` 85.7% → 100%; `lib/screens/settings.dart` 85% → ≥90% | Ships the v1.4l-deferred UI affordance for the tombstone column (ADR-056) |
