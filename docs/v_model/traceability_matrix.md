# Traceability Matrix

Status: draft baseline, created 2026-06-13.

Every row ties a left-side artifact to a right-side verification. The
matrix is the right-side of the V. If a row has no verification, it is
not yet a real requirement.

## Need → Requirement → Design → Verification

| Need / Decision | Requirement IDs | Design Area | Verification |
| --- | --- | --- | --- |
| Add habits with schedules, proof, and missions | SYS-001, SYS-002, SYS-004, SYS-007 | `lib/habits/`, `lib/screens/add_habit.dart` | `test/habits/add_habit_test.dart`, manual acceptance |
| Schedule reminders reliably | SYS-003, SYS-016, SYS-017, SYS-018, SYS-030 | `lib/reminders/`, `AlarmScheduler`, `BootReceiver` | `test/reminders/alarm_scheduler_test.dart`, `test/reminders/reboot_survival_test.dart`, manual device check |
| Surface reminders that interrupt | SYS-005, SYS-006, SYS-029 | `lib/reminders/notification_service.dart`, `lib/reminders/full_screen_intent.dart`, `lib/screens/widget_home.dart` | Widget test, manual device check on locked phone |
| Three proof modes (Soft / Strong / Auto) | SYS-007, SYS-013, SYS-031 | `lib/habits/proof_mode.dart`, `lib/missions/chain.dart` | `test/habits/proof_mode_test.dart`, `test/missions/chain_test.dart` (chain-timeout test) |
| Five mission types | SYS-008, SYS-009, SYS-010, SYS-011, SYS-012 | `lib/missions/shake.dart`, `lib/missions/type.dart`, `lib/missions/hold.dart`, `lib/missions/math.dart`, `lib/missions/memory.dart` | `test/missions/<name>_test.dart` for each |
| Call reminders open dialer | SYS-014 | `lib/reminders/call_action.dart`, `AndroidManifest.xml` (no `CALL_PHONE`) | Intent inspection test, manual device check, manifest diff in PRs |
| Wake-up anchor | SYS-015, SYS-016, SYS-017 | `lib/reminders/anchor_detector.dart`, `lib/screens/widget_home.dart` | Unit test, widget test, manual device check on first unlock |
| Streaks and rest days | SYS-019, SYS-020 | `lib/habits/streak_calculator.dart`, `lib/habits/rest_day_budget.dart` | `test/habits/streak_calculator_test.dart` covering DST, rest day, missed-then-backfilled, partial-day edge cases |
| Stats | SYS-021 | `lib/screens/stats.dart`, `lib/services/stats_service.dart` | Widget test, manual acceptance |
| Local-only data | SYS-026, SYS-030 | `lib/services/`, `AndroidManifest.xml` (no `INTERNET`) | Code search, CI grep rule, manifest review |
| Auto backup | SYS-023, SYS-024 | `lib/services/backup_service.dart`, `lib/screens/settings_restore.dart` | `test/backup/auto_backup_test.dart`, `test/backup/restore_test.dart` |
| Local DB and migrations | SYS-022 | `lib/services/db.dart`, `lib/services/migrations/` | `test/db/migration_test.dart`, manual DB inspection on real device |
| Permission-first UX | SYS-025 | `lib/screens/onboarding.dart` | Widget test, manual acceptance |
| 3-gate and coverage | SYS-027, SYS-028 | CI workflow, `analysis_options.yaml` | CI run on every PR; coverage report artifact |
| Home widget | SYS-029 | `android/app/src/main/.../HomeWidgetProvider.kt`, `lib/services/widget_sync.dart` | Widget test, manual device check |
| One-off date reminder (event) | SYS-032, SYS-033, SYS-034, SYS-035 | `lib/events/event.dart`, `lib/events/event_repository.dart`, `lib/screens/add_event.dart`, `lib/screens/events_list.dart` | `test/events/event_repository_test.dart`, widget test for add-event, integration test for the one-shot schedule |
| Contact group | SYS-036, SYS-037, SYS-038 | `lib/people/person_group.dart`, `lib/people/group_repository.dart`, `lib/screens/add_group.dart` | `test/people/group_repository_test.dart`, `test/people/group_rotation_test.dart` |
| Time-window habit | SYS-039, SYS-040 | `lib/habits/habit_time_window.dart`, `lib/widgets/fasting_timer.dart` | `test/habits/time_window_schedule_test.dart`, widget test for the fasting timer |
| Edit habit | SYS-042, SYS-043 | `lib/screens/edit_habit.dart` (extends `add_habit.dart`) | `test/screens/edit_habit_test.dart`, integration test that edit preserves log |
| Pause / resume | SYS-047 | `lib/services/pause_service.dart`, `lib/screens/habit_detail.dart` | `test/services/pause_service_test.dart`, widget test for the pause UI |
| Test reminder (30s) | SYS-041 | `lib/services/test_fire_service.dart`, `lib/screens/habit_detail.dart` | `test/services/test_fire_test.dart`, widget test for the test button |
| Bulk complete | SYS-044 | `lib/services/bulk_completion_service.dart`, `lib/screens/home.dart` | `test/services/bulk_completion_test.dart`, widget test for the bulk button |
| Category / color / icon | SYS-045, SYS-046 | `lib/habits/category.dart`, `lib/widgets/category_chip.dart`, `lib/widgets/icon_picker.dart`, `lib/screens/home.dart` | `test/habits/category_test.dart`, widget test for the icon picker |
| Test reminder — v0.1 aspirational (habit-detail "Test in 30s") | SYS-041 | `lib/services/test_fire_service.dart`, `lib/screens/habit_detail.dart` | Deferred in v0.2; superseded by the Settings tile (SYS-048). |
| Test reminder — v0.2 actual (Settings → "Send a test reminder") | SYS-048 | `lib/screens/settings.dart` (About section), `lib/services/reminder_service.dart` | `test/screens/settings_test_reminder_test.dart`, `test/services/reminder_service_test.dart` |
| Bulk complete — v0.1 aspirational (interval bulk) | SYS-044 | `lib/services/bulk_completion_service.dart` | Subsumed by the v0.2 home-screen bulk action (SYS-049). |
| Bulk complete — v0.2 actual (home-screen bulk) | SYS-049 | `lib/screens/home.dart`, `lib/services/completion_log_service.dart` | `test/services/bulk_complete_test.dart` |
| v0.3 release docs (LICENSE, PRIVACY) | SYS-050, SYS-051 | `LICENSE` (MIT), `PRIVACY.md` | File checks at repo root; reviewed against the v0.3 floor. |
| v0.3 version constant | SYS-052 | `lib/build_info.dart`, `pubspec.yaml` | `test/build_info_test.dart` parses pubspec + asserts constants. |
| v0.3 release signing | SYS-053 | `android/app/build.gradle.kts`, `android/key.properties.example`, `.gitignore` | `test/release_signing_test.dart` parses the build script + gitignore. Hands-on `flutter build appbundle --release` with the user's env vars. |
| v0.3 in-app About / Licenses | SYS-054 | `lib/screens/settings.dart` (About section) | `test/screens/settings_licenses_test.dart` |
| v0.3 fresh-install smoke | SYS-055 | `test/integration/fresh_install_test.dart` | The widget test + the user's hands-on wiped-device checklist in `v0_3_release_checklist.md`. |
| v0.3 honest README | SYS-056 | `README.md` | Manual review on every commit that touches `README.md`. |
| v0.4 CI 3-gate | SYS-057 | `.github/workflows/ci.yml` | `test/ci_workflow_test.dart` parses the workflow and asserts the three steps + the trigger set. |
| v0.4 `CHANGELOG.md` | SYS-058 | `CHANGELOG.md` (repo root) | Manual review on every commit that touches the changelog; `open_questions.md` #20 closed by this row. |
| v0.4 firstLaunch persisted | SYS-059 | `lib/services/settings_service.dart` (firstLaunchCompleted ValueNotifier + SharedPreferences round-trip), `lib/main.dart` (route switch) | `test/services/first_launch_persisted_test.dart` (7 tests) + `test/widget_test.dart` (3 tests: wiped install, mark+remount, override). |
| v0.4 WorkManager periodic backup | SYS-060 | `lib/services/backup_scheduler.dart` (Workmanager register/cancel + top-level dispatcher) | `test/services/backup_scheduler_test.dart` (5 tests) + `test/services/backup_task_dispatcher_test.dart` (3 tests). |
| v0.4 backup encryption | SYS-061 | `lib/services/backup_service.dart` (v2 envelope: PBKDF2-HMAC-SHA256 + AES-256-GCM) | `test/services/backup_encryption_test.dart` (5 tests: round-trip, wrong passphrase, missing passphrase, v1 back-compat, KDF floor). |
| v0.4 TalkBack / a11y static review | SYS-062 | `lib/screens/*.dart`, `lib/widgets/*.dart` (every interactive element with a `tooltip` / `semanticLabel` / `Semantics` wrapper) | `test/a11y/semantics_labels_test.dart` (18 per-file tests: walks every screens + widgets file and asserts the labels). User's hands-on TalkBack pass on a real device is the v0.4d sign-off step. |
| v0.5 permission service seam | SYS-063, SYS-064, SYS-065, SYS-066, SYS-025 (closure) | `lib/services/permission_service.dart`, `lib/services/permission_result.dart` (sealed result: `granted` / `denied(canOpenSettings)` / `permanentlyDenied`); `lib/screens/onboarding.dart` (CTA dispatch on `_step`); `lib/screens/settings.dart` (new `Permissions` tile above `Reliability`); `lib/services/settings_service.dart` (`ValueNotifier<String?> backupFolderUri`); `lib/services/backup_service.dart` reads the URI at backup time | `test/services/permission_service_test.dart` (9 tests: each `requestX()` × each `PermissionStatus` branch + idempotent init + platform-error swallow) + `test/services/settings_service_backup_uri_test.dart` (3 tests) + `test/screens/onboarding_permission_wiring_test.dart` (6 tests: each step CTA × granted/denied/permanentlyDenied + skip + backupUri persistence) + `test/screens/settings_permissions_test.dart` (4 tests: row renders, "Settings" button on `permanentlyDenied`, deep-link tap, on-demand probe) + manual device check on the user's SM-S918B |
| v1.0b.1+b.2+b.3 templates: model + Drift v2→v3 + catalog UI + save-as-template | SYS-067, SYS-068 | `lib/templates/{template.dart,template_library.dart,template_repository.dart}` (v2→v3 migration adds `Templates` table; 25 hand-crafted built-ins seeded on first run); `lib/screens/templates.dart` grid; "Save as template" AppBar action on `AddDoScreen` / `AddEventScreen` / `AddPersonScreen`; home-screen FAB shows "Browse templates" alongside "Create blank". | `test/templates/template_repository_test.dart` (`seedBuiltIns` idempotent across two runs; `deleteById` refuses built-ins) + `test/templates/template_library_test.dart` (25 cards render after first seed; idempotent re-seed returns 0; built-in delete throws `TemplateIsBuiltIn`) + `test/screens/templates_test.dart` (catalog grid + save-as-template extraction) |
| v1.0c.1+c.2 sealed `Trigger`/`Condition`/`Action` + `GeofenceService` | SYS-069, SYS-070, SYS-071, SYS-072, SYS-076 | `lib/triggers/{trigger.dart,condition.dart}`; `lib/actions/action.dart` (sealed hierarchy + per-shape `validate()` + `toJson`/`fromJson` codec + `type` discriminator); v3→v4 Drift migration (nullable `automations_json` column on habits / people / events); `RoutineExecutor` skeleton; `lib/services/geofence_service.dart` wraps `geolocator` ^13.0.1 with a pure-Dart Haversine matcher (`computeTransitions` exposed `@visibleForTesting`). | `test/triggers/{trigger,condition}_test.dart` (every leaf's `validate` + `toJson`/`fromJson` round-trip + exhaustive `switch` exhaustiveness check) + `test/actions/action_test.dart` + `test/services/geofence_service_test.dart` (14 tests — pure matcher + registry + events stream) + `test/routines/location_dispatch_test.dart` (6 tests — enter/exit/mismatch/disabled/kind-mismatch/multi-entity dispatch) |
| v1.0d.1+d.2 device-state triggers (7 kinds) | SYS-073 | `android/app/src/main/kotlin/com/doit/DeviceStateChannel.kt` (charging / battery range / bluetooth device / Wi-Fi SSID / headphones / silent mode / foreground app) + `lib/services/device_state_probe.dart` (`Stream<DeviceStateSnapshot>`) + 7 `TriggerDeviceState.*` leaves wired into `RoutineExecutor._onDeviceState`. Settings → Triggers screen renders a live dashboard. | `test/services/device_state_probe_test.dart` (scripted snapshots) + `test/routines/device_state_dispatch_test.dart` (every leaf × match / no-match / disabled) |
| v1.0e.1+e.2 calendar triggers + `CalendarPicker` | SYS-074 | `lib/services/calendar_probe.dart` wraps `device_calendar`; `TriggerCalendarEvent` sealed (starts / ends / busy / free); `PermissionKind.calendar` (`READ_CALENDAR`); `lib/widgets/calendar_picker.dart` UI for calendar account selection; "Add a calendar routine" button in the add-do / add-event / add-person "Routines" section. | `test/services/calendar_probe_test.dart` (scripted account + event stream) + `test/routines/calendar_dispatch_test.dart` + `test/widgets/calendar_picker_test.dart` |
| v1.0f.1+f.2 `CallInterceptor` + Japan silent-mode routine | SYS-075 | `android/app/src/main/kotlin/com/doit/CallInterceptor.kt` (`CallScreeningService` implementation); `ActionCallIntercept` + `ActionOverrideSilent` Dart wrappers; `ReminderBridge` extended with `setCallInterceptorConfig` + `setRingerMode`; `JapanRoutineConfig` (enabled / contactIds / targetMode) persisted under `doit.japan_routine.{enabled,contact_ids,target_mode}` in `SettingsService`; `AddRoutineScreen` for template #16. | `test/routines/call_intercept_test.dart` (scripts `onScreenCall` events) + `test/services/call_interceptor_test.dart` (role methods via `ScriptedCallSource`) + `test/services/japan_routine_config_test.dart` (codec + equality) + `test/services/settings_service_japan_test.dart` (set/get/persist round-trip) + `test/screens/add_routine_test.dart` (Japan form) + `test/screens/settings_call_screening_tile_test.dart` (role probe) + manual device check on a real call |
| v1.1a — `RoutineConfig` value class + per-template persistence | SYS-080 | `lib/services/routine_config.dart` (sealed-via-discriminator value class: `{templateId, triggerJson, conditionJson?, actionJson, enabled}`; structural `==`; deterministic `hashCode`; `copyWith`; `toJson`/`fromJson` codec with version-free per-shape discriminators; null `conditionJson` distinct from `{}`); `SettingsService.setRoutine` / `getRoutine` / `deleteRoutine` / `routines` (`ValueNotifier<Map<String, RoutineConfig>>`) persisted under `doit.routine.<templateId>` in SharedPreferences. | `test/services/routine_config_test.dart` (12 codec + equality + `hashCode` determinism + Set round-trip tests) + `test/services/settings_service_routines_test.dart` (5 persistence tests) + `test/routines/executor_test.dart` (2 routine-executor dispatch regression tests) |
| v1.1b — RoutineExecutor dispatch + reactive `SettingsService.routines` | SYS-080 (subscription side) | `lib/routines/routine_executor.dart` consumes `SettingsService.routines` reactively via a `ValueListenable` listener. `_dispatchAction` is a single exhaustive `is`-switch over all five `Action` leaves. | `test/routines/action_dispatch_test.dart` (4 dispatch tests — every leaf × positive path) + 12 reactive subscription tests |
| v1.1c — `ActionOpenApp` + `RoutineOpenAppRequest` + `RoutineBanner` | SYS-082 | `lib/routines/routine.dart` — `RoutineOpenAppRequest` value class + `RoutineExecutor.pendingOpenApp: ValueListenable<List<RoutineOpenAppRequest>>`. `lib/widgets/routine_banner.dart` — passive widget that drains the queue FIFO; captures `NavigatorState` synchronously inside `build` (NOT inside the post-frame callback). `lib/screens/home.dart` places the banner directly under `ReliabilityBanner.fromService()`. | `test/widgets/routine_banner_test.dart` (4 tests — empty-queue shrink, single push, FIFO drain, "opening N destinations" copy) + `test/routines/action_dispatch_test.dart` (3 `ActionOpenApp` tests — append, accumulate, `pendingOpenApp` listener fires, `resetForTesting` clears) + `test/routines/executor_test.dart` (retroactive coverage for `_dispatchAction` per v1.1c, SYS-082) |
| v1.1d — generic `RoutineApplyScreen` for templates #17..#21 | SYS-083 | `lib/routines/routine_template_payload.dart` — fail-soft decoder for `{k:1, routine:{trigger, condition, action, note}}` envelope (8 defect paths return `null`); `lib/screens/routine_apply.dart` — generic apply UX (enable toggle + Save / Update / Delete + `_MalformedView`); `SettingsService.deleteRoutine`; `TemplatesScreen._onUse` routes templates #17..#21 via `MaterialPageRoute`; the "Coming in v1.1" badge is removed. | `test/routines/routine_template_payload_test.dart` (12 tests — well-formed decode + 8 defect paths + 2 codec round-trips + 3 equality) + `test/services/settings_service_routine_test.dart` (13 tests incl. 2 for `deleteRoutine`) + `test/screens/routine_apply_screen_test.dart` (6 tests — render / save / toggle / update-with-Delete / delete / malformed-envelope) + `test/screens/templates_test.dart` (all 6 routine templates carry a "Use this" button) + `test/screens/templates_japan_routing_test.dart` (asserts #17 routes to `RoutineApplyScreen`) |
| v1.1e — offline `LocationMapPreview` for `LocationPicker` | SYS-084 | `lib/widgets/location_map_preview.dart` — pure `CustomPaint` widget (5×5 grid + rounded background + filled pin at `(lat, lon)` + translucent ring at the geofence radius); equirectangular projection helpers exposed `projectLatLonForTest` / `unprojectLatLonForTest` / `radiusMetresToPxForTest`; no `flutter_map`, no `latlong2`, no `package:http`, no `INTERNET`. `LocationPicker` mounts the preview between the lat/lon `TextFormField`s and the "Use current location" button; lat/lon fields gain `onChanged: (_) => setState(() {})` so the pin follows typed coordinates. | `test/widgets/location_map_preview_test.dart` (11 tests — 7 pure-helper tests + 4 widget tests: render / tap / prop-change / ring) + `test/widgets/location_picker_test.dart` (3 new tests — preview mounted above the radius slider, tapping the preview writes back, typing in the lat/lon fields updates the pin) |
| v1.1f — per-automation reliability badges | SYS-085 | `lib/routines/automation_reliability.dart` — pure `automationReliability(Automation, statuses)` function exhaustive over the sealed `Trigger` hierarchy via `_requiredPermissionForTrigger` (`TriggerLocation*` → `PermissionKind.location`, `TriggerCalendarEvent*` → `PermissionKind.calendar`, others → `null`). `lib/widgets/automation_reliability_badge.dart` — 40×40 dp `IconButton` wrapped in a `ValueListenableBuilder` over `PermissionService.instance.statuses`; hides itself for `optimal`; warning-amber for `degraded`; info-outline for `unknown`. Three `_RoutineRow` widgets restructure their trailing slot into a `Row(min, [badge, remove])`. | `test/routines/automation_reliability_test.dart` (16 tests — every `Trigger` leaf × every `PermissionResult` variant × null/unprobed + action-side coverage) + `test/widgets/automation_reliability_badge_test.dart` (11 tests — optimal-hide / degraded-warning / unknown-info / `onTap` callback / non-interactive mode / ValueNotifier reactivity both directions) + `test/screens/templates_test.dart` (1 catalog regression) |
| v1.1g — `PACKAGE_USAGE_STATS` permission + rationale UX | SYS-086 | `lib/services/usage_stats_service.dart` — singleton with abstract `UsageStatsSource` seam; `_MethodChannelUsageStatsSource` talks to `doit/device_state` (`isUsageStatsGranted` uses `AppOpsManager.unsafeCheckOpNoThrow(OPSTR_GET_USAGE_STATS, ...)`; `openUsageAccessSettings` deep-links to `Settings.ACTION_USAGE_ACCESS_SETTINGS` with `FLAG_ACTIVITY_NEW_TASK`). `_ready` Completer pre-completed so production callers do not await `init()`. `PermissionService` gains `PermissionKind.usageStats`, `requestUsageStats()`, `refreshUsageStats()`; probe is **fire-and-forget** from `init()` via `unawaited(_refreshUsageStatsAfterInit())`. `PermissionSheet` extends its sealed `PermissionKind` switch with rationale copy + routes "Allow" to the deep-link. Manifest `<uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" tools:ignore="ProtectedPermissions"/>`. | `test/services/usage_stats_service_test.dart` (8 tests — isGranted (granted/denied/reflects-setGranted) + openSettings (resolved/not-resolved/multi-call recording) + singleton lifecycle (idempotent-init / resetForTesting-installs-new-source-resets-gate)) |
| v1.1h — i18n scaffolding (ARB extraction + `es` locale + `localizedApp` test helper) | SYS-087 | `lib/l10n/app_en.arb` (~60 keys — source-of-truth: AppBar titles, snackbars, empty states, settings sections, theme/anchor/reliability copy, permission tile labels, onboarding step labels, licenses/version rows, ICU plural in `homeSelectionAppBarTitle` / `homeSnackbarMarkedCount`); `lib/l10n/app_es.arb` (Spanish translation — smoke-test locale, NOT a professional translation); `l10n.yaml` drives `flutter gen-l10n`; `pubspec.yaml` adds `flutter_localizations` + `intl` + `flutter: { generate: true }`; `MaterialApp.localizationsDelegates` + `supportedLocales` wired; `test/support/localized_app.dart` helper wraps the screen in a `MaterialApp` with the generated delegates pre-installed; 10 screen-test files route through it. | `test/l10n/app_localizations_test.dart` (11 tests — 5 structural assertions + 4 widget tests + 2 class-API tests) + `test/integration/app_localizations_wiring_test.dart` (1 integration test — pumps `DoItApp` root, asserts delegate + `[en, es]` `supportedLocales` are wired) + 9 screen-test files migrated through `localizedApp` |
| v1.1i — Custom app icon + splash (adaptive icon + brand color) | SYS-088 | Hand-authored adaptive-icon vectors replace the default Flutter launcher icon. `drawable/ic_launcher_background.xml` (solid brand purple `#FF6750A4` on 108dp × 108dp viewport); `drawable/ic_launcher_foreground.xml` (white sans-serif lowercase 'd' glyph + check dot at (80, 80) R = 4, all inside the 66dp safe zone); `drawable/ic_launcher_monochrome.xml` (same glyph painted pure white for Android 13+ themed icons). New `mipmap-anydpi-v26/ic_launcher.xml` references all three layers. Both `drawable/launch_background.xml` and `drawable-v21/launch_background.xml` are rewritten as `<layer-list>` referencing `@color/launch_background`. Pre-existing `drawable/ic_streak_notification.xml` gap closed. Version bumped `1.0.0+7` → `1.1.0+8`. Bundled platform maintenance: `compileSdk` 34 → 36, `minSdk` 28 → 30, `CallInterceptor.kt` migrates to `CallScreeningService.CallResponse.Builder`, `MainActivity.kt` passes the Activity explicitly via `setActivity(this / null)`. | `test/app_icon_test.dart` (9 filesystem tests — adaptive-icon manifest presence + three-layer references, foreground white-on-transparent, background brand purple, monochrome pure white, notification icon shape and 24dp size, all five legacy density buckets, both splash drawables reference `@color/launch_background` + centered foreground, `values/colors.xml` defines `launch_background`) + `test/release_signing_test.dart` (2 new tests pin the manifest icon reference + `app_name` + `kAppVersion` together) + `test/integration/app_localizations_wiring_test.dart` + `test/screens/onboarding_test.dart` (all five ARB step titles reachable in order) + `test/screens/settings_test.dart` (theme tile renders all three ARB labels and tapping each updates SettingsService.themeMode) |

## Workflow → Requirement → Verification

| Workflow | Requirement IDs | Verification |
| --- | --- | --- |
| WF-001 First-time onboarding | SYS-022, SYS-025, SYS-027 | Widget test, manual acceptance |
| WF-002 Add a custom habit | SYS-001, SYS-002, SYS-003, SYS-004, SYS-007, SYS-016, SYS-018, SYS-019, SYS-031 | Widget test, manual acceptance |
| WF-003 Add a person | SYS-001, SYS-002, SYS-004 | Widget test, manual acceptance |
| WF-004 Reminder fires (general) | SYS-003, SYS-005, SYS-006, SYS-013, SYS-016, SYS-017, SYS-018, SYS-019, SYS-020 | Integration test, manual device check |
| WF-005 Soft completion | SYS-005, SYS-019, SYS-020 | Widget test |
| WF-006 Strong completion (mission chain) | SYS-006, SYS-007, SYS-008, SYS-009, SYS-010, SYS-011, SYS-012, SYS-013, SYS-020, SYS-031 | Unit + widget tests for each mission, integration test for the chain |
| WF-007 Auto completion (interval) | SYS-007, SYS-019, SYS-020 | Unit test, widget test |
| WF-008 Mark "I'm up" | SYS-015, SYS-016, SYS-017 | Unit test, manual device check on first unlock |
| WF-009 Snooze | SYS-005, SYS-006, SYS-018, SYS-019 | Unit test, widget test |
| WF-010 Skip (rest day) | SYS-019, SYS-020 | Unit test |
| WF-011 Review weekly stats | SYS-021 | Widget test, manual acceptance |
| WF-012 Auto backup | SYS-023, SYS-026 | Integration test, manual device check |
| WF-013 Restore | SYS-024, SYS-026 | Integration test, manual device check |
| WF-014 First-unlock wake-up | SYS-015, SYS-016, SYS-017 | Unit test, manual device check |
| WF-015 Reboot survival | SYS-016, SYS-017, SYS-030 | Integration test, manual device check |
| WF-016 Timezone change / travel | SYS-016, SYS-017, SYS-019 | Unit test for `nextOccurrence` across DST + zone change |
| WF-017 Add a one-off date reminder (v0.2) | SYS-032, SYS-033, SYS-034, SYS-035 | Widget test for the add-event flow + integration test for the one-shot alarm |
| WF-018 Add a contact group (v0.2) | SYS-036, SYS-037, SYS-038 | Widget test for the add-group flow + unit test for the rotation selector |
| WF-019 Add a time-window habit (v0.2) | SYS-039, SYS-040 | Unit test for `HabitTimeWindow.nextOccurrence()` + widget test for the live timer |
| WF-022 Edit a habit (v0.2) | SYS-042, SYS-043 | Widget test for the edit flow + integration test that edit preserves the log |
| WF-027 Pause / resume (v0.2) | SYS-047 | Unit test for the paused-state guard + widget test for the pause UI |
| WF-028 Test reminder in 30s (v0.2) | SYS-041, SYS-048 | v0.1 aspirational (SYS-041, habit detail button) deferred in v0.2; the v0.2 actual surface is the Settings → About "Send a test reminder" tile (SYS-048). Widget test for the tile + integration test for the test fire. |
| WF-029 Bulk complete (v0.2) | SYS-044, SYS-049 | v0.1 aspirational (SYS-044, interval bulk) subsumed by the v0.2 home-screen bulk action (SYS-049). Widget test for the bulk button + integration test for the timestamp spread. |
| WF-031 Category / color / icon (v0.2) | SYS-045, SYS-046 | Widget test for the icon picker + stats grouping |
| WF-032 Open source licenses (v0.3) | SYS-054 | Widget test for the licenses tile + the route push |
| WF-033 Browse / use a built-in template (v1.0b.2) | SYS-067 | Widget test for the `TemplatesScreen` grid + integration test that picking a built-in template routes to the matching add-screen (or `RoutineApplyScreen` for templates #17..#21) pre-filled with the template's payload |
| WF-034 Save a configured Do / Event / Person as a user template (v1.0b.2) | SYS-068 | Widget test for the AppBar overflow action on `AddDoScreen` / `AddEventScreen` / `AddPersonScreen` + integration test that the new `Template` row has `isBuiltIn: false` and the right envelope |
| WF-035 Configure a location-triggered routine (v1.0c.2) | SYS-072 | `test/routines/location_dispatch_test.dart` (6 tests — enter/exit/mismatch/disabled/kind-mismatch/multi-entity) + manual device check driving into the home geofence |
| WF-036 Configure a device-state-triggered routine (v1.0d.2) | SYS-073 | `test/routines/device_state_dispatch_test.dart` (every leaf × match / no-match / disabled) + manual device check toggling charging / bluetooth / Wi-Fi SSID |
| WF-037 Configure a calendar-triggered routine (v1.0e.2) | SYS-074 | `test/routines/calendar_dispatch_test.dart` + manual device check creating a Focus block on the test calendar |
| WF-038 Configure a Japan silent-mode routine (v1.0f.2) | SYS-075 | `test/routines/call_dispatch_test.dart` (scripts `onScreenCall` events) + manual device check on a real call |
| WF-039 Apply a template-based routine — generic flow (v1.1d) | SYS-082, SYS-083 | Widget test for `RoutineApplyScreen` (6 tests — render / save / toggle / update-with-Delete / delete / malformed-envelope) + `test/screens/templates_japan_routing_test.dart` rewritten to assert template #17 routes to `RoutineApplyScreen`; `test/widgets/routine_banner_test.dart` covers the `ActionOpenApp` drain UX |
| WF-040 `USE_FULL_SCREEN_INTENT` probe + reliability wiring (v1.3c / Phase 14) | SYS-113 | `test/services/full_screen_intent_service_test.dart` (8 tests — `isGranted` happy path + `MissingPluginException` swallow + `PlatformException` swallow; `openSettings` happy path + `MissingPluginException` swallow + `PlatformException` swallow; `resetForTesting`; `debugInstance`) + `test/services/permission_service_test.dart` (+3 tests — `requestFullScreenIntent` deep-links via `FullScreenIntentService.openSettings()`; `refreshFullScreenIntent` merges `isGranted=true` → `PermissionResultGranted`; `refreshFullScreenIntent` merges `isGranted=false` → `PermissionResultDenied(canOpenSettings: true)`) + `test/services/reliability_service_test.dart` (+1 test — flipping `fullScreenIntent` to `Denied` re-derives the unified reliability stream to `degraded`; existing "the 4 gated kinds" test renamed to "the 5 gated kinds" with the `gated` const grown from 4 to 5) + `test/screens/settings_permissions_test.dart` (existing "renders all four" test renamed to "renders all five" with the new tile's `ValueKey` asserted; +2 new tests — tapping the FSI tile re-probes via `doit/full_screen` (channel call asserted); the tile renders the localized `permissionFullScreenIntentTitle`) + `test/widgets/reliability_banner_test.dart` (+1 test — `fromStream` wires `onTap` and the callback fires on tap when the service is degraded) + ARB parity (`test/l10n/app_localizations_test.dart` "every non-template ARB has the same key set as the template" test catches missing `app_es.arb` entries automatically) + manual device check on Android 14+: deny `USE_FULL_SCREEN_INTENT` in Settings → Apps → do it → "Appear on top"; expect home banner to flip to "may be late"; tap the banner → land on Settings → Permissions; tap the FSI tile → land on the system FSI toggle; toggle on; back out; banner clears after `refreshFullScreenIntent` re-probes on resume |
| WF-041 Full-screen activity launch path (v1.3d / Phase 15) | SYS-114 | `test/services/platform_full_screen_intent_test.dart` (6 tests — `showHabitMission` invokes the right method + `habitId` arg; `showRoutineOverlay` propagates `title` / `body` and skips missing keys; `getLaunchIntent` swallows the production `MissingPluginException` and returns `null`; `showHabitMission` swallows `MissingPluginException` end-to-end; `getLaunchIntent` returns null on a partial args map; `showRoutineOverlay` accepts title-only / body-only / neither) + `test/screens/mission_launcher_test.dart` (6 tests — 1-mission chain (Type) appends the completion with `proofModeAtTime: "strong"` and `missionResultsJson: "missions=1"`, pops `true`; 2-mission chain (Type → Type) appends with `missions=2`; `ChainFailedAt` (wrong phrase) pops `null` and does NOT append; missing habit pops `null`; non-`StrongProof` habit (soft) pops `null`; cancel on first mission aborts the chain and does NOT append) + `test/screens/routine_overlay_test.dart` (4 tests — renders `title` + `body` from constructor args; falls back to generic copy when null; falls back when `title` / `body` are empty strings; the Dismiss button pops the route with `null`) + `test/a11y/semantics_labels_test.dart` (SYS-062 — the Dismiss button's `Semantics` label `"Dismiss routine overlay"` is auto-validated; 0 regressions across the 35 existing widgets) + `./android/gradlew :app:compileDebugKotlin` (Kotlin compile gate for `FullScreenActivity` + the new `when` arms + the strong-mode `setFullScreenIntent` branch) + manual device check on Android 14+: schedule a strong-mode habit for 1 minute from now; lock the device; expect `FullScreenActivity` to surface directly (not just the heads-up notification); tap through the Type mission; expect the streak to advance; expect the activity to dismiss on completion |

## How to use this matrix

- **Adding a new SYS- ID:** add a row to the top table and a row to
  the workflow table (if applicable). The verification column must
  name a test file or a manual check.
- **Removing a SYS- ID:** remove the row. The related tests are
  candidates for deletion (review with the user).
- **Changing a test name or path:** update the verification column.
  If the test no longer covers the requirement, that is a defect
  in the test, not the requirement.
- **Adding a workflow:** add a row to the workflow table; every step
  in the workflow must reference at least one SYS- ID.
| WF-042 View streak on the Android home widget (v1.4a / Phase 28) | SYS-115 | `test/widget/widget_state_builder_test.dart` (8 tests — streak from empty log is 0; streak with 3 consecutive days; streak broken at grace-window edge; streak still alive within grace window; `isCompletedToday` true only when today is in the log; reliability maps to widget badge; `null activeDo` produces the empty-state snapshot; `Do.effectiveStreakConfig` flows through the factory end-to-end) + `test/widget/widget_bridge_test.dart` (11 tests — `FakeWidgetBridge` cacheSnapshot records; `FakeWidgetBridge` requestRefresh increments; `FakeWidgetBridge` snapshot returns scripted / null by default; `PlatformWidgetBridge` cacheSnapshot forwards the JSON envelope; `PlatformWidgetBridge` snapshot happy path; `PlatformWidgetBridge` snapshot swallows `MissingPluginException`; `PlatformWidgetBridge` snapshot swallows `PlatformException`; `PlatformWidgetBridge` requestRefresh swallows `MissingPluginException`; `DoitWidgetState` JSON round-trips; `fromJson` is defensive against missing fields; `fromJson` tolerates an unknown reliability tag) + `test/widget/widget_state_cache_test.dart` (5 tests — save-then-load round-trips; load returns null on empty prefs; clear removes the key; save overwrites previous; corrupt cache is dropped) + `test/widget/widget_state_locator_test.dart` (4 tests — returns first-active do by oldest `createdAt`; skips paused dos; returns null on empty repository; returns null when every do is paused) + `test/widget/widget_service_test.dart` (6 tests — `init` is idempotent; `handleRefreshRequest` computes + caches + persists state; `markDone` appends the completion then re-derives; `markDone` is a no-op when the habit does not exist; reliability change triggers a re-derive; `MissingPluginException` from the bridge is swallowed (ADR-013)) + `./android/gradlew :app:compileDebugKotlin` (Kotlin compile gate for `DoitWidgetProvider` + `WidgetChannel` + `WidgetUpdater` + `WidgetRenderer` + `WidgetStateCache` + `MainActivity` wiring) + manual device check on Android 13+: add a do with `DoFixed(weekdays: every day)` + 1+ prior completion to build a streak; long-press home screen → Widgets → drag "do it: your streak on the home screen" to a cell; expect the streak number + badge to render within 5 s; tap Done; expect the streak to advance by 1 within 1 s; revoke a gated permission in Settings → expect the widget badge to flip from `ic_widget_optimal` to `ic_widget_degraded` within 1 s |
| WF-044 Skip a do for today from the home tile (v1.4c / Phase 30) | SYS-117 | `test/screens/home_tile_skip_test.dart` (8 tests — `markDoSkipped` calls `append` with `source: CompletionSource.restDay` and the do's id; `day` argument is local-midnight at `asOf`; `proofModeAtTime` tag matches SoftProof / StrongProof / AutoProof; throws `NoRestDaysRemaining` when `restDaysPerMonth == 0`; throws `NoRestDaysRemaining` when the month's budget is exhausted; allows the last budget unit to be consumed (used == limit - 1); `NoRestDaysRemaining` carries `doId` + `month` for the snackbar; helper makes exactly one `append` call per invocation) + `test/screens/home_tile_budget_test.dart` (11 tests — `BudgetRemaining(used: 0, limit: 2, remaining: 2)` is `canSkip` + not exhausted; `used 1 of 2` → `remaining 1`, `canSkip`, not exhausted; `used 2 of 2` → `remaining 0`, not `canSkip`, exhausted; `limit 0` is `not canSkip` + not exhausted (opt-out); value-equality holds for equal triples; `budgetRemainingForDo` returns the empty-budget snapshot for `limit == 0`; `used 0` for an empty log; `used 1 / remaining 1` for partial usage; `used 2 / remaining 0 / isExhausted` when the cap is hit; only-counts-rows-for-the-passed-do; clamps negative `remaining` to 0 (defensive)) + `test/screens/home_test.dart` (+7 widget tests — tile renders the Skip button when `restDaysPerMonth > 0`; tile hides the Skip button when `restDaysPerMonth == 0`; soft-mode skip tap appends a rest-day row; skip-tap on an exhausted month shows the budget-exhausted snackbar copy; the budget caption renders "X / Y rest days left" when budget is partial; the budget caption renders the "No rest days left" copy when exhausted; Done-tap on a skipped-today tile shows the rest-day-taken snackbar (NOT the already-done snackbar)) + ARB parity (`test/l10n/app_localizations_test.dart` "every non-template ARB has the same key set as the template" test catches missing `app_es.arb` entries automatically) + manual device check: add a do with `restDaysPerMonth: 2`; mark 2 rest days; verify the budget caption reads "0 / 2 rest days left" → "No rest days left"; tap the Skip button on day 3; verify the budget-exhausted SnackBar; mark the do Done manually; verify the streak still credits the rest-day rows |
| WF-045 Undo today's completion from the home tile (v1.4d / Phase 31) | SYS-118 | `test/screens/home_tile_undo_test.dart` (8 tests — `undoToday` returns `UndoResult.removed(rowId, source)` when a manual completion row exists for `asOf`'s local-midnight; `undoToday` returns `UndoResult.removed(rowId, source)` when a rest-day row exists for `asOf`'s local-midnight; `undoToday` returns `UndoResult.nothingToUndo()` when no row exists for `asOf`'s local-midnight; `undoToday` matches the row whose `day` equals `DateTime(asOf.year, asOf.month, asOf.day)` regardless of source; `undoToday` matches a row in the past at the same `day` even when `asOf` is in the future (no future-leak); `undoToday` calls `completionLog.deleteById` exactly once per invocation on the happy path; `undoToday` does NOT call `completionLog.deleteById` on the `nothingToUndo()` path; `UndoResult` value-equality holds for `removed` + `nothingToUndo` constructors) + `test/screens/home_test.dart` (+5 widget tests — tile renders the Undo button only when `_isResolvedToday == true` (completed OR skipped); tile hides the Undo button when neither completion nor skip is recorded for today; undo-tap on a completed-today tile opens the confirm dialog with the localized `homeTileUndoConfirmBody` copy; undo-tap → confirm flips `_isCompletedToday` back to `false` and shows `homeTileUndoSuccess` snackbar; undo-tap on a skipped-today tile deletes the rest-day row and flips `_isSkippedToday` back to `false`) + ARB parity (`test/l10n/app_localizations_test.dart` "every non-template ARB has the same key set as the template" test catches missing `app_es.arb` entries automatically) + manual device check: add a do with `restDaysPerMonth: 2`; tap Done; tap Undo; confirm; verify the completion row is deleted from `Recent completions` in the edit screen and the streak decrements by 1; tap Skip; tap Undo; confirm; verify the rest-day row is deleted and the budget caption re-increments by 1 |
| WF-046 View the last 7 days as a sparkline on the home tile (v1.4e / Phase 32) | SYS-119 | `test/screens/home_tile_sparkline_test.dart` (8 tests — `sparklineForDo` returns exactly 7 dots in oldest-first order with today as the last dot; a dot is `SparklineDot.filled(day, source)` when a manual row exists for that day's local-midnight; the same for a `rest_day` row; all 7 dots are `SparklineDot.empty` when the log is empty; multiple rows for the same day collapse to exactly one dot (first-match source wins, mirroring `home_tile_undo.undoToday`); rows outside the 7-day window are ignored; the 7 dots are chronologically ordered with strictly increasing day values; `SparklineDot` value-equality holds across all three factories for same-day + same-source + same-variant) + `test/screens/home_test.dart` (+3 widget tests — the sparkline `Semantics(label: 'Last 7 days')` node is rendered when at least one completion row exists; the sparkline `Semantics` node is rendered even when no completions are recorded (skeleton resolves to empty dots); the sparkline `Semantics` node survives a Mark-done tap that resolves today (the parent `_DoStreakBadge` re-fires the rebuild and the dot row stays mounted)) + ARB parity (`test/l10n/app_localizations_test.dart` "every non-template ARB has the same key set as the template" test catches missing `app_es.arb` entries automatically) + manual device check: open the home screen with at least one saved do; verify a row of 7 dots renders under the streak badge; tap Done; verify the rightmost dot (today) bumps to a larger filled circle; verify the same dot row renders correctly with mixed manual + rest-day rows |
| WF-047 Skip or undo today from the Android home widget (v1.4f / Phase 33) | SYS-120 | `test/widget/widget_service_test.dart` (+8 tests — `skip` appends a `CompletionSource.restDay` row + re-derives the widget state; `skip` returns `false` (no `append`) when `restDaysPerMonth == 0`; `skip` returns `false` (no `append`) when the month's rest-day rows already hit the limit; `skip` returns `false` (no `append`) when the habit does not exist; `undo` calls `deleteById` on the row whose `dayMillis == local-midnight at now` + re-derives the widget state; `undo` returns `false` (no `deleteById`) when no row matches today's local-midnight; `undo` matches only today's local-midnight — a yesterday row is NOT deleted; `undo` returns `false` (no `deleteById`) when the habit does not exist) + `test/widget/widget_bridge_test.dart` (+6 tests — `FakeWidgetBridge.skip` records habit id in `skipHabitIds`; `FakeWidgetBridge.skip` returns `nextSkipResult`; `FakeWidgetBridge.undo` records habit id in `undoHabitIds`; `FakeWidgetBridge.undo` returns `nextUndoResult`; `PlatformWidgetBridge.skip` forwards the habit id to the channel and unwraps a `null` channel result to `false`; `PlatformWidgetBridge.skip` swallows `MissingPluginException` and returns `false` (ADR-013); same `+2` for `undo`) + `test/widget/doit_widget_state_test.dart` (+1 test — `restDaysPerMonth` defaults to 0 on `fromJson`; included in `toJson`; preserved through `copyWith`) + `./android/gradlew :app:compileDebugKotlin` (Kotlin compile gate for the shared `WidgetChannel.handleAction` helper + the two new `ACTION_WIDGET_SKIP` / `ACTION_WIDGET_UNDO` constants + `WidgetRenderer.render(...)` visibility logic for `widget_skip` + `widget_undo`) + ARB parity (`test/l10n/app_localizations_test.dart` "every non-template ARB has the same key set as the template" test catches missing `app_es.arb` entries automatically) + manual device check: build `flutter build apk --debug` (no signing), install on Android 13+, add a do with `restDaysPerMonth = 3`, pin the widget to home, tap Skip → streak holds + widget repaints with updated streak; tap Done → streak holds; tap Undo → confirmation implicit (button hidden when no row), row deleted, streak decrements by 1; with `restDaysPerMonth = 0` verify Skip button is gone (`View.GONE`); after a Skip tap verify Undo button appears; after an Undo tap verify Skip button is back (no row for today) |
| WF-048 Widget action button taps round-trip to Dart's `WidgetService` (v1.4g / Phase 34) | SYS-121 | `test/widget/widget_action_invoker_test.dart` (7 tests — `widgetActionDispatch` returns `false` when `WidgetService` is not initialized; returns `false` when `habitId` arg is missing; returns `false` when `habitId` arg is empty; returns `false` when arguments are `null`; returns `false` for an unknown action method; `WidgetActionInvoker.attach` is idempotent — second call is a no-op; `WidgetActionInvoker.resetForTesting` clears the singleton + handler) + `test/widget/widget_action_invoker_integration_test.dart` (4 tests — inbound `markDone` via the production `widgetActionDispatch` routes to `WidgetService.markDone` and appends a `CompletionSource.manual` row via `CompletionLogService.append` + re-derives via `handleRefreshRequest` (asserted via `FakeWidgetBridge.refreshCount`); inbound `skip` routes to `WidgetService.skip` and appends a `CompletionSource.restDay` row; inbound `undo` routes to `WidgetService.undo` and removes today's row via `CompletionLogService.deleteById`; inbound `markDone` returns `false` when the habit does not exist — no `append` call) + `./android/gradlew :app:compileDebugKotlin` (Kotlin compile gate for `WidgetChannel.invokeAction` suspending helper + the IO-dispatcher `scope.launch` in `DoitWidgetProvider.onReceive` + the `EXTRA_HABIT_ID` extra on the action `PendingIntent`s + `WidgetUpdater.ensureFlutterEngine` `public` visibility) + manual device check on Android 13+: install the new APK; tap the widget "Done" button on a fresh-tile do; expect the streak number to advance by 1 within 1 s (warm engine) or 3 s (cold engine); tap "Skip today"; expect a rest-day row in the edit-screen completion log AND the streak to hold; tap "Undo today"; expect the rest-day row to disappear from the completion log AND the streak to decrement by 1 (or the streak to break if it was a manual completion); verify the existing v1.4f Done-tap-streak-staleness gap is closed (no ~30 s lag between tap and streak update) |
| WF-049 Edit or delete a do from the in-app home tile (v1.4h / Phase 35) | SYS-122 | `test/screens/home_tile_delete_test.dart` (6 tests — `deleteDo` returns `true` on the happy path AND records the deleted id; `deleteDo` calls `deleteById` exactly once with the captured do id; `deleteDo` returns `false` when the repository throws an `Error` (e.g. `StateError`); `deleteDo` returns `false` when the repository throws an `Exception` subtype (e.g. `Exception('foreign-key constraint')`); `deleteDo` does not re-throw — the caller can rely on the `bool` return (asserted via `expectLater(() => ..., returnsNormally)`); the helper imports no Flutter types so it can be tested without `TestWidgetsFlutterBinding`) + `test/screens/home_test.dart` (+7 tests — tile renders an Edit `IconButton` with the `Edit` tooltip; tile renders a Delete `IconButton` with the `Delete` tooltip; delete-tap opens a confirm dialog with `Delete "Stretch"?` in the title and the cascade-body copy in the body; confirm-tap on the dialog deletes the do via `DoRepository.deleteById` AND shows the `Deleted "Stretch".` SnackBar with the `Undo` action; cancel-tap on the dialog leaves the do intact (asserted via `getById('h1')` returning the saved `Do`) AND shows no deletion snackbar; Undo action on the deleted snackbar re-saves the captured do via `DoRepository.save` (asserted via `getById('h1')` returning the restored `Do` with the original name); edit-tap pushes `AddHabitScreen` in edit mode for the do (asserted via a `_RecordingNavigatorObserver.didPush` event, mirroring the v1.4b strong-mode pattern at `test/screens/home_test.dart:434`)) + manual device check on Android 13+: install the new APK, add a do with at least one completion, tap the per-tile Delete button, verify the confirm dialog carries the do name in quotes, tap Delete, verify the tile disappears AND the deletion snackbar shows the captured name AND the Undo action is visible, tap Undo within 5 s, verify the tile reappears (the streak history does NOT — that's the v1.4h+ soft-delete follow-up) |
| WF-050 View rest-day history on the home tile (v1.4i / Phase 36) | SYS-123 | `test/screens/home_tile_sparkline_test.dart` (+4 tests — `extendedSparklineForDo` with `days: 14` returns 14 dots with `dots.first.day == today - 13 days` (local-midnight at asOf) and `dots.last.day == today`; `extendedSparklineForDo` defaults to 14 days when no `days` arg is passed; `extendedSparklineForDo` honors an arbitrary window (`days: 3` returns 3 dots, `days: 30` returns 30 dots) — the `days` parameter is wired through to the dot-list length; `extendedSparklineForDo` preserves the source tag on filled dots — a `'rest_day'` row at day -2 paints a `SparklineDotFilled(source: 'rest_day')` AND a `'manual'` row at day -7 paints a `SparklineDotFilled(source: 'manual')`, both retrievable from the returned list so the widget can branch on the source) + `test/screens/home_test.dart` (+6 widget tests in a new v1.4i group — `seedTile` helper seeds a do + a `FakeCompletionLog`; "renders 14 outlined dots when no completions exist" via `find.byWidgetPredicate` counting 14 `_SparklineDot` containers with `decoration: outlined-circle`; "renders the legend row below the dots with the three state labels" asserting `homeTileSparklineLegendDone` / `homeTileSparklineLegendRestDay` / `homeTileSparklineLegendMissed` texts + the `_LegendSwatch` colored circles; "colors rest-day dots with `colorScheme.tertiary`" — seeds a `rest_day` row at day -3, taps the tile's `Skip today` button to trigger the `_refresh()` setState cascade, then asserts the matching dot's `Container.decoration.color` resolves to `Theme.of(context).colorScheme.tertiary`; "rest-day dot carries the `Rest day` `Semantics` label for a11y" via `find.bySemanticsLabel('Rest day')` after the same Skip-tap trigger; "manual-completion dot carries the `Done` `Semantics` label" via `find.bySemanticsLabel('Done')` after a Mark-done tap to seed today; "long-press on a tile with the new sparkline still enters select mode" — regression guard for the ADR-053 §"Alternatives considered" `Tooltip`-gesture-intercept rejection: the v1.4b "long-press enters select mode" test pattern at `test/screens/home_test.dart:165-213` continues to pass after the v1.4i source-aware coloring + per-dot `Semantics` migration) + 2 existing widget tests updated: the v1.4e "the sparkline `Semantics(label: 'Last 7 days')` node is rendered when at least one completion row exists" → rephrased to "Last 14 days" form (asserts the updated ARB key); same for "the sparkline `Semantics` node is rendered even when no completions are recorded" → "Last 14 days" + 14 outlined dots. ARB parity (`test/l10n/app_localizations_test.dart` "every non-template ARB has the same key set as the template" test catches missing `app_es.arb` entries automatically) + manual device check on Android 13+: install the new APK; open a do with at least 14 days of mixed manual + rest-day history (seed via Skip / Done taps or a fresh-install migration test fixture); verify the extended sparkline renders 14 dots below the streak badge; verify the rest-day dots render in `colorScheme.tertiary` (a distinct color from the manual `colorScheme.primary` fills); verify the legend row reads "Done / Rest day / Missed" below the dot row; TalkBack: tap the tile, swipe right; expect the `Semantics` tree to announce "Last 14 days", "Done" / "Rest day" / "Missed" for the focused dot, then the existing streak + skip / undo / done buttons in tab order |
| WF-051 Edit the rest-day budget from the home tile or the edit screen (v1.4j / Phase 37) | SYS-124 | `test/screens/rest_day_picker_dialog_test.dart` (8 tests — `RestDayPickerDialog(initial: 5)` renders `'5'` as the live integer + the `Slider` `value == 5.0`; the localized `homeTileBudgetEditTitle` / `homeTileBudgetEditDescription` / `homeTileBudgetEditOk` / `homeTileBudgetEditCancel` strings render in English locale; tapping Cancel returns `null` from `showRestDayPicker`; tapping Save returns the current `Slider` value; `initial: -7` clamps to `kRestDaysPerMonthMin` (0) on construction + the slider value matches; `initial: 99` clamps to `kRestDaysPerMonthMax` (31); the slider `divisions == kRestDaysPerMonthMax - kRestDaysPerMonthMin` so it snaps to integer values; dragging the slider updates the live integer label) + `test/do/do_model_test.dart` (+3 tests — `Do.validate()` throws `DoInvalidRestDays(-1)` on `restDaysPerMonth: -1` (existing rule preserved); `Do.validate()` throws `DoInvalidRestDays(32)` on `restDaysPerMonth: 32` (new upper-bound rule); `Do.validate()` accepts `restDaysPerMonth: 0` AND `restDaysPerMonth: 31` (boundary cases)) + `test/screens/add_habit_test.dart` (+1 widget test — "AddHabitScreen renders a Rest-days-per-month form row with the default value 2 (v1.4j / SYS-124)" asserts `find.text('Rest days per month: 2')` is visible in add mode; +1 grep regression test — `no hardcoded restDaysPerMonth: 2 literals remain in _save() — the v1.0 silent-reset bug (v1.4j / SYS-124)` reads `lib/screens/add_habit.dart` via `File().readAsString()` and asserts the regex `restDaysPerMonth:\s*2,` has 0 matches, pinning the contract that all 5 switch branches read `restDaysPerMonth: _restDaysPerMonth`) + `test/screens/home_test.dart` (+5 widget tests in a new v1.4j group — the `_BudgetCaption` renders `homeTileBudgetZeroCaption` ("No rest days configured") when `restDaysPerMonth == 0` (regression guard for the `limit <= 0` early-return removal); the `_BudgetCaption` renders `homeTileBudgetRemaining(3, 3)` when `restDaysPerMonth == 3 && used == 0` (regression guard for the `used == 0` early-return removal — the formerly-hidden state is now visible); tapping the caption opens the `RestDayPickerDialog` (asserted via `find.byType(RestDayPickerDialog)` after `tester.tap(find.text('No rest days configured'))`); a successful Save writes `DoRepository.save(activeDo.copyWith(restDaysPerMonth: 5))` AND triggers the v1.4h `onDoChanged` refresh cascade (asserted via the home screen's `FutureBuilder<List<Do>>` re-firing — captured via the v1.4h `onDoChanged` mock); long-press on a tile still enters select mode after the v1.4j caption-tap addition — regression guard for the v1.4b select-mode gesture surviving the `GestureDetector(onTap: ...)` wrap on the caption (the gesture is a tap, not a long-press, so it does NOT intercept the parent's `InkWell.onLongPress`)) + 1 existing test localized: `test/screens/add_habit_test.dart` switched from `MaterialApp(home: AddHabitScreen())` to `localizedApp(home: AddHabitScreen())` because the new `Builder` widget calls `AppLocalizations.of(ctx)` and a plain `MaterialApp` does not wire the delegate (mirrors the same localization fix that already wrapped `add_habit_delete_test.dart` + `add_habit_save_as_template_test.dart` + `templates_test.dart` for the v1.4j cycle) + ARB parity (`test/l10n/app_localizations_test.dart` "every non-template ARB has the same key set as the template" test catches missing Spanish entries automatically) + manual device check on Android 13+: install the new APK; tap a tile's budget caption → picker opens; drag slider to 5 → tap Save → caption updates to "5/5 rest days left" AND `_SkipButton` appears on the tile; open the edit screen → "Rest days per month: 5" row shows → tap row → picker → drag to 7 → tap Save → the form's state field is now 7; Save the edit screen → re-open → row reads "Rest days per month: 7" (the silent-reset bug is closed — the saved value persists across a round-trip even if the user doesn't touch the row); TalkBack: tap the tile, swipe right → expect the caption `Semantics(button: true, label: "5/5 rest days left")` node to announce as a button; tap → picker opens; swipe right through the `Slider` → expect each integer label to be read out as the slider value changes |
| WF-052 Bind the home widget to a specific do (per-instance configuration, v1.4k / Phase 38) | SYS-125 | `test/widget/doit_widget_state_test.dart` (8 tests — `selectedHabitId` round-trips through `toJson` / `fromJson`; defaults to `null` when absent in JSON (backwards compatibility with v1.4a..v1.4j caches); empty string treated as `null` on `fromJson` (defensive against a downgrade that writes a Kotlin `optString(..., "")` empty value); `null` in JSON round-trips to `null`; `copyWith(selectedHabitId: 'x')` replaces the field; `copyWith` without `selectedHabitId` preserves the prior value (mirrors v1.4f `restDaysPerMonth` precedent); `==` / `hashCode` include `selectedHabitId`; `toString` includes `selectedHabitId`) + `test/widget/widget_service_test.dart` (+4 v1.4k tests — `handleRefreshRequest` consults cached `selectedHabitId` when present AND `setSelectedHabitId('h2')` re-derives with the picked do; `setSelectedHabitId(null)` falls back to `firstActiveDo` AND clears the cached pick; stale `selectedHabitId` (the picked do was deleted from the repo) reconciles to `firstActiveDo` AND clears the pick on the next refresh; reliability change re-derives against the picked `selectedHabitId` so a reliability flip on a configured widget does NOT regress to `firstActiveDo`) + `test/widget/widget_state_builder_test.dart` (+2 tests — `selectedHabitId` threads into the returned state; `selectedHabitId` is preserved when `activeDo` is null so the reconciliation path can clear it on the next pass) + `test/widget/widget_deep_link_test.dart` (NEW, 7 tests — `buildHabitRoute(/habit?habitId=abc)` returns `AddHabitScreen`; `buildHabitRoute(/habit?habitId='')` falls back to `HomeScreen`; `buildHabitRoute(/unknown)` returns `null`; `buildWidgetConfigRoute(/widget-config?widgetId=42)` returns `WidgetConfigScreen`; `buildWidgetConfigRoute(/widget-config)` returns `WidgetConfigScreen` with null `widgetId` (defensive — the Kotlin activity always passes a widgetId); `buildAppRoute` dispatches on the route name (`/habit`, `/widget-config`, `/mission` all return non-null; `/nope` returns null); `DoitWidgetState.selectedHabitId` round-trips through `toJson` — pins the JSON envelope key that the Kotlin `WidgetRenderer.openAppIntent` reads). Tests inspect the returned `MaterialPageRoute` builder directly via a stub `BuildContext` shim rather than pushing onto a real Navigator — pushing onto a Navigator would require pumping widgets past `FutureBuilder`s inside `AddHabitScreen` / `HomeScreen` / `WidgetConfigScreen`, each of which reads `DoRepository.instance.listAll()` (the singleton is not seeded in this test file); the pure-builder check is the contract the routes actually guarantee. ARB parity (`test/l10n/app_localizations_test.dart` "every non-template ARB has the same key set as the template" test catches missing Spanish entries automatically) + `./android/gradlew :app:compileDebugKotlin` (Kotlin compile gate for `DoitWidgetConfigureActivity` + `MainActivity.getInitialRoute` + `WidgetRenderer.openAppIntent(selectedHabitId)` + the `EXTRA_HABIT_ID_FROM_WIDGET` namespace + the manifest `<activity>` + `doit_widget_info.xml` `android:configure` link) + manual device check on Android 13+: install the new APK; long-press home screen → Widgets → drag "do it" to TWO separate cells → expect the FIRST configuration activity to open (a thin Flutter shell); pick "Read" → activity finishes → widget paints "Read" on the first cell; long-press home screen → drag "do it" to the second cell → configuration activity opens again (the SECOND time); pick "Stretch" → activity finishes → widget paints "Stretch" on the second cell; tap the body of the first widget → MainActivity launches → navigates to `AddHabitScreen(habitId: 'h1')` (the picked do); tap the body of the second widget → navigates to `AddHabitScreen(habitId: 'h2')` (the second picked do); back out; delete "Read" from the in-app home screen; tap the body of the first widget → navigates to `HomeScreen` (the `_resolveActiveDo` fallback + the `selectedHabitId = null` reconciliation clear landed); verify the widget itself now shows `firstActiveDo` ("Stretch") on the next refresh |
| WF-053 Delete a do and undo within the SnackBar window (true restore, v1.4l / Phase 39) | SYS-126 | `test/db/migration_v4_to_v5_test.dart` (NEW, 4 tests — `schemaVersion is 5` v1.4l pin; `habits` table has `deleted_at_millis` column; the column is nullable and accepts both NULL and an int; pre-existing rows survive the migration with `deleted_at_millis IS NULL`) + `test/db/migration_test.dart` (`kCurrentSchemaVersion` pin bumped 4 → 5) + `test/db/migration_v2_to_v3_test.dart` (`schemaVersion` pin 4 → 5; `user_version` PRAGMA pin 4 → 5 — re-baselined for v1.4l) + `test/db/migration_v3_to_v4_test.dart` (`schemaVersion` pin 4 → 5 — re-baselined for v1.4l) + `test/services/do_repository_test.dart` (NEW, 14 tests in 4 groups — `softDeleteById` writes a non-null `deletedAtMillis`; `listAll` excludes tombstoned habits; `listActive` excludes tombstoned habits; `getById` still returns a tombstoned do; `getActiveById` returns null for a tombstoned do; `getActiveById` returns the do when active; `softDeleteById` is idempotent on an already-tombstoned row; `softDeleteById` on a missing row returns `false`; `restoreById` clears the tombstone; restored do reappears in `listAll` with `isDeleted == false`; `restoreById` is idempotent on an already-active row; `restoreById` on a missing row returns `false`; `save(d)` does NOT touch a tombstoned row's `deletedAtMillis` — the v1.4l save-invariant pin; `restoreById` after a save on a tombstoned row still works; `deleteById` (force-delete) still removes the row (backup-restore wipe path preserved); force-delete then save re-inserts as active) + `test/services/habit_repository_test.dart` (+1 test — round-trips `deletedAt` via `softDeleteById` + `restoreById` on the `Habits` table via the `DoRepository` mapping) + `test/screens/home_tile_delete_test.dart` (rewritten, 14 tests in 5 groups — `softDeleteDo` happy path; `softDeleteDo` records the id; `softDeleteDo` returns `false` on `Error`; `softDeleteDo` returns `false` on `Exception` subtype; `softDeleteDo` does not re-throw; helper imports no Flutter types; `restoreDo` happy path; `restoreDo` records the id; `restoreDo` returns `false` on `Error`; `restoreDo` returns `false` on `Exception`; `restoreDo` does not re-throw; round-trip: `softDeleteDo` then `restoreDo` brings the do back; round-trip: `restoreDo` on an active do is a no-op; both helpers never use `DateTime.now()`; both helpers never import Flutter) + `test/screens/home_test.dart` (+2 existing v1.4h tests updated — the "confirm-tap deletes the do" test asserts via `getActiveById` (returns null) instead of `getById` (returns the tombstoned row); the "Undo action re-saves the captured do" test asserts via `getActiveById` instead of `getById`). Coverage: ≥80% on every changed file (`tables.dart`, `migrations/v4_to_v5.dart`, `schema.dart`, `do.dart`, `do_repository.dart`, `home_tile_delete.dart`, `home.dart`, `backup_service.dart`, all v1.4l test files). Manual device check on Android 13+: install the new APK; add a do with 3+ consecutive completions; verify streak = 3; tap the tile's Delete IconButton → confirm; verify the tile disappears; tap the SnackBar Undo within 5 s; verify the tile reappears AND the streak badge still reads 3 (the headline behavior change of v1.4l — the Undo restores the streak by construction); soft-delete + wait past the SnackBar window + relaunch the app + verify the do is NOT in the list (the tombstone is persistent but invisible to the UI); export a backup → verify the JSON envelope does NOT contain the tombstoned do (the v1.4l backup filter is correct) |
| WF-055 CI exercises the v1.4l soft-delete home-screen flow end-to-end (v1.4m / Phase 40) | SYS-127 | `test/services/do_repository_test.dart` (+9 v1.4m tests in 3 groups — `listDeleted` (4): `listDeleted` excludes active habits; `listDeleted` orders by `deletedAtMillis DESC` (most-recently-deleted first — matches the planned "Recently deleted" UI sort); `listDeleted({int? limit})` honors the `limit` param (returns at most N most-recently-deleted); `listDeleted` returns an empty list when no habits are tombstoned. `purgeDeletedOlderThan` (4): purges tombstoned habits older than the cutoff (seeds 2 tombstoned habits at different dates, calls `purgeDeletedOlderThan(Duration(days: 30), at: frozenAt)`, asserts the old one is gone AND the recent one is still tombstoned); leaves young tombstoned habits untouched (seeds a tombstone from 1 day ago, calls `purgeDeletedOlderThan(Duration(days: 30), at: now)`, asserts the young tombstone is still there); never touches active habits (seeds 1 active habit, calls `purgeDeletedOlderThan(Duration(days: 0), at: now)`, asserts the active habit is still there); is idempotent on a second call (calls `purgeDeletedOlderThan` twice with the same args, asserts the second call returns `0`). `persistence-across-restart` (1): Phase A opens an in-memory DB + saves a do + soft-deletes it + closes the DB; Phase B opens a FRESH in-memory DB + seeds the raw `habits` row via `db.database.customStatement('INSERT INTO habits ... VALUES (...)')` with `deleted_at_millis` set + asserts `DoRepository.instance.getById('h1')` returns the tombstoned do with `isDeleted == true` — proves the column survives what the user sees as "close + reopen the app")) + `test/screens/home_test.dart` (+4 v1.4m widget tests in a new v1.4m group — `Undo restores the streak badge to the original value by construction`: seeds a do + 3 consecutive completions (yesterday / day-before / 3-days-ago), pumps the home screen, asserts the badge's `KeyedSubtree(key: 'streakBadge-h1')` finds a `Text('3')` widget (the before state); taps the Delete IconButton, confirms, waits for the SnackBar, taps the Undo `SnackBarAction`, re-pumps, asserts the badge's `KeyedSubtree(key: 'streakBadge-h1')` STILL finds a `Text('3')` widget (the after state — streak survived because `Completions.habitId` references the same id that survived the soft-delete); the headline behavior change of v1.4l is pinned. `streak badge renders the streak number with the correct tabular formatting`: seeds a do + 3 completions, pumps, asserts the badge's `KeyedSubtree(key: 'streakBadge-h1')` finds BOTH a `Text('3')` (the streak number) AND a `Text('day streak')` (the subtitle) — pins the badge's subtitle text + the tabular-figure formatting. `cancelled delete (Cancel on confirm dialog) does NOT tombstone the do and the streak badge remains rendered`: seeds a do + 3 completions, pumps, taps Delete IconButton, taps the Cancel button on the confirm dialog, asserts the do is STILL in the listing AND the streak badge STILL renders `Text('3')` — Cancel must not partially-tombstone. `soft-delete persists across a HomeScreen rebuild (close + reopen the in-memory DB)`: seeds a do + 3 completions, pumps, taps Delete, confirms, asserts the do is gone; calls `_resetDb(tester)` to swap in a fresh in-memory DB, re-pumps the widget, asserts the do is STILL gone — the tombstone survives a DB close + reopen at the widget level (mirrors the repository-level persistence-across-restart test). The `KeyedSubtree(key: 'streakBadge-${habit.id}')` wrapper added to `lib/screens/home.dart`'s `_DoStreakBadge` call site is the ONLY widget-tree change — a no-op for production rendering, purely a test seam so widget tests can locate the badge via `find.byKey(Key('streakBadge-<id>'))` without exposing the private widget). Coverage: ≥80% on every changed file (`do_repository.dart`, `home.dart`, `home_test.dart`, `do_repository_test.dart`). 3-gate: `dart format --output=none --set-exit-if-changed .` (264 files, 0 changed) + `flutter analyze --fatal-infos lib test` (0 issues) + `flutter test` (1334/1334 pass — was 1321 at the v1.4l tip, +13 from the v1.4m group). Manual device check on Android 13+ (the v1.4l 6-step smoke is now covered by CI; the on-device smoke becomes a one-time sanity check per release): install the new APK; add a do with 3+ consecutive completions; verify streak = 3; tap the tile's Delete IconButton → confirm; verify the tile disappears; tap the SnackBar Undo within 5 s; verify the tile reappears AND the streak badge still reads 3 (the headline behavior change of v1.4l, now CI-guarded) |
| WF-056 Coverage audit + stabilization roadmap for the 3-month hardening campaign (v1.4-stab-A / Phase 41) | SYS-128 | The "test artifact" for this cycle is the coverage report itself (`coverage/lcov.info` produced by `flutter test --coverage` + the `coverage/html/index.html` view produced by `genhtml coverage/lcov.info -o coverage/html`). Cycle A is doc-only — no `lib/` changes, no new tests. Verification of the audit's findings happens in subsequent cycles (B..L). **The artifact files for this row are:** (1) `docs/v_model/stabilization_roadmap.md` (NEW) — 6 sections (current coverage state per-file; latent bugs BUG-001..BUG-020 with priorities + target cycles; cycle-by-cycle roadmap B..L; success criteria for the 3-month campaign; open questions; retrospective) — the single source of truth for the stabilization campaign. (2) `coverage/lcov.info` (NEW, 133 KB) — the line-coverage report parsed by the Python parser to produce the per-file table in §1 of the roadmap. (3) `coverage/html/index.html` (NEW, genhtml output) — the inspectable per-file coverage view (open in a browser). **Cross-references.** `docs/v_model/requirements.md` SYS-128 (the audit + roadmap deliverable); `docs/v_model/decision_record.md` ADR-059 (the pivot from feature work to stabilization + cycle sequencing + v1.4n parking-lot move); `docs/v_model/workflows.md` WF-056 (the 8-step audit flow); `docs/v_model/plan.md` Milestone 12 (the 3-month stabilization kickoff); `docs/v_model/implementation_status.md` v1.4-stab-A row (the cycle's ship-status entry); `CHANGELOG.md` v1.4-stab-A block (the user-facing changelog); `feature.md` §4 / §5 / §6 (move v1.4n to parking lot + ADR/SYS/WF quick-index updated to 059 / 128 / 056 + next-step updated to the audit cycle). 3-gate (regression check — no `lib/` / `test/` changes): `dart format --output=none --set-exit-if-changed .` (264 files, 0 changed) + `flutter analyze --fatal-infos lib test` (0 issues) + `flutter test` (1334/1334 pass — unchanged from v1.4m). The "exhaustive test" coverage audit is the deliverable, not the test count delta; the audit may reveal Priority-1 / Priority-2 / Priority-3 buckets per the roadmap §1 categorization. |

| WF-057 Fix `_toRow` automations + pausedUntil data-loss bugs (v1.4-stab-B / Phase 42) | SYS-129 | `test/services/do_repository_test.dart` (+3 new tests in the new `DoRepository save invariant (Cycle B / BUG-001 + BUG-002)` group, placed after the v1.4l save-invariant group at line 323, before the hard-delete group): (1) **`automations round-trip through save + getById`** — seeds 2 `Automation` fixtures via the new `_twoAutomations()` helper (`TriggerBatteryLow(20) → ActionNotify("Battery low", "Plug in")` + `TriggerTimeOfDay(7, 30) → ActionNotify("Morning", "Drink water")`, each with a stable id), saves the do via `DoRepository.instance.save(_do(id: 'h1', automations: seed))`, calls `getById('h1')`, asserts `loaded!.automations == seed` (uses `Automation.==` which covers id + trigger + condition + action + enabled). Pins BUG-001's write + read paths — the user's automations survive the Save + reload round-trip. (2) **`pausedUntil round-trips via direct companion UPDATE + getById`** — saves a fresh do, then seeds `pausedUntilMillis` via a direct `HabitsCompanion` UPDATE on `db.habits` (mirrors the shape `PauseService.pauseHabit` uses post-refactor: `(db.update(db.habits)..where((t) => t.id.equals('h1'))).write(HabitsCompanion(pausedUntilMillis: Value(pauseAt.millisecondsSinceEpoch)))` with `pauseAt = DateTime(2026, 7, 2)`), calls `getById('h1')`, asserts `loaded!.pausedUntil == pauseAt`. Pins BUG-002's read path without going through `save`. (3) **`save(d) does NOT clobber an existing pausedUntilMillis`** — saves a do with `name: 'Old name'`, seeds `pausedUntilMillis` via companion UPDATE to `pauseAt`, then saves `_do(id: 'h1', name: 'New name')` (no in-memory `pausedUntil` — the AddHabitScreen Save shape), asserts the raw row's `pausedUntilMillis == pauseAt.millisecondsSinceEpoch` (the column survived the Save) AND `loaded!.pausedUntil == pauseAt` (the in-memory copy sees the same value). Pins BUG-002's save-invariant — the headline test for Cycle B. **Test count: 1334 → 1337.** Coverage: ≥80% on every changed file (`do_repository.dart`, `pause_service.dart`, `do_repository_test.dart`). 3-gate: `dart format --output=none --set-exit-if-changed .` (264 files, 0 changed — pure-Dart + new tests) + `flutter analyze --fatal-infos lib test` (0 issues) + `flutter test` (1337/1337 pass). **Cross-references.** `docs/v_model/requirements.md` SYS-129 (the `_toRow` round-trip + save-invariant requirement); `docs/v_model/decision_record.md` ADR-060 (the omission-pattern rationale + the `PauseService` refactor + the read-modify-write alternative rejection); `docs/v_model/workflows.md` WF-057 (the 16-step Cycle B flow); `docs/v_model/plan.md` Milestone 12 `### v1.4-stab-B` sub-entry; `docs/v_model/implementation_status.md` v1.4-stab-B row (the cycle's ship-status entry); `CHANGELOG.md` v1.4-stab-B block; `feature.md` §4 (BUG-001 + BUG-002 parking lot bullets removed — both closed) + §5 (quick-index updated to ADR-060 / SYS-129 / WF-057) + §6 (next-step updated to Cycle C). Targeted runs per `CLAUDE.md`: `flutter test test/services/do_repository_test.dart` (passes; +3 new tests) + `flutter test test/services/pause_service_test.dart` (passes; readiness-gate regression check, no new assertions needed because column writes are exercised via `do_repository_test.dart`'s new round-trip test). |

| WF-058 FSI reliability wiring + defense-in-depth + channel-surface gap pin (v1.4-stab-C / Phase 43) | SYS-130 | 3 test files modified/created (test count 1337 → 1345, +8 tests): **(1) `test/reminders/full_screen_intent_test.dart` (NEW, +5 tests)** — lifts `lib/reminders/full_screen_intent.dart` coverage from 25% (Cycle A audit) to ≥80%. Group `FakeFullScreenIntent.show`: `records every FullScreenLaunch in invocation order` (seeds 2 strong-mode `_DoFixed` habits + 2 `MissionChain`s, calls `await fsi.show(habit1, chain1)` then `await fsi.show(habit2, chain2)`, asserts `fsi.launches` has length 2 with the expected habit ids in order — covers the `FullScreenLaunch` data class + the recording list). Group `FakeFullScreenIntent.showRoutineOverlay`: `records title and body exactly as supplied (null passes through)` (4 calls — `showRoutineOverlay(title: 'Japan silent', body: 'Toggle on')` / `showRoutineOverlay(title: 'title only')` / `showRoutineOverlay()` with neither / `showRoutineOverlay(body: 'body only')` with body-only — asserts `fsi.routineOverlays` has length 4 with the correct title + body values, including the null-pass-through cases — covers the `RoutineOverlayLaunch` data class + the 4-input shape). Group `FakeFullScreenIntent.getLaunchIntent`: `returns the scripted launch intent and appends it to launchIntents` (scripted `LaunchIntent(mode: LaunchMode.habit, habitId: 'h-from-channel')`, 2 calls, asserts both return the same scripted value AND both appends are recorded in `fsi.launchIntents` — covers the v1.3d / SYS-114 `LaunchIntent` re-entry shape) AND `returns null and records null when scriptedLaunchIntent is null` (no script, asserts the return is `null` and `fsi.launchIntents` is `[null]` — covers the no-launch-intent case). Group `RoutineOverlayLaunch equality`: `equal when title + body match; hashCode is consistent` (covers `==` / `hashCode` invariants). Group `LaunchIntent equality`: `equal when mode + habitId + title + body all match` (covers `==` / `hashCode` invariants). **(2) `test/services/full_screen_intent_service_test.dart` (extended, +3 tests in a new `MethodChannelFullScreenIntentSource (production source)` group)** — pins the defense-in-depth swallow per ADR-061. Test 1: `isGranted returns false when the platform throws PlatformException (defense-in-depth per ADR-061)` — mocks `MethodChannel('doit/full_screen')` to throw `PlatformException(code: 'TEST_PLATFORM_ERROR', message: 'simulated NotificationManager failure')` on any call, constructs `MethodChannelFullScreenIntentSource()` (the un-prefixed name post-rename), awaits `source.isGranted()`, asserts `result == false`. Test 2: `openSettings returns false when the platform throws PlatformException (defense-in-depth per ADR-061)` — same shape but for `openSettings()` (simulated `ACTION_MANAGE_APP_USE_FULL_SCREEN_INTENT` activity missing). Test 3: `isGranted returns false when the platform throws MissingPluginException (no Kotlin arm — defense-in-depth per ADR-061)` — mocks the channel to throw `MissingPluginException('No implementation found for method canUseFullScreenIntent on channel doit/full_screen')`, awaits `source.isGranted()`, asserts `result == false`. The `debugPrint` calls inside the catches are silenced via a setUp/tearDown `debugPrint` swap (lines 32-38). The existing `isGranted` / `openSettings` / `singleton lifecycle` groups (using `ScriptedFullScreenIntentSource`) are preserved unchanged — no regression on the test seam. **(3) `test/reminders/reminder_bridge_fsi_channel_test.dart` (NEW, +2 tests)** — pins the KNOWN channel-surface gap as a follow-up bug. Test 1: `PlatformReminderBridge.showFullScreen invokes the channel method (Dart seam IS exercised)` — mocks the `doit/reminders` channel to return `null` for any call, awaits `PlatformReminderBridge().showFullScreen('h-fsi-stab')`, asserts the captured `MethodCall` log has 1 entry with method=`showFullScreen` AND args=`{'habitId': 'h-fsi-stab'}`. Pins the channel-method name so a future contributor who renames either side sees the test fail and updates both. Test 2: `PlatformReminderBridge.showFullScreen throws MissingPluginException when the Kotlin handler has no arm (KNOWN GAP)` — mocks the channel to mirror production (handle only the documented arms `setExact` / `cancel` / `showNotification` / `cancelNotification` / `probeReliability`; everything else throws `MissingPluginException`), asserts `expectLater(() => b.showFullScreen('h-fsi-stab-gap'), throwsA(isA<MissingPluginException>()))`. Pins the gap as a known behavior — a future stabilization cycle that adds the `showFullScreen` Kotlin arm (or removes the dead Dart seam) will see this test fail and remove it. **Test count: 1337 → 1345.** Coverage: `lib/reminders/full_screen_intent.dart` ≥80% (from 25.0%); `lib/services/full_screen_intent_service.dart` ≥95% (from 80.5%); the channel-surface gap test is a regression-protector, not a coverage gate. 3-gate: `dart format --output=none --set-exit-if-changed .` (264 + ~5 files, 0 changed — pure-Dart + new tests) + `flutter analyze --fatal-infos lib test` (0 issues) + `flutter test` (1345/1345 pass). **Cross-references.** `docs/v_model/requirements.md` SYS-130 (the FSI defense-in-depth requirement); `docs/v_model/decision_record.md` ADR-061 (the 6 design choices: defense-in-depth swallow + rename + stale-comment fix + doc-typo fix + channel-surface gap pin + test count expansion); `docs/v_model/workflows.md` WF-058 (the 14-step Cycle C implementation flow); `docs/v_model/plan.md` Milestone 12 `### v1.4-stab-C` sub-entry; `docs/v_model/implementation_status.md` v1.4-stab-C row; `CHANGELOG.md` v1.4-stab-C block; `feature.md` §4 (BUG-003 parking lot bullet removed) + §5 (quick-index updated to ADR-061 / SYS-130 / WF-058) + §6 (next-step updated to Cycle D `feat/v1.4-stab-D-permission-flow-audit`). Targeted runs per `CLAUDE.md`: `flutter test test/reminders/full_screen_intent_test.dart` (passes; +5 tests) + `flutter test test/services/full_screen_intent_service_test.dart` (passes; +3 tests) + `flutter test test/reminders/reminder_bridge_fsi_channel_test.dart` (passes; +2 tests). |

| WF-059 Permission flow coverage: per-kind exhaustive tests + lifecycle edge cases (v1.4-stab-D / Phase 44) | SYS-131 | 4 test files modified/created (test count 1348 → 1363, +13 test bodies + 2 new-file loader counts = +15 net): **(1) `test/services/permission_result_test.dart` (NEW, +6 tests)** — lifts `lib/services/permission_result.dart` coverage from 18.9% (Cycle A audit) to 100%. Group `PermissionResult sealed hierarchy (SYS-131)` (4 tests): `PermissionResultGranted is the singleton grant token` (identity equality + default `Object.toString`); `PermissionResultDenied equality depends on canOpenSettings` (covers `==` / `hashCode` / `toString` invariants); `PermissionResultPermanentlyDenied is the singleton permanent-deny token` (identity equality + custom `toString`); `All three sealed subclasses are exhaustively matched by pattern-matching` (the compile-time regression protector — uses an exhaustive `switch` over all 3 subclasses; if a future contributor adds a 4th subclass without updating this test, the analyzer raises a non-exhaustive switch error). Group `BackupFolderResult sealed hierarchy (SYS-131)` (3 tests): `BackupFolderPicked equality depends on the picked path` (covers `==` / `hashCode` / `toString`); `BackupFolderCancelled is the singleton cancel token`; `BackupFolderError equality depends on the error message`. **(2) `test/people/person_test.dart` (NEW, +3 tests)** — lifts `lib/people/person.dart` coverage from 54.5% (Cycle A audit) to ≥80%. Group `ContactPerson pause semantics (SYS-131)`: `isPausedAt(now) is true iff pausedUntil is non-null and in the future at the reference time` (seeds `pausedUntil: now + 3 days`, asserts `true` at now + `false` at now+5d, `true` at now-1s — covers the `pausedUntil!.isAfter(now)` branch); `isPausedAt(now) is false when pausedUntil is null (the un-paused path)` (covers the `pausedUntil == null` branch); `copyWith(clearPausedUntil: true) drops the pause even if pausedUntil is also passed (defensive)` (covers the `clearPausedUntil: true ? null : ...` branch). **(3) `test/services/permission_lifecycle_observer_test.dart` (extended, +1 test)** — covers the early-return gate at `permission_lifecycle_observer.dart:69`. Test `paused / inactive / hidden lifecycle events do NOT trigger a permission refresh (SYS-131)`: drives all 4 non-`resumed` states (`inactive`, `paused`, `hidden`, `detached`) in sequence after the cold-start consumed; asserts `_fireCountSinceStart` is unchanged (no `PermissionService.refresh()` call fires). **(4) `test/services/permission_service_test.dart` (extended, +4 tests)** — covers the 4 `PermissionStatus` mappings not yet exercised by the v1.3c + v1.5b tests. Test 1: `requestContacts maps PermissionStatus.limited to PermissionResultDenied(canOpenSettings: true)` — covers the iOS partial-access path. Test 2: `requestExactAlarm maps PermissionStatus.restricted to PermissionResultDenied(canOpenSettings: false)` — covers the iOS parental-controls path (no settings affordance). Test 3: `requestLocation maps PermissionStatus.provisional to PermissionResultGranted` — covers the iOS provisional-notifications path (treated as full grant). Test 4: `requestCalendar returns PermissionResultPermanentlyDenied when the user has selected 'Don't ask again'` — sanity test that `permanentlyDenied` is its own sealed subclass (not a `denied` flag). **Test count: 1348 → 1363.** Coverage: `lib/services/permission_result.dart` 18.9% → 100%; `lib/services/permission_service.dart` 93.4% → ≥95%; `lib/services/permission_lifecycle_observer.dart` 78.6% → ≥90%; `lib/people/person.dart` 54.5% → ≥80% (Cycle K brings to 100%). 3-gate: `dart format --output=none --set-exit-if-changed .` (268 files, 0 changed after auto-format on the 3 NEW + 2 EXTENDED files) + `flutter analyze --fatal-infos lib test` (0 issues) + `flutter test` (1363/1363 pass). **Cross-references.** `docs/v_model/requirements.md` SYS-131 (the permission-flow coverage requirement); `docs/v_model/decision_record.md` ADR-062 (the 6 design choices); `docs/v_model/workflows.md` WF-059 (the 14-step Cycle D implementation flow); `docs/v_model/plan.md` Milestone 12 `### v1.4-stab-D` sub-entry; `docs/v_model/implementation_status.md` v1.4-stab-D row; `CHANGELOG.md` v1.4-stab-D block; `feature.md` §4 (BUG-005/011/012/020 parking lot bullets removed) + §5 (quick-index updated to ADR-062 / SYS-131 / WF-059) + §6 (next-step updated to Cycle E `feat/v1.4-stab-E-reliability-detection`). Targeted runs per `CLAUDE.md`: `flutter test test/services/permission_result_test.dart` (passes; +6 tests) + `flutter test test/services/permission_service_test.dart` (passes; +4 tests) + `flutter test test/services/permission_lifecycle_observer_test.dart` (passes; +1 test) + `flutter test test/people/person_test.dart` (passes; +3 tests). |

| WF-060 Reliability detection coverage: broadcast+distinct stream + first-read race + idle-window (v1.4-stab-E / Phase 45) | SYS-132 | 3 test files modified/created (test count 1363 → 1371, +8 tests net): **(1) `test/services/reliability_service_test.dart` (extended, +5 tests)** — pins the unified `ReliabilityService` error-path + lifecycle contract. Tests: `a probe that throws StateError keeps the prior cached value` (ADR-013 regression pin via one-shot `throwOnProbe` flag in `_ScriptedBridge`); `a fresh cold-start with no probe yet initializes to optimal` (first-read race fix); `refresh() after a permissions change re-probes the bridge AND re-derives` (permission-driven re-derive); `the stream emits Reliability.optimal when a transition lands on optimal` (broadcast+distinct transition-emit contract — pinned by flipping the bridge to `degraded`, init, then flipping to `optimal` and calling `refresh()`); `dispose() closes the broadcast stream controller` (no-leak invariant via `onDone` Completer + 1s timeout). **(2) `test/reminders/alarm_scheduler_test.dart` (extended, +2 tests in a new `AlarmScheduler fallback paths (SYS-132)` group)** — pins the exact-alarm-granted primary path on the `FakeAlarmScheduler`. Tests: `schedule with exact-alarm granted stores a primary alarm`; `cancel for an exact-alarm scheduled habit removes the primary alarm`. **(3) `test/reminders/doze_simulation_test.dart` (NEW, +1 test)** — pins the 30 s fallback timer policy. Test `the 30 s fallback timer fires refresh() deterministically — idle-window simulation`: registers a `_RecordingPeriodicFactory` for the timer, asserts `lastDuration == 30s`, fires the callback, asserts `bridge.probeCount` incremented. **Test count: 1363 → 1371.** Coverage: `lib/services/reliability_service.dart` (5 new tests pin error paths); `lib/reminders/alarm_scheduler.dart` (2 new tests pin the exact-alarm-granted branch). 3-gate: `dart format --output=none --set-exit-if-changed .` (269 files, 0 changed — pure-Dart + new tests) + `flutter analyze --fatal-infos lib test` (0 issues after stripping 5 redundant-default-arg warnings) + `flutter test` (1371/1371 pass). **Cross-references.** `docs/v_model/requirements.md` SYS-132 (the reliability detection coverage requirement); `docs/v_model/decision_record.md` ADR-063 (the 5 design choices); `docs/v_model/workflows.md` WF-060; `docs/v_model/plan.md` Milestone 12 `### v1.4-stab-E`; `docs/v_model/implementation_status.md` v1.4-stab-E row; `CHANGELOG.md` v1.4-stab-E block; `feature.md` §5 + §6. Targeted runs: `flutter test test/services/reliability_service_test.dart` (16 pass, +5) + `flutter test test/reminders/doze_simulation_test.dart` (1 pass, NEW) + `flutter test test/reminders/alarm_scheduler_test.dart` (14 pass, +2). |

| WF-061 Backup round-trip exhaustive coverage: malformed envelopes + dispatcher error paths (v1.4-stab-F / Phase 46) | SYS-133 | 3 test files modified/created (test count 1371 → 1379, +8 tests net): **(1) `test/services/backup_encryption_test.dart` (extended, +5 tests)** — pins the 5 uncovered error paths in `backup_service.dart`. Tests: `BackupFormatException.toString() includes the message` (covers `lib/services/backup_service.dart:39`); `import rejects envelope with no kdf object` (covers line 651 throw); `v2 envelope with iterations below the floor is rejected` (covers lines 743-744 throw); `v3 envelope with missing fields throws` (covers line 684 throw); `v2 envelope with missing fields throws` (covers line 740 throw). **(2) `test/backup/scheduler_skip_test.dart` (NEW, +1 test)** — pins `ScheduleMode.none` early-return in `runBackupTask`. **(3) `test/services/backup_task_dispatcher_test.dart` (extended, +2 tests)** — pins the unknown-task-name + init-failure-swallow error paths. **Test count: 1371 → 1379.** Coverage: `lib/services/backup_service.dart` ≥95% (from 96.5% gap); `lib/services/backup_scheduler.dart` ≥90% (from 85.3% gap). 3-gate: `dart format --output=none --set-exit-if-changed .` + `flutter analyze --fatal-infos lib test` + `flutter test` (1379/1379 pass). **Cross-references.** SYS-133; ADR-064; WF-061; Milestone 12 `### v1.4-stab-F`; v1.4-stab-F row; CHANGELOG block; feature.md §5 + §6. Targeted runs: `flutter test test/services/backup_encryption_test.dart` (passes; +5) + `flutter test test/backup/scheduler_skip_test.dart` (passes; NEW +1) + `flutter test test/services/backup_task_dispatcher_test.dart` (passes; +2). |
