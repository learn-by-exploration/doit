# System Requirements

Status: draft baseline, created 2026-06-13.

This document is the contract. Every SYS- ID maps to a test or a manual
check via the [traceability matrix](traceability_matrix.md). If a
requirement cannot be verified, it is not a requirement — it is a wish.

## User Need

A single user wants to build daily dos and maintain important
relationships on one Android phone, without the app lying to them about
consecutive runs, without their data leaving the device, and without
the operating system silently killing the alarms. The product fuses
do-tracking, contact-cadence, Alarmy-style strong enforcement, and
(v1.0+) location / device-state / calendar / call-screening routines.

## Product Scope

### In scope for v0.1

- Add a do with name, icon, schedule, proof mode, mission chain,
  consecutive-run policy.
- Add a person from device contacts with cadence, channel, and (if
  Strong) mission chain.
- Schedule a do to fire at the right time on Android.
- Fire the reminder, surface as a high-priority notification, and
  optionally a full-screen intent.
- Complete the reminder in three modes: Soft (one-tap), Strong
  (mission chain), Auto (interval window).
- Track completions, misses, snoozes, and skips honestly in a local log.
- Compute consecutive runs: per-do and overall, with a configurable
  skip-day budget (renamed from rest-day in v1.0/Phase A — see ADR-024).
- Show stats: consecutive run, best run, completion rate, time-of-day.
- Survive device reboot, timezone change, and OEM battery savers
  (with a user prompt to whitelist).
- Auto backup to a user-chosen folder, once per day.
- Restore from a backup file.
- Permission-first UX: every platform interface is explained and
  requested with rationale.
- Local-only data. No network calls with user data.
- All 4 do presets (drink water, call person, morning routine,
  daily todo) work end-to-end.
- All 5 mission types (Shake, Type, Hold, Math, Memory) work
  end-to-end.
- Home screen widget shows next 3 due items.

### Possible later scope (v0.2+)

- iOS port.
- Quick-settings tile for "I'm up" / "Snooze next".
- Voice quick-add ("OK, add: call Dad this Sunday").
- Location-anchored dos (e.g., "At the office, start work routine").
- Barcode / QR mission (with home-printed codes).
- Photo mission (with on-device scene match).
- Encrypted backup (with user passphrase).
- Multiple home widget layouts.
- App lock (biometric / passcode) — to keep family from snoozing for you.
- Theming: dark mode is v0.1; light + custom themes are v0.2.
- Watch / Wear OS companion for quick-done.
- Web companion for read-only stats.

### In scope for v0.2 (committed 2026-06-14)

See [`v0_2_proposal.md`](v0_2_proposal.md) and
[`v0_2_baseline.md`](v0_2_baseline.md) for the full scope. The
8 workflows and 16 SYS-IDs that v0.2 adds (SYS-032..SYS-047) are
committed; the model layer will be extended (categories, events,
groups, time-window schedule, paused state, edit flow). v0.2 does
NOT loosen any v0.1 constraint (still no `INTERNET`, no
`CALL_PHONE`, no cloud, no telemetry).

### Out of scope unless re-approved

- Multiple named users on one device (no family / partner profiles).
- Multi-device sync.
- Cloud backup of any kind (auto or manual).
- A web app.
- Any feature that requires a backend.
- Telemetry, analytics, crash reporting that leaves the device.
- Storing payment cards, bank data, or any credentials.
- CALL_PHONE permission. Always dialer pre-fill.
- Access-card / RFID / NFC emulation. do it reminds you to take
  action; it does not pretend to be the thing.

## System Requirements

| ID | Requirement | Verification |
| --- | --- | --- |
| **SYS-001** | The app shall allow a user to add a do with name, schedule type, schedule parameters, proof mode, mission chain (if Strong), consecutive-run policy. Do names shall be unique within the local DB (case-insensitive, trim); a duplicate shall be rejected at save time with `DuplicateDoName`. | Widget test (`test/screens/add_habit_test.dart`) + manual acceptance |
| **SYS-002** | The app shall allow a user to add a person from device contacts with display name, cadence, channel, mission chain (if Strong). | Widget test (`test/people/add_person_test.dart`) + manual acceptance |
| **SYS-003** | The app shall schedule a do's next occurrence to fire within ±60 sec of the target time on a non-Doze device, via AlarmManager exact alarm. | Unit test for `AlarmScheduler.nextOccurrence()` + integration test (`test/reminders/alarm_scheduler_test.dart`) + manual device check |
| **SYS-004** | The app shall expose four schedule types: Fixed (time-of-day), Interval (every N units), Anchor (relative to a wake-up or other do), Day-of-X (week / month / year). | Unit test for each schedule type's `nextOccurrence()` + widget test for the schedule picker |
| **SYS-005** | A reminder shall surface as a high-priority notification with the do name, due time, and a "Done" / "Open" action. The app shall create one or more Android notification channels for reminders (default channel id: `do.reminders`, importance HIGH). The user may configure sound and vibration per-channel in the system settings. | Widget test + manual device check |
| **SYS-006** | A reminder shall optionally surface as a full-screen intent when the screen is off, locked, or the user has the app in the background. The full-screen activity must show the proof-mode UI. | Integration test (mock Activity) + manual device check on a locked phone |
| **SYS-007** | The app shall support three proof modes per do: Soft (one-tap), Strong (mission chain), Auto (interval window). | Unit test for `DoProofMode` + widget test for each mode's UI |
| **SYS-008** | The Shake-N mission shall derive the count from the accelerometer, must require a magnitude threshold AND an inter-shake spacing, and must not advance when the phone is still. The default magnitude threshold shall be 14.0 m/s², the minimum inter-shake spacing shall be 250 ms, and the maximum inter-shake spacing shall be 1500 ms. These defaults may be overridden per-do; all three values are immutable after the do has at least one completion (see ADR-012). | Unit test for `ShakeDetector.isShake(sample)` + manual device check on a desk |
| **SYS-009** | The Type phrase mission shall require an exact (case-insensitive, trim) match of the user-entered text against the expected phrase. | Unit test for `TypeMission.verify()` |
| **SYS-010** | The Hold-tap mission shall require a continuous press of 3-5 seconds (configurable). Releasing early shall reset the progress. | Unit test for `HoldMission.tick(progress, dt)` + widget test |
| **SYS-011** | The Math mission shall present a random math problem and require a correct answer. Difficulty shall scale (Easy: 1-digit × 1-digit; Hard: 2-digit × 2-digit with addition). After 3 consecutive wrong answers on the same problem, the app shall surface a "take a break" nudge and auto-fail the mission (no fourth attempt); the wrong-answer count is logged. | Unit test for `MathProblem.next(difficulty)` + widget test for the 3-wrong nudge |
| **SYS-012** | The Memory mission shall present a 4×3 grid of card pairs and require all matches within 60 s. Failed attempts shall be logged. | Unit test for `MemoryMission` + widget test |
| **SYS-013** | Strong-mode dos shall require a chain of one or more missions in declared order. Skipping or reordering shall invalidate the proof. | Unit test for `MissionChain.next(current, completed)` |
| **SYS-014** | A call reminder, when tapped, shall open the system dialer with the contact's number pre-filled. The app shall not call `ACTION_CALL`. | Unit test (intent inspect) + manual device check; `AndroidManifest.xml` must not declare `CALL_PHONE` |
| **SYS-015** | The app shall support a wake-up anchor with two detection modes (manual, first-unlock) selectable in settings, plus a "either with confirmation" hybrid. A 4-hour debounce shall prevent a second wake-up anchor event from being recorded within 4 hours of the previous one. | Unit test for `AnchorDetector` + widget test for the anchor setting |
| **SYS-016** | The app shall re-schedule all pending reminders on device boot, timezone change, and DST change. | Integration test (`test/reminders/reboot_survival_test.dart`) + manual device check |
| **SYS-017** | The app shall survive Doze by combining exact-alarm (primary), a foreground-service heartbeat for "I'm up" (best effort), and a user-driven Doze whitelist prompt. | Manual device check with battery-saver + Doze simulation; banner surfaced if not whitelisted |
| **SYS-018** | The app shall re-schedule a snoozed reminder at the chosen snooze time and track the snooze count per occurrence. A snoozed reminder may be snoozed up to 3 times within a single occurrence; further snooze taps shall surface "you've snoozed 3 times — skip or do it" and offer no further snooze option (the user may still Skip with a skip day or accept a consecutive-run break). | Unit test for `ReminderScheduler.snooze()` + widget test for the 3-strike cap |
| **SYS-019** | The app shall compute consecutive runs per do and overall. A run breaks only on a missed day past the grace window. The completion log is the source of truth; the run count is derived. The default grace window shall be 03:00 local time the next day — a do completed between 00:00 and 03:00 shall count for the previous day; the grace window is per-do configurable. | Unit test for `ConsecutiveCounter.compute()` in `test/habits/streak_calculator_test.dart` across at least 20 cases (DST, skip day, missed-then-backfilled, mode change mid-run, partial-day edge cases) |
| **SYS-020** | The app shall support a configurable skip-day budget per do (default 2 / month; class name: `SkipBudget` — was `RestDayBudget` pre-v1.0). A skip day preserves the consecutive run. | Unit test for `SkipBudget.consume()` in `test/habits/rest_day_budget_test.dart` |
| **SYS-021** | The app shall display per-do and overall stats: current consecutive run, best consecutive run, completion rate (30/90/365 days), time-of-day heatmap, missed-day distribution. | Widget test for the stats screen + manual acceptance |
| **SYS-022** | The app shall store all user data in a local SQLite database. The DB schema shall be versioned; a forward-migration shall run on launch if the schema version is older. | Unit test for migrations (`test/db/migration_test.dart`) + DB inspection on a real device |
| **SYS-023** | The app shall auto-backup the local DB to a user-chosen folder once per day, in a versioned JSON file. Files older than 30 days shall be pruned. The backup envelope shall be `{"version": 1, "exported_at": <ISO8601>, "data": {...}}`; a file with `version` higher than the app's supported version shall be rejected with "backup from a newer version"; a file with a lower `version` shall be migrated forward via `lib/services/db/migrations/`. If the SAF write fails, the app shall retry up to 3 times with exponential backoff (1 s, 5 s, 30 s); if all retries fail, a banner shall be shown and the user notified via a "last backup failed" status in Settings. | Integration test (`test/backup/auto_backup_test.dart`) + manual device check |
| **SYS-024** | The app shall restore from a backup file picked via the system file picker. Restore shall be idempotent (re-importing the same file twice produces no duplicates). | Integration test (`test/backup/restore_test.dart`) |
| **SYS-025** | The app shall request all platform permissions with a rationale screen that explains the consequence of denial. | Widget test for the onboarding flow + manual acceptance |
| **SYS-026** | The app shall not perform any network call with user data. Any `http(s)://` or `dart:io` HTTP usage shall be a security defect and shall be removed. | Code search + a CI grep rule that fails on `import 'package:http'` and `Uri.https` outside the dev-only test harness |
| **SYS-027** | The app shall follow the existing `board_box` 3-gate: `dart format --output=none --set-exit-if-changed .` → `flutter analyze --fatal-infos` → `flutter test`. All three must pass with zero failures. | CI run on every PR |
| **SYS-028** | The app shall maintain ≥ 80% test coverage on changed files. Coverage report shall be uploaded as a CI artifact. | `flutter test --coverage` + `genhtml` + report check |
| **SYS-029** | The app shall display a home screen widget that shows the next 3 due items, each with a "Done" button. Tapping "Done" shall mark the item done and refresh the widget. | Widget test + manual device check |
| **SYS-030** | The app shall provide a permission baseline in `AndroidManifest.xml` that matches the v0.1 scope. No permission shall be added without an ADR. | `AndroidManifest.xml` review + diff in PRs that touch it |
| **SYS-031** | The total mission-chain timeout (sum of per-mission `timeout` values) shall not exceed 5 minutes at do-save time. A chain whose total exceeds 5 min shall be rejected with `ChainTimeoutTooLong`. Chains longer than 5 min shall require an explicit user confirmation in v0.2. | Unit test in `test/missions/chain_test.dart` validates that a chain with timeout sum > 5 min throws on save |
| **SYS-032** | The app shall allow a user to add a one-off date-specific reminder ("event") with name, date+time, lead time, optional mission chain, optional recurrence (none / annually). The event shall surface on the home screen "Events" tab. After the event fires, it shall auto-archive (move to an "Archived" list, never deleted for 90 days). | Widget test for the add-event flow + integration test for the one-shot alarm schedule + manual acceptance |
| **SYS-033** | The app shall schedule an event as a one-shot alarm at `event.at - event.leadTime`. The one-shot alarm shall use the same `AlarmScheduler` path as do alarms (exact alarm primary, WorkManager fallback). A duplicate fire of the same event-id shall be deduped. | Unit test for the one-shot schedule computation + integration test for the alarm manager call |
| **SYS-034** | The app shall support a per-event lead time of 5 min, 15 min, 1 h, 1 day, or 1 week. The lead time must be less than the gap from `now` to `event.at`. A lead-time violation at save time shall throw `InvalidLeadTime`. | Unit test for the lead-time validator |
| **SYS-035** | The app shall auto-archive an event 24 h after it fires. Archived events shall be retained for 90 days, then purged by the next nightly backup. The user can browse archived events from the Events tab. | Integration test for the auto-archive job |
| **SYS-036** | The app shall allow a user to add a contact group with 2-10 contacts, a name, a shared cadence, a shared channel, and a shared mission chain. The group shall be a first-class entity in the local DB (separate from `PersonRow`). | Widget test for the add-group flow + integration test for the group CRUD |
| **SYS-037** | For a `Rotation` group, the scheduler shall pick the next contact by `least_recently_contacted`, breaking ties by `id` ascending. The picked contact is recorded in the `person_groups.lastContactedPersonId` field after a completion. | Unit test for the rotation selector across 4 cases (single member, all same timestamp, all different timestamps, ties) |
| **SYS-038** | For an `Any` group, the completion log records which specific contact was contacted. For an `All` group, the next reminder is the member with the oldest `lastContactedAt`. An `All` group with any unresolved members is still schedulable; the user is shown a banner. | Unit test for the any/all selector |
| **SYS-039** | The app shall support a `DoTimeWindow` schedule type (renamed from `HabitTimeWindow` in v1.0/Phase A — see ADR-024) with start time, end time, and weekdays. The `nextOccurrence(from)` shall return the next `start` time on the next active weekday, or `now` if `now` is inside the window. The window's end-time shall be the deadline for completion. | Unit test for `DoTimeWindow.nextOccurrence()` across DST, weekday wrap, and within-window |
| **SYS-040** | The home screen shall display a live timer for any active time-window do: "Fasting, 6h 12m elapsed, 1h 48m remaining" (or "Lunch window, 2h 18m left"). The timer shall update every minute via a `Ticker`. | Widget test + manual device check |
| **SYS-041** | The do detail screen shall expose a "Test in 30s" button. The test fire shall use the existing `AlarmScheduler` with a `test: true` flag, fire the same notification / full-screen intent the real reminder would, and NOT log a completion or reschedule the next occurrence. | Widget test for the test button + integration test for the test fire |
| **SYS-042** | The do `id` shall be immutable after creation. All other fields (name, schedule, proof mode, mission chain, consecutive-run policy, category, color, icon, pausedUntilMillis) shall be mutable via the edit flow. | Unit test for the immutable-id invariant |
| **SYS-043** | An edit of a do shall preserve the completion log and the consecutive-run history. The `id` is stable; only fields change. The run is recomputed from the unchanged log. | Integration test: edit a do, assert the log is intact, assert the run is unchanged |
| **SYS-044** | The app shall allow the user to bulk-complete N occurrences of an interval do (1 ≤ N ≤ daily target, capped at 4 per tap). Each bulk-logged completion shall have a timestamp spread across the missed window range, not all at the bulk-log moment. | Widget test for the bulk button + integration test for the timestamp spread |
| **SYS-045** | The app shall support a `DoCategory` enum (renamed from `HabitCategory` in v1.0/Phase A — see ADR-024): `health`, `mind`, `relationships`, `productivity`, `home`, `other`. A new `category` field on `DoRow` (DB row, `category` column) defaults to `other`. The stats screen shall group by category. | Unit test for the enum + widget test for the stats grouping |
| **SYS-046** | The app shall support a per-do icon from a 64-icon Material Symbols set. The icon shall be stored as a string key on `DoRow` (DB column `iconName`; e.g., `local_drink`, `directions_run`, `self_improvement`). A null icon defaults to the category's canonical icon. | Widget test for the icon picker |
| **SYS-047** | The app shall support a paused state for dos and persons. A `pausedUntilMillis` field on `DoRow` and `PersonRow` (nullable). When set and in the future, the scheduler shall not fire reminders for that row. A paused period shall not break consecutive runs. | Unit test for the paused-state guard + widget test for the pause UI |
| **SYS-048** | The app shall expose a "Send a test reminder" tile in Settings → About. Tapping the tile shall schedule a one-shot alarm 10 seconds in the future via the same `AlarmScheduler` path as a real do alarm, with a `test: true` flag on the `AlarmEvent`. The test fire shall render the same notification / full-screen intent the real reminder would, and shall NOT log a completion or reschedule the next occurrence. (Back-fill; originally cited by `acceptance_run_v2.md` for WF-028.) | Widget test for the tile + integration test for the test fire |
| **SYS-049** | The home screen shall expose a "Bulk complete" action for interval dos with ≥ 2 missed occurrences. The action shall log 1–4 completions per tap, each with a timestamp spread across the missed window range (not all at the tap moment). A SnackBar shall confirm the count landed. (Back-fill; originally cited by `acceptance_run_v2.md` for WF-029.) | Widget test for the bulk button + integration test for the timestamp spread |
| **SYS-050** | The repository shall include a `LICENSE` (MIT) at the project root, copyright the project owner, and be the sole license for the project. The license text shall be the verbatim text from the Open Source Initiative. No `LICENSE` change shall land without an ADR. | File check on `LICENSE` exists at repo root + grep that the text matches the MIT template |
| **SYS-051** | The repository shall include a `PRIVACY.md` at the project root that discloses (1) the on-device data inventory, (2) what the app does **not** do (no server, no analytics, no telemetry, no advertising ID, no install ID, no crash reporter, no cloud backup), (3) the no-`INTERNET` enforcement, and (4) honest caveats about unimplemented features whose schema slots exist. The privacy notice shall not claim more than the code does. | File check on `PRIVACY.md` exists at repo root + manual review of the four sections |
| **SYS-052** | The app's version string shall be a single source of truth: a `kAppVersion` / `kAppVersionCode` constant in `lib/build_info.dart` that mirrors the `pubspec.yaml` `version:` field. A test (`test/build_info_test.dart`) shall parse `pubspec.yaml` and assert the two stay in sync; the 3-gate shall fail on drift. | Unit test in `test/build_info_test.dart` |
| **SYS-053** | The release build shall read `android/key.properties` (gitignored) and wire a real `signingConfigs.release` block in `android/app/build.gradle.kts`. The `release` buildType shall reference `signingConfigs.getByName("release")` and shall NOT set `isMinifyEnabled` (R8 / minify is off for v0.3). If `key.properties` is missing, the build shall fall back to the debug signing config so dev builds keep working. The keystore files (`*.jks`, `*.der`, `key.properties`) shall never be committed. | Unit test in `test/release_signing_test.dart` parses the build file + `.gitignore` check + hands-on `flutter build appbundle --release` |
| **SYS-054** | The Settings → About section shall include a "Open source licenses" `ListTile` that opens the standard Flutter `showLicensePage` (passing `applicationName: 'do it'`, `applicationVersion: kAppVersion`, `applicationLegalese: 'Local-only. No telemetry. No accounts.'`). The static version row in the About section shall read `kAppVersion`. (See `WF-032`.) | Widget test in `test/screens/settings_licenses_test.dart` |
| **SYS-055** | A widget test in `test/integration/fresh_install_test.dart` shall simulate a wiped-device install: empty Drift DB → `OnboardingScreen` renders → "done" path is exercised → `HomeScreen` renders → add a `DoFixed` → fire a test reminder via `ReminderService.scheduleTestReminder()` → assert the scheduler received the request. A 2-paragraph wiped-device checklist in `docs/v_model/v0_3_release_checklist.md` § Fresh-install smoke test shall be ticked off by the user on a wiped phone (or emulator) before handing the apk to friends. | Widget test in `test/integration/fresh_install_test.dart` + manual checklist sign-off |
| **SYS-056** | The `README.md` Status section shall be honest: it shall not say "Implementation has not started" once v0.1 code has shipped, and shall not claim a future milestone as "done". It shall point to `docs/v_model/implementation_status.md` for the current slice. | Manual review on every commit that touches `README.md` |
| **SYS-057** | The repository shall include a `.github/workflows/ci.yml` file that runs the 3-gate (`dart format` → `flutter analyze --fatal-infos` → `flutter test`) on every `pull_request` and `push` to `main`. A static-analysis test (`test/ci_workflow_test.dart`) shall parse the workflow file and assert the three steps are present in the right order. | Unit test in `test/ci_workflow_test.dart` parses the workflow + green CI run on the v0.4a.1 tip |
| **SYS-058** | The repository shall include a `CHANGELOG.md` at the project root with sections for v0.1, v0.2, v0.3, and v0.4. Each section shall list the headline features and the bug fixes. The v0.4 section shall be appended in the v0.4d sign-off commit. Closes open question #20. | File check on `CHANGELOG.md` exists at repo root + section review |
| **SYS-059** | The `firstLaunch` flag shall be persisted across app restarts via `SharedPreferences` (or an equivalent in the `SettingsService`). The hard-coded `firstLaunch = true` in `lib/main.dart` shall be replaced with a service read. A test (`test/services/first_launch_persisted_test.dart`) shall close the service, reopen it, and assert the flag is still the value it was set to. | Unit test in `test/services/first_launch_persisted_test.dart` |
| **SYS-060** | `BackupService.scheduleNightlyBackup()` shall register a `workmanager` periodic task (24-hour frequency) that invokes the existing `runBackup()` entrypoint. The Kotlin side shall register the Dart callback via `WorkmanagerPlugin`'s `setPluginRegistrantCallback`. A test shall mock the `Workmanager` interface and assert the call. `PRIVACY.md` shall no longer disclose the "scheduling call not yet wired" caveat. | Unit test for the scheduler call + the Kotlin `BackupWorker.kt` registering the callback |
| **SYS-061** | `kBackupFormatVersion` shall be 2. The export flow shall (1) generate a 16-byte salt via `Random.secure()`, (2) derive a 32-byte AES-256-GCM key via PBKDF2-HMAC-SHA256 with ≥ 100,000 iterations, (3) generate a 12-byte nonce, (4) encrypt the JSON envelope, (5) write `{"version": 2, "kdf": {...}, "ciphertextB64": "...", "nonceB64": "..."}` to the file. The import flow shall support v1 (plain JSON, back-compat) and v2 (passphrase + encrypted). A test (`test/services/backup_encryption_test.dart`) shall round-trip with a passphrase. `PRIVACY.md` shall no longer disclose the "plain JSON backups" caveat. | Unit test in `test/services/backup_encryption_test.dart` |
| **SYS-062** | Every interactive element in `lib/screens/*.dart` and `lib/widgets/*.dart` (every `Button`, `IconButton`, `ListTile`, `TextField`, and tap-able `GestureDetector`) shall have a `Semantics` label, a `tooltip`, or a `semanticLabel`. A static-analysis test (`test/a11y/semantics_labels_test.dart`) shall walk the widget tree of every public screen and assert the labels. The user's hands-on TalkBack pass on a real device or emulator is the v0.4d step. | Unit test in `test/a11y/semantics_labels_test.dart` + manual device check |
| **SYS-063** | Onboarding step 0 shall invoke `PermissionService.requestNotifications()` to request `POST_NOTIFICATIONS` (Android 13+). The CTA advances the step on `granted`; on `denied` (one-shot) the CTA stays enabled and a "Try again" affordance is rendered; on `permanentlyDenied` a "Go to Android Settings" button is rendered that calls `PermissionService.openAppSettings()`. The widget layer shall not call `permission_handler` directly — the call goes through the service seam. (v0.5 / ADR-016.) | Service test (`test/services/permission_service_test.dart`) + widget test (`test/screens/onboarding_permission_wiring_test.dart`) + manual device check |
| **SYS-064** | Onboarding step 1 shall invoke `PermissionService.requestContacts()` to request `READ_CONTACTS`. Same shape as SYS-063. Revoking `READ_CONTACTS` after adding a person pauses all person-based dos and surfaces a banner; re-granting resumes. (v0.5 / ADR-016. Closes open question #5.) | Service test + widget test for the onboarding step + manual device check |
| **SYS-065** | Onboarding step 2 shall invoke `PermissionService.requestExactAlarm()` to request `SCHEDULE_EXACT_ALARM` / `USE_EXACT_ALARM` (Android 12+). On Android 12+ this is a system policy permission, not a runtime permission, so the most common result is `denied` with the deep-link affordance as the primary recovery path. The home-screen reliability banner shall reflect the SYS-065 result. (v0.5 / ADR-016.) | Service test + widget test for the onboarding step + manual device check |
| **SYS-066** | Onboarding step 3 shall invoke `PermissionService.requestBackupFolder()` to open the SAF folder picker (`ACTION_OPEN_DOCUMENT_TREE`). On a non-null `treeUri`, the URI is persisted to `SettingsService.backupFolderUri` (a `ValueNotifier<String?>`) and the step advances. On cancellation, the step advances anyway — the backup folder is skippable per ADR-014 step 6. A revoked SAF URI pauses auto-backup and surfaces a banner; the user re-picks from Settings → Restore. (v0.5 / ADR-016. Closes open question #6.) | Service test + widget test for the onboarding step + `test/services/settings_service_backup_uri_test.dart` + manual device check |
| **SYS-067** | The app shall ship a curated library of at least 25 templates (Do / Event / Person / Routine) seeded into the `Templates` Drift table on first run via `TemplateLibrary.seedBuiltIns(TemplateRepository.instance)`. Phase B ships 19 templates (12 Do + 3 Person + 4 Event). Phase F adds 6 routine templates to reach the 25-template quota. The seed is idempotent (`INSERT OR IGNORE` keyed on `id`) and runs from `AppDatabaseService.init()` AFTER the v2→v3 migration completes, guarded by `from < 3` so existing v3 users do not re-seed. A user that deletes a built-in does not get it back on the next launch. Each built-in carries `kTemplateFormatVersion = 1` envelope with the matching inner key (`"do"`, `"event"`, `"person"`). Routine templates' inner payload is opaque in Phase B (Phase F adds the `RoutineTemplatePayload` decoder). | Widget test (`test/screens/templates_test.dart`: 25 cards render after first seed; idempotent re-seed returns 0) + service test (`test/services/template_repository_test.dart`: `seedBuiltIns` is idempotent across two runs; `deleteById` refuses built-ins) |
| **SYS-068** | The user shall be able to save any configured Do / Event / Person as a reusable user-saved template from the AppBar overflow action "Save as template" on `AddHabitScreen` / `AddEventScreen` / `AddPersonScreen`. The action captures the current form state (NOT the persisted row) into a new `Template` row with `isBuiltIn: false`. The catalog screen lists user templates alongside built-ins (a flat list per `ADR-020` § decision 4). A user template may be deleted via long-press → confirm → `TemplateRepository.instance.deleteById(id)`. Built-ins are read-only — `deleteById` on a built-in throws `TemplateIsBuiltIn`. Templates are restored automatically via the existing backup service (they are a regular Drift table). | Widget test (`test/screens/add_event_test.dart`: save-as-template writes a row with `isBuiltIn: false` and the right envelope) + integration test (`test/services/template_repository_test.dart`: round-trip a user-saved template through save + listAll + delete) |
| **SYS-069** | The app shall expose a sealed `Trigger` type in `lib/triggers/trigger.dart` with five top-level subclasses: `TriggerTimeOfDay`, `TriggerLocationEnter` / `TriggerLocationExit` (sealed pair, both extend a private `TriggerLocation` mixin), `TriggerDeviceState` (sealed), `TriggerCalendarEvent` (sealed), `TriggerCallIncoming` (sealed). Each subclass carries its own `validate()` (throws on malformed input — e.g. geofence radius out of range, calendar event-id missing) and a stable string `type` discriminator used by `toJson` / `fromJson`. A new trigger shape is added by adding a new subclass, not by editing an enum. (v1.0 / Phase C PR 1.) | Unit test in `test/triggers/trigger_test.dart` covering each subclass's validate + toJson / fromJson round-trip + exhaustive `switch` exhaustiveness check |
| **SYS-070** | The app shall expose a sealed `Condition` type in `lib/triggers/condition.dart` supporting AND / OR boolean composition: `ConditionAnd`, `ConditionOr`, plus leaves `ConditionTimeWindow`, `ConditionDayOfWeek`, `ConditionCalendarBusy`, `ConditionBatteryRange`, `ConditionSilentMode`. A `null` condition on an `Automation` is a no-op (the trigger fires unconditionally subject to the action's own validation). The same `type` discriminator / `toJson` / `fromJson` pattern as `Trigger`. (v1.0 / Phase C PR 1.) | Unit test in `test/triggers/condition_test.dart` covering each leaf's validate + AND/OR round-trip |
| **SYS-071** | The app shall expose a sealed `Action` type in `lib/actions/action.dart` with five top-level subclasses: `ActionNotify` (wraps the existing `NotificationService`), `ActionFullScreen` (wraps `FullScreenIntent`), `ActionCallIntercept` (Japan routine, Phase F), `ActionOverrideSilent` (Japan routine, Phase F), `ActionOpenApp` (quick-action). Each action has a `validate()` (e.g. `ActionNotify.body` must be non-empty after trim) and a `type` discriminator. The executor runs the action's side effect via the existing service seams — the action type does not import `package:flutter/*` or the platform. (v1.0 / Phase C PR 1.) | Unit test in `test/actions/action_test.dart` covering each subclass's validate + toJson / fromJson round-trip |
| **SYS-072** | The app shall support geofence enter / exit triggers via `lib/services/geofence_service.dart`. The service subscribes to a position stream (`geolocator` ^13.0.1 per ADR-021), runs a pure-Dart Haversine matcher against the registered `TriggerLocation` circles (radius 50m..5000m, enforced by `TriggerLocation.validate()`), and emits `GeofenceEntered` / `GeofenceExited` on a broadcast `Stream<GeofenceEvent>`. The `RoutineExecutor` subscribes to that stream in `init()` and dispatches matching `Automation`s via the existing `shouldFire` predicate. The matcher is exposed `@visibleForTesting` (a top-level `computeTransitions(...)`) so unit tests can drive the pure-Dart path without going through the service's `register` flow. Coarse-location (`ACCESS_COARSE_LOCATION`) is sufficient for every radius the user can pick. (v1.0 / Phase C PR 2 / ADR-021.) | Service test (`test/services/geofence_service_test.dart`: 14 tests covering the pure matcher, registry, and events stream) + dispatch test (`test/routines/location_dispatch_test.dart`: 6 tests covering enter/exit/mismatch/disabled/kind-mismatch/multi-entity dispatch) |
| **SYS-073** | The app shall support seven device-state trigger shapes — `TriggerDeviceState.charging` (boolean), `TriggerDeviceState.batteryRange` (low/high), `TriggerDeviceState.bluetoothDevice` (named device), `TriggerDeviceState.wifiSsid` (SSID match), `TriggerDeviceState.headphones` (boolean), `TriggerDeviceState.silentMode` (off / vibrate / on), `TriggerDeviceState.foregroundApp` (package name) — fed by a `DeviceStateProbe` Kotlin platform channel + Dart `Stream<DeviceStateSnapshot>`. (v1.0 / Phase D / ADR-022.) | Service test (`test/services/device_state_probe_test.dart`: scripted snapshots) + dispatch test (`test/routines/device_state_dispatch_test.dart`) |
| **SYS-074** | The app shall support calendar event triggers via a `CalendarProbe` service that watches the user's selected Google account (chosen in the first calendar-touch onboarding step) and emits `TriggerCalendarEvent.starts`, `TriggerCalendarEvent.ends`, `TriggerCalendarEvent.busy`, `TriggerCalendarEvent.free` events. (v1.0 / Phase E / ADR-023.) | Service test (`test/services/calendar_probe_test.dart`: scripted account + event stream) + dispatch test |
| **SYS-075** | The app shall support a Japan silent-mode call-screening routine: when an incoming call matches a configured contact AND the ringer is in silent mode, the routine (a) snaps the ringer to `RINGER_MODE_NORMAL`, (b) plays the contact's ringtone at max volume, (c) launches the full-screen `FullScreenActivity` with the caller's name + photo, (d) on dismiss restores the prior ringer mode. The routine is `ActionCallIntercept` + `ActionOverrideSilent` triggered by `TriggerCallIncoming.fromContacts`. **The configuration is persisted as a `JapanRoutineConfig` (enabled, contactIds, targetMode) in `SettingsService` under three keys `doit.japan_routine.{enabled,contact_ids,target_mode}`. The `AddRoutineScreen` (template #16 entry-point) writes the config and pushes the contact list to `CallInterceptorService.configure(...)`. On Save, the screening service matches the new list on the next incoming call. (v1.0 / Phase F / ADR-019.)** | Integration test (`test/routines/call_intercept_test.dart`: scripts `onScreenCall` events) + manual device check on a real call + widget test for the `AddRoutineScreen` form (`test/screens/add_routine_test.dart`) + Settings tile test (`test/screens/settings_test_reminder_test.dart` extension) |
| **SYS-079** | The app shall support an opt-in call-screening role via `RoleManager.createRequestRoleIntent(RoleManager.ROLE_CALL_SCREENING)`. The role is **not** a runtime permission (`BIND_SCREENING_SERVICE` is signature-protected and granted at install time). The Settings → Permissions → Call-screening tile and an onboarding step (step 4, after the backup folder) surface the role status and fire the OS request flow. The new `CallInterceptorService.isCallScreeningRoleHeld()` / `requestCallScreeningRole()` methods land on the existing `doit/call_interceptor` method channel. The home-screen reliability banner shows "Japan routine unavailable — grant the call-screening role in Settings" when the role is not held AND a Japan routine is enabled. The routine silently no-ops on Android < Q (the role does not exist) and on missing plugin (the test seam returns `false`). (v1.0 / Phase F PR 2 / ADR-019 follow-up.) | Channel test (`test/services/call_interceptor_test.dart`: role methods via `ScriptedCallSource`) + onboarding step test (`test/screens/onboarding_call_screening_step_test.dart`) + Settings tile test |
| **SYS-080** | The app shall expose a `RoutineConfig` value class — `{ templateId, triggerJson, conditionJson?, actionJson, enabled }` — for the five template-driven routines (templates #17..#21, the *Routine* kind). Each `RoutineConfig` is persisted under `doit.routine.<templateId>` in `SharedPreferences` via `SettingsService.setRoutine(templateId, config)` / `getRoutine(templateId)`. The codec is version-free (each per-shape JSON object carries its own `type` discriminator) and the class has structural `==`, a deterministic `hashCode`, a `copyWith`, and a `toJson` / `fromJson` round-trip. A null `conditionJson` is distinct from a present-but-empty one. The `JapanRoutineConfig` class is **not** migrated to this format (it stays on its three v1.0 legacy keys; see ADR-025). (v1.1 / SYS-080 / ADR-025.) | Unit test (`test/services/routine_config_test.dart`: equality, hash determinism + Set round-trip, codec round-trip, malformed-payload errors) + Settings-service test (`test/services/settings_service_routine_test.dart`: set/get/delete round-trip per templateId) |
| **SYS-076** | The app shall expose a `PermissionKind.location` entry on the `PermissionService` enum, probe `Permission.location` (which maps to `ACCESS_COARSE_LOCATION`) in `init()`, expose `requestLocation()`, render the "Location" tile in `PermissionSheet` with a city-block-accurate rationale ("Do it uses your approximate location to fire routines when you arrive at or leave a place you have chosen. Location stays on this device."), and surface a Settings → Permissions affordance to re-probe. A denied coarse-location is a soft failure: geofence triggers silently no-op (the home-screen reliability banner shows "Location off" only if the user has at least one `TriggerLocation*` automation registered). (v1.0 / Phase C PR 2 / ADR-021.) | Service test (`test/services/permission_service_test.dart`: 5th probe added, `requestLocation` mapping) + widget test for the sheet's Location entry |

## Platform Constraints

- **Android 9+ (API 28+).** API 28 is the floor; minSdkVersion 28.
- **Compile/target SDK 34.** Matches `board_box` / `card_box`.
- **Exact alarm permission on Android 12+** is a user-granted
  permission. The app must detect denial and fall back gracefully.
- **Notification permission on Android 13+** is a runtime permission.
  The app must request it with rationale.
- **Doze mode** suspends alarms. The app must prompt the user to
  disable battery optimization and the OEM's auto-start toggle, with
  a one-tap deep link to the system settings.
- **OEM battery savers** (Xiaomi, Oppo, Vivo, Honor, Samsung) kill
  background work even with whitelist. The app must show an OEM-aware
  guide card.
- **No `CALL_PHONE` permission.** Call reminders open the dialer via
  `Intent.ACTION_DIAL` with the number pre-filled in the URI.
- **No `READ_CALL_LOG` permission.** The app does not read call logs
  for cadence auto-priority; the user configures cadence manually or
  per-person.
- **No `INTERNET` permission** for user data. The app must function
  fully offline.

## MVP Success Criteria

A v0.1 build is acceptable if, after a 14-day real-device run on the
user's primary phone, all of the following are true:

1. do it fired a reminder for each of the 4 active presets within
   ±60 seconds of its target time, for ≥ 95% of scheduled occurrences.
2. The user completed at least 70% of the reminders with the
   configured proof mode.
3. The completion log matches the user's honest memory.
4. The app survived at least one device reboot without dropping
   reminders.
5. The app survived at least one timezone change without producing
   duplicate or dropped occurrences.
6. The backup file was written on at least 13 of the 14 nights.
7. The backup was successfully restored on a second device (or after
   uninstall / reinstall).
8. The 3-gate passed with zero failures on every commit during the
   14 days.

> **Note (v1.0/Phase A, see ADR-024):** The MVP criteria above were
> written before the rename. They still apply, but the entity under
> test is now a **Do** (not a "habit") and the consecutive-run counter
> replaces the term "streak".

If any criterion fails, fix it before adding features.
| **SYS-082** | The app shall wire `ActionOpenApp` (the fifth leaf of the sealed `Action` hierarchy, ADR-021) end-to-end. The executor's `_dispatchAction` is a single exhaustive `is`-switch over all five leaves, with each leaf's side effect wrapped in a `_safe(label, fn)` helper that swallows platform exceptions behind `kDebugMode`'s `debugPrint` (so a broken service does not break the dispatch chain). `ActionOpenApp` carries a `route` string validated to start with `/`. The executor cannot push routes itself (it is a non-Flutter singleton; the layer boundary per `.claude/rules/lib-routines.md` is Flutter-free); instead it appends a `RoutineOpenAppRequest` value class `{ route, at }` to a `ValueNotifier<List<RoutineOpenAppRequest>>` (`pendingOpenApp`) on the executor. A new passive `RoutineBanner` widget drains the queue: it captures the `NavigatorState` synchronously inside `build` (NOT inside the post-frame callback, to avoid "Looking up a deactivated widget's ancestor is unsafe"), schedules an `addPostFrameCallback`, and pushes each request FIFO via `navigator.pushNamed(req.route)`, calling `executor.clearPendingOpenApp()` after the drain. The banner renders `SizedBox.shrink()` when the queue is empty (zero layout cost in the steady state). The home screen places the banner directly under `ReliabilityBanner.fromService()` in its Column. (v1.1 / SYS-082 / ADR-026.) | Widget test (`test/widgets/routine_banner_test.dart`: 4 tests covering empty-queue shrink, single push, FIFO drain, and the "opening N destinations" copy) + dispatch tests (`test/routines/executor_test.dart` + 4 sibling dispatch tests cover the five-leaf switch) |
| **SYS-083** | The app shall provide a generic apply UX for the five template-driven routines (templates #17..#21 — calendar / location / device-state / charging / timer). Per the v1.0 sign-off, these five templates previously carried a "Coming in v1.1" badge with no real apply flow; v1.1d ships that flow. A `RoutineTemplatePayload` value class decodes the template's `payloadJson` envelope (`{k: 1, routine: {trigger, condition, action, note}}`) and exposes `trigger / condition / action / note` strings plus a `toRoutineConfig({required enabled})` that emits a `RoutineConfig` with the `routine_placeholder.v1` sentinel on `triggerJson` / `actionJson`. The decoder is fail-soft: malformed JSON, a non-object envelope, a missing `routine` key, a non-object `routine`, a non-string `trigger` / `action`, an empty `trigger` / `action`, or a non-string `condition` all return `null` (NOT throw). A new `RoutineApplyScreen` widget renders the template's name + description + decoded trigger / condition / action as read-only chips (the per-template picker UIs are deferred to v1.1e+). The screen has an enable toggle (seeds `RoutineConfig.enabled`) and Save / Update / Delete buttons. Save calls `SettingsService.setRoutine(payload.toRoutineConfig(enabled: _enabled))` and pops the route (guarded by `Navigator.of(context).canPop()` so the root-mounted test case is a clean no-op). When a saved config already exists for the template, the button label changes to "Update" and a Delete button appears; Delete calls `SettingsService.deleteRoutine(templateId)`. The malformed-envelope path renders a `_MalformedView` (icon + "Could not load routine template X" copy) with NO Save button. The templates catalog (`TemplatesScreen`) routes templates #17..#21 to this screen via `MaterialPageRoute`. The "Coming in v1.1" badge is removed from every routine template (replaced by a "Use this" button). (v1.1 / SYS-083 / ADR-027.) | Unit test (`test/routines/routine_template_payload_test.dart`: 12 tests covering well-formed decode + 8 defect paths + 2 codec round-trips + 3 equality tests) + Settings-service test (`test/services/settings_service_routine_test.dart`: 13 tests including 2 for the new `deleteRoutine`) + Widget test (`test/screens/routine_apply_screen_test.dart`: 6 tests covering render / save / toggle / update-with-Delete / delete / malformed-envelope) + Templates catalog test (`test/screens/templates_test.dart`: 1 test updated to assert all 6 routine templates carry a "Use this" button) + Templates routing test (`test/screens/templates_japan_routing_test.dart`: 1 test rewritten to assert template #17 routes to `RoutineApplyScreen` instead of showing the "Routines land in v1.1." snackbar) |
| **SYS-084** | The app shall provide an offline "map" preview in the location picker (the Phase C / SYS-076 follow-up). The widget, `LocationMapPreview` (`lib/widgets/location_map_preview.dart`), is a `StatelessWidget` that paints a stylised world canvas (5×5 grid + rounded background) and draws a filled pin at the chosen `(latitude, longitude)` plus a translucent ring at the geofence radius (1° latitude ≈ 111 320 m, scaled through the same equirectangular projection that maps the pin). The widget exposes one callback — `onLatLonChanged(lat, lon)` — invoked on `onTapDown` and `onPanUpdate`. Coordinates are projected through `projectLatLonForTest` / `unprojectLatLonForTest` (pinned visible window: lat ∈ [-85°, 85°], lon ∈ [-180°, 180°], 8 px edge margin); out-of-range coords are clamped before being reported. The widget imports nothing beyond `package:flutter/material.dart` (no `flutter_map`, no `latlong2`, no `package:http`); it does not fetch tiles, does not need `INTERNET`, and does not change the v0.1 permission baseline. The `LocationPicker` sheet (`lib/widgets/location_picker.dart`) mounts the preview between the lat/lon TextFormFields and the "Use current location" button. The widget reads from the picker's `_latCtrl` / `_lonCtrl` (parsed as `double`, falling back to `0` on parse failure or empty text) and the `_radiusMeters` state; on tap, it writes back to both controllers with `toStringAsFixed(6)` and calls `setState` so the form re-validates. The lat / lon `TextFormField`s fire `onChanged: (_) => setState(() {})` so the pin follows typed coordinates in real time. (v1.1 / SYS-084 / ADR-028.) | Unit tests (`test/widgets/location_map_preview_test.dart`: 11 tests — 7 pure-helper tests for `projectLatLonForTest` / `unprojectLatLonForTest` / `radiusMetresToPxForTest` covering origin / corners / clamp / round-trip / 0-metres / positive-metres; 4 widget tests for render / tap / prop-change / ring) + Picker tests (`test/widgets/location_picker_test.dart`: 3 new tests — preview is mounted above the radius slider, tapping the preview writes back to the lat/lon controllers, typing into the lat/lon fields updates the preview pin) |
| **SYS-085** | The app shall render a per-automation reliability badge in the trailing slot of each routine list row across the three add screens (`lib/screens/add_habit.dart:_RoutineRow`, `lib/screens/add_person.dart:_PersonRoutineRow`, `lib/screens/add_event.dart:_EventRoutineRow`). The badge — `AutomationReliabilityBadge` (`lib/widgets/automation_reliability_badge.dart`) — is a 40×40dp `IconButton` whose state is derived by a pure function, `automationReliability(Automation, statuses)` (`lib/routines/automation_reliability.dart`), exhaustive over the sealed `Trigger` hierarchy via `_requiredPermissionForTrigger`: `TriggerLocation*` → `PermissionKind.location`, `TriggerCalendarEvent*` → `PermissionKind.calendar`, `TriggerDeviceState*` / `TriggerCallIncoming*` / `TriggerTimeOfDay` → `null` (no runtime gate at v1.1f). The function maps each `PermissionResult` (`PermissionResultGranted` / `PermissionResultDenied(canOpenSettings:)` / `PermissionResultPermanentlyDenied`) to `AutomationReliability.optimal` / `degraded` / `unknown`. The badge hides itself (`SizedBox.shrink`) for `optimal` so the common case does not clutter the trailing slot; for `degraded` it shows `Icons.warning_amber_rounded` in `colorScheme.onErrorContainer` (matching the v1.0 `ReliabilityBanner` visual language); for `unknown` it shows `Icons.info_outline` in `colorScheme.onSecondaryContainer`. It reads `PermissionService.instance.statuses` (`ValueListenable<Map<PermissionKind, PermissionResult?>>`) through a `ValueListenableBuilder`, so the row reactively re-renders after the user grants or revokes a permission — no `setState` in the parent row. The badge is non-interactive when no `onTap` callback is supplied; the three add screens do not pass an `onTap` at v1.1f (a future PR will pass one that opens an `AlertDialog` with the rationale + a deep link to Settings → Permissions). The `TriggerCallIncoming*` arm is reserved for v1.2 (a new `PermissionKind.callScreening` will fold the role-state into the badge once `RoleManager` is wired through `PermissionService`). (v1.1 / SYS-085 / ADR-029.) | Unit tests (`test/routines/automation_reliability_test.dart`: 16 tests covering every `Trigger` leaf × every `PermissionResult` variant × null/unprobed + action-side coverage) + Widget tests (`test/widgets/automation_reliability_badge_test.dart`: 11 tests covering optimal-hide / degraded-warning / unknown-info / `onTap` callback / non-interactive mode / ValueNotifier reactivity both directions) |
| **SYS-086** | The app shall probe and surface the `PACKAGE_USAGE_STATS` special-access permission for the planned `TriggerForegroundApp` (v1.2 — "do X when I open app Y"). Android does NOT show a runtime prompt for this permission; the user MUST navigate to Settings → Special access → Usage access and toggle do it on. A new `PermissionKind.usageStats` entry is added to the sealed `PermissionKind` enum; `PermissionService` probes it through a new `UsageStatsService` singleton (`lib/services/usage_stats_service.dart`) which talks to a Kotlin method channel (`doit/device_state`, two new methods: `isUsageStatsGranted` and `openUsageAccessSettings`). The probe uses `AppOpsManager.unsafeCheckOpNoThrow(OPSTR_GET_USAGE_STATS, Process.myUid(), packageName)` and the deep-link target is `Settings.ACTION_USAGE_ACCESS_SETTINGS` with `FLAG_ACTIVITY_NEW_TASK`. The probe is fire-and-forget from `PermissionService.init()` (via `unawaited(_refreshUsageStatsAfterInit())`) so the real-async `MethodChannel.invokeMethod` call does NOT block `init()` in widget-test fake-async zones (ADR-030 lesson). `PermissionSheet` extends its sealed `PermissionKind` switch with the rationale copy "Allows do it to fire 'do X when I open app Y' routines (coming in v1.2). Android does not show a popup for this — you will need to toggle do it on in the next screen." and routes the "Allow" CTA to the deep-link (no system dialog is possible). `PermissionKind.usageStats` is added to the manifest with `tools:ignore="ProtectedPermissions"` (the permission is opt-in only — the user is never blocked from using do it for declining). `Settings._PermissionTile._reProbe` calls `PermissionService.refreshUsageStats()` on this kind (the only kind that re-probes rather than re-requests). The Settings → Triggers tile and any future `AutomationReliabilityBadge` for a `TriggerForegroundApp`-using routine update reactively via the `PermissionService.statuses` `ValueNotifier` when the user returns from system Settings. (v1.1 / SYS-086 / ADR-030.) | Unit tests (`test/services/usage_stats_service_test.dart`: 8 tests covering isGranted (granted/denied/reflects-setGranted) + openSettings (resolved/not-resolved/multi-call recording) + singleton lifecycle (idempotent-init / resetForTesting-installs-new-source-resets-gate)) |
| **SYS-087** | The app shall provide i18n scaffolding so a future translator can ship a non-English locale without re-deriving copy from `MaterialApp` and `Scaffold` literals. A new `lib/l10n/` directory holds the ARB catalogs: `app_en.arb` is the source-of-truth (~60 keys for AppBar titles, snackbars, empty states, permission tile labels, settings sections, theme/anchor/reliability copy, onboarding step labels, the licenses and version rows, and the ICU plural in `homeSelectionAppBarTitle` / `homeSnackbarMarkedCount`), and `app_es.arb` is the first translation (Spanish — the user's second language). A top-level `l10n.yaml` drives `flutter gen-l10n` (arb-dir, template, output-class, output-dir, nullable-getter: false), and `pubspec.yaml` adds `flutter_localizations: { sdk: flutter }` + `intl: any` + `flutter: { generate: true }`. The generated `AppLocalizations` (under `lib/l10n/gen/`) is wired into `MaterialApp.localizationsDelegates` + `supportedLocales`. The three visible screens (home, settings, onboarding) read every user-facing string from `AppLocalizations.of(context)`. A new `test/support/localized_app.dart` helper wraps the screen in a `MaterialApp` with the generated delegates pre-installed so existing widget tests do not crash on the `AppLocalizations.of(context)!` bang in production code. Unsupported locales fall back to the first entry in `supportedLocales` (English). (v1.1 / SYS-087 / ADR-031.) | Unit + widget tests (`test/l10n/app_localizations_test.dart`: 11 tests — 5 structural assertions on the ARB catalogs (presence, `@@locale` headers, key-set parity across locales, ICU plural metadata), 4 widget tests (Spanish/English resolution, unsupported-locale fallback to English, ICU plural branches in `es`), 2 class-API tests on `supportedLocales` / delegate types) + 9 existing screen test files updated to route through `localizedApp` (`test/screens/{home,settings,onboarding}_test.dart` + `settings_permissions_test.dart` + `settings_licenses_test.dart` + `settings_test_reminder_test.dart` + `settings_call_screening_tile_test.dart` + `onboarding_permission_wiring_test.dart` + `onboarding_call_screening_step_test.dart` + `test/integration/fresh_install_test.dart`) |
| **SYS-088** | The app shall ship a custom launcher icon and splash branded to the seed color (`#FF6750A4`). The launcher icon is an Android adaptive icon (API 26+) declared at `android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml`, referencing three hand-authored vector drawables: `drawable/ic_launcher_background.xml` (solid brand purple on the 108dp adaptive-icon canvas), `drawable/ic_launcher_foreground.xml` (a white sans-serif lowercase 'd' glyph — stem at x ∈ [23, 29], bowl centered on (54, 54) with outer radius 25 and inner radius 16, evenOdd fill carves the counter — plus a small filled check dot at (80, 80) radius 4), and `drawable/ic_launcher_monochrome.xml` (the same glyph painted pure white; Android 13+ themed icons recolor this layer against the user's wallpaper-derived tint and drop the background). The `AndroidManifest.xml:66` `android:icon="@mipmap/ic_launcher"` reference is unchanged — Android resolves `@mipmap/ic_launcher` to the adaptive-icon XML on API 26+ and to the legacy `mipmap-{mdpi,hdpi,xhdpi,xxhdpi,xxxhdpi}/ic_launcher.png` files on API 21..25. The pre-existing `drawable/ic_streak_notification.xml` resource gap called out in `architecture_options.md:191-192` is closed in the same PR; the file is a monochrome white-on-transparent copy of the foreground glyph with the check dot dropped (the dot is unreadable at 24dp). Both `drawable/launch_background.xml` and `drawable-v21/launch_background.xml` are rewritten as a `<layer-list>` that references a new named color resource `@color/launch_background` (defined in `values/colors.xml` as `#FF6750A4`, the brand purple; AAPT2 rejects inline color values in `drawable-v21/` `<item android:drawable>`) and layers the foreground vector centered on a 96dp × 96dp box. The `?android:colorBackground` reference in the API 21+ variant is dropped (splash stays on-brand in both light and dark mode). No new permissions are introduced. The version bumps from `1.0.0+7` to `1.1.0+8` in `pubspec.yaml` + `lib/build_info.dart`, mirrored by `test/release_signing_test.dart`'s version-pin assertions and pinned by two new tests (`AndroidManifest` icon reference resolves to the adaptive-icon XML on API 26+; `app_name` + `kAppVersion` + pubspec version agree). (v1.1 / SYS-088 / ADR-032.) | Filesystem tests (`test/app_icon_test.dart`: 9 tests covering adaptive-icon manifest presence + three-layer references, foreground white-on-transparent, background brand purple, monochrome pure white (no brand purple), notification icon shape and 24dp size, all five legacy density buckets present, both splash drawables reference `@color/launch_background` + centered foreground, `values/colors.xml` defines `launch_background` as the brand purple) + release_signing_test extensions (2 new tests pinning the manifest icon reference + `app_name` + `kAppVersion` + pubspec version together) |

| **SYS-098** | The app shall render a reminder notification when the platform-side `AlarmManager` fires an alarm. Phase A's `NotificationService` left `show` and `dismiss` as no-ops; v1.2e wires both ends end-to-end. `ReminderBridge` exposes `showNotification({alarmId, habitName, body?, strongMode})` and `cancelNotification(alarmId)`; `PlatformReminderBridge` invokes the matching `doit/reminders` method-channel calls. `PlatformNotificationService.show(event)` forwards `(alarmId, habitName, body, strongMode)` to the bridge and wraps the call in a `kDebugMode`-gated try/catch per ADR-013; `dismiss(id)` forwards `id.value` to `bridge.cancelNotification` and matches the alarmId (NEVER the most-recent notification — prevents a sibling alarm from being accidentally cancelled). The Kotlin side (`MainActivity.buildReminderNotification(ctx, alarmId, habitName, body, strongMode)`) builds the `NotificationCompat.Builder` with channel id `doit.reminders`, title = `habitName`, body = `body ?? "Time for <habitName>"`, "Done" action for soft mode / "Open" action for strong mode, high priority, `autoCancel`, and posts via `NotificationManager.notify(alarmId, …)`. The Kotlin channel registration is idempotent and runs in `MainActivity.configureFlutterEngine` so the first alarm never races the channel-create. `ReminderService.onFireAlarm(alarmId)` is the inbound handler the Kotlin `AlarmReceiver` calls via `ReminderChannelProxy.fireAlarm(ctx, alarmId)` → `channel?.invokeMethod("fireAlarm", {"alarmId": …})`. The handler looks up the scheduled entry via `AlarmScheduler.lookupForFire(alarmId)`, builds a `ReminderEvent`, calls `notifications.show`, then either re-schedules the next habit occurrence (via `habit.nextOccurrence(entry.at)` + `scheduler.schedule`) or archives a one-shot event (via `EventRepository.instance.archive`). Strong-mode habits additionally call `fullScreen.show(habit, chain)` as a best-effort preload (the Kotlin `FullScreenActivity` host itself is hardened in a v1.2 follow-up). `PlatformAlarmScheduler` keeps a richer Dart-side mirror `Map<AlarmId, ScheduledAlarm>` carrying `habitName`, `strongMode`, and `eventId` so the inbound handler does not need a DB round-trip. (v1.2e / Phase 5 / SYS-098.) | Unit tests (`test/services/platform_notification_service_test.dart`: 5 tests pinning `show` forwards body+strongMode, `dismiss` forwards alarmId, show/dismiss paths are independent, broken-bridge exception is swallowed per ADR-013) + (`test/services/platform_alarm_scheduler_test.dart`: +6 tests in `lookupForFire` group covering unknown id / habit entry / strongMode bit / event entry / cancel clears / rescheduleAll clears) + (`test/services/reminder_service_test.dart`: +5 tests in `onFireAlarm` group covering unknown id no-op / habit alarm shows+re-schedules / strong-mode full-screen intent / event alarm archives / phantom-habit shows but does not re-schedule) |

| **SYS-099** | The app shall wire `ActionFullscreen` (a leaf of the sealed `Action` hierarchy, ADR-021) so a routine that fires a "full-screen overlay" actually opens the translucent activity. `FullScreenIntent` (the seam between Dart and the Kotlin `ReminderChannelProxy`) is extended with a second method, `showRoutineOverlay({String? title, String? body})`, alongside the existing `show(Do, MissionChain)` (the habit-driven strong-mode full-screen path, untouched in this PR). `PlatformFullScreenIntent` implements it by invoking a new `doit/full_screen` method-channel call `showRoutineOverlay` with a `{title?, body?}` envelope (both fields optional — present iff non-null; the channel does not accept the Dart `?'key'` null-aware syntax, so the impl builds the map imperatively). Both the platform call and the new channel call go through a `kDebugMode`-gated `_safe(label, fn)` helper per ADR-013 so a missing Kotlin plugin does NOT bubble out of the executor's action-dispatch path (the matching engine still publishes `AutomationFired`; only the side effect is suppressed). The `FakeFullScreenIntent` (test seam) records every overlay as a `RoutineOverlayLaunch` value class (`title?, body?, ==, hashCode`) into a `routineOverlays` list, alongside the existing `missions` list. `RoutineExecutor._dispatchAction` dispatches `ActionFullscreen` by awaiting `ReminderService.instance.fullScreen.showRoutineOverlay()`. The overlay is one-shot — the executor publishes a fire event and the user dismisses the activity; no second invocation. (v1.2f / Phase 6b / SYS-099.) | Unit tests (`test/routines/action_dispatch_test.dart`: +2 tests pinning the overlay call records the right envelope fields and that a missing `ReminderService` is swallowed per ADR-013) + `FakeFullScreenIntent` extension test (already covered by the in-place inversion of the prior no-op test in the same file) |

| **SYS-100** | The app shall wire `ActionCallIntercept` (a leaf of the sealed `Action` hierarchy, ADR-021) so a routine that fires "mute this call" / "auto-decline this call" / etc. actually drives the Kotlin call-screen role. `CallSource` (the seam between Dart and the Kotlin `CallScreeningService`) is extended with `recordRoutineDecision(CallInterceptDecision decision)`. `_MethodChannelCallSource` invokes a new `doit/calls` method-channel call `recordRoutineDecision` with `{decision: decision.name}` (the enum's `name` is the on-wire wire format so the Kotlin side does not need a parallel string-table). `ScriptedCallSource` (test seam) records every decision into a `routineDecisions: List<CallInterceptDecision>` list. `CallInterceptorService.recordRoutineDecision(decision)` is a thin pass-through to the current `CallSource`. `RoutineExecutor._dispatchAction` dispatches `ActionCallIntercept` by awaiting `CallInterceptorService.instance.recordRoutineDecision(action.decision)`. The decision is mutable — subsequent calls overwrite prior decisions (the Kotlin `CallScreeningService.respondToCall` path keeps only the latest non-null response). A `MissingPluginException` is swallowed behind `kDebugMode`'s `debugPrint` per ADR-013 so the executor's fire event still publishes even when the role is not held. (v1.2f / Phase 6c / SYS-100.) | Unit tests (`test/routines/action_dispatch_test.dart`: +2 tests pinning `ActionCallIntercept` records one decision per fire, and a no-op variant for each of the four `CallInterceptDecision` values: `accept`, `mute`, `decline`, `silent`) + (`test/routines/call_dispatch_test.dart`: 1 existing test updated to assert `source.routineDecisions.length == 1` instead of asserting the no-op) |

| **SYS-101** | The app shall let the user pause cadence reminders for a single `ContactPerson` until a chosen date. `Person` (the abstract base in `lib/people/person.dart`) gains a `pausedUntil: DateTime?` field; `ContactPerson.copyWith({pausedUntil, clearPausedUntil})` round-trips it (the `clear*` flag is the same shape as `Do.copyWith(clearPausedUntil: …)` — necessary because the constructor parameter is nullable and we cannot distinguish "set to null" from "leave unchanged"). The Drift schema (`person` table) gains a `paused_until_millis INTEGER` column with a `v5_to_v6` migration that backfills existing rows to `NULL`. `PersonRepository.save` writes the field as `pausedUntil?.millisecondsSinceEpoch`; `PersonRepository.getById` reads it back via `DateTime.fromMillisecondsSinceEpoch`. `Person.isPausedAt(DateTime now)` returns `pausedUntil != null && pausedUntil.isAfter(now)`. The Add Person screen (`lib/screens/add_person.dart`) gains a Pause section visible ONLY after a contact has been picked (the section's visibility gates on `_pickedName != null` so the "Pause until X" affordance cannot appear for a save with no contact); the section has a `_PersonPauseRow` widget with a `showDatePicker` launcher, a "Clear" button (sets `pausedUntil = null`), and a read-only "Paused until <formatted date>" label. The Person Groups screen (`lib/screens/person_groups.dart`) renders a `_PersonPausedChip` (a `Chip(label: Text('Paused'))`) as the `secondary` slot of the per-person `CheckboxListTile` whenever that person is currently paused — a quick visual signal in the multi-select group picker. (v1.2f / Phase 6d / SYS-101.) | Repository tests (`test/services/person_repository_test.dart`: +2 tests — `pausedUntil` round-trips as `millisecondsSinceEpoch` and `clearPausedUntil: true` via `copyWith` zeros the field while preserving every other field) + Widget test (`test/screens/add_person_test.dart`: +1 test asserting the Pause section is hidden until a contact is picked — a quick smoke test; the full picker UX is covered by the existing pick-contact-then-save test) |

| **SYS-102** | The home-screen tile subtitle for a `DoFixed` habit shall include the weekday set, not just the time. The prior one-line string was `"Fixed — HH:MM"`; the new string is `"<weekday-set> · HH:MM"` where `<weekday-set>` is: `"Every day"` (set `{1..7}`), `"Weekdays"` (`{1..5}`), `"Weekends"` (`{6,7}`), a single abbreviation for singleton sets (`"Mon"`, `"Tue"`, ..., `"Sun"`), or a comma-separated ascending-order list of abbreviations for arbitrary subsets (`"Mon, Wed, Fri"`). The new label is produced by a pure top-level function `String describeDo(Do h)` in a new file `lib/do/do_description.dart` (no Flutter imports — fits the model-purity rule from `.claude/rules/lib-do.md`, so it is unit-testable without a harness). The order of weekday abbreviations is fixed Mon..Sun regardless of the order in the caller's set literal — the function sorts the set ascending before rendering. The home screen's `_HabitTile` widget calls `describeDo(habit)` instead of the prior private `_describe(Do)` method (the latter is removed; its body is inlined into `describeDo`). Other schedule kinds (`DoInterval`, `DoAnchor`, `DoDayOfX`, `DoTimeWindow`) keep their prior one-line labels. Out-of-range weekday codes (anything outside `1..7`) fall through to `"?"` — defensive only; `DoFixed.validate()` rejects them on save so this branch should never fire for a persisted row. (v1.2f / Phase 6e / SYS-102.) | Unit tests (`test/do/do_description_test.dart`: +10 tests covering every-day / weekdays / weekends / single weekday (Mon, Sun, Fri, Sat boundary cases) / arbitrary subset with unsorted-input ordering / every non-`DoFixed` branch kept the prior label) + Widget smoke test (`test/screens/home_test.dart`: +1 test pumping the home screen with a `{1, 3, 5}` `DoFixed` and asserting the tile subtitle renders `"Mon, Wed, Fri · 09:00"`) |

| **SYS-103** | Tapping a `degraded` or `unknown` `AutomationReliabilityBadge` in the three add screens (`add_habit` / `add_person` / `add_event`) shall open an `AlertDialog` that disambiguates the three remediation paths a routine can need: (a) trigger-side permission denied / unknown — the dialog shows the matching `PermissionKind` title + current status (Granted / Denied / Permanently denied / Not yet probed) + the rationale copy from the canonical `permissionKindMeta` module + an "Open settings" CTA that calls `PermissionService.openAppSettings()` (or `requestUsageStats()` for the special-access kind, since `usageStats` has no generic app-settings page); (b) no permission gate (today only `TriggerTimeOfDay`, where reliability lives in the app-wide `Reliability` enum) — the dialog surfaces a "this trigger does not need a runtime permission" note and hides the Open settings CTA; (c) action-side permission (v1.2+ — `ActionOverrideSilent` needs `ACCESS_NOTIFICATION_POLICY`, contact-requiring actions need `READ_CONTACTS`) is reserved for a future PR. The dialog is built by `showAutomationReliabilityDialog(BuildContext, {required Automation})` (`lib/widgets/automation_reliability_dialog.dart`); it is a pure `StatelessWidget` (no `setState`, no `Future`-side-effects on the rendering path) wrapped in `Semantics(button: true, label: 'Open settings', excludeSemantics: true)` so the SYS-062 a11y scanner and TalkBack both find the FilledButton. The shared rationale module `permissionKindMeta` (`lib/services/permission_kind_meta.dart`, promoted from the prior private `_meta` constant in `permission_sheet.dart`) is the single source of truth for the per-`PermissionKind` title + icon + rationale copy, so `PermissionSheet` and this dialog never drift. (v1.2h / Phase 8 / SYS-103.) | Widget tests (`test/widgets/automation_reliability_dialog_test.dart`: 4 tests — location trigger renders Title + Status: Denied + rationale + Open settings CTA; calendar trigger renders Calendar title + rationale; usage-stats trigger renders Usage access title + Open settings CTA wired to `requestUsageStats`; time-of-day trigger renders the no-permission-gate copy and hides the Open settings CTA) + a11y test (`test/a11y/semantics_labels_test.dart`: SYS-062 scanner now passes for `lib/widgets/automation_reliability_dialog.dart` after the Semantics wrapper) |

| **SYS-104** | The app shall re-probe every runtime permission when the user returns from system Settings (`AppLifecycleState.resumed`). The prior fire-and-forget probe from `PermissionService.init()` (ADR-030) is stale by the time the user toggles a permission in Settings → Special access → Usage access — the most common flow that needs a re-probe — so the Settings → Permissions tile and the per-automation reliability badges (SYS-085 / SYS-103) would show a stale status until the next cold launch. v1.2i wires a `WidgetsBindingObserver` whose `didChangeAppLifecycleState(resumed)` calls `PermissionService.refresh()` via `unawaited(_safeRefresh())`. The observer tracks a `_coldStartSeen` bool so the FIRST `resumed` event (the OS bringing the app to the foreground after a cold launch — `init()` has already probed) is a no-op; every subsequent `resumed` (the user returning from Settings) fires the re-probe. Non-resumed lifecycle events (`paused`, `inactive`, `detached`) are ignored. `PermissionService.refresh()` re-probes every `permission_handler` kind in parallel via `Future.wait` over six `_safeProbe` futures, then sequentially re-probes `usageStats` and `callScreening`. Each `_safeProbe` wraps `Permission.X.status` in try/catch — a single probe failure keeps the prior value (a failed re-probe is NOT a downgrade) and the batch continues (ADR-013 follow-up). (v1.2i / Phase 9 / SYS-104.) | Unit tests (`test/services/permission_lifecycle_observer_test.dart`: 3 tests — cold-start `resumed` is a no-op; second `resumed` fires the `statuses` notifier; non-resumed events are ignored) + (`test/services/permission_service_test.dart`: +4 tests — `refresh()` re-probes all six `permission_handler` kinds; `refresh()` merges `granted` into every kind; `refresh()` swallows a single probe failure without aborting the batch; `refresh()` re-probes `usageStats` and `callScreening` separately) |

| **SYS-105** | The home screen shall surface a one-shot `DstTransitionBanner` (`lib/widgets/dst_transition_banner.dart`) whenever the schedule engine silently reschedules one or more habit times because of a DST clock change. The banner receives a `List<DstAdjustment>` (one entry per silently-shifted habit — habitId, oldLocalTime, newLocalTime, direction ∈ {forward, backward}); it renders `SizedBox.shrink()` when the list is empty (zero layout cost in the steady state, so it can sit unconditionally under `ReliabilityBanner.fromService()` without a render-tree check on every build). Singular copy for one drop ("Your 06:30 do has moved to 07:30"), plural copy for two+ ("3 do have shifted after the clock change — tap to review"). The banner exposes a single dismiss `IconButton` keyed by a banner-level identifier (not by habitId — DST shifts affect multiple habits at once and the user dismisses the banner as a unit, not per-habit). Tapping the banner body opens a `DstAdjustmentDetailScreen` (out of scope for v1.2j — the screen itself is a v1.2 follow-up; v1.2j ships the banner + its test coverage only). (v1.2j / Phase 10 / SYS-105.) | Widget tests (`test/widgets/dst_transition_banner_test.dart`: 5 tests — empty list renders `SizedBox.shrink()`; one entry shows singular copy; two+ entries show plural copy; forward vs backward shifts render the right arrow glyph; dismiss IconButton fires the onDismissed callback once per tap) |

| **SYS-106** | The home screen shall surface a one-shot `StreakRecoveryCard` (`lib/widgets/streak_recovery_card.dart`) for each habit whose consecutive-run counter reports 3+ missed days, and shall hide the card automatically once the user marks the habit done. The card receives `(habitId, missedDays, onResume, onDismiss)`; it renders nothing when `missedDays < 3` (zero layout cost, defensive against a future caller that passes a stale snapshot). The primary `FilledButton` ("I'm back") calls `onResume(habitId)` which the home-screen wrapper binds to `HabitRepository.markDoneToday(habitId)`; the secondary `IconButton` (close) calls `onDismiss(habitId)` which the wrapper binds to a `_dismissedRecoveryCards: Set<String>` state so the user can shelve a habit's card without going through Settings. The card is keyed by `habitId` so multiple cards can coexist on the home screen (one per broken-streak habit), each dismissed independently. (v1.2j / Phase 10 / SYS-106.) | Widget tests (`test/widgets/streak_recovery_card_test.dart`: 2 tests — `< 3 missed days` renders nothing; primary button fires `onResume(habitId)` and close IconButton fires `onDismiss(habitId)` exactly once per tap) |

| **SYS-107** | `ReminderService` shall schedule pre-notification heads-up reminders at 5-min and 1-min before each habit fire time, and shall cancel them when the underlying habit is cancelled or rescheduled. `ReminderService.schedulePreAlarms({alarmId, fireAt, now})` enqueues a 5-min heads-up when the lead time `fireAt.difference(now) > 5 * 60 s` and a 1-min heads-up when the lead time `> 60 s`; lead times at or below those thresholds are silently skipped (no spurious pre-alarm noise for fires inside the next minute). The caller passes `now` explicitly so the method is unit-testable without a `DateTime.now()` stub. `ReminderService.cancelPreAlarms(alarmId)` forwards to `bridge.cancelPreAlarms(alarmId)` so a cancelled habit leaves no dangling pre-alarms. The `ReminderBridge` interface gains `schedulePreAlarm({alarmId, leadTimeSeconds})` and `cancelPreAlarms(alarmId)` abstract methods; both production (`PlatformReminderBridge`) and test (`FakeReminderBridge`) implement them. A `MissingPluginException` from either new method is swallowed behind `kDebugMode`'s `debugPrint` per ADR-013 — the underlying alarm still fires on time. (v1.2j / Phase 10 / SYS-107.) | Unit tests (`test/reminders/reminder_service_test.dart`: +5 tests pinning `schedulePreAlarms` enqueues both 5-min and 1-min heads-ups when the lead time is `> 5 * 60 s`; `schedulePreAlarms` enqueues only the 1-min heads-up when `5 * 60 s >= lead time > 60 s`; `schedulePreAlarms` is a no-op when `lead time <= 60 s`; `cancelPreAlarms(alarmId)` forwards to `bridge.cancelPreAlarms`; a `MissingPluginException` from either method is swallowed per ADR-013 — uses a `_ThrowingPreAlarmBridge` test fake) |

| **SYS-108** | The app shall offer a hard-delete affordance on the edit screen (`AddHabitScreen` in edit mode) with a confirm dialog that names the do and warns about completion-log loss. The popup menu in the app bar (currently `Save as template…`) gains a `Delete…` entry ONLY when `widget.habitId != null` (edit mode); new-do mode has no menu entry at all. Tapping the entry opens an `AlertDialog` titled `Delete "<name>"?` with destructive copy `"This will remove the do and its completion log. This cannot be undone."` and two actions: `Cancel` (no-op pop) and `Delete` (pop `true`). After the dialog pops with `true`, the screen calls `DoRepository.instance.deleteById(habitId)` and pops the route with `true` so the caller (`HomeScreen._onTileTap`) can refresh the home list immediately — the deleted tile disappears without waiting for the next `AppLifecycleState.resumed`. A `deleteById` failure (e.g., a Drift error) is caught with `on Object` and surfaced as a `Delete failed. Please try again.` snackbar; the screen stays mounted so the user can retry. Test seams: a public typedef `AddHabitScreenState = _AddHabitScreenState` exposes the state class to widget tests; an `@visibleForTesting Future<void> Function(String id)? deleteOverride` on the state lets tests inject a throwing delete to exercise the failure branch without monkey-patching the repository singleton. (v1.2k / Phase 11a / SYS-108.) | Widget tests (`test/screens/add_habit_delete_test.dart`: 6 tests — new-do mode has no menu entry; edit mode shows `Delete…`; tapping opens the confirm dialog with the habit name in the title and `This cannot be undone.` in the body; Cancel keeps the screen and the row intact; Delete calls `DoRepository.deleteById` and pops the route; a `deleteById` failure shows the error snackbar and does NOT pop) |

| **SYS-109** | The Math and Type missions shall share a single wrong-attempt bookkeeping module (`MissionWrongAttempts` in `lib/missions/mission_attempts.dart`, pure Dart, no Flutter) so the user never sees a behavior gap between mission kinds. The constant `kMissionMaxWrongAttempts = 3` is the single source of truth; per-instance overrides via the `maxWrong` constructor argument are allowed for tests. After the user has committed a wrong attempt the screen reads `errorLabel()` — for the first (n-1) attempts the copy is `Wrong. N attempt(s) left.` (where N is the remaining count), and on the n-th attempt the copy is the take-a-break nudge `Take a break. The mission will end.` per SYS-011. `recordWrong()` returns `true` on the n-th wrong so the screen can pop with `null` (auto-fail) — there is no fourth attempt. Shake, Hold, and Memory missions do NOT use this module; they time-out instead of failing per-attempt. (v1.2l / Phase 11b / SYS-109.) | Unit tests (`test/missions/mission_attempts_test.dart`: 9 tests — constant is 3, fresh state has 3 remaining, first wrong leaves 2 remaining and does not auto-fail, third wrong auto-fails with the take-a-break label, `errorLabel()` returns null before any wrong, `errorLabel()` returns the attempts-left copy after each non-final wrong, `errorLabel()` returns the take-a-break nudge on the final wrong, `maxWrong` is honored per-instance, `missionTakeBreakNudge` constant is non-empty and ends with a period) + Widget tests (`test/screens/mission_math_test.dart`: 3 existing tests unchanged but now exercise the shared module; `test/screens/mission_type_test.dart`: 2 new tests — first two wrong answers decrement the attempts-left label, three wrong answers auto-fail the mission) |

| **SYS-110** | The edit-habit screen (`AddHabitScreen` in edit mode) shall render a `CompletionLogSection` that lists the most-recent `kCompletionLogSectionMaxRows = 30` completions for the habit, newest-first, sorted by `completedAtMillis` descending. Each row shows the local-day (`YYYY-MM-DD`), the local-time (`HH:MM`), and the `source` (manual / notification / mission / rest_day). A trailing delete icon opens an `AlertDialog` titled `Delete this completion?` with the row description (`"YYYY-MM-DD at HH:MM (source). This will shorten your streak by one day."`) and two actions: `Cancel` (no-op pop) and `Delete` (pop `true`, themed with the error color). A confirmed delete calls `CompletionLogService.instance.deleteById(row.id)`; on success, a `"Completion removed."` snackbar is shown and the list rebuilds via `_refresh()` (a `setState` that re-loads the future — the `FutureBuilder` is the source of truth for the rows, so a delete never desyncs the list). A `deleteById` failure is caught with `on Object` and surfaced as `"Could not delete entry."` — no row is removed. The section is rendered only in the edit branch (`if (_isEdit)`), so a new-do screen never shows a completion log. Older rows beyond the 30-cap surface the text `"N older entries are hidden."` below the list. (v1.2m / Phase 11c / SYS-110.) | Widget tests (`test/widgets/completion_log_section_test.dart`: 5 tests — empty state shows `"No completions yet."` and the `completion_log.section` key; three seeded rows in non-chronological order render three delete keys in newest-first order (positions compared via `getTopLeft`); tapping a delete icon opens the confirm dialog with the `Delete this completion?` title; Cancel leaves the row in the DB; Confirm calls `deleteById`, removes the row, and shows the `"Completion removed."` snackbar) |

| **SYS-111** | The stats screen (`lib/screens/stats.dart`) shall render three new per-habit lines — a 30-day completion count + month-over-month (MoM) delta, a 30-day completion rate, and a 7-day bar chart — and the app shall expose a `Do.effectiveStreakConfig(...)` factory method on the sealed `Do` base class so the per-do `graceWindowOverride` field (Phase 11f) flows through end-to-end. Concretely: (a) `_HabitStats` gains four new fields — `int completionsLast30Days` (distinct local-day count in the last 30 inclusive of `asOfDay`), `int completionsPrev30Days` (distinct local-day count in the 30 days immediately before that), `int completionRate30` (integer percent 0..100 = `completionsLast30Days * 100 ~/ 30`, clamped), and `List<int> completionsLast7Days` (length 7, day 0 = today, count of completions per local-calendar day, NOT de-duped). The 30-day window and prior-30-day window are computed by a private `_last30Window(DateTime asOf)` helper that returns a closed `[start, end]` window + a `prevStart..prevEnd` window, both clipped at `asOf`. The MoM delta is rendered as `+N%` / `-N%` / `0%` when `prev30 > 0` (signed integer percent of `(last30 - prev30) * 100 ~/ prev30`), `"new"` when `prev30 == 0 && last30 > 0`, and `"0%"` when both are zero. (b) `Do.effectiveStreakConfig({required SkipBudget skipBudget})` is added to the sealed base class; it returns `StreakConfig(graceWindow: graceWindowOverride ?? kDefaultGraceWindow, skipBudget: skipBudget)`. The factory is the canonical way for a `StreakConfig` consumer to honor per-do grace overrides — previously the field was inert because every consumer hard-coded the default. (c) `kDefaultGraceWindow` is a new top-level `const Duration kDefaultGraceWindow = Duration(hours: 3)` in `lib/do/consecutive_counter.dart` (replaces three hard-coded `Duration(hours: 3)` literals). (d) The stats screen's streak calculation now routes through the new factory — `h.effectiveStreakConfig(skipBudget: SkipBudget(doId: h.id, monthlyLimit: h.restDaysPerMonth))` instead of the prior hard-coded literal. (e) The stats screen's `_load()` captures a single frozen `asOf = DateTime.now()` at the top so the streak's "is the run still alive at asOf?" check, the 30-day window end, and the 7-day bucket labels all stay consistent across the per-habit loop. (f) The `_Last7DaysChart` widget renders 7 thin vertical `Container` bars inside a `Row`, each wrapped in `Semantics(label: '<N> completions, <when>')` so TalkBack reads the value, and the tallest bar is colored `colorScheme.primary` while others use `surfaceContainerHighest`. The whole chart is keyed `ValueKey('stats.last7')`. (g) The new lines are each individually keyed (`'stats.last30'`, `'stats.rate30'`) so widget tests can pin them. (h) Settings (`lib/screens/settings.dart`) gains a new `_StatsTile` between the Reliability section and the Backup section — a `ListTile` with `Icons.bar_chart_outlined`, label `"Stats"` and subtitle `"Streaks, completion rate, 7-day chart."` that pushes `StatsScreen` via `MaterialPageRoute`. Three new l10n keys (`settingsSectionStats`, `settingsStatsTitle`, `settingsStatsSubtitle`) are added to both `app_en.arb` and `app_es.arb`. The existing home-AppBar `Icons.bar_chart` button stays (quick path; Settings is the discoverable path). (i) `RoutineBanner` (`lib/widgets/routine_banner.dart`) auto-clear behavior is pinned by a new widget test file `test/widgets/routine_banner_clear_test.dart` (3 tests) — empty queue renders `SizedBox.shrink()`, non-empty queue renders `"Opening …"` for exactly one frame then drains via the existing `addPostFrameCallback`, and an out-of-band `clearPendingOpenApp()` collapses the banner back to zero size. (v1.3a / Phase 12 / SYS-111.) | Unit tests (`test/habits/habit_model_test.dart`: +10 tests in two new groups — `Do.graceWindowOverride` pins the field default / copyWith set / copyWith clear / copyWith preserve / `Duration.zero` honored; `Do.effectiveStreakConfig` pins default / override honored / zero override / `skipBudget` forwarded verbatim / works on every `Do` subclass) + Widget tests (`test/screens/stats_test.dart`: +6 tests — completion rate renders as `"On time: N%"`, last-7-day chart renders 7 bars (7 `Semantics` nodes), MoM delta renders `"0%"` when both windows are zero, MoM delta renders `"new"` when `prev30 == 0 && last30 > 0`, per-do `graceWindowOverride` flows through the factory end-to-end without throwing) + Widget tests (`test/widgets/routine_banner_clear_test.dart`: 3 tests — empty queue renders zero-size box; non-empty queue shows `"Opening …"` for one frame then drains; out-of-band `clearPendingOpenApp()` collapses the banner on next build) |
| **SYS-112** | The app shall expose a single `ReliabilityService.instance` singleton that is the unified source-of-truth for the app-wide `Reliability` enum. The service is a `Stream<Reliability>` (broadcast, `distinct()`) plus a `ValueListenable<Reliability>` mirror, both backed by the same internal `ValueNotifier<Reliability>`. The service owns: (a) the `bridge.probeReliability()` round-trip (re-run on `init`, on `refresh()`, and on a 30 s `Timer.periodic` fallback); (b) the `PermissionService.statuses` listener (re-derives the value on every change to a `PermissionKind` in the gated set `{location, calendar, callScreening, usageStats}`); (c) the `kReliabilityCacheTtl = Duration(seconds: 30)` constant (moved from `PlatformAlarmScheduler`). The combine rule: if the bridge probe returned `Reliability.degraded`, the value is `degraded`; else if any gated kind is `PermissionResultDenied` / `PermissionResultPermanentlyDenied`, the value is `degraded`; else the value is `optimal`. The initial value is `Reliability.optimal` (NOT `unknown`) — closes the v1.3a first-read race where the very first read of `PlatformAlarmScheduler.reliability` returned `unknown` for a fully optimal device. The home-screen `ReliabilityBanner` and the settings page `_ReliabilityRow` BOTH bind to `ReliabilityService.instance.notifier` via `ValueListenableBuilder<Reliability>` so a state change is visible on the next frame without a relaunch. The `PlatformAlarmScheduler.reliability` getter is a thin pass-through to `ReliabilityService.instance.value` (with a `try / on StateError` fallback to `Reliability.optimal` for standalone-scheduler unit tests) so existing widget tests that pass a `Reliability` directly to the banner keep working. `ReliabilityBanner.fromStream({VoidCallback? onTap})` is the new recommended factory; the previous `ReliabilityBanner.fromService` is kept as a `@Deprecated` thin shim for one cycle. `PermissionLifecycleReProbe` (`AppLifecycleState.resumed` after the cold start) calls `ReliabilityService.instance.refresh()` in addition to `PermissionService.refresh()`. `main.dart` wires the service via `await ReliabilityService.init(bridge: bridge, permissionService: PermissionService.instance)` AFTER `PermissionService.init()` and AFTER the bridge is constructed. `AutomationReliability` (per-automation reliability for triggers) stays separate (per ADR-029) — the badge + dialog continue to consume `PermissionService.statuses` directly. (v1.3b / Phase 13 / SYS-112.) | Unit tests (`test/services/reliability_service_test.dart`: ~10 tests — initial value is `optimal` (no first-read race); `refresh()` re-probes `bridge.probeReliability()`; subscribing to `statuses` re-derives the value when `location` / `calendar` / `callScreening` / `usageStats` flips to `Denied` / `PermanentlyDenied`; subscribing to `statuses` does NOT re-derive when an unrelated kind flips; the 30 s timer fires `refresh()` (via an injectable periodic factory); the stream emits `distinct()` (no duplicate values); `resetForTesting()` clears the singleton + closes the stream controller; `init()` is idempotent; a probe failure keeps the prior value (per ADR-013); the 4 gated kinds are the only ones that flip the service to `degraded`) + Unit tests (`test/services/platform_alarm_scheduler_test.dart`: reliability group rewritten — `reliability` getter delegates to `ReliabilityService.instance.value`; getter returns `optimal` when the service is not init'd; getter reflects a permissions change to a gated kind) + Widget tests (`test/widgets/reliability_banner_test.dart`: 2 new tests — `fromStream` renders nothing when the service is optimal; `fromStream` rebuilds when the service value flips to degraded) + Widget test seeding updated in `test/screens/home_test.dart` + `test/screens/settings_test.dart` so the unified service is init'd in `setUp` |
| **SYS-113** | The app shall surface the Android 14+ `USE_FULL_SCREEN_INTENT` special-access permission as a first-class gated reliability signal. Specifically: (a) `PermissionKind.fullScreenIntent` is a new enum entry on `lib/services/permission_service.dart` (after `callScreening`, before `backupFolder`); the permission is **opt-in** — declining does NOT block any feature (the user keeps getting the notification fallback), mirroring the v1.1g `PACKAGE_USAGE_STATS` precedent. (b) The manifest declares `<uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" tools:ignore="ProtectedPermissions" />` so the user can toggle it on in Settings. (c) A new `FullScreenIntentService` singleton (`lib/services/full_screen_intent_service.dart`) is the Dart-side wrapper around the `doit/full_screen` MethodChannel; it exposes `isGranted() → Future<bool>` and `openSettings() → Future<bool>` mirroring the `UsageStatsService` shape. (d) The Kotlin side (`android/app/src/main/kotlin/com/doit/FullScreenIntentChannel.kt`) handles `canUseFullScreenIntent` + `openFullScreenIntentSettings` over the same channel and resolves the API 32 / 33 / 34 asymmetry on the platform side: API < 32 returns `true` for `canUseFullScreenIntent` (implicit-grant); API 32 / 33 probes `NotificationManager.canUseFullScreenIntent()` and falls back to `Settings.ACTION_APPLICATION_SETTINGS` for the deep-link (no FSI-specific Settings activity exists pre-34); API 34+ uses `Settings.ACTION_MANAGE_APP_USE_FULL_SCREEN_INTENT`. (e) `PermissionService.requestFullScreenIntent()` deep-links via `FullScreenIntentService.openSettings()` and `refreshFullScreenIntent()` re-probes the channel probe and merges the result into `statuses` — both mirror the `requestUsageStats` / `refreshUsageStats` shape. The probe runs as a sequential special-access probe in `PermissionService.refresh()` (after the parallel `Future.wait` batch) and as a fire-and-forget call from `PermissionService.init()`. (f) `PermissionKind.fullScreenIntent` joins the `_kReliabilityGatedKinds` set in `lib/services/reliability_service.dart` (now 5 elements: `location`, `calendar`, `callScreening`, `usageStats`, `fullScreenIntent`). A denial on the new kind flips the unified reliability stream to `Reliability.degraded` and the home-screen banner shows the "may be late" copy. (g) The Settings → Permissions screen (`lib/screens/settings.dart`) gains a 5th `_PermissionTile` for `fullScreenIntent` (icon `Icons.open_in_full`, ARB title `permissionFullScreenIntentTitle`); tapping the row calls `PermissionService.refreshFullScreenIntent()`. The home-screen `ReliabilityBanner` gains an `onTap` callback that pushes `SettingsScreen` so the user is one tap from the tile. (h) `PermissionKindMeta` (`lib/services/permission_kind_meta.dart`) gains the matching `title` + `icon` + `rationale` entry. (i) The Kotlin `FullScreenIntentChannel` is attached in `MainActivity.configureFlutterEngine` and detached in `onDestroy`. The activity launch path (`PlatformFullScreenIntent.showHabitMission` / `showRoutineOverlay`) remains stubbed for Phase 6a proper — the new probe channel is shaped so Phase 6a can extend it without re-doing this wiring. (v1.3c / Phase 14 / SYS-113.) | Unit tests (`test/services/full_screen_intent_service_test.dart`: 8 tests — `isGranted` happy path returns the channel value; `isGranted` swallows `MissingPluginException` (ADR-013); `isGranted` swallows `PlatformException`; `openSettings` returns the channel value; `openSettings` swallows `MissingPluginException`; `openSettings` swallows `PlatformException`; `resetForTesting` clears the singleton; `debugInstance` returns the same instance back-to-back) + Unit tests (`test/services/permission_service_test.dart`: 3 new tests — `requestFullScreenIntent` deep-links via `FullScreenIntentService.openSettings()`; `refreshFullScreenIntent` merges `isGranted=true` into `statuses[fullScreenIntent]` as `PermissionResultGranted`; `refreshFullScreenIntent` merges `isGranted=false` into `statuses[fullScreenIntent]` as `PermissionResultDenied(canOpenSettings: true)`; the existing tearDown clears the `doit/full_screen` mock) + Unit tests (`test/services/reliability_service_test.dart`: 1 new test — flipping `fullScreenIntent` to `Denied` re-derives the unified reliability stream to `degraded`; the existing "the 4 gated kinds" test is renamed to "the 5 gated kinds" and the `gated` const grows from 4 to 5) + Widget tests (`test/screens/settings_permissions_test.dart`: 2 new tests — tapping the `fullScreenIntent` tile re-probes via `doit/full_screen` (the `canUseFullScreenIntent` channel call is asserted); the tile renders the localized `permissionFullScreenIntentTitle`; the existing "renders all four" test is renamed to "renders all five" and asserts the new tile's `ValueKey`) + Widget tests (`test/widgets/reliability_banner_test.dart`: 1 new test — `fromStream` wires `onTap` and the callback fires on tap when the service is degraded) + ARB parity (`test/l10n/app_localizations_test.dart` "every non-template ARB has the same key set as the template" test catches missing `app_es.arb` entries automatically) + Coverage: ≥80% on every changed file (`full_screen_intent_service.dart`, `permission_service.dart` lines 200-440 region, `reliability_service.dart` gated-set block, `permission_kind_meta.dart`, `settings.dart` `_PermissionsRow` + `_reProbe`, `reliability_banner.dart` `onTap` plumbing) |
| **SYS-114** | The app shall close the v1.3c-deferred activity launch path so that a strong-mode habit reminder fires `FullScreenActivity` end-to-end and a routine-fired overlay surfaces `RoutineOverlayScreen`. Specifically: (a) A real `FullScreenActivity` Kotlin class (`android/app/src/main/kotlin/com/doit/FullScreenActivity.kt`) extends `FlutterActivity` and sets the lockscreen-bypass window flags in `onCreate`: `WindowManager.LayoutParams.FLAG_SHOW_WHEN_LOCKED | FLAG_TURN_SCREEN_ON | FLAG_DISMISS_KEYGUARD | FLAG_KEEP_SCREEN_ON` (API 27+; on API < 27 the activity still surfaces but without keyguard dismissal). The activity's `getInitialRoute()` encodes the intent extras into a query string: `/mission?mode=habit&habitId=<id>` for strong-mode habit launches, `/mission?mode=overlay&title=<t>&body=<b>` for routine-fired overlays. The activity is theme `@style/LaunchTheme` (same as `MainActivity`) and the manifest declares it with `exported="false"`, `taskAffinity=""`, `excludeFromRecents="true"`, `launchMode="singleTask"`, `showOnLockScreen="true"`, `turnScreenOn="true"`, `showWhenLocked="true"`. (b) `FullScreenIntentChannel.kt` grows two new `when` arms: `showHabitMission(ctx, args)` builds `FullScreenActivity.habitIntent(ctx, habitId)` and starts it with `FLAG_ACTIVITY_NEW_TASK`; `showRoutineOverlay(ctx, args)` builds the overlay intent with optional `title` / `body` extras. Both return `true` on a resolved launch. The existing probe handlers are unchanged. (c) `MainActivity.buildReminderNotification` splits the strong-mode branch: the strong-mode `openIntent` targets `FullScreenActivity` (with `habitId` extra) and `NotificationCompat.Builder.setFullScreenIntent(openPi, /* highPriority= */ true)` is added so the OS launches the activity directly on a locked device; the strong-mode `Open` action button reuses the same `fsiPi`. Soft-mode notifications keep the existing `MainActivity` openPi (no FSI). (d) A new `lib/screens/mission_launcher.dart` chain-level orchestrator widget loads the habit by id via `DoRepository.instance.getById(habitId)`; rejects (and pops with `null`) when the habit is missing, not in `StrongProof` mode, or carries an empty chain. The widget iterates the `MissionChain`, pushing the matching `MissionXxxScreen` for each `Mission` and `await`ing the pop value; a `null` pop (cancel / timeout) aborts the chain. Once all inputs are collected, `MissionChainExecutor.run(chain, inputs)` is invoked; on `ChainPassed`, the completion is appended via `CompletionLogService.instance.append` with `source: CompletionSource.mission`, `proofModeAtTime: <strong|soft|auto>`, and `missionResultsJson: "missions=<N>"`; on `ChainFailedAt` / `ChainTimedOut`, the widget pops with `null` (the streak stays broken per the v1.1f grace-window contract). (e) A new `lib/screens/routine_overlay_screen.dart` banner widget renders the `title` / `body` from the launch intent; null / empty values fall back to `"Routine alert"` + a generic explanation. The Dismiss button is ≥ 64 dp and wrapped in `Semantics(button: true, label: "Dismiss routine overlay", ...)` per `.claude/rules/lib-screens.md` (SYS-062). (f) `lib/main.dart`'s `MaterialApp` gains `onGenerateRoute` that resolves `/mission` to `MissionLauncherScreen` (default) or `RoutineOverlayScreen` (when `mode=overlay`) based on the initial-route query string parsed by `FullScreenActivity.getInitialRoute()`. The existing `home:` switch is unchanged (`onGenerateRoute` is additive). (g) `lib/services/platform_full_screen_intent.dart` gains `Future<LaunchIntent?> getLaunchIntent()` which reads `getLaunchIntent` over the `doit/full_screen` channel; the `_safeResult` wrapper swallows the production `MissingPluginException` and returns `null` (the canonical read is the initial-route query string). `LaunchIntent` + `LaunchMode` (a new immutable class + enum) live in `lib/reminders/full_screen_intent.dart`; the `FullScreenIntent` interface grows the read; `FakeFullScreenIntent` records the scripted / last launch intent. (h) No `wakelock_plus` package added — pubspec stays clean; the wake-lock is held at the Android Window level via `FLAG_KEEP_SCREEN_ON` on the activity (released automatically when the activity is destroyed). (i) No new `<uses-permission>` — the `USE_FULL_SCREEN_INTENT` baseline from v1.3c (SYS-113) covers the launch path. (v1.3d / Phase 15 / SYS-114.) | Unit tests (`test/services/platform_full_screen_intent_test.dart`: 6 tests — `showHabitMission` invokes the channel with the right `habitId` arg; `showRoutineOverlay` propagates `title` / `body` and skips missing keys; `getLaunchIntent` swallows the production `MissingPluginException` and returns `null`; `showHabitMission` swallows `MissingPluginException` end-to-end; `getLaunchIntent` returns null on a partial args map; `showRoutineOverlay` accepts title-only / body-only / neither) + Widget tests (`test/screens/mission_launcher_test.dart`: 6 tests — 1-mission chain (Type) appends the completion with `proofModeAtTime: "strong"` and `missionResultsJson: "missions=1"`, pops `true`; 2-mission chain (Type → Type) appends with `missions=2`; `ChainFailedAt` (wrong phrase) pops `null` and does NOT append; missing habit pops `null`; non-`StrongProof` habit (soft) pops `null`; cancel on first mission aborts the chain and does NOT append) + Widget tests (`test/screens/routine_overlay_test.dart`: 4 tests — renders `title` + `body` from constructor args; falls back to generic copy when null; falls back when `title` / `body` are empty strings; the Dismiss button pops the route with `null`) + A11y (`test/a11y/semantics_labels_test.dart` SYS-062 static analyzer — the Dismiss button's `Semantics` label `"Dismiss routine overlay"` is auto-validated; 0 regressions across the existing 35 widgets) + The existing `test/reminders/reminder_service_test.dart` + `test/routines/action_dispatch_test.dart` (which drive `FakeFullScreenIntent`) keep passing — the `launches` / `routineOverlays` recording is unchanged in shape (the new `launchIntents` list is additive) + Coverage: ≥80% on every changed file (`platform_full_screen_intent.dart`, `full_screen_intent.dart`, `mission_launcher.dart`, `routine_overlay_screen.dart`, `main.dart` `onGenerateRoute`) |

| **SYS-115** | The app shall surface a single Android home-screen widget (`com.doit.DoitWidgetProvider`) that renders the user’s first-active do, current streak number, a "Done" button, and the unified `Reliability` badge — closes the v1.2g-deferred `feature.md` §2.8 "B9 — Widget re-arm indicator" requirement. Specifically: (a) A new `DoitWidgetState` value class (`lib/widget/doit_widget_state.dart`, pure-Dart, no Flutter import) is the single contract between Dart and Kotlin; fields are `habitId`, `habitName`, `streakNumber`, `isCompletedToday`, `reliability` (mirrors the Kotlin enum, encoded as JSON tag `optimal`/`degraded`/`unknown`), and `asOf` (frozen reference time). The class is `@immutable`, value-equals (`==` + `hashCode`), and JSON-round-trippable via `toJson()` / `fromJson()`. `fromJson` is defensive — missing fields default to empty / 0 / `unknown`, an unrecognized reliability tag falls back to `unknown`, an unparseable `asOfIso` falls back to epoch. (b) A new `WidgetBridge` abstract class + `PlatformWidgetBridge` impl (`lib/widget/widget_bridge.dart`) mirrors the `ReminderBridge` shape: `Future<void> cacheSnapshot(DoitWidgetState)`, `Future<void> requestRefresh()`, `Future<DoitWidgetState?> snapshot()`. The platform impl uses a `MethodChannel("doit/widget")` and wraps every call in a `_safe` closure (ADR-013) that swallows `MissingPluginException` + `PlatformException` and returns null / completes normally. A `FakeWidgetBridge` records `cachedSnapshots` + `refreshCount` for tests. (c) A new `WidgetStateCache` (`lib/widget/widget_state_cache.dart`) is a `SharedPreferences`-backed singleton with `save(DoitWidgetState)` / `load()` / `clear()` / `cached` getter / `resetForTesting()`; it stores a JSON envelope under the key `doit.widget.cached_v1`. The Kotlin `WidgetStateCache.cachedFromPrefs(ctx)` reads the same key on cold-start so the widget is never blank between OS process-kill and first Dart frame. (d) A new pure-Dart `WidgetStateBuilder.buildWidgetState({required Do activeDo, required List<CompletionLogEntry> completions, required Reliability reliability, required DateTime asOf, required SkipBudget skipBudget})` (in `lib/widget/widget_state_builder.dart`) is the pure factory: the streak is computed via `ConsecutiveCounter.compute(... do.effectiveStreakConfig(...))`; `isCompletedToday` checks whether `asOf’s local-calendar day` appears in the completion list; the reliability is mapped 1:1 to the badge enum; a `null activeDo` produces the empty-state snapshot (`habitId=""`, `habitName=""`, `streakNumber=0`, `isCompletedToday=false`). No Flutter import allowed — pinned by the unit test suite compiling cleanly without `package:flutter/*`. (e) A new `firstActiveDo({DoRepository? repository, DateTime? now})` locator (`lib/widget/widget_state_locator.dart`) sorts `DoRepository.listAll()` ascending by `createdAt` and returns the first non-paused entry (or `null`); the reference time is caller-supplied so widget tests are deterministic. (f) A new `WidgetService` singleton (`lib/services/widget_service.dart`, `singleton-with-_ready` per `.claude/rules/lib-services.md`) owns the home-widget lifecycle: `init({WidgetBridge, DoRepository, CompletionLogService, ReliabilityService, WidgetStateCache?})` is idempotent, subscribes to `ReliabilityService.reliability` so every value change triggers a re-derive, primes the cache + platform on init, and exposes `Future<void> handleRefreshRequest()` + `Future<void> markDone(String habitId)`. `markDone` is a no-op when the do does not exist; otherwise it appends via `CompletionLogService` with `source: CompletionSource.manual` and then re-derives. All public methods are async; any platform-side failure is swallowed per ADR-013 so the next legitimate event triggers a clean re-derive. `main.dart` wires the service AFTER `ReliabilityService.init(...)` so the reliability subscription is live at first widget refresh. (g) The Kotlin side (`android/app/src/main/kotlin/com/doit/`) ships three new files: `DoitWidgetProvider : AppWidgetProvider` (lifecycle: `onUpdate` reads `WidgetStateCache.cachedFromPrefs(ctx)` FIRST for cold-start fallback, then attempts the Dart round-trip via `WidgetUpdater.refreshIds(ctx, ids)`; `onEnabled` + `onDisabled` lifecycle hooks; `onReceive` dispatches `ACTION_REFRESH_WIDGET`); `WidgetUpdater.refreshAll(ctx) / refreshIds(ctx, ids)` (boots a one-shot `FlutterEngine` via `FlutterEngineCache`, applies the cached `RemoteViews`, then triggers a Dart round-trip); `WidgetChannel.kt` object with `attach(engine)` / `detach()` / `setAppContext(ctx)` mirroring `FullScreenIntentChannel.kt`, dispatching `snapshot` + `cacheSnapshot` + `markDone` MethodChannel calls. `WidgetRenderer.render(ctx, state)` / `renderEmpty(ctx)` / `renderError(ctx, reason)` apply the `RemoteViews` to the supplied `appWidgetIds`; the reliability-string-to-icon mapping mirrors the Kotlin enum (`optimal`→`ic_widget_optimal`, `degraded`→`ic_widget_degraded`, `unknown`→`ic_widget_unknown`). (h) `AndroidManifest.xml` gains ONE new `<receiver>` block inside `<application>`: `<receiver android:name=".DoitWidgetProvider" android:exported="false"><intent-filter><action android:name="android.appwidget.action.APPWIDGET_UPDATE"/></intent-filter><meta-data android:name="android.appwidget.provider" android:resource="@xml/doit_widget_info"/></receiver>`. No new `<uses-permission>` — widget rendering needs none. (i) Resources: `res/xml/doit_widget_info.xml` (4×2 cell, `updatePeriodMillis=1800000`, `targetCellWidth=4 targetCellHeight=2`, `widgetCategory="home_screen"`, `description="@string/widget_description"`, `initialLayout=@layout/widget_medium`); `res/layout/widget_medium.xml` (vertical `LinearLayout` with three rows — habit-name + reliability badge, streak number + "day streak" subtitle, "Done" `ImageButton`; tap-target `FrameLayout` root with id `widget_root`); `res/drawable/widget_bg.xml` (12 dp rounded rect), `res/drawable/ic_widget_optimal|degraded|unknown|done.xml` (monochrome vector icons); 5 new `<string>` entries in `res/values/strings.xml` (`widget_description`, `widget_done_content_description`, `widget_streak_content_description`, `widget_streak_subtitle`, `widget_empty_state`, `widget_reliability_badge`). The widget is `resizeMode=horizontal|vertical` capable but ships single-size (medium) only; small / large variants are v1.4b. (j) Body-tap behavior: opens `MainActivity` via `PendingIntent.getActivity` (single-top) — deep-linking to a specific do is v1.4b; the "Done" tap dispatches `markDone(habitId)` over the channel, which appends via `CompletionLogService` then re-derives + refreshes. (v1.4a / Phase 28 / SYS-115.) | Unit tests (`test/widget/widget_state_builder_test.dart`: 8 tests — streak from empty log is 0; streak with 3 consecutive days; streak broken at grace-window edge; streak still alive within grace window; `isCompletedToday` true only when today is in the log; reliability maps to widget badge; `null activeDo` produces the empty-state snapshot; `Do.effectiveStreakConfig` flows through the factory end-to-end) + Unit tests (`test/widget/widget_bridge_test.dart`: 11 tests — `FakeWidgetBridge` cacheSnapshot records; `FakeWidgetBridge` requestRefresh increments; `FakeWidgetBridge` snapshot returns scripted / null by default; `PlatformWidgetBridge` cacheSnapshot forwards the JSON envelope; `PlatformWidgetBridge` snapshot happy path; `PlatformWidgetBridge` snapshot swallows `MissingPluginException`; `PlatformWidgetBridge` snapshot swallows `PlatformException`; `PlatformWidgetBridge` requestRefresh swallows `MissingPluginException`; `DoitWidgetState` JSON round-trips; `fromJson` is defensive against missing fields; `fromJson` tolerates an unknown reliability tag) + Unit tests (`test/widget/widget_state_cache_test.dart`: 5 tests — save-then-load round-trips; load returns null on empty prefs; clear removes the key; save overwrites previous; corrupt cache is dropped) + Unit tests (`test/widget/widget_state_locator_test.dart`: 4 tests — returns first-active do by oldest `createdAt`; skips paused dos; returns null on empty repository; returns null when every do is paused) + Unit tests (`test/widget/widget_service_test.dart`: 6 tests — `init` is idempotent; `handleRefreshRequest` computes + caches + persists state; `markDone` appends the completion then re-derives; `markDone` is a no-op when the habit does not exist; reliability change triggers a re-derive; `MissingPluginException` from the bridge is swallowed (ADR-013)) + Coverage: ≥80% on every changed file (`doit_widget_state.dart`, `widget_bridge.dart`, `widget_state_builder.dart`, `widget_state_cache.dart`, `widget_state_locator.dart`, `widget_service.dart`) |
| **SYS-116** | The home screen (`lib/screens/home.dart`) shall render a streak number + per-tile "Done" button on every `_HabitTile`, mirroring the Android home-screen widget (v1.4a / SYS-115) so the user gets feature-parity on both surfaces. Specifically: (a) `_HabitTile` becomes a `StatefulWidget` with `_HabitTileState` holding `_busy` and `_isCompletedToday` flags. (b) A new pure-Dart helper `lib/screens/home_tile_streak.dart` exposes `int streakForDo({required Do activeDo, required List<CompletionLogEntry> completions, required DateTime asOf})` — calls `ConsecutiveCounter.compute({log: completions, config: activeDo.effectiveStreakConfig(skipBudget: SkipBudget(doId: activeDo.id, monthlyLimit: activeDo.restDaysPerMonth)), asOf: asOf}).currentStreak`. A second pure-Dart helper `bool isCompletedOnDay({required List<CompletionLogEntry> completions, required DateTime asOf})` is the local-calendar-day presence check used by the tile to gate the "already done" SnackBar. (c) A new pure-Dart helper `lib/screens/home_tile_completion.dart` exposes `Future<void> markDoDone({required Do activeDo, required DateTime asOf, required CompletionLogService completionLog})` — calls `completionLog.append(habitId: activeDo.id, day: DateTime(asOf.year, asOf.month, asOf.day), source: CompletionSource.manual, proofModeAtTime: _proofModeTag(activeDo.proofMode))`. The `_proofModeTag` helper mirrors the widget's `_proofModeTag` (ADR-046 — kept in lockstep manually until a future PR extracts both into `lib/do/proof_mode_tag.dart`). (d) A new `_DoStreakBadge` sub-widget renders the streak with `titleLarge` typography + tabular figures + `colorScheme.primary`, paired with the localized `homeTileStreakLabel` ("day streak") subtitle. The badge reads from a `FutureBuilder<List<CompletionRow>>` over `CompletionLogService.instance.listForHabit(habit.id)` and converts each row via `DateTime.fromMillisecondsSinceEpoch(r.dayMillis)`. (e) A new `_DoneButton` sub-widget replaces the v0.1 SnackBar stub. For a soft/auto do, a tap calls `markDoDone(...)`, flips `_isCompletedToday = true`, shows the `homeSnackbarMarkedDone` ("Marked done.") SnackBar, and renders a filled `Icons.check_circle`. For a strong do, the tap pushes `MissionLauncherScreen` (the v1.3d / SYS-114 chain orchestrator) via `Navigator.push` and `await`s the pop; a `true` pop (ChainPassed) flips `_isCompletedToday = true`; a `null` pop keeps the streak broken per the v1.1f grace-window contract. A re-tap on an already-done tile shows the `homeTileAlreadyDoneTooltip` SnackBar (no second append — `CompletionLogService.append` already dedupes on `(habitId, day)`). (f) Four new ARB keys (`homeTileMarkDone`, `homeTileStreakLabel`, `homeTileAlreadyDoneTooltip`, `homeTileStrongModeHint`) are added to both `app_en.arb` and `app_es.arb`. The existing `homeSnackbarMarkedDone` is re-used. (g) No new `<uses-permission>`, no new pubspec deps — the change is pure-Dart + a single ARB addition + the stateful rewrite. (v1.4b / Phase 29 / SYS-116 / ADR-046 / WF-043.) | Unit tests (`test/screens/home_tile_streak_test.dart`: 12 tests — `streakForDo` returns 0 for empty log, N for N consecutive days, honors per-do `graceWindowOverride`, `asOf` at 23:59 vs 00:01 of next day flips within the grace window, helper is pure (no `DateTime.now()`), 1000-completion log stress < 50 ms, helper wires `restDaysPerMonth` into `SkipBudget`; `isCompletedOnDay` returns false for empty log, true for matching midnight, true for mid-day timestamp on the same day, false for tomorrow) + Unit tests (`test/screens/home_tile_completion_test.dart`: 4 tests — `markDoDone` calls `append` with `source: CompletionSource.manual` and the do's id; `day` argument is local-midnight at `asOf`; `proofModeAtTime` tag matches SoftProof / StrongProof / AutoProof; helper makes exactly one append call per invocation) + Widget tests (`test/screens/home_test.dart`: +7 tests — tile renders the streak number; tile renders "day streak" subtitle; soft-mode tile "Mark done" tap appends to the completion log; already-done tile "Mark done" tap is a dedup no-op; tile SnackBar shows "Marked done." on soft-mode tap; strong-mode tile "Mark done" tap pushes MissionLauncherScreen (asserted via NavigatorObserver); tile Done IconButton is findable by tooltip) + ARB parity (`test/l10n/app_localizations_test.dart` "every non-template ARB has the same key set as the template" test catches missing `app_es.arb` entries automatically) + Coverage: ≥80% on every changed file (`home_tile_streak.dart`, `home_tile_completion.dart`, `home.dart` `_HabitTileState` + `_DoStreakBadge` + `_DoneButton`) |
SYS-117** | The home screen (`lib/screens/home.dart`) shall expose a per-tile "Skip today" affordance that consumes one unit from the do's monthly rest-day budget and renders a small caption indicating how many rest days are left, mirroring the v1.4b streak+Done UX surface so the user gets feature-parity on the rest-day path between the in-app tile and the future Android home-screen widget. Specifically: (a) `_HabitTile` (`_HabitTileState`) grows an `_isSkippedToday` flag, set to `true` when the user taps the new `_SkipButton` (the flag drives the `homeTileSkipAlready` SnackBar copy and the muted `_BudgetCaption` once the month is exhausted). A combined getter `_isResolvedToday = _isCompletedToday \|\| _isSkippedToday` gates the icon swap between the active `_DoneButton` and the muted "Rest day taken" affordance. (b) A new pure-Dart helper `lib/screens/home_tile_skip.dart` exposes `Future<void> markDoSkipped({required Do activeDo, required DateTime asOf, required CompletionLogService completionLog})`. The helper reads the do's `restDaysPerMonth`; if `<= 0` it throws `NoRestDaysRemaining(activeDo.id, asOf.year, asOf.month)` (a dedicated exception class so the UI can show `homeTileSkipBudgetExhausted` instead of letting `SkipBudget.consume` raise a less-actionable message). It then fetches the month's rest-day rows via `completionLog.listRestDaysInMonth(activeDo.id, year, month)` and re-throws `NoRestDaysRemaining` if the count has already hit the limit. It then constructs a `SkipBudget(doId: activeDo.id, monthlyLimit: activeDo.restDaysPerMonth).consume(asOf)` (defensive — if the DB has somehow over-counted past the limit, the `SkipBudgetExhausted` is converted to `NoRestDaysRemaining` so the contract stays single-message), and finally calls `completionLog.append(habitId: activeDo.id, day: DateTime(asOf.year, asOf.month, asOf.day), source: CompletionSource.restDay, proofModeAtTime: proofModeTag(activeDo.proofMode))`. The streak calculator (`ConsecutiveCounter.compute`) already credits `CompletionSource.restDay` rows identically to manual rows, so a rest-day entry preserves the streak end-to-end. (c) A new pure-Dart helper `lib/screens/home_tile_budget.dart` exposes `Future<BudgetRemaining> budgetRemainingForDo({required Do activeDo, required DateTime asOf, required CompletionLogService completionLog})`. The result is an immutable `BudgetRemaining { used, limit, remaining }` value class with `canSkip = remaining > 0` and `isExhausted = remaining == 0 && limit > 0` derived flags (a `limit == 0` do is "opted out" → not exhausted). The helper fetches `completionLog.listRestDaysInMonth(...)`, computes the used count, and clamps `remaining = (limit - used).clamp(0, limit)` so a do whose `restDaysPerMonth` was lowered mid-month does not render a negative `remaining`. (d) A new `_SkipButton` sub-widget renders to the LEFT of `_DoneButton` only when `habit.restDaysPerMonth > 0`. The button uses `Icons.bedtime` (active) / `Icons.bedtime_outlined` (muted, when `_isSkippedToday == true` or the month is exhausted), and its tooltip is `homeTileSkipToday` / `homeTileSkipAlready`. The tap handler calls `markDoSkipped(...)` inside a `try/catch on NoRestDaysRemaining`; on success it sets `_isSkippedToday = true` + shows `homeTileSkipSuccess` ("Rest day taken — streak holds."); on `NoRestDaysRemaining` it shows `homeTileSkipBudgetExhausted` ("No rest days left this month."). The `_DoneButton`'s post-tap SnackBar now branches on `_isSkippedToday` so a Done tap on a skipped-today tile shows `homeTileSkipAlready` ("Rest day taken") rather than `homeTileAlreadyDoneTooltip` (which would be confusing — the row IS resolved, just by a different mechanism). (e) `_DoStreakBadge` grows a `Future<BudgetRemaining> budget` parameter and a nested `FutureBuilder` rendering `_BudgetCaption`: when `limit > 0`, the caption reads `homeTileBudgetRemaining` ("{remaining}/{limit} rest days left"); when `limit == 0 || remaining == 0`, the caption falls back to `homeTileBudgetNoRemaining` ("No rest days left"); when the `FutureBuilder` is mid-fetch, it renders `SizedBox.shrink()` (no layout shift). The caption uses `bodySmall` typography + `colorScheme.onSurfaceVariant` and wraps in `Semantics(label: "Rest day budget")` for the a11y bar. (f) Six new ARB keys (`homeTileSkipToday`, `homeTileSkipAlready`, `homeTileSkipSuccess`, `homeTileSkipBudgetExhausted`, `homeTileBudgetRemaining`, `homeTileBudgetNoRemaining`) are added to both `app_en.arb` and `app_es.arb` — the existing ARB parity test catches missing Spanish entries. (g) The shared `proofModeTag(DoProofMode)` helper extracted in v1.4c (ADR-047 — the v1.4b `_proofModeTag` inline copies in `do_repository.dart` + `home_tile_completion.dart` are now consolidated in `lib/do/proof_mode_tag.dart`; `mission_launcher.dart` is left untouched because its `'unknown'` defensive contract differs). (h) No new `<uses-permission>`, no new pubspec deps, no new Drift tables — the rest-day budget was already a `RestDayBudgets` snapshot table in v0.5, and the `CompletionSource.restDay` enum value already existed; v1.4c is pure-Dart + a stateful tile extension + ARB additions. (v1.4c / Phase 30 / SYS-117 / ADR-047 / WF-044.) | Unit tests (`test/screens/home_tile_skip_test.dart`: 8 tests — `markDoSkipped` calls `append` with `source: CompletionSource.restDay` and the do's id; `day` argument is local-midnight at `asOf`; `proofModeAtTime` tag matches SoftProof / StrongProof / AutoProof; throws `NoRestDaysRemaining` when `restDaysPerMonth == 0`; throws `NoRestDaysRemaining` when the month's budget is exhausted; allows the last budget unit to be consumed (used == limit - 1); `NoRestDaysRemaining` carries `doId` + `month` for the snackbar; helper makes exactly one `append` call per invocation) + Unit tests (`test/screens/home_tile_budget_test.dart`: 11 tests — `BudgetRemaining(used: 0, limit: 2, remaining: 2)` is `canSkip` + not exhausted; `used 1 of 2` → `remaining 1`, `canSkip`, not exhausted; `used 2 of 2` → `remaining 0`, not `canSkip`, exhausted; `limit 0` is `not canSkip` + not exhausted (opt-out, not exhausted); value-equality holds for equal triples; `budgetRemainingForDo` returns the empty-budget snapshot for `limit == 0`; `used 0` for an empty log; `used 1 / remaining 1` for partial usage; `used 2 / remaining 0 / isExhausted` when the cap is hit; only-counts-rows-for-the-passed-do; clamps negative `remaining` to 0 (defensive)) + Widget tests (`test/screens/home_test.dart`: +7 tests — tile renders the Skip button when `restDaysPerMonth > 0`; tile hides the Skip button when `restDaysPerMonth == 0`; soft-mode skip tap appends a rest-day row; skip-tap on an exhausted month shows the budget-exhausted snackbar copy; the budget caption renders "X / Y rest days left" when budget is partial; the budget caption renders the "No rest days left" copy when exhausted; Done-tap on a skipped-today tile shows the rest-day-taken snackbar (NOT the already-done snackbar)) + ARB parity (`test/l10n/app_localizations_test.dart` "every non-template ARB has the same key set as the template" test catches missing `app_es.arb` entries automatically) + Coverage: ≥80% on every changed file (`home_tile_skip.dart`, `home_tile_budget.dart`, `proof_mode_tag.dart`, `do_repository.dart` line removal of the inline `_proofModeTag`, `home_tile_completion.dart` line removal of the inline `_proofModeTag`, `home.dart` `_HabitTileState` + `_SkipButton` + `_BudgetCaption` + the `_DoneButton` post-tap branch) |
| **SYS-118** | The home screen (`lib/screens/home.dart`) shall expose a per-tile "Undo today's completion" affordance on every `_HabitTile` when `_isResolvedToday == true` (i.e., the user has either tapped Done or tapped Skip earlier today), mirroring the v1.2m / SYS-108 / WF-025 `CompletionLogSection` pattern but at the tile surface so the user can correct an accidental tap without leaving the home screen. Specifically: (a) A new pure-Dart helper `lib/screens/home_tile_undo.dart` exposes `Future<UndoResult> undoToday({required Do activeDo, required DateTime asOf, required CompletionLogService completionLog})`. The helper fetches `completionLog.listForHabit(activeDo.id)`, filters for the row whose `dayMillis == DateTime(asOf.year, asOf.month, asOf.day).millisecondsSinceEpoch`, and either calls `completionLog.deleteById(row.id)` returning `UndoResult.removed(rowId, source)` or returns `UndoResult.nothingToUndo()` if no row matches today. The result is an immutable value class with `==` / `hashCode` for testability. (b) A new `_UndoButton` sub-widget renders to the RIGHT of `_SkipButton` and to the LEFT of `_DoneButton`, only when `_isResolvedToday == true`. The button uses `Icons.undo` and a localized tooltip (`homeTileUndoToday`). The tap handler opens an `AlertDialog` titled `homeTileUndoConfirm` ("Undo today's completion?") with body `homeTileUndoConfirmBody` ("This will remove the entry for today and shorten your streak by one day.") and Confirm / Cancel actions; on confirm, the handler calls `undoToday(...)` and (a) on `UndoResult.removed` flips `_isCompletedToday = false` + `_isSkippedToday = false` + shows `homeTileUndoSuccess` ("Completion removed.") or (b) on `UndoResult.nothingToUndo` shows `homeTileUndoNotToday` ("Nothing to undo for today."). The dialog cancel path is a no-op (tile state unchanged, no SnackBar). (c) Four new ARB keys (`homeTileUndoToday`, `homeTileUndoConfirm`, `homeTileUndoConfirmBody`, `homeTileUndoSuccess`, `homeTileUndoNotToday`) are added to both `app_en.arb` and `app_es.arb` — the existing ARB parity test catches missing Spanish entries automatically. (d) The undo is single-tile and local-midnight-bounded: a row written at 23:59 yesterday is NOT undoable today. The existing `CompletionLogService.deleteById(rowId)` already exists (added in v1.2m alongside `CompletionLogSection`); v1.4d re-uses it without modification. (e) No new `<uses-permission>`, no new pubspec deps — pure-Dart + a stateful tile extension + ARB additions. (v1.4d / Phase 31 / SYS-118 / ADR-048 / WF-045.) | Unit tests (`test/screens/home_tile_undo_test.dart`: 8 tests — `undoToday` finds today's row by `dayMillis == local-midnight at asOf`; `undoToday` calls `deleteById` with the matching row's id; `undoToday` returns `UndoResult.removed` on success; `undoToday` returns `UndoResult.nothingToUndo` when no row matches today; `UndoResult.removed` is value-equal across `==` regardless of `source`; `UndoResult.nothingToUndo` is a singleton-like value-equal; helper makes exactly one `deleteById` call per invocation when found; helper makes zero `deleteById` calls when nothing to undo) + Widget tests (`test/screens/home_test.dart`: +5 tests — tile renders the Undo button when `_isCompletedToday == true`; tile renders the Undo button when `_isSkippedToday == true` (rest-day completion is also undoable); tile hides the Undo button when `_isResolvedToday == false`; undo tap opens the confirm dialog; confirm-on-dialog tap calls `deleteById` for today's row + flips `_isResolvedToday` to false + shows the success SnackBar) + ARB parity (`test/l10n/app_localizations_test.dart` "every non-template ARB has the same key set as the template" test catches missing `app_es.arb` entries automatically) + Coverage: ≥80% on every changed file (`home_tile_undo.dart`, `home.dart` `_UndoButton` + the `_HabitTileState` `_isResolvedToday` getter + the post-undo `setState`) |
| **SYS-119** | The home screen (`lib/screens/home.dart`) shall surface a 7-day streak history sparkline row under the streak badge on every `_HabitTile`, mirroring the v1.2m / SYS-108 / WF-025 `CompletionLogSection` review-row pattern but at the tile surface so the user gets a one-glance view of the last week without leaving the home screen. Specifically: (a) A new pure-Dart helper `lib/screens/home_tile_sparkline.dart` exposes `Future<List<SparklineDot>> sparklineForDo({required Do activeDo, required DateTime asOf, required CompletionLogService completionLog})`. The helper fetches `completionLog.listForHabit(activeDo.id)`, builds the 7-day window `[asOf - 6 days .. asOf]` (local-midnight each), then for each day emits a `SparklineDot.filled(day, source)` (with `source` = first-matching row's source tag) if a row exists for that day's `dayMillis`, a `SparklineDot.future(day)` if the day is in the future of `asOf` (defensive — the widget is robust to a frozen `asOf` ahead of the wall clock), or a `SparklineDot.empty(day)` otherwise. The `SparklineDot` hierarchy is a sealed class with three value-equal subclasses (`SparklineDotFilled`, `SparklineDotEmpty`, `SparklineDotFuture`); the `source` field on `SparklineDotFilled` is preserved so a future widget variant can branch on `rest_day` vs `manual` for a different glyph without re-fetching the log. (b) A new `_Sparkline` sub-widget renders below the streak badge inside `_DoStreakBadge`'s right-aligned `Column`, wrapped in a single `Semantics(label: l.homeTileSparklineSemantics, readOnly: true)` node so screen readers announce "Last 7 days" / "Últimos 7 días" once instead of 7 separate dots. The widget is a `FutureBuilder<List<SparklineDot>>` over `sparklineForDo(...)`. While the future is in flight, the widget renders `_SparklineSkeleton` (7 outlined 6 dp dots) to reserve space; on resolve it renders 7 `_SparklineDot` circles (6 dp outlined by default, 8 dp filled for today when `_isResolvedToday == true`). The today-dot size bump is driven by the parent `_DoStreakBadge`'s `resolvedToday` hint (re-derived from the same `completions` future the streak badge already holds), so no second `listForHabit` round-trip is added. (c) One new ARB key (`homeTileSparklineSemantics`) is added to both `app_en.arb` and `app_es.arb` — the existing ARB parity test catches missing Spanish entries automatically. (d) No new `<uses-permission>`, no new pubspec deps — pure-Dart + a single ARB addition + the stateful widget extension. (v1.4e / Phase 32 / SYS-119 / ADR-049 / WF-046.) | Unit tests (`test/screens/home_tile_sparkline_test.dart`: 8 tests — `sparklineForDo` returns exactly 7 dots in oldest-first order with today as the last dot; a dot is `SparklineDot.filled(day, source)` when a manual row exists for that day's local-midnight; the same for a `rest_day` row; all 7 dots are `SparklineDot.empty` when the log is empty; multiple rows for the same day collapse to exactly one dot (first-match source wins, mirroring `home_tile_undo.undoToday`); rows outside the 7-day window are ignored; the 7 dots are chronologically ordered with strictly increasing day values; `SparklineDot` value-equality holds across all three factories for same-day + same-source + same-variant) + Widget tests (`test/screens/home_test.dart`: +3 tests — the sparkline `Semantics` node is rendered when at least one completion row exists; the sparkline `Semantics` node is rendered even when no completions are recorded (skeleton resolves to empty dots); the sparkline `Semantics` node survives a Mark-done tap that resolves today (the parent `_DoStreakBadge` re-fires the rebuild and the dot row stays mounted)) + ARB parity (`test/l10n/app_localizations_test.dart` "every non-template ARB has the same key set as the template" test catches missing Spanish entries automatically) + Coverage: ≥80% on every changed file (`home_tile_sparkline.dart`, `home.dart` `_Sparkline` + `_SparklineDot` + `_SparklineSkeleton`) |
| **SYS-120** | The Android home-screen widget (`res/layout/widget_medium.xml` + `com.doit.DoitWidgetProvider`) shall expose two new `ImageButton`s — "Skip today" and "Undo today" — that bring the widget to feature-parity with the in-app `_HabitTile` (v1.4c / SYS-117 Skip + v1.4d / SYS-118 Undo). Specifically: (a) A new `Future<bool> skip(String habitId)` method on `WidgetService` (`lib/services/widget_service.dart`) mirrors the in-app `markDoSkipped` contract: returns `false` if the do does not exist, if `restDaysPerMonth <= 0`, or if `CompletionLogService.listRestDaysInMonth(...)` returns a count `>= restDaysPerMonth`; otherwise appends a row via `completionLog.append(habitId, day: local-midnight at now, source: CompletionSource.restDay, proofModeAtTime: proofModeTag(activeDo.proofMode))` and re-derives via `handleRefreshRequest()`. Returns `true` on the happy path. (b) A new `Future<bool> undo(String habitId)` method on `WidgetService` mirrors the in-app `undoToday` contract: lists the habit's rows, finds the row whose `dayMillis == DateTime(now.year, now.month, now.day).millisecondsSinceEpoch`, calls `completionLog.deleteById(row.id)` on first match (first-match-wins tiebreak — matches the v1.4e sparkline + v1.4d undo pattern), and re-derives; returns `true` on the happy path or `false` if no row matches today (no `deleteById` call). (c) The `WidgetBridge` interface (`lib/widget/widget_bridge.dart`) gains `Future<bool> skip(String habitId)` and `Future<bool> undo(String habitId)`. `PlatformWidgetBridge` dispatches via the `doit/widget` MethodChannel's `skip` + `undo` arms and wraps each in the existing `_safeResult<bool>` ADR-013 swallow so a `MissingPluginException` / `PlatformException` surfaces as `false` (NOT a throw). `FakeWidgetBridge` records the habit ids in `skipHabitIds` + `undoHabitIds` and exposes `nextSkipResult` / `nextUndoResult` defaults. (d) The Kotlin side (`android/app/src/main/kotlin/com/doit/WidgetChannel.kt`) gets a shared `private fun handleAction(call: MethodCall, result: MethodChannel.Result, action: String)` helper that reads the `habitId` argument, calls `WidgetUpdater.refreshAll(ctx)` (which boots a one-shot `FlutterEngine` + repaints every bound widget id), and returns `true`. The `when` block is reduced to three arms (`"markDone"`, `"skip"`, `"undo"`) each routing to `handleAction(call, result, "...")`. (e) `android/app/src/main/kotlin/com/doit/DoitWidgetProvider.kt` gains two new action constants: `ACTION_WIDGET_SKIP = "com.doit.WIDGET_SKIP"` and `ACTION_WIDGET_UNDO = "com.doit.WIDGET_UNDO"`, with matching dispatch arms in `onReceive()` mirroring the existing `ACTION_MARK_DONE` pattern. The constants are also exported so `WidgetRenderer` can build the matching `PendingIntent.getBroadcast` for the `ImageButton` taps. (f) `res/layout/widget_medium.xml` grows two new `ImageButton`s between the bottom spacer and the existing Done button: `@+id/widget_skip` (icon `@drawable/ic_widget_skip`, contentDescription `@string/widget_skip_content_description`) and `@+id/widget_undo` (icon `@drawable/ic_widget_undo`, contentDescription `@string/widget_undo_content_description`). Both are `ImageButton`s (NOT plain `ImageView`s) with `android:background="?android:attr/selectableItemBackgroundBorderless"` so the tap ripple is visible. All three buttons share the same row so the layout is a 3-column `LinearLayout` when all are visible. (g) `WidgetRenderer.render(...)` reads the cached `DoitWidgetState` JSON and reads the new `restDaysPerMonth` field to decide `widget_skip` visibility: `View.GONE` when `restDaysPerMonth == 0` (a do with no rest-day budget configured never gets a Skip button — mirrors the in-app `_SkipButton` hide rule from SYS-117). It also reads `isCompletedToday` to decide `widget_undo` visibility: `View.GONE` when `isCompletedToday == false` (no row to undo — mirrors the in-app `_UndoButton` hide rule from SYS-118). Two new private helpers `skipIntent(ctx, id)` + `undoIntent(ctx, id)` return `PendingIntent.getBroadcast(...)` targeting `DoitWidgetProvider` with the corresponding `ACTION_WIDGET_SKIP` / `ACTION_WIDGET_UNDO` extras (`EXTRA_HABIT_ID`). (h) `DoitWidgetState` (`lib/widget/doit_widget_state.dart`) grows a `final int restDaysPerMonth` field (default 0) — included in `toJson()` / `fromJson()` so the renderer can read it from the cache without a Dart round-trip; the field is plumbed through `WidgetStateBuilder.buildWidgetState(...)` from `activeDo.restDaysPerMonth`. (i) Resources: `res/drawable/ic_widget_skip.xml` (moon glyph, 24 dp monochrome vector) + `res/drawable/ic_widget_undo.xml` (curved-arrow glyph, 24 dp monochrome vector); two new `<string>` entries in `res/values/strings.xml` (`widget_skip_content_description`, `widget_undo_content_description`); two new ARB keys in both `app_en.arb` + `app_es.arb` (`widgetSkipToday` / `widgetUndoToday`) so the l10n parity test catches missing translations. (j) No new `<uses-permission>`, no new pubspec deps, no new Drift tables, no new MethodChannels — v1.4f re-uses the existing `doit/widget` channel and the existing `DoitWidgetProvider` BroadcastReceiver. (k) Latent gap (NOT a defect of v1.4f, but worth noting): the existing widget "Done" tap (v1.4a) only triggers `WidgetUpdater.refreshAll(ctx)` from the Kotlin `ACTION_MARK_DONE` broadcast — it does NOT round-trip the `markDone` call to Dart's `WidgetService.markDone` like the Skip + Undo paths do in v1.4f. The widget still shows the correct state on the next refresh cycle because the Dart side recomputes on every reliability change AND on the next `markDone`-adjacent event, but a power-cycle race between the manual tile-Done tap and a freshness probe can leave the widget stale for ~30 s. Closure candidate for v1.4g+. (v1.4f / Phase 33 / SYS-120 / ADR-050 / WF-047.) | Unit tests (`test/widget/widget_service_test.dart`: +8 tests — `skip` appends a rest-day completion with `source: CompletionSource.restDay` then re-derives the widget state (one new snapshot on top of the prime refresh); `skip` returns `false` when `restDaysPerMonth == 0` (no `append` call); `skip` returns `false` when the month's rest-day rows already hit `restDaysPerMonth` (no `append` call); `skip` returns `false` when the habit does not exist (no `append` call); `undo` deletes today's row via `deleteById` then re-derives the widget state; `undo` returns `false` when no completion row matches today's local-midnight (no `deleteById` call); `undo` only matches today's `dayMillis == local-midnight at now` — a yesterday row is NOT deleted; `undo` returns `false` when the habit does not exist (no `deleteById` call)) + Unit tests (`test/widget/widget_bridge_test.dart`: +6 tests — `FakeWidgetBridge.skip` records habit id in `skipHabitIds`; `FakeWidgetBridge.skip` returns `nextSkipResult`; `FakeWidgetBridge.undo` records habit id in `undoHabitIds`; `FakeWidgetBridge.undo` returns `nextUndoResult`; `PlatformWidgetBridge.skip` forwards the habit id to the channel and unwraps a `null` channel result to `false`; `PlatformWidgetBridge.skip` swallows `MissingPluginException` and returns `false` (ADR-013); same `+2` for `undo`) + Unit tests (`test/widget/doit_widget_state_test.dart`: +1 test — `restDaysPerMonth` defaults to 0 on `fromJson`; included in `toJson`; preserved through `copyWith`) + Coverage: ≥80% on every changed file (`widget_service.dart`, `widget_bridge.dart`, `doit_widget_state.dart`, `widget_state_builder.dart`) + Manual device check: build `flutter build apk --debug` (no signing), install on Android 13+, add a do with `restDaysPerMonth = 3`, pin the widget to home, tap Skip → streak holds + budget caption decrements; tap Done → streak holds; tap Undo → confirmation implicit (button hidden when no row), row deleted, streak decrements by 1; with `restDaysPerMonth = 0` verify Skip button is gone (Visibility.GONE) |
| **SYS-121** | The Android home-screen widget shall round-trip every action button tap — Done, Skip today, and Undo today — to the Dart-side `WidgetService` via the `doit/widget` MethodChannel so the actual completion write / rest-day append / row delete happens in Dart (single source of truth for the completion log), closing the latent v1.4a / v1.4f gap that left the widget buttons repainting without ever persisting the state change. Specifically: (a) `WidgetService.markDone(String habitId)` (`lib/services/widget_service.dart`) returns `Future<bool>` (was `Future<void>`): returns `false` if the habit does not exist or the append throws; otherwise appends via `CompletionLogService.append(habitId, day: local-midnight at now, source: CompletionSource.manual, proofModeAtTime: proofModeTag(activeDo.proofMode))`, re-derives via `handleRefreshRequest()`, and returns `true`. The `Future<void>` → `Future<bool>` signature change is in-place because the only caller in the v1.4a cycle was the in-app tile which ignored the return value — the v1.4g widget-side caller reads it. (b) A new singleton `WidgetActionInvoker` (`lib/widget/widget_action_invoker.dart`) installs a handler on the `doit/widget` MethodChannel via `setMethodCallHandler`. The handler routes the three inbound `MethodCall`s (`markDone` / `skip` / `undo`) to the top-level `widgetActionDispatch(MethodCall)` function which calls `WidgetService.instance.markDone(habitId)` / `.skip(habitId)` / `.undo(habitId)` and returns the `bool` result to the Kotlin side. Outbound methods (`cacheSnapshot` / `requestRefresh` / `snapshot`) and any other method fall through to `null`. (c) `WidgetService.init(...)` calls `await WidgetActionInvoker.attach()` immediately after the singleton is set — `attach()` is idempotent (a second call resolves immediately to the existing `Completer<void> _ready`). The handler's lifecycle is bound to the `WidgetService` singleton via `resetForTesting` cleanup so a hot-restart in dev leaves no stale channel handler. (d) The Kotlin side (`android/app/src/main/kotlin/com/doit/WidgetChannel.kt`) gains a new suspending `suspend fun invokeAction(ctx: Context, action: String, habitId: String): Boolean` that ensures the `FlutterEngine` is alive via `WidgetUpdater.ensureFlutterEngine(ctx)` (now `public` — was `private`), posts `invokeMethod(action, mapOf("habitId" to habitId))` to the platform main thread (because `MethodChannel.invokeMethod` must run on the platform thread), and awaits the result via `CompletableDeferred<Boolean>.await()` with a 5 s `withTimeoutOrNull` ceiling. Returns `false` on timeout, missing channel, invalid `action`, empty `habitId`, or any throwable. The existing `handleAction(...)` outbound helper is preserved for backward compatibility (unused on the inbound path post-v1.4g; the dispatch from `DoitWidgetProvider` uses `invokeAction` instead). (e) `android/app/src/main/kotlin/com/doit/DoitWidgetProvider.kt` replaces each of the three action arms (`ACTION_MARK_DONE` / `ACTION_WIDGET_SKIP` / `ACTION_WIDGET_UNDO`) in `onReceive` with: read `habitId` from `intent.getStringExtra(EXTRA_HABIT_ID)` (preferred) or `WidgetStateCache.cachedFromPrefs(ctx)?.optString("habitId")` (fallback for stale-state widgets whose `PendingIntent` was created before v1.4g), `scope.launch { WidgetChannel.invokeAction(ctx, "<action>", habitId); WidgetUpdater.refreshAll(ctx) }` on `Dispatchers.IO` so the BroadcastReceiver doesn't block. A new companion constant `EXTRA_HABIT_ID = "com.doit.EXTRA_HABIT_ID"` is exported. (f) `android/app/src/main/kotlin/com/doit/WidgetRenderer.kt` carries the habit id to the action `PendingIntent`s: `markDoneIntent(ctx, id, habitId)` / `skipIntent(ctx, id, habitId)` / `undoIntent(ctx, id, habitId)` each `putExtra(EXTRA_HABIT_ID, habitId)`. The `habitId` is read from the cached `DoitWidgetState` JSON (`state.optString("habitId", "")` — defensive — empty string falls back to the cache on the Kotlin side). (g) No new `<uses-permission>`, no new pubspec deps, no new Drift tables, no new MethodChannels — v1.4g re-uses the existing `doit/widget` channel in the INBOUND direction (Kotlin → Dart) instead of the OUTBOUND direction the v1.4a + v1.4f widget flows had used. The bidirectional contract is documented in the file header comments on `widget_action_invoker.dart` + `WidgetChannel.kt` + `DoitWidgetProvider.kt`. (h) Failure semantics: the Dart-side `WidgetService` returns `false` on any error (missing habit, append throws, delete throws, log locked) and the dispatcher returns `false` to the platform. The Kotlin side's `invokeAction` returns `false` on timeout (5 s ceiling — generous to accommodate the cold-start FlutterEngine boot) and on any throwable. Either side's `false` causes the widget to repaint with the cached state via the follow-up `WidgetUpdater.refreshAll(ctx)` — the widget is never left in a stale "post-tap" visual state. (v1.4g / Phase 34 / SYS-121 / ADR-051 / WF-048.) | Unit tests (`test/widget/widget_action_invoker_test.dart`: 7 tests — `widgetActionDispatch` returns `false` when `WidgetService` is not initialized; `widgetActionDispatch` returns `false` when the `habitId` arg is missing; `widgetActionDispatch` returns `false` when the `habitId` arg is empty; `widgetActionDispatch` returns `false` when arguments are `null`; `widgetActionDispatch` returns `false` for an unknown action method; `WidgetActionInvoker.attach` is idempotent — second call is a no-op; `WidgetActionInvoker.resetForTesting` clears the singleton + handler) + Integration tests (`test/widget/widget_action_invoker_integration_test.dart`: 4 tests — inbound `markDone` via the production dispatcher routes to `WidgetService.markDone` and appends a row via `CompletionLogService.append` with `source: CompletionSource.manual` + re-derives via `handleRefreshRequest` (asserted via `FakeWidgetBridge.refreshCount`); inbound `skip` routes to `WidgetService.skip` and appends a `CompletionSource.restDay` row; inbound `undo` routes to `WidgetService.undo` and removes today's row via `CompletionLogService.deleteById`; inbound `markDone` returns `false` when the habit does not exist — no `append` call) + Coverage: ≥80% on every changed file (`widget_action_invoker.dart`, `widget_service.dart`, `WidgetChannel.kt`, `WidgetUpdater.kt`, `DoitWidgetProvider.kt`, `WidgetRenderer.kt`) + Manual device check on Android 13+: install the new APK; tap the widget "Done" button on a fresh-tile do; expect the streak number to advance by 1 within 1 s (the visible streak number update is the proof the completion row was appended + the cache was refreshed + the RemoteViews were repainted); tap "Skip today"; expect a rest-day row in the edit-screen completion log AND the streak to hold (no break); tap "Undo today"; expect the rest-day row to disappear from the completion log AND the streak to decrement by 1 (or the streak to break if it was a manual completion); verify the existing v1.4f Done-tap-streak-staleness gap is closed (no ~30 s lag between tap and streak update) |
| **SYS-122** | The in-app home tile (`_HabitTile` at `lib/screens/home.dart`) shall expose discoverable per-tile Edit and Delete affordances so the user can mutate the do set without having to know the long-press → select-mode → app-bar-trash gesture that v0.2 shipped as the only path. Specifically: (a) `_HabitTile` grows a new `final VoidCallback? onDoChanged` prop; the home screen binds it to `_refresh()` (the existing `FutureBuilder<List<Do>>`-re-fetch helper at `lib/screens/home.dart:85`) so a successful delete (or an edit that pops `true` per WF-022) immediately re-fetches the list and the deleted tile disappears. (b) A new pure-Dart helper `lib/screens/home_tile_delete.dart` exposes `Future<bool> deleteDo({required Do activeDo, required DoRepository repository})`. The helper calls `repository.deleteById(activeDo.id)` and translates any throwable (DB locked, FK constraint, drift exception) into a `bool` return — `true` on the happy path, `false` on any throw. Pure-Dart, no Flutter import, no `DateTime.now()`. (c) `_HabitTileState` grows two new private handlers: `_onEditPressed()` re-uses the existing `_HomeScreenState._onTileTap` (`lib/screens/home.dart:120`) contract — pushes `AddHabitScreen(habitId: widget.habit.id)` and `await`s the popped `bool`; on `true` it calls `widget.onDoChanged?.call()`. `_onDeletePressed()` opens an `AlertDialog` with the title `homeTileDeleteConfirm(habit.name)` ("Delete \"X\"?") and body `homeTileDeleteConfirmBody` ("This will remove the do and all of its completions. You can undo for a few seconds after."). On confirm it captures `messenger = ScaffoldMessenger.of(context)` BEFORE the async gap, sets `_busy = true`, awaits `deleteDo(activeDo: widget.habit, repository: DoRepository.instance)`, clears `_busy` on return, and branches on the `bool`: on `true` it calls `widget.onDoChanged?.call()` then shows a SnackBar `homeSnackbarDoDeleted(habit.name)` with a `SnackBarAction` labeled `homeSnackbarDoDeletedUndo` ("Undo") whose `onPressed` re-saves the captured `Do` reference via `DoRepository.save(widget.habit)` (note: `Do.validate()` runs inside `save()` and the captured reference is already valid); on `false` it shows `homeSnackbarDoDeleteFailed` ("Could not delete. Try again.") WITHOUT removing the tile. The re-save failure path is defensive — if the user created a new do with the same name in the gap, `DuplicateDoName` is swallowed and the user can add the do back manually via the FAB. (d) Two new private sub-widgets in `home.dart` mirror the existing `_SkipButton` / `_UndoButton` / `_DoneButton` shape: `_EditButton({required VoidCallback onPressed})` renders an `IconButton` with `Icons.edit_outlined` and the `homeTileEdit` tooltip (no busy state — the edit screen is a navigation push, not an in-flight write). `_DeleteButton({required bool busy, required VoidCallback onPressed})` renders an `IconButton` with `Icons.delete_outline` and the `homeTileDelete` tooltip; the `busy` flag swaps the icon for a `CircularProgressIndicator` and disables the `onPressed` (matches `_SkipButton` pattern). (e) The action row at `lib/screens/home.dart:618-652` (the existing `Row` of `if (!selectMode)`) inserts `_EditButton` + `_DeleteButton` leftmost (BEFORE the existing `_SkipButton` / `_UndoButton` / `_DoneButton` cluster) so the destructive-action cluster is visually distinct from the completion-action cluster. Both new buttons are hidden in select-mode (they ride on the `if (!selectMode)` gate, matching the other action buttons). The new buttons render unconditionally — there is no per-do hide rule (every do can be edited and deleted; the long-press path had the same contract). (f) i18n: 7 new ARB keys (`homeTileEdit`, `homeTileDelete`, `homeTileDeleteConfirm(doName)`, `homeTileDeleteConfirmBody`, `homeSnackbarDoDeleted(doName)`, `homeSnackbarDoDeletedUndo`, `homeSnackbarDoDeleteFailed`) added to both `app_en.arb` AND `app_es.arb` in lockstep so the existing `test/l10n/app_localizations_test.dart` parity test catches missing translations. (g) No new `<uses-permission>`, no new pubspec deps, no new Drift tables, no new MethodChannels, no Kotlin changes — v1.4h is pure-Dart. The `Do` model is captured by reference (it's immutable per `lib/do/do.dart:160` `@immutable`); the captured reference is valid for re-save inside the Undo closure without a deep clone. The completion-log rows ARE cascade-deleted with the do (FK pragma on `CompletionLogService`), so the undo restores the do row but does NOT restore the streak history — a v1.4h+ follow-up could snapshot the completion log for a true undo. The trade-off is documented in the `_onDeletePressed` header comment. (v1.4h / Phase 35 / SYS-122 / ADR-052 / WF-049.) | Unit tests (`test/screens/home_tile_delete_test.dart`: 6 tests — `deleteDo` returns `true` on the happy path AND records the deleted id; `deleteDo` calls `deleteById` exactly once with the captured do id; `deleteDo` returns `false` when the repository throws an `Error` (e.g. `StateError`); `deleteDo` returns `false` when the repository throws an `Exception` subtype (e.g. `Exception('foreign-key constraint')`); `deleteDo` does not re-throw — the caller can rely on the `bool` return (asserted via `expectLater(() => ..., returnsNormally)`); the helper imports no Flutter types so it can be tested without `TestWidgetsFlutterBinding`) + Widget tests (`test/screens/home_test.dart`: +7 tests — tile renders an Edit `IconButton` with the `Edit` tooltip; tile renders a Delete `IconButton` with the `Delete` tooltip; delete-tap opens a confirm dialog with `Delete "Stretch"?` in the title and the cascade-body copy in the body; confirm-tap on the dialog deletes the do via `DoRepository.deleteById` AND shows the `Deleted "Stretch".` SnackBar with the `Undo` action; cancel-tap on the dialog leaves the do intact (asserted via `getById('h1')` returning the saved `Do`) AND shows no deletion snackbar; Undo action on the deleted snackbar re-saves the captured do via `DoRepository.save` (asserted via `getById('h1')` returning the restored `Do` with the original name); edit-tap pushes `AddHabitScreen` in edit mode for the do (asserted via a `_RecordingNavigatorObserver.didPush` event, mirroring the v1.4b strong-mode pattern at `test/screens/home_test.dart:434`)) + Coverage: ≥80% on every changed file (`home.dart`, `home_tile_delete.dart`) + Manual device check on Android 13+: install the new APK, add a do with at least one completion, tap the per-tile Delete button, verify the confirm dialog carries the do name in quotes, tap Delete, verify the tile disappears AND the deletion snackbar shows the captured name AND the Undo action is visible, tap Undo within 5 s, verify the tile reappears (the streak history does NOT — that's the v1.4h+ soft-delete follow-up) |
| **SYS-123** | The in-app home tile (`_HabitTile` at `lib/screens/home.dart`) shall surface the **last 14 days** of completion history as an extended sparkline below the existing v1.4e / SYS-119 7-day streak badge, color-coded to distinguish manual completions (`CompletionSource.manual`) from rest-day rows (`CompletionSource.restDay`) so the user can see, at a glance, which days in the past fortnight were intentionally skipped. Specifically: (a) The existing pure-Dart helper `lib/screens/home_tile_sparkline.dart` grows a new top-level function `Future<List<SparklineDot>> extendedSparklineForDo({required Do activeDo, required DateTime asOf, required CompletionLogService completionLog, int days = 14})` which takes a configurable `days` parameter (default 14 — a fortnight, half a calendar month, fits comfortably at 360 dp) and returns one dot per day in the `[asOf - (days - 1) days .. asOf]` window (local-midnight each). The original `sparklineForDo` (v1.4e / SYS-119) becomes a thin backwards-compatible wrapper around `extendedSparklineForDo` with `days: 7` so the v1.4e 7-day helper keeps its exact return shape and no caller breaks. (b) The existing `SparklineDot` sealed hierarchy is unchanged; the v1.4i widget reads `SparklineDotFilled.source` to branch on `'manual'` vs `'rest_day'` and picks the `Theme.of(context).colorScheme.primary` (manual) or `Theme.of(context).colorScheme.tertiary` (rest-day) color. The widget also renders a `Semantics(label: ...)` node per dot (NOT a per-dot `Tooltip` — see ADR-053 §"Alternatives considered" for why `Tooltip` was rejected: 14 small dots × 3 localized messages = 42 competing tooltips, plus `Tooltip`'s internal `GestureDetector` intercepts the parent `_HabitTile`'s `onLongPress` select-mode gesture). (c) `_Sparkline` (v1.4e / SYS-119 / WF-046) is extended with three new optional constructor parameters — `int days = 14` (the window length; defaults to the v1.4i 14-day value), `Color? restDayColor` (the color used for `rest_day` filled dots), `bool showLegend = true` (whether to render the inline legend row below the dots). The tile invocation at `lib/screens/home.dart` passes `restDayColor: Theme.of(context).colorScheme.tertiary` so the rest-day color tracks the active theme. The existing v1.4e test surface (`test/screens/home_test.dart` "the sparkline `Semantics(label: 'Last 7 days')` node is rendered when at least one completion row exists" + "the sparkline `Semantics` node is rendered even when no completions are recorded" + "the sparkline `Semantics` node survives a Mark-done tap that resolves today") is updated to the new `Semantics(label: 'Last 14 days')` / `'Últimos 14 días'` text. (d) A new inline legend row (`_SparklineLegend`) renders below the dot row whenever `showLegend == true`. Three `_LegendSwatch` entries — a filled primary-color circle + `homeTileSparklineLegendDone` ("Done" / "Hecho"); a filled tertiary-color circle + `homeTileSparklineLegendRestDay` ("Rest day" / "Día de descanso"); an outlined circle + `homeTileSparklineLegendMissed` ("Missed" / "Perdido") — using `Theme.of(context).textTheme.labelSmall`. The legend is the discoverability mechanism for the source-aware coloring; the rest-day dot would otherwise look like "a Done in a different color" without explanation. (e) i18n: 6 new ARB keys (`homeTileSparklineRestDayTooltip`, `homeTileSparklineDoneTooltip`, `homeTileSparklineMissedTooltip`, `homeTileSparklineLegendDone`, `homeTileSparklineLegendRestDay`, `homeTileSparklineLegendMissed`) added to both `app_en.arb` AND `app_es.arb` in lockstep. The existing `homeTileSparklineSemantics` key is updated from "Last 7 days" → "Last 14 days" (and `Últimos 7 días` → `Últimos 14 días`). The existing `test/l10n/app_localizations_test.dart` "every non-template ARB has the same key set as the template" test catches missing Spanish entries automatically. (f) No new `<uses-permission>`, no new pubspec deps, no new Drift tables, no new MethodChannels, no Kotlin changes — v1.4i is pure-Dart. The `extendedSparklineForDo` helper is pure-Dart (no Flutter import, no `DateTime.now()`); the helper re-uses the same `completionLog.listForHabit(activeDo.id)` query that backs the v1.4e `sparklineForDo` + v1.4d `undoToday` + v1.4b `streakForDo` so no second round-trip is added. (g) The widget re-fetch trigger on a tile-state change (e.g., Skip today → rest-day row appears) re-uses the existing `_HomeScreenState._refresh()` cascade: the parent's `FutureBuilder<List<Do>>` re-fires → the streak badge's `completions` future re-fires → `_Sparkline`'s `FutureBuilder` re-fetches `extendedSparklineForDo` → the new rest-day dot paints. No `ChangeNotifier` / `Stream` is added. (v1.4i / Phase 36 / SYS-123 / ADR-053 / WF-050.) | Unit tests (`test/screens/home_tile_sparkline_test.dart`: +4 tests — `extendedSparklineForDo` with `days: 14` returns 14 dots with `dots.first.day == today - 13 days` and `dots.last.day == today` (local-midnight at asOf); `extendedSparklineForDo` defaults to 14 days when no `days` arg is passed; `extendedSparklineForDo` honors an arbitrary window (3 and 30) — the `days` parameter is wired through to the dot-list length; `extendedSparklineForDo` preserves the source tag on filled dots — a `'rest_day'` row at day -2 paints a `SparklineDotFilled(source: 'rest_day')` AND a `'manual'` row at day -7 paints a `SparklineDotFilled(source: 'manual')`, both retrievable from the returned list so the widget can branch on the source) + Widget tests (`test/screens/home_test.dart`: +6 tests in a new v1.4i group — `seedTile` helper seeds a do + a `FakeCompletionLog`; "renders 14 outlined dots when no completions exist" (asserts via `find.byWidgetPredicate` that 14 `_SparklineDot` containers with `decoration: outlined-circle` are mounted in the row); "renders the legend row below the dots with the three state labels" (asserts `homeTileSparklineLegendDone` / `homeTileSparklineLegendRestDay` / `homeTileSparklineLegendMissed` texts + the `_LegendSwatch` colored circles); "colors rest-day dots with `colorScheme.tertiary`" (seeds a `rest_day` row at day -3, taps the tile's `Skip today` button to trigger the `_refresh()` setState cascade, then asserts the matching dot's `Container.decoration.color` resolves to `Theme.of(context).colorScheme.tertiary`); "rest-day dot carries the `Rest day` `Semantics` label for a11y" (asserts `find.bySemanticsLabel('Rest day')` finds the matching `_SparklineDot` after the same Skip-tap trigger); "manual-completion dot carries the `Done` `Semantics` label" (same shape with a `manual` row at day -5 + a Mark-done tap to seed today); "long-press on a tile with the new sparkline still enters select mode" — regression guard for the ADR-053 §"Alternatives considered" `Tooltip`-gesture-intercept rejection: the v1.4b "long-press enters select mode" test pattern at `test/screens/home_test.dart:165-213` must continue to pass after the v1.4i source-aware coloring + per-dot `Semantics` migration; the `Tooltip` removal is the explicit guard) + 2 existing widget tests updated: "the sparkline `Semantics(label: 'Last 7 days')` node is rendered when at least one completion row exists" → rephrased to "the sparkline `Semantics(label: 'Last 14 days')` node is rendered when at least one completion row exists" (asserts the updated ARB key); same for "the sparkline `Semantics` node is rendered even when no completions are recorded" → "Last 14 days" form + 14 outlined dots. ARB parity (`test/l10n/app_localizations_test.dart` "every non-template ARB has the same key set as the template" test catches missing `app_es.arb` entries automatically) + Coverage: ≥80% on every changed file (`home_tile_sparkline.dart`, `home.dart` `_Sparkline` + `_SparklineDot` + `_SparklineLegend` + `_LegendSwatch`) + Manual device check on Android 13+: install the new APK; open a do with at least 14 days of mixed manual + rest-day history (seed via Skip / Done taps or a fresh-install migration test fixture); verify the extended sparkline renders 14 dots below the streak badge; verify the rest-day dots render in `colorScheme.tertiary` (a distinct color from the manual `colorScheme.primary` fills); verify the legend row reads "Done / Rest day / Missed" below the dot row; TalkBack: tap the tile, swipe right; expect the `Semantics` tree to announce "Last 14 days", "Done" / "Rest day" / "Missed" for the focused dot, then the existing streak + skip / undo / done buttons in tab order |
| **SYS-124** | The in-app home tile (`_HabitTile` at `lib/screens/home.dart`) AND the `AddHabitScreen` edit form shall expose an **in-app edit affordance** for the `restDaysPerMonth` field, closing two long-standing v1.0 gaps: (1) the home tile's `_BudgetCaption` is purely informational — tapping it does nothing, leaving the budget caption as a read-only surface; (2) `AddHabitScreen._save()` hardcodes `restDaysPerMonth: 2` in all 5 schedule branches (`fixed` / `interval` / `anchor` / `dayOfX` / `custom`), so editing a do with `restDaysPerMonth: 3` and hitting Save without touching any other field silently resets the value to 2 (a silent-reset bug). Specifically: (a) A new shared dialog `lib/screens/rest_day_picker_dialog.dart` exports `Future<int?> showRestDayPicker(BuildContext context, {required int initial})` + a `RestDayPickerDialog(initial: int)` `StatefulWidget` that renders an `AlertDialog` containing a `Slider(min: 0, max: 31, divisions: 31, value: _value, label: '$_value', onChanged: (v) => setState(() => _value = v.round()))` with a live integer label above the slider, plus `Save` (`homeTileBudgetEditOk`) and `Cancel` (`homeTileBudgetEditCancel`) actions. The dialog clamps the initial value to `[0, 31]` on construction (defensive against stale DB rows from a future schema migration), and uses the same `Slider` contract (`divisions = max - min` so the slider snaps to whole numbers) as the v1.4h-style inline controls. The Save button calls `Navigator.of(context).pop(_value)`; Cancel calls `Navigator.of(context).pop()`. (b) `_BudgetCaption` at `lib/screens/home.dart` grows `final VoidCallback? onTap` + `final String zeroCaption` constructor params and DROPS the two early-returns (`limit <= 0` + `used == 0`) so the caption renders in all 3 budget states (zero budget / partial use / exhausted) with the local "Rest days per month: N" / "N/M rest days left" / "No rest days left this month" copy. The caption is wrapped in `Semantics(button: true, label: captionText, child: GestureDetector(behavior: HitTestBehavior.opaque, onTap: onTap, child: ...))` so TalkBack reads "No rest days configured, button" (or "3/3 rest days left, button" when partial) and the user can tap anywhere on the caption to open the picker. (c) `_HabitTileState` grows `_onBudgetCaptionTapped()` which captures `messenger = ScaffoldMessenger.of(context)` BEFORE the `await showRestDayPicker(...)` async gap, awaits the picker (returning early on `null`), then awaits `DoRepository.instance.save(widget.habit.copyWith(restDaysPerMonth: picked))`. On success the handler shows `messenger.showSnackBar(SnackBar(content: Text(l.homeSnackbarBudgetUpdated(picked))))` and calls `widget.onDoChanged?.call()` to trigger the v1.4h `_refresh()` cascade (the same prop the v1.4h `_DeleteButton` uses). On throw (`DoInvalidRestDays` if validation is somehow bypassed, or any drift exception) the handler shows `homeSnackbarBudgetUpdateFailed` ("Could not update budget. Try again.") WITHOUT removing the tile — the caption stays visible at the prior value. (d) `AddHabitScreen` (`lib/screens/add_habit.dart`) grows `int _restDaysPerMonth = 2` state field, loads it in `_loadExisting()` from `_original.restDaysPerMonth` (preserving the original value in edit mode — fixes the silent-reset bug), replaces all 5 hardcoded `restDaysPerMonth: 2` literals in `_save()` at `:911, :926, :945, :960, :981` with `restDaysPerMonth: _restDaysPerMonth`, and grows `_pickRestDaysPerMonth()` which calls `showRestDayPicker(context, initial: _restDaysPerMonth)` and `setState`s on the non-null result. The form body grows a new "Rest days per month: N" `ListTile` (or equivalent outlined control) NEAR the proof-mode row that opens the same picker; tap → picker → Save → state updates → form re-renders. (e) `Do.validate()` (`lib/do/do.dart:284-286`) adds the upper-bound check `if (restDaysPerMonth < 0 \|\| restDaysPerMonth > 31) throw DoInvalidRestDays(restDaysPerMonth);` so `DoInvalidRestDays` is the single source of truth for the invariant (the picker clamps inline, `validate()` is the defensive second line — mirrors the v1.4e `sparklineForDo` defensive convention). The exception class's `super(...)` message is updated from "must be >= 0" to "must be in 0..31." to reflect the new range. (f) i18n: 7 new ARB keys added to BOTH `app_en.arb` AND `app_es.arb` in lockstep — `homeTileBudgetZeroCaption` ("No rest days configured" / "Sin días de descanso"), `homeTileBudgetEditTitle` ("Rest days per month" / "Días de descanso al mes"), `homeTileBudgetEditDescription` ("How many rest days you can take each month. Resets on the 1st." / "Cuántos días de descanso puedes tomar cada mes. Se reinicia el día 1."), `homeTileBudgetEditOk` ("Save" / "Guardar"), `homeTileBudgetEditCancel` ("Cancel" / "Cancelar"), `homeSnackbarBudgetUpdated(value)` ("Rest-day budget set to {value}." / "Días de descanso actualizados a {value}."), `homeSnackbarBudgetUpdateFailed` ("Could not update budget. Try again." / "No se pudo actualizar. Inténtalo de nuevo."), and `addHabitRestDaysLabel(value)` ("Rest days per month: {value}" / "Días de descanso al mes: {value}"). The existing `test/l10n/app_localizations_test.dart` "every non-template ARB has the same key set as the template" test catches missing Spanish entries automatically. (g) No new `<uses-permission>`, no new pubspec deps, no new Drift tables, no new MethodChannels, no Kotlin changes — v1.4j is pure-Dart. The picker helper imports Flutter (the dialog widget requires a `BuildContext`), but the budget-write path is the same `DoRepository.save(Do)` already in production; no new write API is added. (v1.4j / Phase 37 / SYS-124 / ADR-054 / WF-051.) | Unit tests (`test/screens/rest_day_picker_dialog_test.dart`: 8 tests — `RestDayPickerDialog(initial: 5)` renders `'5'` as the live integer + the `Slider` `value == 5.0`; the localized `homeTileBudgetEditTitle` / `homeTileBudgetEditDescription` / `homeTileBudgetEditOk` / `homeTileBudgetEditCancel` strings render in English locale; tapping Cancel returns `null` from `showRestDayPicker`; tapping Save returns the current `Slider` value; `initial: -7` clamps to `kRestDaysPerMonthMin` (0) on construction + the slider value matches; `initial: 99` clamps to `kRestDaysPerMonthMax` (31); the slider `divisions == kRestDaysPerMonthMax - kRestDaysPerMonthMin` so it snaps to integer values; dragging the slider updates the live integer label) + `test/do/do_model_test.dart` (+3 tests — `Do.validate()` throws `DoInvalidRestDays(-1)` on `restDaysPerMonth: -1` (existing rule preserved); `Do.validate()` throws `DoInvalidRestDays(32)` on `restDaysPerMonth: 32` (new upper-bound rule); `Do.validate()` accepts `restDaysPerMonth: 0` AND `restDaysPerMonth: 31` (boundary cases)) + Widget tests (`test/screens/add_habit_test.dart`: +1 test — "AddHabitScreen renders a Rest-days-per-month form row with the default value 2 (v1.4j / SYS-124)" asserts `find.text('Rest days per month: 2')` is visible in add mode; +1 grep regression test — `no hardcoded restDaysPerMonth: 2 literals remain in _save() — the v1.0 silent-reset bug (v1.4j / SYS-124)` reads `lib/screens/add_habit.dart` via `File().readAsString()` and asserts the regex `restDaysPerMonth:\s*2,` has 0 matches, pinning the contract that all 5 switch branches read `restDaysPerMonth: _restDaysPerMonth`) + `test/screens/home_test.dart` (+5 widget tests in a new v1.4j group — the `_BudgetCaption` renders `homeTileBudgetZeroCaption` ("No rest days configured") when `restDaysPerMonth == 0` (regression guard for the `limit <= 0` early-return removal); the `_BudgetCaption` renders `homeTileBudgetRemaining(3, 3)` when `restDaysPerMonth == 3 && used == 0` (regression guard for the `used == 0` early-return removal — the formerly-hidden state is now visible); tapping the caption opens the `RestDayPickerDialog` (asserted via `find.byType(RestDayPickerDialog)` after `tester.tap(find.text('No rest days configured'))`); a successful Save writes `DoRepository.save(activeDo.copyWith(restDaysPerMonth: 5))` AND triggers the v1.4h `onDoChanged` refresh cascade (asserted via the home screen's `FutureBuilder<List<Do>>` re-firing — captured via the v1.4h `onDoChanged` mock); long-press on a tile still enters select mode after the v1.4j caption-tap addition — regression guard for the v1.4b select-mode gesture surviving the `GestureDetector(onTap: ...)` wrap on the caption (the gesture is a tap, not a long-press, so it does NOT intercept the parent's `InkWell.onLongPress`)) + 1 existing test localized: `test/screens/add_habit_test.dart` switched from `MaterialApp(home: AddHabitScreen())` to `localizedApp(home: AddHabitScreen())` because the new `Builder` widget calls `AppLocalizations.of(ctx)` and a plain `MaterialApp` does not wire the delegate (mirrors the same localization fix that already wrapped `add_habit_delete_test.dart` + `add_habit_save_as_template_test.dart` + `templates_test.dart` for the v1.4j cycle). ARB parity (`test/l10n/app_localizations_test.dart` "every non-template ARB has the same key set as the template" test catches missing `app_es.arb` entries automatically) + Coverage: ≥80% on every changed file (`rest_day_picker_dialog.dart`, `do.dart`, `add_habit.dart`, `home.dart`) + Manual device check on Android 13+: install the new APK; tap a tile's budget caption → picker opens; drag slider to 5 → tap Save → caption updates to "5/5 rest days left" AND `_SkipButton` appears on the tile; open the edit screen → "Rest days per month: 5" row shows → tap row → picker → drag to 7 → tap Save → the form's state field is now 7; Save the edit screen → re-open → row reads "Rest days per month: 7" (the silent-reset bug is closed — the saved value persists across a round-trip even if the user doesn't touch the row); TalkBack: tap the tile, swipe right → expect the caption `Semantics(button: true, label: "5/5 rest days left")` node to announce as a button; tap → picker opens; swipe right through the `Slider` → expect each integer label to be read out as the slider value changes |
| **SYS-125** | The Android home widget (`DoitWidgetProvider` at `android/app/src/main/kotlin/com/doit/`) shall be **configurable per-instance** so a user who binds the do it widget multiple times (e.g., once on the launcher home screen and once on a secondary home-screen page) sees a different do on each widget, instead of every widget instance showing the same `firstActiveDo` (the v1.4a / SYS-115 behavior). The configuration flow rides on the existing Android `AppWidget` configuration activity pattern — when the user drags the widget from the launcher's widget picker to a home cell, the launcher calls `APPWIDGET_CONFIGURE` on `DoitWidgetConfigureActivity` BEFORE the first `onUpdate`. Specifically: (a) `DoitWidgetState` (`lib/widget/doit_widget_state.dart`) grows a new optional `String? selectedHabitId` field, threaded into `toJson` / `fromJson` / `copyWith` / `==` / `hashCode` / `toString`. The field defaults to `null` (matches the v1.4a..v1.4j caches); `fromJson` treats an empty string as `null` (defensive against a downgrade that writes a Kotlin `optString(..., "")` empty value). (b) `buildWidgetState` (`lib/widget/widget_state_builder.dart`) grows a new `String? selectedHabitId` named parameter that flows into the returned `DoitWidgetState` in both the active and the empty-state paths so the field persists across null-`activeDo` passes (the reconciliation path). (c) `WidgetService.handleRefreshRequest` (`lib/services/widget_service.dart`) consults `WidgetStateCache.cached?.selectedHabitId` FIRST when picking the active do; on a non-null + non-empty + resolvable cached pick, `_doRepository.getById(pick)` returns the picked `Do` and the widget renders that do; on a null / empty / unresolvable cached pick the service falls back to `firstActiveDo` (the v1.4a behavior). On the unresolvable path (the picked do was deleted while the widget was bound), the next cached state has `selectedHabitId = null` so a future re-bind starts fresh without a stale pick hanging in the JSON envelope — the reconciliation clear is durable. (d) `WidgetService` grows a new public method `Future<bool> setSelectedHabitId(String? habitId)` called from the Kotlin `DoitWidgetConfigureActivity` via the `doit/widget` MethodChannel (or, in tests, directly). The method writes a fresh `DoitWidgetState` to the cache + platform with the picked id (or clears the selection on `null`) and asks the platform to repaint; returns `true` on a clean write, `false` when the service is disposed. (e) `lib/widget/widget_config_screen.dart` (NEW) renders the Flutter-side picker that the Android configuration activity hosts. The screen is a `StatefulWidget` that reads `DoRepository.instance.listAll()` via `FutureBuilder<List<Do>>` and renders a `ListView.separated` of `_PickerRow` `ListTile`s (one per do, with a chevron); tapping a row pops `Navigator.of(context).pop<String>(habitId)`. When `listAll` returns an empty list (the user has zero dos), an `_EmptyState` widget renders a `Icons.add_task` glyph + the localized `widgetConfigureEmptyState` copy + a "Back to do it" `FilledButton` (label `widgetConfigureBackToHome`) that pops `null` so the launcher hands control back to MainActivity. The screen takes a `proxy: WidgetServiceProxy` constructor arg (default `const WidgetServiceProxy()`) for testability — the proxy forwards `setSelectedHabitId` to the live `WidgetService.instance`. (f) `lib/widget/widget_service_proxy.dart` (NEW) — `class WidgetServiceProxy { const WidgetServiceProxy(); Future<bool> setSelectedHabitId(String? habitId) => WidgetService.instance.setSelectedHabitId(habitId); }` — the indirection layer that lets widget tests inject a fake without reaching into the singleton. (g) `lib/app_router.dart` (NEW) — top-level route table extracted from `lib/main.dart` for testability (Dart top-level `_private` functions are not reachable across library boundaries). The module exports `buildAppRoute(RouteSettings) → Route<dynamic>?` (dispatches `/mission` → `buildMissionRoute` (v1.3d / SYS-114), `/habit` → `buildHabitRoute` (v1.4k), `/widget-config` → `buildWidgetConfigRoute` (v1.4k); non-matching routes return `null` so `MaterialApp` falls back to its `home:` switch), plus the three individual builders. `buildHabitRoute` resolves `/habit?habitId=...` to `AddHabitScreen(habitId: id)` (falls back to `HomeScreen` when `habitId` is empty — mirrors the `_unknownMissionRoute` empty-scaffold fallback so a stale tap does not surface a broken edit screen). `buildWidgetConfigRoute` resolves `/widget-config?widgetId=...` to `WidgetConfigScreen(widgetId: ...)`. `lib/main.dart` `DoItApp` switches its `onGenerateRoute:` from the previous `_buildMissionRoute` to `buildAppRoute`. (h) The widget body-tap PendingIntent carries the cached `selectedHabitId` to MainActivity. `WidgetRenderer.openAppIntent` (`android/app/src/main/kotlin/com/doit/WidgetRenderer.kt`) grows a `selectedHabitId: String` param; the Kotlin reads `state.optString("selectedHabitId", "")` and adds `MainActivity.EXTRA_HABIT_ID` to the Intent extras when `selectedHabitId.isNotEmpty()`. `MainActivity` (`android/app/src/main/kotlin/com/doit/MainActivity.kt`) grows `override fun getInitialRoute(): String?` that reads `EXTRA_HABIT_ID`, clears the extra (one-shot semantics — the value is consumed on first frame so a subsequent rebuild does not re-route), and returns `"/habit?habitId=${Uri.encode(habitId)}"` (or `null` when the extra is absent — the launcher's normal flow is unaffected). The companion gains `const val EXTRA_HABIT_ID = "com.doit.EXTRA_HABIT_ID_FROM_WIDGET"` — distinct from `DoitWidgetProvider.EXTRA_HABIT_ID` (different key namespace — the provider's extra is consumed by `WidgetChannel`, the activity's extra is consumed by `MainActivity.getInitialRoute`). (i) `DoitWidgetConfigureActivity.kt` (NEW) — a thin `FlutterActivity` shell that hosts the configurator Flutter UI. `getInitialRoute(): String` reads `Intent.EXTRA_APPWIDGET_ID` (the launcher's standard mechanism for passing the AppWidget id into the configuration activity) and returns `"/widget-config?widgetId=$widgetId"`. `configureFlutterEngine` is intentionally NOT used (no Kotlin channels — the screen talks to `WidgetService.instance` directly via the proxy, mirroring the v1.3d `FullScreenActivity` thin-Flutter shell precedent). The activity is declared in `AndroidManifest.xml` with `android:exported="true"` + `android:launchMode="singleTask"` + `android:taskAffinity=""` + `android:excludeFromRecents="true"` + `android:theme="@style/LaunchTheme"` + a `<intent-filter><action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" /></intent-filter>`. `android/app/src/main/res/xml/doit_widget_info.xml` adds `android:configure="com.doit.DoitWidgetConfigureActivity"` to the `<appwidget-provider>` element so the launcher routes configuration through the activity. `android/app/src/main/res/values/strings.xml` adds `<string name="widget_configure_label">Choose a do for do it</string>` for the launcher's chooser. (j) i18n: 2 new ARB keys added to BOTH `app_en.arb` AND `app_es.arb` in lockstep — `widgetConfigureTitle` ("Choose a do for do it" / "Elige una tarea para do it") + `widgetConfigureEmptyState` ("Add a do in do it to use the home widget." / "Añade una tarea en do it para usar el widget de inicio.") + `widgetConfigureBackToHome` ("Back to do it" / "Volver a do it"). The existing `test/l10n/app_localizations_test.dart` "every non-template ARB has the same key set as the template" test catches missing Spanish entries automatically. (k) No new `<uses-permission>` (verified against `docs/v_model/architecture_options.md` §"Permission baseline"), no new pubspec deps, no new Drift tables, no new public method-channel namespaces (the existing `doit/widget` namespace is reused with a new `setSelectedHabitId` arm — adds to the `WidgetActionInvoker` dispatch table). (v1.4k / Phase 38 / SYS-125 / ADR-055 / WF-052.) | Unit tests (`test/widget/doit_widget_state_test.dart`: 8 tests — `selectedHabitId` round-trips through `toJson` / `fromJson`; defaults to `null` when absent in JSON (backwards compatibility with v1.4a..v1.4j caches); empty string is treated as `null` on `fromJson` (defensive against a downgrade that writes a Kotlin `optString(..., "")` empty value); `null` in JSON round-trips to `null`; `copyWith(selectedHabitId: 'x')` replaces the field; `copyWith` without `selectedHabitId` preserves the prior value (mirrors v1.4f `restDaysPerMonth` precedent); `==` / `hashCode` include `selectedHabitId`; `toString` includes `selectedHabitId`) + `test/widget/widget_service_test.dart` (+4 v1.4k tests — `handleRefreshRequest` consults cached `selectedHabitId` when present + `setSelectedHabitId('h2')` re-derives with the picked do; `setSelectedHabitId(null)` falls back to `firstActiveDo` and clears the cached pick; stale `selectedHabitId` (the picked do was deleted from the repo) reconciles to `firstActiveDo` AND clears the pick on the next refresh; reliability change re-derives against the picked `selectedHabitId` so a reliability flip on a configured widget does not regress to `firstActiveDo`) + `test/widget/widget_state_builder_test.dart` (+2 tests — `selectedHabitId` threads into the returned state; `selectedHabitId` is preserved when `activeDo` is null so the reconciliation path can clear it on the next pass) + `test/widget/widget_deep_link_test.dart` (NEW, 7 tests — `buildHabitRoute(/habit?habitId=abc)` returns `AddHabitScreen`; `buildHabitRoute(/habit?habitId='')` falls back to `HomeScreen`; `buildHabitRoute(/unknown)` returns `null`; `buildWidgetConfigRoute(/widget-config?widgetId=42)` returns `WidgetConfigScreen`; `buildWidgetConfigRoute(/widget-config)` returns `WidgetConfigScreen` with null `widgetId` (defensive — the Kotlin activity always passes a widgetId); `buildAppRoute` dispatches on the route name (`/habit`, `/widget-config`, `/mission` all return non-null; `/nope` returns null); `DoitWidgetState.selectedHabitId` round-trips through `toJson` — pins the JSON envelope key that the Kotlin `WidgetRenderer.openAppIntent` reads). Tests inspect the returned `MaterialPageRoute` builder directly via a stub `BuildContext` shim rather than pushing onto a real Navigator — pushing onto a Navigator would require pumping widgets past `FutureBuilder`s inside `AddHabitScreen` / `HomeScreen` / `WidgetConfigScreen`, each of which reads `DoRepository.instance.listAll()` (the singleton is not seeded in this test file); the pure-builder check is the contract the routes actually guarantee. ARB parity (`test/l10n/app_localizations_test.dart` "every non-template ARB has the same key set as the template" test catches missing Spanish entries automatically) + `./android/gradlew :app:compileDebugKotlin` (Kotlin compile gate for `DoitWidgetConfigureActivity` + `MainActivity.getInitialRoute` + `WidgetRenderer.openAppIntent(selectedHabitId)` + the `EXTRA_HABIT_ID_FROM_WIDGET` namespace + the manifest `<activity>` + `doit_widget_info.xml` `android:configure` link) + Coverage: ≥80% on every changed file (`doit_widget_state.dart`, `widget_state_builder.dart`, `widget_service.dart`, `widget_config_screen.dart`, `widget_service_proxy.dart`, `app_router.dart`, `main.dart`) + Manual device check on Android 13+: install the new APK; long-press home screen → Widgets → drag "do it" to TWO separate cells → expect the FIRST configuration activity to open (a thin Flutter shell); pick "Read" → activity finishes → widget paints "Read" on the first cell; long-press home screen → drag "do it" to the second cell → configuration activity opens again (the SECOND time); pick "Stretch" → activity finishes → widget paints "Stretch" on the second cell; tap the body of the first widget → MainActivity launches → navigates to `AddHabitScreen(habitId: 'h1')` (the picked do); tap the body of the second widget → navigates to `AddHabitScreen(habitId: 'h2')` (the second picked do); back out; delete "Read" from the in-app home screen; tap the body of the first widget → navigates to `HomeScreen` (the `_resolveActiveDo` fallback + the `selectedHabitId = null` reconciliation clear landed); verify the widget itself now shows `firstActiveDo` ("Stretch") on the next refresh |

| **SYS-126** | The `Habits` table shall support **soft-delete tombstones** via a nullable `deletedAtMillis` column so the home tile Delete + Undo flow preserves the do row (and therefore the completion log, the streak counter, the routine-executor registry, and the widget cached id) across the Undo action, closing the v1.4h / SYS-122 trade-off documented in `ADR-052 §8` + `lib/screens/home.dart:553-561` that the Undo path "does NOT restore the completion-log rows". Specifically: (a) `lib/services/db/tables.dart` grows a new `IntColumn get deletedAtMillis => integer().nullable()();` on `Habits` (mirrors the `Events.archivedAtMillis` precedent at `tables.dart:153`). The column is nullable; existing rows default to NULL on the v4→v5 migration. (b) `lib/services/db/migrations/v4_to_v5.dart` (NEW) exports `Future<void> migrateV4ToV5(Migrator m, AppDatabase db) async { await m.database.customStatement('ALTER TABLE habits ADD COLUMN deleted_at_millis INTEGER'); }`. `lib/services/db/schema.dart` bumps `kCurrentSchemaVersion` 4 → 5 and adds `if (from < 5) { await migrateV4ToV5(m, this); }` to `MigrationStrategy.onUpgrade`. The migration is a single `addColumn` (nullable) so it is safe to run on a populated production DB. (c) `lib/do/do.dart` adds a new `final DateTime? deletedAt;` field to the base `Do` (default `null` in constructor), a `bool get isDeleted => deletedAt != null;` helper, and extends `copyWith` with `DateTime? deletedAt, bool clearDeletedAt = false` (the explicit-clear flag mirrors `Event.copyWith(clearArchived:)` at `lib/events/event.dart:118`). All 5 subclasses (`DoFixed`, `DoInterval`, `DoAnchor`, `DoDayOfX`, `DoTimeWindow`) thread the field through their constructors and `copyWith` methods; `==` and `hashCode` include `deletedAt`. (d) `DoRepository` (`lib/services/do_repository.dart`) gains `Future<bool> softDeleteById(String id, {required DateTime at})` that runs `UPDATE habits SET deleted_at_millis = ? WHERE id = ? AND deleted_at_millis IS NULL` (returns `true` on a clean update, `false` on a missing or already-tombstoned row — idempotent), `Future<bool> restoreById(String id)` that runs `UPDATE habits SET deleted_at_millis = NULL WHERE id = ? AND deleted_at_millis IS NOT NULL` (idempotent — returns `false` on an already-active row), and `Future<Do?> getActiveById(String id)` that filters tombstoned rows (`getById` keeps its current "return whatever is there" semantics so the restore path can fetch a tombstoned row). `listAll` and `listActive` add `..where((t) => t.deletedAtMillis.isNull())` so the UI never surfaces tombstones. `_toRow` DOES NOT write `deletedAtMillis` (per the Drift `insertOnConflictUpdate` invariant — unspecified columns keep their existing value) so `save(d)` cannot accidentally resurrect or tombstone a row through a Save click. `deleteById` is preserved for the `BackupService.importFrom` wipe path (and any future admin / debug tools). (e) `lib/screens/home_tile_delete.dart` replaces `deleteDo` with `softDeleteDo({required Do activeDo, required DateTime at, required DoRepository repository})` (calls `repository.softDeleteById(activeDo.id, at: at)`, returns `bool`) and adds `restoreDo({required Do tombstonedDo, required DoRepository repository})` (calls `repository.restoreById(tombstonedDo.id)`, returns `bool`). Both helpers catch throwables, return `bool`, no Flutter import, no `DateTime.now()`. (f) `lib/screens/home.dart` `_HabitTileState._onDeletePressed` switches the confirm-tap path to `softDeleteDo(activeDo: widget.habit, at: DateTime.now(), repository: DoRepository.instance)` and the Undo SnackBar action to `restoreDo(tombstonedDo: capturedHabit, repository: DoRepository.instance)`. The `_onDeletePressed` KDoc is rewritten to drop the (inaccurate) v1.4h "FK pragma cascade-deleted the completion log" claim and to accurately describe the soft-delete + restoreById flow (the completion log, the rest-day budget, and the widget cached id all survive because the row is preserved). (g) `lib/services/backup_service.dart` bumps `kBackupPayloadSchemaVersion` 2 → 3 and the habits export query adds `..where((t) => t.deletedAtMillis.isNull())` so a backup envelope is the user's "truth at export time" — tombstoned dos are filtered out (the tombstone is an undo affordance, not user data). The `restDayBudgets` export filter becomes `..where((t) => t.habitId.isIn(activeHabitIds))` to also drop the budgets for tombstoned habits (the `activeHabitIds` set is built from the filtered habits list). The `deletedAtMillis` field IS written into the envelope (nullable int) so a backup round-trip is lossless — a future schema change that decides tombstones ARE user data restores them on read. (h) No new `<uses-permission>`, no new pubspec deps, no new Drift tables, no new MethodChannels, no Kotlin changes — v1.4l is pure-Dart + a single Drift migration. The Drift codegen wires `deletedAtMillis` (Dart) ↔ `deleted_at_millis` (SQL) automatically. (v1.4l / Phase 39 / SYS-126 / ADR-056 / WF-053.) | Unit tests (`test/db/migration_v4_to_v5_test.dart`: 4 tests — `schemaVersion is 5` (v1.4l pin); `habits` table has `deleted_at_millis` column; the column is nullable and accepts both NULL and an int; pre-existing rows survive the migration with `deleted_at_millis IS NULL`) + `test/db/migration_test.dart` (`kCurrentSchemaVersion` pin bumped 4 → 5) + `test/services/do_repository_test.dart` (NEW, 14 tests in 4 groups — `softDeleteById` writes a non-null `deletedAtMillis`; `listAll` excludes tombstoned habits; `listActive` excludes tombstoned habits; `getById` still returns a tombstoned do; `getActiveById` returns null for a tombstoned do; `getActiveById` returns the do when active; `softDeleteById` is idempotent on an already-tombstoned row (returns `false`); `softDeleteById` on a missing row returns `false`; `restoreById` clears the tombstone; `restored` do reappears in `listAll` with `isDeleted == false`; `restoreById` is idempotent on an already-active row (returns `false`); `restoreById` on a missing row returns `false`; `save(d)` does NOT touch a tombstoned row's `deletedAtMillis` (the v1.4l invariant — pin); `restoreById` after a save on a tombstoned row still works; `deleteById` (force-delete) still removes the row (backup-restore wipe path preserved); force-delete then save re-inserts as active) + `test/services/habit_repository_test.dart` (+1 test — round-trips `deletedAt` via `softDeleteById` + `restoreById` on the `Habits` table via the `DoRepository` mapping) + `test/screens/home_tile_delete_test.dart` (rewritten — 14 tests in 5 groups covering `softDeleteDo` + `restoreDo` + the round-trip + the imports-not-Flutter check + helper error handling) + `test/screens/home_test.dart` (+2 existing v1.4h tests updated — the "confirm-tap deletes the do" test now asserts via `getActiveById` (returns null) instead of `getById` (returns the tombstoned row); the "Undo action re-saves the captured do" test now asserts via `getActiveById` instead of `getById`) + `test/db/migration_v2_to_v3_test.dart` (`schemaVersion` pin 4 → 5; `user_version` PRAGMA pin 4 → 5) + `test/db/migration_v3_to_v4_test.dart` (`schemaVersion` pin 4 → 5) + Coverage: ≥80% on every changed file (`tables.dart`, `migrations/v4_to_v5.dart`, `schema.dart`, `do.dart`, `do_repository.dart`, `home_tile_delete.dart`, `home.dart`, `backup_service.dart`, all v1.4l test files) + Manual device check on Android 13+: install the new APK; add a do with 3+ consecutive completions; verify streak = 3; tap the tile's Delete IconButton → confirm; verify the tile disappears; tap the SnackBar Undo within 5 s; verify the tile reappears AND the streak badge still reads 3 (the headline behavior change of v1.4l — the Undo restores the streak by construction); soft-delete + wait past the SnackBar window + relaunch the app + verify the do is NOT in the list (the tombstone is persistent but invisible to the UI); export a backup → verify the JSON envelope does NOT contain the tombstoned do (the v1.4l backup filter is correct)

| **SYS-127** | The CI test suite shall **exercise the v1.4l soft-delete home-screen flow end-to-end** so the headline behavior change of v1.4l (Undo restores the streak by construction) is guarded against regression without requiring on-device verification. Specifically: (a) `test/services/do_repository_test.dart` grows a `listDeleted` group with 4 tests — `listDeleted` excludes active habits (only tombstoned rows); `listDeleted` orders by `deletedAtMillis DESC` (most-recently-deleted first — matches the planned "Recently deleted" UI sort); `listDeleted({int? limit})` honors the `limit` param; `listDeleted` returns an empty list when no habits are tombstoned. (b) `test/services/do_repository_test.dart` grows a `purgeDeletedOlderThan` group with 4 tests — purges tombstoned habits older than the cutoff; leaves young tombstoned habits untouched; never touches active habits; is idempotent on a second call (returns 0). Both methods accept a `DateTime at` argument (the caller passes a frozen reference — no `DateTime.now()` inside the repository, matching the v0.2 / Phase A "no clock in the model" rule). (c) `test/services/do_repository_test.dart` grows a `persistence-across-restart` group with 1 test that proves the tombstone survives a HomeScreen rebuild — Phase A opens an in-memory DB, saves a do, soft-deletes it, and closes the DB; Phase B opens a FRESH in-memory DB, seeds the raw `habits` row with `deleted_at_millis` set via raw SQL `INSERT INTO habits ... VALUES (..., ?)` (the same column the migration added), and asserts `DoRepository.instance.getById(...)` returns the tombstoned do with `isDeleted == true` (proving the column survives across what an end-user sees as "close + reopen the app"). (d) `test/screens/home_test.dart` grows 4 widget tests — `Undo restores the streak badge to the original value by construction` (the headline behavior change — seeds a do + 3 consecutive completions, pumps the widget, taps Delete IconButton, confirms, waits for the SnackBar, taps the Undo `SnackBarAction`, re-pumps, asserts the streak badge's `KeyedSubtree(key: 'streakBadge-<id>')` finds a `Text('3')` widget — proving the streak counter survived the soft-delete because `Completions.habitId` references the same id that survived); `streak badge renders the streak number with the correct tabular formatting` (seeds a do with 3 completions, pumps, asserts the badge's `KeyedSubtree` finds a `Text('3')` AND the `Text('day streak')` subtitle); `cancelled delete (Cancel on confirm dialog) does NOT tombstone the do and the streak badge remains rendered` (seeds a do + 3 completions, pumps, taps Delete IconButton, taps the Cancel button on the confirm dialog, asserts the do is still in the listing and the streak badge still renders `Text('3')`); `soft-delete persists across a HomeScreen rebuild (close + reopen the in-memory DB)` (seeds a do + 3 completions, pumps the widget, taps Delete, confirms, asserts the do is gone, calls `_resetDb(tester)` to swap in a fresh in-memory DB, re-pumps, asserts the do is STILL gone — the tombstone survives a DB close + reopen). (e) The `_DoStreakBadge` call site at `lib/screens/home.dart` gains a `KeyedSubtree(key: Key('streakBadge-${habit.id}'), child: _DoStreakBadge(...))` wrapper so widget tests can locate the badge via `find.byKey(Key('streakBadge-<id>'))` without exposing the private `_DoStreakBadge` widget. The KeyedSubtree wrapper is the ONLY widget-tree change (the badge rendering + the streak calculation are untouched). (f) Total new tests: 13 (4 listDeleted + 4 purgeDeletedOlderThan + 1 persistence-across-restart + 4 widget). Test count: 1321 → 1334. Coverage: ≥80% on every changed file (`do_repository.dart`, `home.dart`, `home_test.dart`, `do_repository_test.dart`). No production code path changes outside the KeyedSubtree seam — `listDeleted` and `purgeDeletedOlderThan` are new public methods on `DoRepository` that the v1.4n "Recently deleted" UI surface will consume. The methods are added now so the data-layer contract is pinned before the UI work begins (a "tests first, then UI" inversion of the usual cycle, justified by the v1.4l data-layer landing being its own PR — pinning the API early prevents the v1.4n UI from coupling to a not-yet-tested shape). (v1.4m / Phase 40 / SYS-127 / ADR-058 / WF-055.) | Unit tests as described above (4 listDeleted + 4 purgeDeletedOlderThan + 1 persistence-across-restart + 4 widget) — 13 new tests, all deterministic (seeded DB + frozen reference time + raw-SQL seeding via Drift's `customStatement`) + Coverage: ≥80% on every changed file (`do_repository.dart`, `home.dart`, `home_test.dart`, `do_repository_test.dart`) + 3-gate: `dart format --output=none --set-exit-if-changed .` (264 files, 0 changed) + `flutter analyze --fatal-infos lib test` (0 issues) + `flutter test` (1334/1334 pass) |

| **SYS-128** | The project shall **kick off a 3-month stabilization campaign** (`docs/v_model/stabilization_roadmap.md` — NEW, single source of truth) with a coverage audit + a sequenced roadmap for 11 stabilization cycles (Cycles B..L). The campaign targets: (a) **≥90% line coverage** on every file in `lib/` (up from the current "≥80% on changed files" rule); (b) **100% coverage** on the pure-Dart model layer (`lib/do/`, `lib/people/`, `lib/habits/`, `lib/missions/`, `lib/events/`); (c) closing every known latent bug (BUG-001..BUG-020 enumerated in the roadmap doc); (d) accessibility audit (every screen has TalkBack labels, contrast ≥ 4.5:1, font-scale tested at 1.0x / 1.3x / 1.6x); (e) i18n test exhaustive (every ARB key tested in both `en` and `es` locales); (f) E2E tests for the 10 critical user flows; (g) performance benchmarks (widget rebuild, SQL query N+1 detection, APK size); (h) 0 skipped tests (current rule preserved). The stabilization campaign is a deliberate pivot from feature work to hardening — the user stated "we have 3 month to stabilise the app and have exhaustive test". The v1.4n "Recently deleted" UI surface (the planned next feature cycle) moves to post-stabilization parking lot; it is sequenced as Cycle H inside the stabilization window because it consumes the v1.4m `listDeleted` + `purgeDeletedOlderThan` API surface and completes the v1.4l feature. Specifically for Cycle A (this audit cycle): (a) `flutter test --coverage` produces `coverage/lcov.info` (1334/1334 tests pass, 8812/13638 lines covered = 64.61% overall, with 33 Priority-1 files below 80%, 31 Priority-2 files at 80-90%, 59 Priority-3 files at ≥90%); (b) `docs/v_model/stabilization_roadmap.md` (NEW) catalogs: §1 current coverage state with the per-file table; §2 latent bugs inventory (BUG-001..BUG-020 with priorities + target cycles); §3 cycle-by-cycle roadmap (Cycles B..L, sequenced by priority); §4 success criteria for the 3-month campaign; §5 open questions for the user; §6 Cycle A retrospective; (c) every subsequent stabilization cycle updates the roadmap doc (mark cycles done, add findings, re-prioritize); (d) Cycle A ships NO code changes, NO new tests — it is a pure doc + coverage report cycle. The "test" deliverable is the coverage report itself (`coverage/lcov.info` + the per-file table in §1); (e) no new `<uses-permission>`, no new pubspec deps, no new Drift tables, no new MethodChannels, no Kotlin changes — Cycle A is docs-only. (v1.4-stab-A / Phase 41 / SYS-128 / ADR-059 / WF-056.) | Doc-only cycle — no new unit tests. The "test" deliverable is the coverage report (`flutter test --coverage` → `coverage/lcov.info`) + the per-file table in `docs/v_model/stabilization_roadmap.md §1`. 3-gate: `dart format --output=none --set-exit-if-changed .` (0 changed — no Dart code modified) + `flutter analyze --fatal-infos lib test` (0 issues) + `flutter test` (1334/1334 pass — no test changes). The stabilization roadmap doc has 6 sections (§1 coverage state, §2 latent bugs, §3 cycle-by-cycle roadmap, §4 success criteria, §5 open questions, §6 Cycle A retrospective) + the full 123-file per-file table. |

| **SYS-129** | The `DoRepository._toRow` / `_fromRow` mapping shall **round-trip the `automations_json` and `paused_until_millis` columns** so that (a) BUG-001 (write + read of `automations_json`) is closed — the user's custom automation rules survive every Save click; and (b) BUG-002 (write of `paused_until_millis`) is closed — Save clicks on a paused habit do NOT silently resume the pause. Specifically: (1) `_toRow` MUST add `automationsJson: encodeAutomationList(d.automations)` (writing the column with the user's automation list, mapping an empty list to SQL `NULL` to match the `EventRepository` / `PersonRepository` convention); `_toRow` MUST remove `pausedUntilMillis: d.pausedUntil?.millisecondsSinceEpoch` (mirroring the v1.4l `deletedAtMillis` omission pattern — Drift's `insertOnConflictUpdate` semantics preserve the existing column value when the new row doesn't specify it, so a Save click that doesn't explicitly pause/resume must not clobber the existing pause state). (2) `_fromRow` MUST decode the `automations_json` column via `decodeAutomationList(r.automationsJson)` and thread the result through every subclass constructor (`DoFixed`, `DoInterval`, `DoAnchor`, `DoDayOfX`, `DoTimeWindow`); the read path MUST also continue to decode `pausedUntilMillis` as before (the omission is only on the write path). (3) `DoRepository.save` becomes **content-only + pause-preserving** (joining the v1.4l tombstone-preserving invariant); the KDoc on `save` MUST be updated to document the dual invariant. (4) `PauseService.pauseHabit` and `PauseService.resumeHabit` MUST be refactored to write the `paused_until_millis` column directly via a `HabitsCompanion` UPDATE (bypassing `DoRepository.save`) because the omission pattern means `save()` no longer writes the column. The methods become the explicit writers of the column. (5) `test/services/do_repository_test.dart` MUST grow a `DoRepository save invariant (Cycle B / BUG-001 + BUG-002)` group with 3 tests — `automations round-trip through save + getById` (BUG-001 round-trip — seed 2 `Automation` fixtures, save, getById, assert list round-trips); `pausedUntil round-trips via direct companion UPDATE + getById` (BUG-002 read path — seed `pausedUntilMillis` via a `HabitsCompanion` UPDATE, getById, assert `Do.pausedUntil` matches); `save(d) does NOT clobber an existing pausedUntilMillis` (BUG-002 save-invariant — seed via companion UPDATE, save a fresh `Do` with no in-memory `pausedUntil`, assert the raw column's `pausedUntilMillis` STILL equals the seeded timestamp + the in-memory copy sees the same value). (6) No new `<uses-permission>`, no new pubspec deps, no Drift migration (the columns already exist), no Kotlin changes — Cycle B is pure-Dart + 3 new tests. (v1.4-stab-B / Phase 42 / SYS-129 / ADR-060 / WF-057.) | 3 new unit tests in `test/services/do_repository_test.dart` (Cycle B group: automations round-trip + pausedUntil round-trip + pausedUntil save-invariant), all deterministic (in-memory Drift `NativeDatabase.memory()` + frozen reference time + 2 seeded `Automation` fixtures covering 2 distinct Trigger leaves). Test count: 1334 → 1337. Coverage: ≥80% on every changed file (`do_repository.dart`, `pause_service.dart`, `do_repository_test.dart`). 3-gate: `dart format --output=none --set-exit-if-changed .` (264 files, 0 changed — pure-Dart + new tests) + `flutter analyze --fatal-infos lib test` (0 issues) + `flutter test` (1337/1337 pass). The pause service refactor is covered by the existing `pause_service_test.dart` readiness-gate tests (no new assertions needed — the `pauseHabit` / `resumeHabit` calls now go through a direct `HabitsCompanion` UPDATE which is exercised via `do_repository_test.dart`'s round-trip test). |

| **SYS-130** | The full-screen-intent (FSI) launch path shall **degrade gracefully on every channel-level failure mode** so a device whose `NotificationManager.canUseFullScreenIntent()` throws (Android 12-13 OEM quirk), whose `USE_FULL_SCREEN_INTENT` Kotlin handler is missing (`MissingPluginException`), or whose `ACTION_MANAGE_APP_USE_FULL_SCREEN_INTENT` activity is not present (older OEM ROMs) still lets the user navigate the app, see the reliability banner as `degraded`, and reach Settings via the deep-link tile. Specifically: (1) `lib/services/full_screen_intent_service.dart`'s `MethodChannelFullScreenIntentSource.isGranted()` MUST swallow BOTH `MissingPluginException` (the no-Kotlin-arm case) AND `PlatformException` (the channel-level failure case) and return `false` rather than propagating. The swallow pattern is INTENTIONALLY identical to `ReliabilityService._safeProbe` at `lib/services/reliability_service.dart` — the v1.4-stab-C audit extends ADR-013's "missing plugin must not crash the app" posture to all channel errors on the FSI path. (2) `MethodChannelFullScreenIntentSource.openSettings()` MUST swallow the same two exception types and return `false`; the caller (`PermissionSheet` + the home-screen reliability banner) degrades gracefully to a generic Settings entry-point. (3) The class-level KDoc on `MethodChannelFullScreenIntentSource` MUST document the defense-in-depth swallow as INTENTIONAL per ADR-013 + ADR-061 — a reader who is tempted to "fix" by removing the catches MUST first read both ADRs and audit every caller of `isGranted` + `openSettings` for `null`-safety (the contract is `Future<bool>`, not `Future<bool?>` — there is no error path). (4) `lib/reminders/full_screen_intent.dart`'s file-level header MUST NOT reference `wakelock_plus` (the v1.0 stale-doc issue — the production code uses `FLAG_KEEP_SCREEN_ON` in `android/app/src/main/kotlin/com/doit/FullScreenActivity.kt:47-56`, NOT `wakelock_plus`; verified via `pubspec.yaml` having 0 `wakelock_plus` matches). The header MUST reference the actual production mechanism. (5) `docs/v_model/notification_reliability.md:496` MUST say "API 34+" (not "API 14+") — `USE_FULL_SCREEN_INTENT` was introduced in API 34 (Android 14); the typo has been misleading readers for ~2 months. (6) `test/reminders/full_screen_intent_test.dart` (NEW) MUST lift `lib/reminders/full_screen_intent.dart` coverage from 25% (Cycle A audit) to ≥80% by exercising the `FakeFullScreenIntent.show` invocation-order recording, the `showRoutineOverlay` 4-input shape (title-only / body-only / neither / both), the `getLaunchIntent` scripted-`LaunchIntent` round-trip, and the `RoutineOverlayLaunch` / `LaunchIntent` equality + `hashCode` invariants. (7) `test/services/full_screen_intent_service_test.dart` (extended) MUST gain a `MethodChannelFullScreenIntentSource (production source)` group with 3 tests pinning the defense-in-depth `PlatformException` + `MissingPluginException` → `false` contract on `isGranted` + `openSettings`. (8) `test/reminders/reminder_bridge_fsi_channel_test.dart` (NEW) MUST pin the KNOWN channel-surface gap: `ReminderBridge.showFullScreen` Dart seam IS exercised (the channel method is invoked), AND the production Kotlin handler returns `notImplemented()` (which Flutter translates to `MissingPluginException`); a future stabilization cycle will either remove the dead Dart arm or add the matching Kotlin arm. The gap is INERT today (no production callers per repo-wide grep) but is documented as a known follow-up bug. (9) Total new tests: 8 (3 full_screen_intent + 3 full_screen_intent_service + 2 reminder_bridge_fsi_channel). Test count: 1337 → 1345. Coverage: `lib/reminders/full_screen_intent.dart` ≥80% (from 25.0%); `lib/services/full_screen_intent_service.dart` ≥95% (from 80.5%). No new `<uses-permission>` (the `USE_FULL_SCREEN_INTENT` permission is already declared at `AndroidManifest.xml:83-85`); no new pubspec deps; no Drift migration; no Kotlin changes — Cycle C is pure-Dart + docs + new tests. (v1.4-stab-C / Phase 43 / SYS-130 / ADR-061 / WF-058.) | 8 new unit tests as described above (3 full_screen_intent + 3 full_screen_intent_service + 2 reminder_bridge_fsi_channel), all deterministic (seeded `FakeFullScreenIntent` + `ScriptedFullScreenIntentSource` + `MethodChannel` mock via `TestDefaultBinaryMessengerBinding`). Test count: 1337 → 1345. Coverage: ≥80% on every changed file (`full_screen_intent.dart`, `full_screen_intent_service.dart`, `notification_reliability.md`). 3-gate: `dart format --output=none --set-exit-if-changed .` (264 + ~4 files, 0 changed — pure-Dart + docs) + `flutter analyze --fatal-infos lib test` (0 issues) + `flutter test` (1345/1345 pass). The 2 `PlatformException` + 1 `MissingPluginException` tests on the production source are the headline assertion — `isGranted` + `openSettings` MUST return `false` when the channel throws (the v0.4b-release-fix lesson, extended to the FSI path per ADR-061). |

| **SYS-131** | The v1.4-stab-A audit's permission-flow coverage gaps shall be closed via a **test-only Cycle D** that (a) directly tests every `PermissionResult` sealed subclass (`PermissionResultGranted`, `PermissionResultDenied(canOpenSettings:)`, `PermissionResultPermanentlyDenied`) plus every `BackupFolderResult` subclass (`BackupFolderPicked`, `BackupFolderCancelled`, `BackupFolderError`) so the mapping at `lib/services/permission_service.dart:267-379` is covered at the result-type level; (b) directly tests `ContactPerson.isPausedAt(now)` and `copyWith(clearPausedUntil: true)` so `lib/people/person.dart` lifts from 54.5% to ≥80% (Cycle K brings it to 100%); (c) tests the lifecycle observer's early-return for non-`resumed` lifecycle states (`inactive`, `paused`, `hidden`, `detached`) so the gate at `lib/services/permission_lifecycle_observer.dart:69` is covered; (d) extends `test/services/permission_service_test.dart` with 4 tests covering the four `PermissionStatus` mappings not yet exercised by the v1.3c tests — `limited` → `PermissionResultDenied(canOpenSettings: true)`, `restricted` → `PermissionResultDenied(canOpenSettings: false)`, `provisional` → `PermissionResultGranted`, plus a `permanentlyDenied` sanity test on `requestCalendar`. Specifically: (1) `test/services/permission_result_test.dart` (NEW) MUST add 6 tests across 2 groups — `PermissionResult sealed hierarchy (SYS-131)` (4 tests: singleton Granted equality, Denied equality on canOpenSettings, singleton PermanentlyDenied equality, exhaustive `switch` pattern-match over all 3 subclasses) + `BackupFolderResult sealed hierarchy (SYS-131)` (3 tests: Picked equality on path, singleton Cancelled, Error equality on message). (2) `test/people/person_test.dart` (NEW) MUST add 3 tests in the `ContactPerson pause semantics (SYS-131)` group covering the `isPausedAt` future/expired/null branches + `copyWith(clearPausedUntil: true)`. (3) `test/services/permission_lifecycle_observer_test.dart` (extended) MUST add 1 test in the `paused / inactive / hidden lifecycle events do NOT trigger a permission refresh (SYS-131)` group — drives all 4 non-`resumed` states and asserts `_fireCountSinceStart` is unchanged. (4) `test/services/permission_service_test.dart` (extended) MUST add 4 tests in a new `v1.4-stab-D / Phase 44 / SYS-131` block. (5) Total new tests: 13 (6 result + 3 person + 1 lifecycle + 4 service — the headline metric; +15 net in the full-suite count because the result and lifecycle test files are NEW). Test count: 1348 → 1363. Coverage: `lib/services/permission_result.dart` 18.9% → 100%; `lib/services/permission_service.dart` 93.4% → ≥95%; `lib/services/permission_lifecycle_observer.dart` 78.6% → ≥90%; `lib/people/person.dart` 54.5% → ≥80%. (6) Closes: BUG-005 (the v1.1f-deferred `callScreening` probe now has both happy-path + denial-path coverage via the `requestCallScreening` + `refreshCallScreening` existing tests at `test/services/permission_service_test.dart:565-606`), BUG-011 (`PermissionResult` direct tests added), BUG-012 (partial — `person.dart` at ≥80%; Cycle K brings to 100%), BUG-020 (lifecycle observer edge cases covered). (7) No new `<uses-permission>`, no new pubspec deps, no Drift migration, no Kotlin changes — Cycle D is pure-Dart + new tests + docs only. (v1.4-stab-D / Phase 44 / SYS-131 / ADR-062 / WF-059.) | 13 new unit tests as described above (6 result + 3 person + 1 lifecycle + 4 service), all deterministic (no platform channels — the result/person tests are pure Dart; the service tests mock the `permission_handler` MethodChannel via `TestDefaultBinaryMessengerBinding`; the lifecycle test drives the observer directly via `AppLifecycleState` enum). Test count: 1348 → 1363. Coverage: ≥80% on every changed file (`permission_result.dart`, `permission_service.dart`, `permission_lifecycle_observer.dart`, `person.dart`). 3-gate: `dart format --output=none --set-exit-if-changed .` (268 files, 0 changed after auto-format on the 3 NEW files + 2 EXTENDED files) + `flutter analyze --fatal-infos lib test` (0 issues — the only `--fatal-infos` hit was on the unused import in `test/services/permission_result_test.dart`, fixed by auto-format) + `flutter test` (1363/1363 pass). |
| **SYS-133** | The v1.4-stab-A audit's backup round-trip coverage gaps shall be closed via a **test-only Cycle F** that (a) pins the malformed-envelope / missing-KDF / iterations-floor error paths in `lib/services/backup_service.dart`; (b) pins the `runBackupTask` dispatcher + `ScheduleMode.none` skip path in `lib/services/backup_scheduler.dart`; (c) extends `test/services/backup_task_dispatcher_test.dart` for the unknown-task-name and init-failure-swallow contracts. Specifically: (1) `test/services/backup_encryption_test.dart` (extended) MUST add 5 tests covering `BackupFormatException.toString()`, missing-kdf-object rejection, v2 iterations-floor rejection, v3 missing-fields rejection, v2 missing-fields rejection. (2) `test/backup/scheduler_skip_test.dart` (NEW) MUST add 1 test exercising the `ScheduleMode.none` early-return in `runBackupTask`. (3) `test/services/backup_task_dispatcher_test.dart` (extended) MUST add 2 tests for unknown-task-name + init-failure-swallow. (4) Total new tests: 8. Test count: 1371 → 1379. Coverage: `lib/services/backup_service.dart` ≥95| **SYS-133** | The v1.4-stab-A audit's backup round-trip coverage gaps shall be closed via a **test-only Cycle F** that (a) pins the malformed-envelope / missing-KDF / iterations-floor error paths in `lib/services/backup_service.dart`; (b) pins the `runBackupTask` dispatcher + `ScheduleMode.none` skip path in `lib/services/backup_scheduler.dart`; (c) extends `test/services/backup_task_dispatcher_test.dart` for the unknown-task-name and init-failure-swallow contracts. Specifically: (1) `test/services/backup_encryption_test.dart` (extended) MUST add 5 tests covering `BackupFormatException.toString()`, missing-kdf-object rejection, v2 iterations-floor rejection, v3 missing-fields rejection, v2 missing-fields rejection. (2) `test/backup/scheduler_skip_test.dart` (NEW) MUST add 1 test exercising the `ScheduleMode.none` early-return in `runBackupTask`. (3) `test/services/backup_task_dispatcher_test.dart` (extended) MUST add 2 tests for unknown-task-name + init-failure-swallow. (4) Total new tests: 8. Test count: 1371 → 1379. Coverage: `lib/services/backup_service.dart` ≥95%; `lib/services/backup_scheduler.dart` ≥90%. (5) No new `<uses-permission>`, no new pubspec deps, no Drift migration, no Kotlin changes — Cycle F is pure-Dart + new tests + docs only. (v1.4-stab-F / Phase 46 / SYS-133 / ADR-064 / WF-061.) | 8 new unit tests as described above (5 encryption + 1 scheduler-skip + 2 dispatcher), all deterministic (in-memory Drift `NativeDatabase.memory()` + scripted file fixtures + Workmanager mocked via `TestDefaultBinaryMessengerBinding`). Test count: 1371 → 1379. Coverage: ≥80% on every changed file (`backup_service.dart`, `backup_scheduler.dart`, `backup_task_dispatcher.dart`). 3-gate: `dart format --output=none --set-exit-if-changed .` + `flutter analyze --fatal-infos lib test` + `flutter test` (1379/1379 pass). |
| **SYS-134** | The v1.4l data-layer tombstone semantics (ADR-056) shall surface in the v1.4g-deferred UI affordance: a "Target paused" badge on the home tile whenever a `DoAnchor` do references a tombstoned (soft-deleted) target. Specifically: (1) `lib/widgets/do_anchor_paused_badge.dart` (NEW) MUST render a compact, accessible badge (`Semantics(label: ...)` + a `Tooltip` + a Theme-derived `colorScheme.tertiary` fill that meets WCAG 4.5:1 contrast) on the `_HabitTile` row whenever the supplied `Do` is a `DoAnchor` AND `DoRepository.getActiveById(targetDoId)` returns `null` AFTER `getById` returns a non-`null` (i.e., the target row exists with `deletedAt != null`). (2) The badge MUST render below the streak badge — the existing tile's icon row at `lib/screens/home.dart:797-806` already hosts a pause indicator; the badge MUST integrate into the same row to avoid layout shift. (3) `lib/screens/home.dart`'s `_HabitTileState.build` MUST look up the target's tombstone state once per build (cached in `_targetTombstoned` field; cleared in `didUpdateWidget`); the lookup uses the existing `DoRepository.getById(targetDoId)` API (NOT a new method — `getActiveById` filters tombstones out, which is the OPPOSITE of what the badge needs). (4) `lib/l10n/app_en.arb` MUST add `doAnchorTargetPaused` key with body "Target paused" + `doAnchorTargetPausedHelp` key with body "The habit this anchor was tracking has been paused or deleted. Restore it from Settings to resume this anchor." + their matching Spanish counterparts in `lib/l10n/app_es.arb` (ARB parity guard test in Cycle I will fail if either key is missing in either locale). (5) `test/widgets/do_anchor_paused_badge_test.dart` (NEW) MUST add 4 tests — `renders when target is tombstoned (DoAnchor with deletedAt)`, `hides when target is active (no deletedAt)`, `Semantics label is present for TalkBack`, `color contrast meets 4.5:1 against the theme's surface color (computed at test time)`. (6) `test/screens/home_test.dart` (extended) MUST add 1 test — `home tile shows Target paused badge when the DoAnchor points at a tombstoned habit`, asserting the badge's `KeyedSubtree(key: 'doAnchorTargetPaused-<habit.id>')` finds the badge widget in the tree. (7) `test/screens/home_tile_sparkline_test.dart` (extended) MUST add 1 test pinning BUG-019 — `sparkline renders empty when only 1 completion exists (avoids over-stretched single-point line)`. (8) Total new tests: 6. Test count: 1379 → 1385. Coverage: `lib/screens/home.dart` 85.7% → ≥90%; `lib/screens/home_tile_sparkline.dart` 78.6% → ≥85%; `lib/widgets/do_anchor_paused_badge.dart` 0% → ≥90%. (9) Closes: BUG-004 (the v1.4l-deferred UI affordance lands), BUG-019 (sparkline edge cases pinned). (10) No new `<uses-permission>`, no new pubspec deps, no Drift migration, no Kotlin changes — Cycle G is pure-Dart + new widget + new tests + new ARB keys. (v1.4-stab-G / Phase 47 / SYS-134 / ADR-065 / WF-062.) | 6 new tests as described above (4 badge + 1 home integration + 1 sparkline), all deterministic (seeded in-memory Drift + fake repository seams + theme-extract pattern for contrast). Test count: 1379 → 1385. Coverage: ≥80% on every changed file (`home.dart`, `home_tile_sparkline.dart`, `do_anchor_paused_badge.dart`). 3-gate: `dart format --output=none --set-exit-if-changed .` + `flutter analyze --fatal-infos lib test` + `flutter test` (1385/1385 pass). Targeted: `flutter test test/widgets/do_anchor_paused_badge_test.dart test/screens/home_test.dart test/screens/home_tile_sparkline_test.dart`. |
| **SYS-135** | The v1.4l data-layer tombstone (ADR-056) and the v1.4m `purgeDeletedOlderThan` API (SYS-127) shall surface in a top-level `Recently deleted` screen at route `/recently-deleted` — the v1.4l-deferred UI surface for the tombstone column. Specifically: (1) `lib/screens/recently_deleted_screen.dart` (NEW) MUST be a `StatefulWidget` that calls `DoRepository.instance.listDeleted()` on `initState` (newest-deleted first per the v1.4m ordering), renders a `ListView` of one `_Row` per tombstoned do with each row offering a Restore `IconButton` (`Icons.restore`) and a Delete-forever `IconButton` (`Icons.delete_forever`). (2) The Restore action MUST call `DoRepository.instance.restoreById(id)` and surface a success or failed snackbar via `ScaffoldMessenger.of(context).showSnackBar`; the failed snackbar fires when `restoreById` returns `false` (the tombstone was already force-purged). (3) The Delete-forever action MUST be gated by an `AlertDialog` confirm with the destructive verb repeated in the confirm CTA; on confirm it MUST call `DoRepository.instance.deleteById(id)` inside a try/catch and surface the success or failed snackbar. (4) The empty state MUST render a localized "Nothing here — deleted dos stay for 30 days before being purged" copy (mentions the v1.4m 30-day TTL per ADR-057). (5) The error state MUST render a `Retry` button that re-runs the `listDeleted` future. (6) `lib/app_router.dart` MUST add `/recently-deleted` to the existing 3-route switch (`buildAppRoute`) returning a `MaterialPageRoute<void>` wrapping the screen. (7) `lib/screens/settings.dart` MUST add a `ListTile` (key: `settings.recently_deleted`) in the Backup section that pushes `const RecentlyDeletedScreen()` via `Navigator.of(context).push` (matches the existing Settings restore tile pattern). (8) `lib/l10n/app_en.arb` MUST add 13 ARB keys: `recentlyDeletedTitle`, `recentlyDeletedEmpty`, `recentlyDeletedRestoreAction`, `recentlyDeletedDeleteForeverAction`, `recentlyDeletedDeleteForeverConfirm`, `recentlyDeletedDeleteForeverConfirmBody`, `recentlyDeletedDeleteForeverConfirmCta`, `recentlyDeletedDeleteForeverCancel`, `recentlyDeletedRestoreSuccess`, `recentlyDeletedRestoreFailed`, `recentlyDeletedDeleteForeverFailed`, `recentlyDeletedRetry`, `recentlyDeletedSubtitle` (placeholder: `name`, `when`); plus `recentlyDeletedSettingsTitle` + `recentlyDeletedSettingsSubtitle` for the Settings tile. `lib/l10n/app_es.arb` MUST add matching Spanish translations for all 15 keys. (9) `test/screens/recently_deleted_screen_test.dart` (NEW) MUST add 12 tests — list-loaded, list-empty, restore-happy-path, restore-failed (hard-deleted row), delete-forever-happy-path, delete-forever-cancel, delete-forever-confirm-dialog, error-state (Retry key not rendered in happy path), navigation-from-settings, SnackBar-success, SnackBar-failed, ARB-parity (Spanish locale). (10) Total new tests: 12. Test count: 1388 → 1400. Coverage: `lib/screens/recently_deleted_screen.dart` 0% → 100% (new file); `lib/app_router.dart` 85.7% → 100%; `lib/screens/settings.dart` 85% → ≥90%. (11) No new `<uses-permission>`, no new pubspec deps, no Drift migration, no Kotlin changes — Cycle H is pure-Dart + new screen + new route + new settings tile + new tests + new ARB keys. (v1.4-stab-H / Phase 48 / SYS-135 / ADR-066 / WF-063.) | 12 new tests as described above (12 widget-level), all deterministic (in-memory Drift `NativeDatabase.memory()` + localized app helper). Test count: 1388 → 1400. Coverage: ≥80% on every changed file (`recently_deleted_screen.dart`, `app_router.dart`, `settings.dart`). 3-gate: `dart format --output=none --set-exit-if-changed .` + `flutter analyze --fatal-infos lib test` + `flutter test` (1400/1400 pass). Targeted: `flutter test test/screens/recently_deleted_screen_test.dart test/widget/widget_deep_link_test.dart`. |
| **SYS-136** | The ARB catalog (`lib/l10n/app_en.arb` + `lib/l10n/app_es.arb`) and the generated `AppLocalizations` shall have exhaustive test coverage: (1) `test/l10n/app_localizations_test.dart` MUST add a new group `AppLocalizations per-key + locale tests` with 12 testWidgets that pin: (a) every non-placeholder and placeholder ARB key resolves through `AppLocalizations.delegate.load(Locale('en'))` with a non-empty string, (b) the mirror of (a) for `Locale('es')`, (c) the v1.4-stab-G + v1.4-stab-H keys (`doAnchorTargetPaused`, `recentlyDeletedTitle`, `recentlyDeletedDeleteForeverConfirm`, `recentlyDeletedRestoreSuccess`, `recentlyDeletedSettingsTitle`) have BOTH the English and Spanish copy asserted verbatim, (d) the placeholder interpolation for `homeTileBudgetRemaining(remaining, limit)` works in both locales (verbatim assertion), (e) the same pin for `homeSnackbarBudgetUpdated`, (f) the same pin for `addHabitRestDaysLabel`, (g) the same pin for `settingsAboutAppVersion`, (h) the same pin for `permissionBackupFolderSet`, (i) the same pin for `recentlyDeletedSubtitle` (the v1.4-stab-H placeholder shape), (j) the en `homeSelectionAppBarTitle` plural branches resolve at counts 0/1/5 (mirror of the existing es pin), (k) every placeholder-bearing ARB key in `app_en.arb` has a paired `@<key>` metadata block (gen-l10n drops keys without metadata at runtime). (2) `test/l10n/locale_render_test.dart` (NEW) MUST add 8 testWidgets that pin: (a) `HomeScreen` empty state renders English copy under `locale=en`, (b) mirror of (a) for `locale=es`, (c) Settings section headers resolve in `locale=en` (7 strings: `settingsAppBarTitle` + 6 section headers + `recentlyDeletedSettingsTitle`), (d) mirror of (c) for `locale=es`, (e) `RecentlyDeletedScreen` (NEW, v1.4-stab-H) renders English title + empty state, (f) mirror of (e) for `locale=es`, (g) `HomeScreen` renders without `RenderFlex` overflow at `TextScaler.linear(1.0)` + `locale=en` (uses `tester.takeException()` pin), (h) `RecentlyDeletedScreen` renders without overflow at `1.0x` + `locale=es`. (3) The pre-existing "every non-template ARB has the same key set as the template" test MUST continue to pass (the parity guarantee that the ARB catalog is identical across locales key-by-key). (4) `test/l10n/app_localizations_test.dart` MUST lift the `arbs` map to file scope (so both groups can read it) and load it lazily via a file-scope `ensureArbsLoaded()` helper. (5) Test count: 1401 → 1421 (+20). Coverage: `app_localizations_es.dart` from 7.0% to ≥70%; `app_localizations_en.dart` stays ≥80%. (6) No production-code changes — pure-test + docs cycle. No new `<uses-permission>`, no new pubspec deps, no Drift migration, no Kotlin changes. (v1.4-stab-I / Phase 49 / SYS-136 / ADR-067 / WF-064.) | 20 new tests as described above (12 per-key + locale + 8 locale-render), all deterministic (in-memory Drift + `localizedApp` helper, no `Random`, no `DateTime.now()`). Test count: 1401 → 1421 (+20). Coverage: ≥70% on `app_localizations_es.dart` (was 7.0%); ≥80% on `app_localizations_en.dart`. 3-gate: `dart format --output=none --set-exit-if-changed .` + `flutter analyze --fatal-infos lib test` (0 issues) + `flutter test` (1421/1421 pass). Targeted: `flutter test test/l10n/app_localizations_test.dart test/l10n/locale_render_test.dart`. |
| **SYS-137** | The app's accessibility surface (TalkBack labels, color contrast, font-scale reflow) shall have exhaustive test coverage on the 5 critical screens: (1) `test/a11y/contrast_test.dart` (NEW) MUST add 7 tests with top-level WCAG-2.x `relativeLuminance(Color)` and `contrastRatio(Color, Color)` helpers: (a) `relativeLuminance: black = 0, white = 1` (boundary conditions), (b) `contrastRatio: black-on-white = 21:1 (max)` (boundary), (c) `contrastRatio: same color = 1:1 (min)` (boundary), (d) `contrastRatio is symmetric (a, b) == (b, a)`, (e) `dark theme primary on background meets WCAG AA body (≥ 4.5:1)` (pin `Theme.dark()` onSurface-vs-surface), (f) `light theme primary on background meets WCAG AA body (≥ 4.5:1)` (pin `Theme.light()`), (g) `error / destructive CTA contrast meets a tight-but-readable bar (≥ 2.7:1, M3-light pin)` (documents that M3-light's `error / onError` measures ~2.98:1 — just under the 3:0 AA-Large bar — and pins at the readability floor so a future regression below 2.7:1 surfaces loudly). (2) `test/a11y/font_scale_test.dart` (NEW) MUST add 7 testWidgets that mount `HomeScreen` and `RecentlyDeletedScreen` at the 3 Android Material You `TextScaler.linear` presets and assert no `RenderFlex` overflow (`tester.takeException() == null`): (a) home at 1.0x, (b) home at 1.3x, (c) home at 1.6x (largest), (d) recently-deleted at 1.0x, (e) recently-deleted at 1.3x, (f) recently-deleted at 1.6x, (g) `locale=es home-screen renders without overflow at font-scale 1.6x` (cross-locale smoke for the Spanish copy ~30% longer on average). (3) `test/a11y/every_screen_test.dart` (NEW) MUST add 15 tests covering 5 critical screens × 3 checks each: (a) `[<screen>] participates in the Semantics sweep` (source has `Semantics | tooltip | semanticLabel | excludeFromSemantics | title: Text(...)` — the `ListTile`-passive-label contract from v0.4c.2 / SYS-062), (b) `[<screen>] composes with Theme.of(context) (no screen-level colorScheme override)` (regex-pin `colorScheme: ColorScheme(...)` absent — a screen-level ColorScheme override would defeat the app-wide contrast budget), (c) `[<screen>] declares a Scaffold + AppBar landmark` (TalkBack navigation landmark). The 5 critical screens are: `home.dart`, `add_habit.dart`, `add_person.dart`, `add_event.dart`, `settings.dart`. The other 9 screens in `lib/screens/` are exercised by Cycle K's E2E flow mount (not in Cycle J scope per plan §Cycle J risk note). (4) The pre-existing `test/a11y/semantics_labels_test.dart` (v0.4c.2 / SYS-062) MUST continue to pass — Cycle J's contribution is the per-screen smoke that links the file-level exhaustive sweep to the critical screens. (5) Test count: 1422 → 1451 (+29 = 7 contrast + 7 font-scale + 15 every-screen). Coverage: no production-code coverage delta (Cycle J is pure-test + docs; the contrast + font-scale checks pin the existing behavior). (6) No production-code changes — pure-test + docs cycle. No new `<uses-permission>`, no new pubspec deps, no Drift migration, no Kotlin changes. (v1.4-stab-J / Phase 50 / SYS-137 / ADR-068 / WF-065.) | 29 new tests as described above (7 contrast + 7 font-scale + 15 every-screen), all deterministic (in-memory Drift + `MediaQuery` textScaler override + source-string regex pins). Test count: 1422 → 1451 (+29). Coverage: ≥80% on `test/a11y/contrast_test.dart` (no production-code coverage delta). 3-gate: `dart format --output=none --set-exit-if-changed .` + `flutter analyze --fatal-infos lib test` (0 issues) + `flutter test` (1451/1451 pass). Targeted: `flutter test test/a11y/contrast_test.dart test/a11y/font_scale_test.dart test/a11y/every_screen_test.dart` + `flutter test test/a11y/semantics_labels_test.dart` (the pre-existing sweep). |